1. **VERDICT: CONDITIONAL PASS.** Defaulting only the mzPeak writer’s inferred `spectrum_type` to MS1 avoids the panic and does not change stored `ms_level`, centroid/profile routing, or reverse ms-level emission (`visitor.rs:1750`, `visitor.rs:1771`, `visitor.rs:1778`). Objection: calling it “safe” is overstated because your reverse writer explicitly says `ms_level == 0` must not assert MS1 (`src/reverse/imzml_writer.rs:503`, `src/reverse/imzml_writer.rs:1422`); this is an archive-compatibility fallback, not a semantic proof.
2. **VERDICT: PASS WITH COVERAGE GAP.** Normal no-extension stems still derive `out.imzML`/`out.ibd`, and explicit `.imzML`/`.imzml` still keeps XML and swaps sidecar (`src/cli.rs:326`, `src/cli.rs:654`, `src/cli.rs:672`). For `-o out.mzpeak`, behavior changes from `out.imzML` to `out.mzpeak.imzML`; acceptable if `-o` is truly a stem, but add a test documenting that expectation. The dotted-stem test is correct (`src/cli.rs:663`).
3. **VERDICT: SOUND, NOT PROVEN.** The explanation matches the code: preflight can prefer declared `IMS:1000070`, but the vendored mzdata reader later ignores that and resolves sidecar via `path.with_extension("ibd")`, so `foo.rev.imzML` looks for `foo.rev.ibd` or, depending on the other resolver, can disagree with expectations (`src/integrity/preflight.rs:51`, `src/integrity/preflight.rs:114`, `vendor/mzdata/src/io/imzml/reader.rs:1437`). A single falsifier: in a fresh temp dir, run `imzML -> mzPeak -> reverse` with a non-dotted output stem, then re-forward that reverse output and assert UUID/checksum consistency; any UUID mismatch there means this is a real pipeline bug, not stale/dotted-stem fallout.
4. **VERDICT: MIXED.** Deferring ISSUE-2 is right; Latin-1 tolerance in vendored mzdata is not a trivial patch because attribute decoding is spread through the parser and can corrupt CV/value handling (`out/campaign/CAMPAIGN-ISSUES.md:47`). Deferring ISSUE-5 is weaker: sidecar resolution is small and concrete, and the current mismatch between preflight and mzdata open-path behavior is exactly the sort of “minor” edge case that produces misleading campaign failures (`src/integrity/preflight.rs:108`, `vendor/mzdata/src/io/imzml/reader.rs:1431`).
FIX FIRST: add/document the `-o out.mzpeak` expectation test and either fix ISSUE-5 now or mark dotted-stem re-forward unsupported until fixed.
1. **VERDICT: CONDITIONAL PASS.** Defaulting only the mzPeak writer’s inferred `spectrum_type` to MS1 avoids the panic and does not change stored `ms_level`, centroid/profile routing, or reverse ms-level emission (`visitor.rs:1750`, `visitor.rs:1771`, `visitor.rs:1778`). Objection: calling it “safe” is overstated because your reverse writer explicitly says `ms_level == 0` must not assert MS1 (`src/reverse/imzml_writer.rs:503`, `src/reverse/imzml_writer.rs:1422`); this is an archive-compatibility fallback, not a semantic proof.
2. **VERDICT: PASS WITH COVERAGE GAP.** Normal no-extension stems still derive `out.imzML`/`out.ibd`, and explicit `.imzML`/`.imzml` still keeps XML and swaps sidecar (`src/cli.rs:326`, `src/cli.rs:654`, `src/cli.rs:672`). For `-o out.mzpeak`, behavior changes from `out.imzML` to `out.mzpeak.imzML`; acceptable if `-o` is truly a stem, but add a test documenting that expectation. The dotted-stem test is correct (`src/cli.rs:663`).
3. **VERDICT: SOUND, NOT PROVEN.** The explanation matches the code: preflight can prefer declared `IMS:1000070`, but the vendored mzdata reader later ignores that and resolves sidecar via `path.with_extension("ibd")`, so `foo.rev.imzML` looks for `foo.rev.ibd` or, depending on the other resolver, can disagree with expectations (`src/integrity/preflight.rs:51`, `src/integrity/preflight.rs:114`, `vendor/mzdata/src/io/imzml/reader.rs:1437`). A single falsifier: in a fresh temp dir, run `imzML -> mzPeak -> reverse` with a non-dotted output stem, then re-forward that reverse output and assert UUID/checksum consistency; any UUID mismatch there means this is a real pipeline bug, not stale/dotted-stem fallout.
4. **VERDICT: MIXED.** Deferring ISSUE-2 is right; Latin-1 tolerance in vendored mzdata is not a trivial patch because attribute decoding is spread through the parser and can corrupt CV/value handling (`out/campaign/CAMPAIGN-ISSUES.md:47`). Deferring ISSUE-5 is weaker: sidecar resolution is small and concrete, and the current mismatch between preflight and mzdata open-path behavior is exactly the sort of “minor” edge case that produces misleading campaign failures (`src/integrity/preflight.rs:108`, `vendor/mzdata/src/io/imzml/reader.rs:1431`).
FIX FIRST: add/document the `-o out.mzpeak` expectation test and either fix ISSUE-5 now or mark dotted-stem re-forward unsupported until fixed.
