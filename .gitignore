# Rust build artifacts
/target

# Local dataset directory — kept entirely out of the repo (large binaries + sample data).
/data/

# Local conversion outputs / acceptance artifacts (large)
/out/

# Local Obsidian research vault (kept local — not part of the repo)
/knowledge/
data/imzml-examples/

# scheduled-task runtime lock (not source)
.claude/scheduled_tasks.lock
