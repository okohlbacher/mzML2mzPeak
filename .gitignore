# Rust build artifacts
/target

# Large binary sidecar fetched in plan 00-02 — must never be committed.
# (The small .imzML header in data/ IS tracked and is intentionally not ignored.)
data/*.ibd

# Local conversion outputs / acceptance artifacts (large)
/out/

# Local Obsidian research vault (kept local — not part of the repo)
/knowledge/
