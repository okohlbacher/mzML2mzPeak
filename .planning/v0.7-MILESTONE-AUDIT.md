# v0.7 Milestone Audit — Upstreaming, de-vendoring & spec-governed round-trip / conformance hardening

**Date:** 2026-06-09 · **Verdict:** ✅ PASS (buildable scope) · 380 tests green · adversarially reviewed (CODEX) + hardened

## Release gate (met)
v0.7 ships when Phases **24, 25, 26, 28** are done. All done:

| Phase | Req(s) | Evidence |
|-------|--------|----------|
| 23 Upstream rebase | REB-01 | `5021eed` — mzpeak `a5c222c` + mzdata `0.64.2`; 2/3 patches dropped (upstreamed); pwiz 139/139 |
| 24 Spec alignment & CV governance | SPEC-01/02/03, CVG-01/02 | reverse `<cvList>` driven by `cv_list()` (no-drift by construction); TODO(F9) resolved; CV-by-CURIE guard; extension contract + narrowed proposal queue |
| 25 Forward declared-geometry (GEO-F) | GEOF-01 | declared-grid threading + consistency guard (no fabrication from inconsistent grid); fwd↔rev symmetry assertion |
| 26 Reverse `<sourceFileList>` (RSRC) | RSRC-01 | `source_files[]` re-emitted into reverse `.imzML`; provenance round-trip |
| 28 L2 conformance (F10) | L2-01 | `--conformance l2` bounded value-equal + transform record (`MS:1002312`, file-level + array-index, single CURIE source); real `data_processing` step |

## Adversarial review (CODEX) — 6 fixes applied + regression-tested
1. dangling `transform.data_processing_ref` → register a real numpress `data_processing` step.
2. `lossy_mz` could drift from `mz_chunking` → derive from the chunking strategy.
3. **reverse re-emitted OBSERVED extents as DECLARED `<scanSettings>` geometry** (defeated the Phase-25 guard) → emit IMS:1000042/43 only when `pixel_count_source == Declared`. New `Synthetic_InconsistentGrid` fixture + tests.
4. L2 rel-err not fail-closed on NaN/inf + divided by output → fail-closed; rel-err against the source value.
5. `merge_masked` ignored missing intensity (m/z longer than intensity) → length preflight.
6. over-exposed API tightened (`TransformRecord` fields `pub(crate)`).

## Health
- Build clean; **380 tests** (lib + integration) green; PXD001283 acceptance `#[ignore]` unchanged.
- E2E: forward convert + `--conformance l2 --verify` PASS; **mzPeakValidator validates the output** (catalog 1.3).
- Docs: README reframed (imzML + mzML, both directions; imaging = demonstrator pre-spec); spec / S3 example bucket / validator linked.

## Deferred / NON-BLOCKING (tracked, not gating the v0.7 release)
- **Phase 22** UPS-01/03 — chunk_series + mzPeakValidator PRs, HELD by owner.
- **Phase 29** DVN-01/02 — de-vendor, gated on chunk_series merged + mzdata 0.64.2 published to crates.io.
- **Phase 27 / SDRF** relocated to **v0.8** (`milestones/v0.8-DESIGN-DRAFT.md`, Phases 30–37).

## Minor finding (backlog)
- mzPeakValidator warns "no mzpeak version declared; defaulted to mzpeak-0.9" — the converter does not write a
  `mzpeak`/profile version into the archive. Small conformance gap → backlog 999.6.
