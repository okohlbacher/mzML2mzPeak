---
phase: 20-optical-image-auto-discovery-auto-embed
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - tests/fixtures/imaging/Synthetic_OpticalRef.imzML
  - tests/fixtures/imaging/Synthetic_OpticalRef.ibd
  - tests/fixtures/imaging/Synthetic_OpticalMultimodal.imzML
  - tests/fixtures/imaging/Synthetic_OpticalMultimodal.ibd
  - tests/fixtures/imaging/Synthetic_OpticalMissing.imzML
  - tests/fixtures/imaging/Synthetic_OpticalMissing.ibd
  - tests/fixtures/imaging/optical_2x2.png
  - tests/optical_autodiscovery.rs
  - docs/mzpeak-imaging-spec-suggestions.md
autonomous: true
requirements: [OPT-01, OPT-02, OPT-03, OPT-04]
must_haves:
  truths:
    - "Synthetic fixtures with IMS:1006008 (single TIFF, multimodal TIFF+PNG, and a missing-file ref) exist and drive end-to-end tests"
    - "Each synthetic fixture has a sibling .ibd whose UUID/SHA-1 match its imzML fileDescription so the ImagingReader preflight passes"
    - "Converting the IMS:1006008 fixture with NO --image produces an images/image_0000.* member + an images[] entry readable by MzPeakReader"
    - "A missing referenced image emits a warning and conversion still succeeds (exit 0, spectra present)"
    - "--image X + an imzML referencing X embeds X once; --image A + imzML→B yields image_0000=A, image_0001=B"
    - "A non-TIFF (.png) reference embeds verbatim with media_type image/png"
    - "docs Edit 7 documents the auto-discovery behavior + non-TIFF embed + soft-fail + dedup/ordering"
  artifacts:
    - path: "tests/optical_autodiscovery.rs"
      provides: "end-to-end auto-discovery integration tests over the synthetic fixtures + MzPeakReader read-back"
      contains: "MzPeakReader"
    - path: "tests/fixtures/imaging/Synthetic_OpticalRef.imzML"
      provides: "imzML with one IMS:1006008 → sibling TIFF fixture"
      contains: "IMS:1006008"
    - path: "tests/fixtures/imaging/Synthetic_OpticalRef.ibd"
      provides: "sibling .ibd whose UUID/checksum matches Synthetic_OpticalRef.imzML so preflight passes"
    - path: "docs/mzpeak-imaging-spec-suggestions.md"
      provides: "Edit 7 extended with auto-discovery + non-TIFF + soft-fail + dedup/order"
      contains: "IMS:1006008"
  key_links:
    - from: "tests/optical_autodiscovery.rs"
      to: "mzml2mzpeak::write::convert_with"
      via: "the real production embed seam"
      pattern: "convert_with"
    - from: "Synthetic_OpticalRef.imzML"
      to: "tests/fixtures/imaging/optical_4x3.tiff"
      via: "IMS:1006008 relative location"
      pattern: "optical_4x3.tiff"
    - from: "Synthetic_OpticalRef.imzML"
      to: "tests/fixtures/imaging/Synthetic_OpticalRef.ibd"
      via: "fileDescription UUID/SHA-1 ↔ .ibd preflight"
      pattern: "IMS:1000080"
---

<objective>
Prove OPT-01..04 end-to-end with synthetic fixtures and extend the spec doc. Build small imzML
fixtures carrying `IMS:1006008` (a single-TIFF case, a multimodal TIFF+PNG case, and a
missing-referenced-file case) — EACH with a sibling `.ibd` whose UUID/checksum match so the
ImagingReader preflight passes — an integration test suite that drives the REAL `convert_with` seam
with NO `--image` and reads the produced archive back via `MzPeakReader`, and the documentation
update to Edit 7 of the imaging-spec-suggestions doc.

Purpose: this is the acceptance plan — it does not introduce new behavior but locks the four
requirements behind checkable end-to-end assertions (auto-embed with no flag, soft-fail, dedup,
non-TIFF) and closes the "three places" doc obligation (no schema change since no ImageEntry field
was added). OPT-01..04 are listed in `requirements` here because this plan VERIFIES all four; the
canonical IMPLEMENTATIONS live in Plan 01 (OPT-01) and Plan 02 (OPT-02/03/04).

Output: three synthetic `.imzML` fixtures + their three sibling `.ibd` files + a tiny `.png` fixture,
`tests/optical_autodiscovery.rs`, and the extended Edit 7 spec section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-optical-image-auto-discovery-auto-embed/20-CONTEXT.md
@knowledge/cv/CV terms - optical image.md

<interfaces>
<!-- The production seam under test (Plans 01+02): -->
  pub fn convert_with(reader, out_path, image_paths: &[PathBuf], opts, geometry, input_path: Option<&Path>) -> Result<ConversionOutcome, WriteError>
  // input_path = Some(&imzml) triggers IMS:1006008 auto-discovery (Plan 02).
  pub fn convert(reader, out_path, image_paths) -> Result<(), WriteError>  // passes input_path=None ⇒ NO auto-discovery (back-compat)

<!-- Existing fixtures + test pattern to mirror (tests/image_import.rs): -->
  // tests/fixtures/imaging/Example_Processed.imzML — a 9-spectrum 3×3 MS1 grid, observed_max Nx=Ny=3.
  // tests/fixtures/imaging/Example_Processed.ibd — its matching sidecar (772 bytes); preflight checks
  //   the imzML fileDescription UUID + SHA-1 against THIS file. The new fixtures need their OWN sibling
  //   .ibd with a matching UUID/checksum (or reuse Example_Processed's UUID/checksum verbatim).
  // tests/fixtures/imaging/optical_4x3.tiff — a tiny valid 4×3 grayscale classic TIFF (W=4,H=3).
  // tests/image_import.rs — opens the fixture via ImagingReader, converts, re-reads with
  //   MzPeakReader, asserts metadata.imaging.images[] (the read-back pattern to reuse).
  // Example_Processed.imzML has NO <sampleList> — the new fixtures ADD a <sampleList>/<sample>
  //   carrying IMS:1006008 (the existing structure: cvList → fileDescription → referenceableParamGroupList → run).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: synthetic IMS:1006008 fixtures (single, multimodal, missing) + sibling .ibd + tiny PNG</name>
  <read_first>
    - tests/fixtures/imaging/Example_Processed.imzML (the full structure to base the fixtures on — cvList, fileDescription incl. the UUID/SHA-1 params, run, spectrumList; read fully)
    - tests/fixtures/imaging/Example_Processed.ibd EXISTS (772 bytes) — copy it to the new fixture stems so each synthetic imzML has a matching sibling .ibd
    - tests/image_import.rs:1-60 (the fixture conventions + optical_4x3.tiff dims W=4 H=3)
    - knowledge/cv/CV terms - optical image.md (IMS:1006008/11/12/13/15/17 accessions + value-types to author correct cvParams)
    - .planning/phases/20-optical-image-auto-discovery-auto-embed/20-CONTEXT.md (<specifics> "create small SYNTHETIC fixtures" list)
    - src/schema/optical.rs (the accessions parse_optical_images dispatches on — fixtures must match)
    - src/read/reader.rs (the ImagingReader preflight UUID-vs-.ibd / checksum check the fixtures must satisfy — grep for "UUID mismatch" / checksum)
  </read_first>
  <files>tests/fixtures/imaging/Synthetic_OpticalRef.imzML, tests/fixtures/imaging/Synthetic_OpticalRef.ibd, tests/fixtures/imaging/Synthetic_OpticalMultimodal.imzML, tests/fixtures/imaging/Synthetic_OpticalMultimodal.ibd, tests/fixtures/imaging/Synthetic_OpticalMissing.imzML, tests/fixtures/imaging/Synthetic_OpticalMissing.ibd, tests/fixtures/imaging/optical_2x2.png</files>
  <action>
    Author three synthetic imzML fixtures by COPYING the Example_Processed.imzML structure (same
    cvList/fileDescription/referenceableParamGroupList/run/spectrumList so they pass preflight + read)
    and INSERTING a `<sampleList count="1"><sample id="sample1"> … </sample></sampleList>` block
    carrying optical-image cvParams.

    .ibd SIDECARS (REQUIRED — the ImagingReader preflight validates each imzML's fileDescription
    UUID/SHA-1 against a sibling .ibd; a mismatch errors with "UUID mismatch" / checksum failure, so
    each synthetic imzML MUST have a matching sibling .ibd). Do BOTH of the following so they stay
    consistent: (a) COPY the committed tests/fixtures/imaging/Example_Processed.ibd to the three new
    stems — Synthetic_OpticalRef.ibd, Synthetic_OpticalMultimodal.ibd, Synthetic_OpticalMissing.ibd;
    (b) set each new .imzML's fileDescription UUID (IMS:1000080 universally-unique-identifier) AND its
    .ibd SHA-1 checksum (IMS:1000091 ibd SHA-1) to EXACTLY match the copied Example_Processed.ibd —
    the simplest correct path is to reuse Example_Processed.imzML's UUID + SHA-1 cvParam values
    VERBATIM (since each .ibd is a byte-for-byte copy of Example_Processed.ibd, its UUID + SHA-1 are
    identical). Do NOT invent fresh UUIDs/checksums — they would not match the copied bytes and would
    fail preflight late in Task 2.

    Fixture 1 — Synthetic_OpticalRef.imzML: one `<cvParam cvRef="IMS" accession="IMS:1006008"
    name="optical image location" value="optical_4x3.tiff"/>` (a RELATIVE sibling pointing at the
    committed tests/fixtures/imaging/optical_4x3.tiff), plus descriptive siblings: IMS:1006015 staining
    value="H&E", IMS:1006017 alignment method value="manual", IMS:1006011 (of-analysed-sample).

    Fixture 2 — Synthetic_OpticalMultimodal.imzML: TWO IMS:1006008 in one <sample> — value="optical_4x3.tiff"
    (TIFF) AND value="optical_2x2.png" (the new PNG below), exercising the ≥2-per-sample multimodal path
    and the non-TIFF embed.

    Fixture 3 — Synthetic_OpticalMissing.imzML: one IMS:1006008 value="does_not_exist.tiff" (a
    MISSING sibling) for the OPT-03 soft-fail test.

    Create tests/fixtures/imaging/optical_2x2.png: a minimal valid 2×2 PNG. Generate it from a tiny
    committed byte sequence (a known-valid 2×2 PNG is small) — write the raw bytes via a build step or
    commit them directly; it only needs to be a real PNG file (its bytes are embedded verbatim; no
    decoder runs). Confirm its first bytes are the PNG magic \x89PNG so is_tiff returns false.

    Keep every fixture SMALL (no large downloads — CI constraint). Do NOT edit the existing
    Example_Processed fixtures. NO fenced code blocks in this plan file; the fixtures themselves are XML/PNG.
  </action>
  <verify>
    <automated>ls -1 tests/fixtures/imaging/Synthetic_Optical*.imzML tests/fixtures/imaging/Synthetic_Optical*.ibd tests/fixtures/imaging/optical_2x2.png && grep -l "IMS:1006008" tests/fixtures/imaging/Synthetic_Optical*.imzML | wc -l && for f in tests/fixtures/imaging/Synthetic_Optical*.imzML; do uuid=$(grep -o 'IMS:1000080[^/]*value="[^"]*"' "$f" | grep -o 'value="[^"]*"'); ref=$(grep -o 'IMS:1000080[^/]*value="[^"]*"' tests/fixtures/imaging/Example_Processed.imzML | grep -o 'value="[^"]*"'); echo "$f uuid=$uuid ref=$ref"; [ "$uuid" = "$ref" ] && echo "  UUID MATCH" || echo "  UUID MISMATCH"; done</automated>
  </verify>
  <acceptance_criteria>
    - Three Synthetic_Optical*.imzML fixtures exist; all three contain an IMS:1006008 cvParam.
    - Three sibling .ibd files exist (Synthetic_OpticalRef.ibd, Synthetic_OpticalMultimodal.ibd, Synthetic_OpticalMissing.ibd), each a copy of Example_Processed.ibd.
    - Each synthetic imzML's fileDescription UUID (IMS:1000080) matches its sibling .ibd (reusing Example_Processed's UUID/SHA-1 verbatim) — verified by the Task-1 verify step BEFORE Task 2 runs, so a preflight mismatch surfaces here, not late in Task 2.
    - Synthetic_OpticalMultimodal.imzML contains exactly two IMS:1006008 cvParams (multimodal).
    - tests/fixtures/imaging/optical_2x2.png exists and begins with the PNG magic bytes (verifiable: first byte 0x89, then "PNG").
    - Each fixture opens via ImagingReader (preflight passes — UUID/checksum aligned to its sibling .ibd), provable in Task 2's test that converts them.
  </acceptance_criteria>
  <done>Synthetic_OpticalRef / Synthetic_OpticalMultimodal / Synthetic_OpticalMissing .imzML + their three sibling .ibd + optical_2x2.png committed; IMS:1006008 (+ descriptive attrs) present; each imzML UUID/SHA-1 matches its .ibd (preflight-valid, verified in Task 1); fixtures small.</done>
</task>

<task type="auto">
  <name>Task 2: end-to-end auto-discovery tests + Edit 7 spec extension</name>
  <read_first>
    - tests/image_import.rs (the full ImagingReader → convert → MzPeakReader → metadata.imaging.images[] read-back pattern to reuse — read fully)
    - src/write/convert.rs (convert_with signature + the auto-discovery seam from Plan 02)
    - src/schema/optical.rs (parse_optical_images — for any direct-parse assertions)
    - docs/mzpeak-imaging-spec-suggestions.md:105-165 (Edit 7 optical section to EXTEND — read fully)
    - .planning/phases/20-optical-image-auto-discovery-auto-embed/20-CONTEXT.md (<decisions> + <domain> for the behavior to document)
  </read_first>
  <files>tests/optical_autodiscovery.rs, docs/mzpeak-imaging-spec-suggestions.md</files>
  <action>
    Create tests/optical_autodiscovery.rs MIRRORING tests/image_import.rs's harness (ImagingReader →
    convert_with(Some(&imzml)) → MzPeakReader::new(out) → metadata.imaging.images[]). Write these
    end-to-end tests:

    (a) auto_embed_with_no_image_flag: convert Synthetic_OpticalRef.imzML with EMPTY --image and
    input_path=Some(&fixture); assert MzPeakReader opens the archive, metadata.imaging.images[] has one
    entry, archive_path == "images/image_0000.tiff", media_type "image/tiff", width 4, height 3 (the
    committed optical_4x3.tiff dims), and the produced archive has the expected spectrum count (spectra
    survived). (OPT-01)

    (b) descriptive_attrs_mapped: same fixture; assert the images[0] entry carries the H&E staining +
    alignment-method mapping (role "optical" + the modality/derived_subtype/provenance field the Plan-02
    mapping chose). (OPT-02)

    (c) missing_referenced_image_soft_fails: convert Synthetic_OpticalMissing.imzML with EMPTY --image;
    assert convert_with returns Ok, the archive opens, spectra are present, and NO images[] entry exists
    for the missing file (it was skipped). The conversion did NOT fail. (OPT-03)  — assert exit-success
    behavior at the library level (Ok) since the CLI just propagates it.

    (d) explicit_image_still_hard_fails: convert any fixture with --image pointing at a NON-EXISTENT
    path; assert convert_with returns Err (the --image strict contract is unchanged). (OPT-03 asymmetry)

    (e) dedup_same_path_embeds_once: convert Synthetic_OpticalRef.imzML (which references
    optical_4x3.tiff) WITH --image pointing at the SAME optical_4x3.tiff; assert images[] has exactly
    ONE entry for that file (dedup by canonical path). (OPT-04)

    (f) ordering_image_first_then_discovered: convert Synthetic_OpticalRef.imzML (references
    optical_4x3.tiff) WITH --image pointing at a DIFFERENT image; assert images[0] is the --image one
    (image_0000) and images[1] is the auto-discovered one (image_0001). (OPT-04)

    (g) non_tiff_embeds_verbatim: convert Synthetic_OpticalMultimodal.imzML (TIFF + PNG) with EMPTY
    --image; assert images[] has TWO entries, one media_type "image/tiff" and one "image/png" with
    width 0/height 0 (the PNG verbatim, dims omitted). (OPT-01 non-TIFF)

    Then EXTEND docs/mzpeak-imaging-spec-suggestions.md Edit 7 (the v0.5 "optical images as separate
    TIFF ZIP members" section): add a subsection documenting (1) forward AUTO-DISCOVERY — the converter
    follows the source imzML's IMS:1006008 location(s), resolves relative to the .imzML, and embeds with
    no --image flag; (2) the non-TIFF generalization — images are no longer TIFF-only; non-TIFF formats
    embed verbatim with media_type by extension and width/height omitted, TIFF (incl .svs by magic) gets
    first-IFD dims; (3) SOFT-FAIL — a missing/unreadable auto-discovered image warns and is skipped
    (auxiliary, not part of the L1 contract), while explicit --image stays hard-fail; (4) coexist +
    dedup + the deterministic ordering (--image first, then discovered in document order, same resolved
    path embedded once); (5) descriptive-attr capture (IMS:1006013/15/17 + subject → role/derived_subtype/
    modality + provenance). Note explicitly that NO schema change was needed (ImageEntry's existing
    IMG-05 fields cover the mapping) so the three-places rule is satisfied by src + doc with schema
    unchanged. Tag the additions consistently with the doc's existing version-tag style. NO fenced code
    blocks in THIS plan file.
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -5 && cargo test --test optical_autodiscovery 2>&1 | tail -30 && grep -c "IMS:1006008" docs/mzpeak-imaging-spec-suggestions.md</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` succeeds (CLI + lib compile).
    - `cargo test --test optical_autodiscovery` passes all seven tests (a)–(g).
    - Test (a) proves auto-embed with NO --image: images/image_0000.tiff member + images[] entry, spectra present.
    - Test (c) proves the missing-ref conversion returns Ok with spectra present and no entry for the missing file (soft-fail), while test (d) proves --image to a missing path still Errs.
    - Test (e) proves --image X + imzML→X yields ONE images[] entry; test (f) proves --image-first ordering.
    - Test (g) proves the PNG embeds verbatim with media_type "image/png".
    - docs/mzpeak-imaging-spec-suggestions.md Edit 7 mentions auto-discovery + non-TIFF + soft-fail + dedup/order (grep for IMS:1006008 count increased; the new subsection present).
  </acceptance_criteria>
  <done>tests/optical_autodiscovery.rs covers OPT-01..04 end-to-end over the synthetic fixtures with MzPeakReader read-back, all green; Edit 7 extended; schema unchanged (documented).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Synthetic fixture imzML IMS:1006008 → resolved path | The fixtures EXERCISE the attacker-influenced location path (relative sibling, missing file). A traversal fixture is optional but the missing-file fixture proves soft-fail does not crash. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Tampering | test fixtures pointing at real sibling files | accept | Fixtures reference only committed siblings under tests/fixtures/imaging; the missing-file fixture proves soft-fail, not a traversal exploit (the traversal rejection is unit-tested in Plan 01 + integration-tested in Plan 02). |
| T-20-SC | Tampering | npm/pip/cargo installs | mitigate | This plan adds NO new crates (PNG bytes are committed verbatim; .ibd sidecars are copies of an existing fixture; tests reuse serde_json + MzPeakReader already pinned) — no package legitimacy gate needed. |
</threat_model>

<verification>
- `cargo build` + `cargo test --test optical_autodiscovery` pass.
- All four requirements (OPT-01..04) have at least one green end-to-end assertion.
- Each synthetic fixture's UUID/SHA-1 matches its sibling .ibd (preflight passes — confirmed in Task 1).
- Edit 7 documents the new behavior; schema/imaging.json unchanged (no ImageEntry field added).
</verification>

<success_criteria>
- Synthetic IMS:1006008 fixtures (single / multimodal / missing) + their sibling .ibd + tiny PNG committed and preflight-valid.
- End-to-end tests prove auto-embed-without-flag, soft-fail asymmetry, dedup, ordering, non-TIFF verbatim, descriptive mapping — all via the real convert seam + MzPeakReader.
- Spec doc Edit 7 extended; three-places obligation met (src + doc, schema unchanged by design).
</success_criteria>

<output>
Create `.planning/phases/20-optical-image-auto-discovery-auto-embed/20-03-SUMMARY.md` when done.
</output>
</content>
