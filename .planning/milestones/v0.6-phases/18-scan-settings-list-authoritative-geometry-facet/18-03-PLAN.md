---
phase: 18-scan-settings-list-authoritative-geometry-facet
plan: 03
type: execute
wave: 3
depends_on: ["18-02"]
files_modified:
  - tests/scan_settings.rs
autonomous: true
requirements: [GEO-03]
must_haves:
  truths:
    - "Feeding a DECLARED-geometry source (Synthetic_FullGeometry: grid 3×3, pixel 100µm, max dim 300µm, scan-pattern child terms) through BOTH scan_settings_list_from_geometry AND assemble_imaging_metadata yields an imaging block whose geometry EQUALS the geometry carried in scan_settings_list — proven NON-VACUOUSLY over real declared 3×3/100µm/300µm values"
    - "scan_settings_list params use the correct IMS accessions and the UO:0000017 µm unit on the µm geometry terms (and NO unit on grid + scan-pattern terms)"
    - "Converting a committed fixture through the public convert path and re-opening the archive shows a well-formed scan_settings_list block (id + parameters[]) in file_index().metadata — proving emission + index-written-last wiring through the public seam"
  artifacts:
    - path: "tests/scan_settings.rs"
      provides: "GEO-03 two-level proof: (1) geometry-projection derived-copy test over declared geometry, (2) convert-path structural emission test over the public seam"
      min_lines: 90
  key_links:
    - from: "tests/scan_settings.rs"
      to: "src/schema/scan_settings.rs::scan_settings_list_from_geometry + src/write/writer.rs::assemble_imaging_metadata"
      via: "feed one parsed ImagingRunMetadata into both projections + assert geometry equality"
      pattern: "scan_settings_list_from_geometry"
    - from: "tests/scan_settings.rs"
      to: "MzPeakReader.file_index().metadata"
      via: "read scan_settings_list block back from the produced archive (convert-path test)"
      pattern: "file_index\\(\\)\\.metadata"
---

<objective>
Prove GEO-03 NON-VACUOUSLY with a TWO-LEVEL proof, working around the fixture reality that NO
single committed fixture pairs a DECLARED scanSettings grid WITH a paired `.ibd` (the only
declared-geometry fixture, `Synthetic_FullGeometry.imzML`, is parse-only — it has no `.ibd`; the
convertible fixtures `Example_Processed` / `Example_Continuous` declare NO scanSettings grid):

  LEVEL 1 — GEOMETRY-PROJECTION derived-copy test (the meaningful, non-vacuous one): parse the
  declared-geometry fixture, feed the resulting `ImagingRunMetadata` into BOTH
  `scan_settings_list_from_geometry` AND `assemble_imaging_metadata`, and assert the imaging-block
  geometry fields EQUAL the geometry carried in the scan_settings_list params, over REAL declared
  3×3 grid / 100µm pixel / 300µm max-dim / scan-pattern child-term values. No `.ibd` required.

  LEVEL 2 — CONVERT-PATH structural emission test (wiring): convert the `Example_Processed`
  fixture through the public convert seam and re-open the archive with the reference
  `MzPeakReader`, asserting the file-level metadata CONTAINS a well-formed `scan_settings_list`
  entry (id + parameters[]) — proving emission + index-written-last wiring through the public path.

Document IN THIS PLAN that the full convert-path-WITH-declared-geometry is covered at the
geometry-projection level (Level 1) precisely because no fixture pairs a declared grid with an
`.ibd`; Level 2 proves the facet is emitted through the public seam (geometry may be sparse for
Example_Processed — that is acceptable for the wiring assertion).

Purpose: lock the authoritative-facet ⇔ derived-copy invariant (Level 1) AND the public-seam
emission wiring (Level 2) so any future drift fails loudly in CI — without a vacuous test.
Output: `tests/scan_settings.rs` integration test (two `#[test]` functions).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-scan-settings-list-authoritative-geometry-facet/18-CONTEXT.md

<interfaces>
<!-- FIXTURE REALITY (confirmed) — NO single fixture pairs a declared grid WITH an .ibd:
     - tests/fixtures/imaging/Synthetic_FullGeometry.imzML: DECLARED geometry (IMS:1000042/43 grid
       3×3; IMS:1000046/47 pixel size 100µm; IMS:1000044/45 max dim 300µm; scan-pattern child terms
       IMS:1000401/413/480/491) but NO paired .ibd → PARSE-ONLY (geometry_parse.rs::full_geometry
       calls parse_scan_settings on it; it CANNOT go through the full convert path).
     - tests/fixtures/imaging/Example_Processed.imzML: HAS .ibd (convertible via the cv_list.rs
       seam) but declares NO scanSettings grid.
     ⇒ Level 1 uses Synthetic_FullGeometry (parse + project, no convert/.ibd); Level 2 uses
       Example_Processed (convert through the public seam, geometry sparse). -->
const FULL_GEOMETRY: &str = "tests/fixtures/imaging/Synthetic_FullGeometry.imzML"; // declared, no .ibd
const PROCESSED_FIXTURE: &str = "tests/fixtures/imaging/Example_Processed.imzML";  // .ibd, no grid

<!-- LEVEL 1 entry points (pure, no archive, no .ibd): -->
mzml2mzpeak::schema::parse_scan_settings(&Path) -> Result<ImagingRunMetadata, _>
mzml2mzpeak::schema::scan_settings_list_from_geometry(&ImagingRunMetadata) -> Vec<ScanSettings>
// assemble_imaging_metadata is in src/write/writer.rs — Plan 18-02 exposes it pub(crate)
// (or test via the write_run_metadata + imaging_metadata() seam if pub(crate) is unavailable).
mzml2mzpeak::write::writer::assemble_imaging_metadata(Some(&geom)) -> ImagingMetadata
// ImagingMetadata fields (src/schema/metadata.rs): pixel_count{x,y}, pixel_size_um{x,y} (AxisPair<f64>),
//   max_dimension_um{x,y} (AxisPair<i64>), absolute_offset_um{x,y}, scan_pattern (CURIE String).
// ScanSettingsParam fields (src/schema/scan_settings.rs): cv_ref/accession/name/value/unit_cv_ref/unit_accession.

<!-- LEVEL 2 convert+read-back seam to mirror EXACTLY (tests/cv_list.rs):
     ImagingReader::open(fixture) -> convert_with(reader, &out, &[], &EncodingOptions::legacy(),
     Some(&geom)) -> MzPeakReader::new(&out) -> file_index().metadata.get("scan_settings_list").
     Use the geometry-aware convert_with(Some(&geom)) (geom parsed from the fixture, even if sparse)
     so the facet is actually EMITTED through the public path — the back-compat convert() wrapper
     passes geometry=None and would OMIT the key (per Plan 18-02), so it is NOT usable for Level 2.
     Committed fixtures only; NO --image, NO network/.ibd beyond what Example_Processed satisfies. -->
mzml2mzpeak::read::ImagingReader::open(path) -> ImagingReader
mzml2mzpeak::write::{convert_with, EncodingOptions}
mzpeak_prototyping::MzPeakReader::new(&out).file_index().metadata: HashMap<String, serde_json::Value>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LEVEL 1 — geometry-projection derived-copy test over DECLARED geometry (non-vacuous, no .ibd)</name>
  <files>tests/scan_settings.rs</files>
  <read_first>
    - tests/geometry_parse.rs::full_geometry (proves Synthetic_FullGeometry parses to grid 3×3, pixel 100µm, max dim 300µm, child terms IMS:1000401/413/480/491 — the EXACT declared values to assert against)
    - src/schema/scan_settings.rs (scan_settings_list_from_geometry + ScanSettingsParam field names cv_ref/accession/name/value/unit_cv_ref/unit_accession)
    - src/write/writer.rs lines 526-564 (assemble_imaging_metadata — the projection that must EQUAL the facet; how to reach it: pub(crate) per 18-02, else the write_run_metadata + imaging_metadata() seam)
    - src/schema/metadata.rs (ImagingMetadata field names: pixel_count{x,y}, pixel_size_um{x,y}, max_dimension_um{x,y}, scan_pattern — the derived-copy targets)
  </read_first>
  <behavior>
    TDD DISCIPLINE: write these assertions FIRST, then run. The test is meaningful BECAUSE the
    Synthetic_FullGeometry fixture carries REAL declared 3×3 / 100µm / 300µm geometry — the equality
    assertions exercise actual values, not None==None (non-vacuous).
    - parse_scan_settings(Synthetic_FullGeometry) yields grid_x/y=3, pixel_size_x/y=100.0, max_dimension_x/y=300, scan_pattern=IMS:1000413 (+ the other three child terms).
    - scan_settings_list_from_geometry(&geom): exactly one entry; IMS:1000042/43 values "3"/"3" (NO unit); IMS:1000046/47 values "100" (unit_cv_ref="UO", unit_accession="UO:0000017"); IMS:1000044/45 values "300" (unit "UO:0000017"); a presence-only IMS:1000413 scan-pattern param (value empty/None, NO unit).
    - assemble_imaging_metadata(Some(&geom)): pixel_count{x:3,y:3}; pixel_size_um{x:100.0,y:100.0}; max_dimension_um{x:300,y:300}; scan_pattern="IMS:1000413".
    - EQUALITY (derived copy): imaging pixel_count.x/y == facet IMS:1000042/43 values; imaging pixel_size_um.x/y == facet IMS:1000046/47 values; imaging max_dimension_um.x/y == facet IMS:1000044/45 values; imaging scan_pattern CURIE == the facet's presence-only scan-pattern param accession.
  </behavior>
  <action>
    Create `tests/scan_settings.rs` with a Level-1 test `geometry_projection_derived_copy_matches`.
    `let geom = parse_scan_settings(Path::new(FULL_GEOMETRY)).expect(...)`. Feed `&geom` into BOTH
    `scan_settings_list_from_geometry(&geom)` and `assemble_imaging_metadata(Some(&geom))` (reach
    assemble_imaging_metadata via the `pub(crate)`/public visibility Plan 18-02 provides; if only
    the write_run_metadata + imaging_metadata() seam is reachable, drive it through that — prefer
    the least-invasive path the codebase exposes).

    Assert, over the REAL declared values:
    (1) ACCESSION/UNIT SHAPE on the facet: IMS:1000046/47 and IMS:1000044/45 params carry
        unit_cv_ref=="UO" and unit_accession=="UO:0000017"; IMS:1000042/43 grid params and the
        presence-only IMS:1000413 scan-pattern param carry NO unit_accession; the scan-pattern param
        has cv_ref/accession/name set and value empty/None.
    (2) DERIVED-COPY EQUALITY: imaging pixel_count.x/y EQUAL facet IMS:1000042/43 values (3,3);
        imaging pixel_size_um.x/y EQUAL facet IMS:1000046/47 (100,100); imaging max_dimension_um.x/y
        EQUAL facet IMS:1000044/45 (300,300); imaging scan_pattern CURIE EQUALS the facet
        scan-pattern param accession (IMS:1000413). Parse param `value` strings to numbers where the
        imaging field is numeric (compare 3 == "3".parse(), 100.0 == "100".parse(), etc.).
    Add a doc-comment at the top of this test stating WHY Level 1 stands in for the full
    convert-path-with-declared-geometry: no committed fixture pairs a declared grid with an `.ibd`,
    so the authoritative-facet ⇔ derived-copy invariant over declared geometry is proven at the
    projection level here; Level 2 (Task 2) proves public-seam emission separately.
  </action>
  <verify>
    <automated>cargo test --test scan_settings geometry_projection_derived_copy_matches</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test scan_settings geometry_projection_derived_copy_matches` passes.
    - The test feeds the SAME parsed ImagingRunMetadata (declared 3×3/100µm/300µm from Synthetic_FullGeometry) into both scan_settings_list_from_geometry AND assemble_imaging_metadata.
    - Equality is asserted over REAL declared values (grid 3×3, pixel 100µm, max dim 300µm, scan_pattern IMS:1000413) — NON-VACUOUS (not None==None).
    - µm params IMS:1000044/45/46/47 carry unit_accession "UO:0000017"; grid IMS:1000042/43 + the scan-pattern param carry NO unit.
    - A doc-comment records that Level 1 covers declared-geometry derived-copy because no fixture pairs a declared grid with an .ibd.
  </acceptance_criteria>
  <done>The derived-copy invariant is proven non-vacuously over real declared geometry, with no .ibd dependency.</done>
</task>

<task type="auto">
  <name>Task 2: LEVEL 2 — convert-path structural emission test through the public seam</name>
  <files>tests/scan_settings.rs</files>
  <read_first>
    - tests/cv_list.rs (the convert+reopen+metadata.get seam to mirror exactly — temp_out, open_fixture, convert/MzPeakReader::new, file_index().metadata.get, remove_file cleanup)
    - src/write/convert.rs (convert_with signature after 18-02 — the geometry-aware path that actually EMITS the facet; the back-compat convert() passes None and omits the key)
    - src/schema/scan_settings.rs (the param shape: id + parameters[] with cv_ref/accession/name — what a well-formed entry looks like)
  </read_first>
  <action>
    Add a Level-2 test `convert_path_emits_scan_settings_list` to the SAME `tests/scan_settings.rs`.
    Mirror tests/cv_list.rs structure (temp_out helper, open the committed Example_Processed
    fixture, MzPeakReader::new, file_index().metadata, remove_file cleanup). Drive emission through
    the PUBLIC seam with the geometry-aware path so the facet is actually written:
    `let geom = parse_scan_settings(Path::new(PROCESSED_FIXTURE)).expect(...)` (sparse/all-None is
    fine — Example_Processed declares no grid), then
    `convert_with(reader, &out, &[], &EncodingOptions::legacy(), Some(&geom))`. (Do NOT use the
    back-compat `convert(reader,&out,&[])` here — it passes geometry=None and OMITS the
    scan_settings_list key per Plan 18-02, which would make this test unable to observe emission.)

    Assertions (STRUCTURAL — geometry may be sparse for this fixture; that is acceptable, this test
    proves the facet is emitted through the public path + index-written-last wiring):
    (1) `metadata.get("scan_settings_list")` is `Some` and a JSON array with at least one entry.
    (2) The first entry is WELL-FORMED: required key `id` (non-empty string) and `parameters`
        (a JSON array, possibly empty for a sparse fixture).
    (3) Every param present (if any) carries the required keys `cv_ref`, `accession`, `name`
        (string, non-empty) — proving the emitted shape matches schema/scan_settings.json.
    Clean up the temp archive at the end (mirror cv_list.rs `remove_file`).
  </action>
  <verify>
    <automated>cargo test --test scan_settings convert_path_emits_scan_settings_list</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test scan_settings convert_path_emits_scan_settings_list` passes.
    - The test converts Example_Processed through the public `convert_with(.., Some(&geom))` seam (NOT the convert() wrapper) and reads scan_settings_list back via MzPeakReader.file_index().metadata.
    - The block is asserted PRESENT and well-formed: array with ≥1 entry, each entry has id (non-empty string) + parameters (array); any present param has cv_ref/accession/name.
    - Geometry may be sparse (Example_Processed declares no grid) — the test does NOT require declared geometry; it proves emission + index-written-last wiring through the public path.
  </acceptance_criteria>
  <done>The scan_settings_list facet is proven to be emitted through the public convert seam and read back well-formed by the reference reader.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| produced archive → MzPeakReader (Level 2) | Read-back of the converter's own output via the reference reader. No external untrusted input; committed fixture only. No HIGH threats. |
| declared-geometry fixture → projections (Level 1) | Pure parse + project, no archive/.ibd; consumes the already-validated ImagingRunMetadata. No new untrusted-input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Tampering | silent facet⇔imaging drift over DECLARED geometry shipping undetected | mitigate | Level 1 is the non-vacuous gate: equality assertions over real 3×3/100µm/300µm fail CI on any divergence between the authoritative facet and its derived copy. |
| T-18-06 | Tampering | public-seam emission/wiring regressing (facet silently not written) | mitigate | Level 2 converts through the public convert_with seam and asserts the block is present + well-formed in the produced archive — a dropped emission fails CI. |
</threat_model>

<verification>
- `cargo test --test scan_settings` green (both Level 1 and Level 2 tests).
- Level 1 depends only on the committed parse-only Synthetic_FullGeometry fixture (no `.ibd`).
- Level 2 depends only on the committed Example_Processed fixture (no `--image`/network beyond its `.ibd`).
</verification>

<success_criteria>
GEO-03: the derived-copy invariant is proven NON-VACUOUSLY over real declared geometry (Level 1:
Synthetic_FullGeometry 3×3/100µm/300µm projected into both scan_settings_list_from_geometry and
assemble_imaging_metadata, geometry equal with correct IMS accessions + UO:0000017 µm unit), and the
facet is proven to be EMITTED through the public convert seam and read back well-formed (Level 2:
Example_Processed convert_with(Some(&geom)) → MzPeakReader.file_index().metadata contains a
well-formed scan_settings_list). The two-fixture split is documented because no single fixture pairs
a declared grid with an `.ibd`.
</success_criteria>

<output>
Create `.planning/phases/18-scan-settings-list-authoritative-geometry-facet/18-03-SUMMARY.md` when done
</output>
</content>
</invoke>
