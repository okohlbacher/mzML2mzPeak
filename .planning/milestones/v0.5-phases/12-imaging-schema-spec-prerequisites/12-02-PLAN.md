---
phase: 12-imaging-schema-spec-prerequisites
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/mzpeak-imaging-spec-suggestions.md
autonomous: true
requirements: [SPEC-01]
must_haves:
  truths:
    - "Spec doc Edit 7 describes TIFF-only optical images stored as separate ZIP members images/image_NNNN.tiff registered Other, with descriptive metadata (incl. sha256/size/affine) in metadata.imaging.images[]"
    - "The images.parquet-blob + CV-registration design is preserved but clearly marked as a future/richer option F8 (not v1)"
    - "Edit 8 (imaging index block) shows mz_range, pixel_count_source, images[], and a note that index.json is written last"
    - "The doc's schema/imaging.json snippet matches the in-repo schema (pixel_count optional, max_dimension_um integer, new fields present)"
  artifacts:
    - path: "docs/mzpeak-imaging-spec-suggestions.md"
      provides: "rewritten Edit 7 (TIFF-separate-file design), updated Edit 8, F8 future-option, F1-corrected imaging.json snippet"
      contains: "images/image_"
  key_links:
    - from: "docs/mzpeak-imaging-spec-suggestions.md (Edit 8 / Part B imaging.json snippet)"
      to: "schema/imaging.json (in-repo)"
      via: "the doc snippet mirrors the in-repo schema field set and types"
      pattern: "pixel_count_source"
---

<objective>
Rewrite spec-doc **Edit 7** to the locked v0.5 TIFF design and update **Edit 8** + the doc's
`schema/imaging.json` snippet so the spec matches the in-repo schema and the chosen storage approach.
TIFF images become separate ZIP members (`images/image_NNNN.tiff`) registered in `FileIndex` as
`Other` (name-only), with all descriptive metadata (incl. `sha256`/`size_bytes`/`affine`) living in
`metadata.imaging.images[]`. The previous `images.parquet`-blob + CV-registration design is demoted to
a clearly-marked future/richer option (F8). Apply the F1 self-corrections (`pixel_count` optional,
`max_dimension_um` integer) to the doc's schema snippet, add `mz_range`/`pixel_count_source`/`images[]`,
and add the "index.json written last" note.

Purpose: The spec doc must match the chosen TIFF design and the in-repo schema before Phases 13/15 cite
it (CODEX BLOCKER-3). Delivers SPEC-01.
Output: Updated `docs/mzpeak-imaging-spec-suggestions.md` (Edit 7 rewritten, Edit 8 updated, Part B
imaging.json snippet F1-corrected + new fields, F8 future option marked, Part C inventory in sync).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-imaging-schema-spec-prerequisites/12-CONTEXT.md
@.planning/NEXT-ROADMAP-DRAFT.md

<read_first>
- docs/mzpeak-imaging-spec-suggestions.md — Edit 7 (lines ~95-105, the `image` entity / images.parquet
  blob design to REWRITE), Edit 8 (lines ~107-122, the index block to UPDATE), Part B `schema/imaging.json`
  snippet (lines ~212-232, the F1 corrections + new fields), Part C change inventory (lines ~236-264).
- .planning/NEXT-ROADMAP-DRAFT.md section U2 (lines 48-94) — the LOCKED TIFF-separate-member design: ZIP
  API (start_other/add_file_from_read), images/image_NNNN.tiff naming, FileEntry name-only / descriptive
  metadata in images[], affine convention (1-based top-left y-down full-extent, a=(Nx-1)/(W-1),
  e=(Ny-1)/(H-1), maps "image_px -> ms_px", registration_quality "assumed_full_extent"), and the
  illustrative index.json shape (lines 81-93). Section B F8 (line 111) = the future-rich blob+CV design home.
- .planning/phases/12-imaging-schema-spec-prerequisites/12-CONTEXT.md — LOCKED spec-doc decisions.
- schema/imaging.json (in-repo, AFTER plan 12-01 lands its fields) — the snippet in the doc must mirror
  it. NOTE: this plan is wave 1 alongside 12-01; if 12-01 has not yet updated the in-repo schema, mirror
  the LOCKED field set from 12-CONTEXT / NEXT-ROADMAP-DRAFT section U2 (they are identical by design) so
  doc and repo converge regardless of execution order.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Edit 7 to TIFF-separate-ZIP-member design; demote blob/CV design to F8</name>
  <files>docs/mzpeak-imaging-spec-suggestions.md</files>
  <action>
    Replace the body of **Edit 7** (the "New image entity" / `images.parquet` blob + CV-registration
    text) with the locked v0.5 design: optical images are TIFF-only, stored as separate ZIP members
    named `images/image_NNNN.tiff` (NNNN = 0-based import order), added through the writer's ZIP API
    (`ZipArchiveWriter::start_other` / `add_file_from_read`) and registered in `FileIndex` as an `Other`
    entry by member name ONLY (the `FileEntry` carries name/entity_type/data_kind and CANNOT hold
    descriptive fields). State that ALL per-image descriptive metadata lives in
    `metadata.imaging.images[]` keyed to the member by `archive_path`: `archive_path`, `source_name`
    (original basename), `media_type` ("image/tiff"), `width`, `height`, `sha256`, `size_bytes`, and an
    `affine` object. Describe the affine as a 1-based, top-left-origin, y-down, full-extent display hint
    (NOT true registration): matrix [a,0,1,0,e,1] with (x_ms,y_ms)=(a*col+b*row+c, d*col+e*row+f),
    a=(Nx-1)/(W-1), e=(Ny-1)/(H-1), degenerate W==1/H==1 means that axis maps to constant 1,
    maps:"image_px -> ms_px", registration_quality:"assumed_full_extent". Note: dimensions read from
    the first IFD via the `tiff` crate; bytes copied verbatim; no EXIF/orientation correction; reverse
    image export is OUT OF SCOPE for v0.5. Then DEMOTE the prior design: keep the `images.parquet`
    LargeBinary blob + image-role/modality + CV-registration-term text but move it under a clearly
    labelled subheading such as "Future / richer option (F8 — deferred, NOT v0.5)" so it is preserved
    as the future-rich path, not the v1 spec. Do not delete `schema/image.json` from Part B; mark it as
    governing the F8 future option.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/mzML2mzPeak && grep -q 'images/image_' docs/mzpeak-imaging-spec-suggestions.md && grep -q 'metadata.imaging.images' docs/mzpeak-imaging-spec-suggestions.md && grep -q 'assumed_full_extent' docs/mzpeak-imaging-spec-suggestions.md && grep -qiE 'F8.*(future|defer)|(future|richer) option' docs/mzpeak-imaging-spec-suggestions.md && echo EDIT7_OK</automated>
  </verify>
  <acceptance_criteria>
    - Edit 7 mentions images/image_NNNN.tiff (grep images/image_ matches), metadata.imaging.images[],
      and registration_quality:"assumed_full_extent".
    - The blob/CV design is retained under an explicit F8 future-option label (grep matches; prints EDIT7_OK).
    - The ZipArchiveWriter Other-entry name-only storage contract is stated in prose.
  </acceptance_criteria>
  <done>Edit 7 rewritten to TIFF-separate-member design with affine-in-index; blob/CV design preserved but marked F8 future option.</done>
</task>

<task type="auto">
  <name>Task 2: Update Edit 8 + Part B imaging.json snippet + Part C inventory to match in-repo schema</name>
  <files>docs/mzpeak-imaging-spec-suggestions.md</files>
  <action>
    Update **Edit 8** (the imaging index block) so the example JSON shows the v0.5 shape: add
    mz_range{min,max}, pixel_count_source (illustrate the value "observed_max"), and an images[] array
    with one entry mirroring the section-U2 shape (archive_path, source_name, media_type, width, height,
    sha256, size_bytes, affine{type,matrix,maps,registration_quality}). Add a sentence stating
    index.json (mzpeak_index.json) is written LAST — after the full spectrum pass and after any image
    members are added — because the aggregates (pixel_count observed_max, mz_range) depend on the full
    pass. Then update the Part B `schema/imaging.json` snippet to MATCH the in-repo schema: apply the F1
    self-corrections — change the snippet's required from ["is_imaging", "pixel_count"] to
    ["is_imaging", "coordinate_base"] (pixel_count optional), and change max_dimension_um x/y from number
    to integer; then ADD pixel_count.z (integer), pixel_count_source (enum declared|observed_max),
    mz_range (object min/max number), and images (array of items with the eight fields + an affine const
    sub-object), keeping additionalProperties:false (top-level and on the new nested objects). Finally
    update the Part C change inventory row #7 ("image entity + registration") to reflect the
    TIFF-separate-member v1 design with the blob design noted as F8, and ensure row #8
    (metadata.imaging block) notes the new fields. The doc snippet and the in-repo `schema/imaging.json`
    (plan 12-01) must end up field-for-field consistent.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/mzML2mzPeak && grep -q 'mz_range' docs/mzpeak-imaging-spec-suggestions.md && grep -q 'pixel_count_source' docs/mzpeak-imaging-spec-suggestions.md && grep -qiE 'written last|index.*last' docs/mzpeak-imaging-spec-suggestions.md && grep -qF '"is_imaging", "coordinate_base"' docs/mzpeak-imaging-spec-suggestions.md && ! grep -qF '"is_imaging", "pixel_count"' docs/mzpeak-imaging-spec-suggestions.md && echo EDIT8_OK</automated>
    <automated>cd /Users/kohlbach/Claude/mzML2mzPeak && python3 -c "import re,json; t=open('docs/mzpeak-imaging-spec-suggestions.md').read(); m=[b for b in re.findall(r'\`\`\`json(.*?)\`\`\`', t, re.S) if '\"title\": \"imaging\"' in b]; assert m, 'imaging.json snippet missing'; d=json.loads(m[0]); md=d['properties']['max_dimension_um']['properties']; assert md['x']['type']=='integer' and md['y']['type']=='integer', 'max_dimension_um must be integer'; assert {'images','mz_range','pixel_count_source'} <= set(d['properties']); print('SNIPPET_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Edit 8 example shows mz_range, pixel_count_source, and images[]; a "written last" note is present.
    - Part B schema/imaging.json snippet required = ["is_imaging", "coordinate_base"] (NOT
      ["is_imaging", "pixel_count"]), max_dimension_um x/y are integer, and the new fields
      (pixel_count.z, pixel_count_source, mz_range, images) are present with additionalProperties:false.
    - The grep chain prints EDIT8_OK (mz_range + pixel_count_source + written-last + corrected required +
      old required absent).
    - The doc snippet is field-for-field consistent with the in-repo schema/imaging.json from plan 12-01.
  </acceptance_criteria>
  <done>Edit 8 updated with mz_range/pixel_count_source/images[]/written-last note; Part B imaging.json snippet F1-corrected + new fields; Part C inventory in sync; doc matches in-repo schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec doc -> implementer/reader | The spec doc is the normative reference Phases 13/15 and external readers follow; a doc that contradicts the in-repo schema or the chosen storage design produces non-conformant archives. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Repudiation | doc vs in-repo schema drift | mitigate | Task 2 forces the doc's imaging.json snippet to match the in-repo schema (required set, integer max_dimension_um, new fields, additionalProperties:false); grep gate + 12-01's schema-agreement tests catch drift. |
| T-12-04 | Tampering | loose image-storage contract in spec | mitigate | Edit 7 pins TIFF-only, deterministic member name, name-only FileEntry, const-pinned affine fields — a malformed/over-broad image block has no normative home (blob path explicitly deferred to F8, not v1). |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Doc-only change; this plan installs nothing. No package-manager surface. |
</threat_model>

<verification>
- Task 1 grep chain: Edit 7 carries images/image_, metadata.imaging.images, assumed_full_extent, and an
  F8 future-option marker (prints EDIT7_OK).
- Task 2 grep chain: Edit 8 / Part B carry mz_range, pixel_count_source, a written-last note, the
  corrected ["is_imaging", "coordinate_base"] required, and NO ["is_imaging", "pixel_count"]
  (prints EDIT8_OK).
- Cross-check by eye that the Part B snippet field set equals the in-repo schema/imaging.json from plan
  12-01 (same property names + types).
</verification>

<success_criteria>
- SPEC-01 satisfied: Edit 7 rewritten to the TIFF-separate-ZIP-member + affine-in-index design with the
  blob/CV design demoted to a clearly-marked F8 future option; Edit 8 updated with mz_range,
  pixel_count_source, images[], and the index-written-last note; the F1 self-corrections applied to the
  doc's schema/imaging.json snippet so doc and repo agree.
- Both grep gates print their OK sentinels.
</success_criteria>

<output>
Create `.planning/phases/12-imaging-schema-spec-prerequisites/12-02-SUMMARY.md` when done.
Record: where the F8 future-option block now lives, and confirmation that the Part B snippet matches the
in-repo schema field-for-field.
</output>
