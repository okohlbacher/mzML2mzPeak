---
phase: 05-verification-roundtrip-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/verify/mod.rs
  - src/verify/report.rs
  - src/verify/compare.rs
  - src/lib.rs
autonomous: true
requirements: [VER-03]
must_haves:
  truths:
    - "The crate compiles with a new `verify` module exposing VerificationReport, Mismatch, VerifyError"
    - "The per-axis comparator detects an L1 (Δ=0) mismatch at the source's stored float width without widening f32→f64"
    - "The per-axis comparator accepts an L2 relative-error tolerance per axis"
  artifacts:
    - path: "src/verify/mod.rs"
      provides: "verify module barrel re-exporting the report + comparator public surface"
      contains: "pub mod report"
    - path: "src/verify/report.rs"
      provides: "VerificationReport, per-check result structs, Mismatch, VerifyError (thiserror)"
      contains: "pub struct VerificationReport"
    - path: "src/verify/compare.rs"
      provides: "per-axis L1/L2 numeric comparator branching on NumArray width"
      contains: "ConformanceLevel"
    - path: "src/lib.rs"
      provides: "pub mod verify registration"
      contains: "pub mod verify"
  key_links:
    - from: "src/verify/compare.rs"
      to: "src/schema/tolerance.rs"
      via: "import ToleranceContract::{L1,L2} / ConformanceLevel — never re-encode"
      pattern: "ToleranceContract"
    - from: "src/verify/compare.rs"
      to: "src/read/record.rs"
      via: "branch on NumArray::{F32,F64} source variant, compare at stored width"
      pattern: "NumArray::(F32|F64)"
---

<objective>
Stand up the `src/verify/` module scaffold and the two interface-defining pieces every
downstream plan binds against: the `VerificationReport` / `Mismatch` / `VerifyError`
contracts (`report.rs`) and the per-axis L1/L2 numeric comparator (`compare.rs`).

Purpose: This is the contract wave. Plan 02 (orchestrator + ion image) and Plan 03 (the
integration harness) consume `VerificationReport`, `VerifyError`, and the comparator — they
receive these contracts in the plan rather than exploring the codebase for them. The
comparator carries the load-bearing correctness fact for VER-03: an L1 check compares at the
SOURCE stored float width (f32-vs-f32, f64-vs-f64) with Δ=0 and NEVER widens via the
NON-CANONICAL `NumArray::as_f64()`.

Output: `src/verify/mod.rs`, `src/verify/report.rs`, `src/verify/compare.rs`, and the
one-line `pub mod verify;` registration in `src/lib.rs`. Unit-tested in-module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-verification-roundtrip-layer/05-CONTEXT.md
@.planning/phases/05-verification-roundtrip-layer/05-RESEARCH.md
@.planning/phases/05-verification-roundtrip-layer/05-PATTERNS.md

<interfaces>
<!-- Contracts the executor must bind against. Extracted from the codebase — do not re-explore. -->

From src/schema/tolerance.rs (re-exported as mzml2mzpeak::schema::{ConformanceLevel, ToleranceContract}):
```
pub enum ConformanceLevel { L1BitForBit, L2Transformed }
pub struct ToleranceContract { pub level: ConformanceLevel, pub mz_rel_err: f64, pub intensity_rel_err: f64 }
pub const ToleranceContract::L1  // mz_rel_err=0.0, intensity_rel_err=0.0
pub const ToleranceContract::L2  // mz_rel_err=1e-7, intensity_rel_err=1e-3
```

From src/read/record.rs (re-exported as mzml2mzpeak::read::record::NumArray):
```
pub enum NumArray { F32(Vec<f32>), F64(Vec<f64>) }
pub fn NumArray::len(&self) -> usize
// as_f64() is NON-CANONICAL (record.rs:53-62) — do NOT use it for an L1 Δ=0 check.
```

Error-enum analog — src/write/writer.rs:56-111 (WriteError): `#[derive(Debug, thiserror::Error)]`,
`#[from]` arms for upstream errors + structured named-field domain variants.
`#[source]` (not `#[from]`) when a second arm also wraps std::io::Error — src/read/stream.rs:55-56.

Result-struct STYLE analog — src/schema/tolerance.rs:22-31 (`#[derive(Debug, Clone, Copy, PartialEq)]`
plain-data struct with doc-per-field).

Barrel + lib.rs registration analog — src/write/mod.rs:23-29, src/lib.rs:16-19.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: verify module scaffold + report.rs (VerificationReport, Mismatch, VerifyError)</name>
  <files>src/verify/mod.rs, src/verify/report.rs, src/lib.rs</files>
  <read_first>
    - src/write/mod.rs:23-29 (barrel: declare all submods, then re-export) — the exact shape to mirror for src/verify/mod.rs
    - src/lib.rs:16-19 (module registration — add `pub mod verify;` as the fifth line)
    - src/write/writer.rs:56-111 (WriteError: thiserror enum with `#[from]` + structured named-field arms — the VerifyError analog)
    - src/read/stream.rs:55-56 (`#[source]` not `#[from]` to avoid a duplicate `From<io::Error>` impl)
    - src/schema/tolerance.rs:22-31 (plain-data result-struct STYLE: derives + doc-per-field)
    - 05-PATTERNS.md lines 55-85 (thiserror enum pattern + the named VerifyError arms) and 154-171 (report.rs assignment)
    - 05-RESEARCH.md lines 168-171, 250-261, 387-388 (VerificationReport shape, the VerifyError arm names, Mismatch fields)
  </read_first>
  <behavior>
    - Test: a VerificationReport can be constructed and its per-check pass/fail fields read back (e.g. count result, coordinate result, per-axis m/z + intensity results, ion-image result, bounded `Vec<Mismatch>`).
    - Test: a VerifyError structured variant (e.g. DuplicateCoordinate { x, y, z }) formats a non-empty Display message and is matchable via `matches!`.
    - Test: the crate compiles with `pub mod verify;` registered and the public surface re-exported from mod.rs.
  </behavior>
  <action>
    Create `src/verify/mod.rs` mirroring `src/write/mod.rs:23-29`: declare `pub mod report; pub mod compare;`
    up front (ion_image + verify submodules are added by Plan 02 — declare them now ONLY if you also create
    empty stub files; otherwise Plan 02 adds the `pub mod` lines, matching how write/mod declared all three at
    once — prefer declaring `report` and `compare` here and let Plan 02 append `ion_image`/`verify`). Re-export
    `pub use report::{VerificationReport, Mismatch, MismatchAxis, VerifyError};`. Add a `//!` header citing
    VER-01..VER-04 + CONTEXT Area 1/4 (house style, PATTERNS lines 104-108).

    Add `pub mod verify;` to `src/lib.rs` as the fifth module line after `pub mod write;` (PATTERNS lines 41-53).

    Create `src/verify/report.rs`:
    - `VerifyError` — `#[derive(Debug, thiserror::Error)]` mirroring `WriteError`'s shape (writer.rs:56-111).
      Arms: `#[from] crate::read::ReadError` (source open via ImagingReader::open); an open-output arm wrapping
      the `MzPeakReader::new` `std::io::Error` as `OpenOutput(#[source] std::io::Error)` using `#[source]` (NOT
      `#[from]`) to avoid a duplicate `From<io::Error>` impl per stream.rs:55-56; structured named-field domain
      variants `CountMismatch { source_count: usize, output_count: usize }`, `MissingMetadata { index: u64 }`,
      `NoScan { index: u64 }`, `CoordMissing { index: u64 }`, `DuplicateCoordinate { x: i64, y: i64, z: Option<i64> }`,
      `UnpairedSourcePixel { x: i64, y: i64, z: Option<i64> }`, `MissingDataFacet { index: u64 }`,
      `MissingPeaksFacet { index: u64 }`. Each `#[error("...")]` message is concrete and actionable (RESEARCH
      lines 250-261; PATTERNS lines 80-85). Keep `anyhow` OUT of this module (CLAUDE.md; CONTEXT Discretion).
    - `MismatchAxis` — a small enum `{ Mz, Intensity }` so a mismatch records which axis differed (CONTEXT Area 4).
    - `Mismatch` — plain-data struct: `{ coord: (i64, i64, Option<i64>), index: u64, axis: MismatchAxis,
      element: usize, src_val: f64, out_val: f64 }` (src/out values widened to f64 for the REPORT only — this is
      reporting, not an L1 comparison; document that the comparison itself happens at stored width). Borrow the
      derive + doc-per-field STYLE from `ToleranceContract` (tolerance.rs:22-31).
    - Per-check result structs / fields on `VerificationReport`: a count result (source vs output counts + bool),
      a coordinate result (paired count + bool), separate `mz` and `intensity` per-axis results (each a bool +
      mismatch count), an ion-image sanity result (bool + a count of disagreeing cells), and a bounded
      `mismatches: Vec<Mismatch>` capped at a const `MAX_REPORTED_MISMATCHES` (choose 20 per RESEARCH Open Q2)
      plus a `total_mismatches: usize` running count so a fully-wrong file does not grow the Vec unbounded
      (Security V5 / RESEARCH lines 423-425). Add a convenience `VerificationReport::passed(&self) -> bool` that
      ANDs every per-check bool.
    - Unit tests in `#[cfg(test)] mod tests`: construct a report and assert `passed()`; construct a
      `VerifyError::DuplicateCoordinate` and assert its Display is non-empty and it `matches!` the variant.
    No fenced code blocks in implementation beyond the structures named here; follow the cited analogs verbatim.
  </action>
  <verify>
    <automated>cargo test --lib verify::report</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` succeeds; `src/lib.rs` contains `pub mod verify;`.
    - `src/verify/mod.rs` re-exports `VerificationReport`, `Mismatch`, `MismatchAxis`, `VerifyError`.
    - `VerifyError` is `#[derive(Debug, thiserror::Error)]` with a `#[from] crate::read::ReadError` arm and an
      `OpenOutput(#[source] std::io::Error)` arm (NOT a second `#[from] io::Error`).
    - `src/verify/report.rs` contains `pub struct VerificationReport`, `pub struct Mismatch`, and a
      `MAX_REPORTED_MISMATCHES` const (= 20).
    - `cargo test --lib verify::report` runs ≥2 real assertions and passes (no zero-test pass).
    - `grep -v '^//' src/verify/compare.rs src/verify/report.rs | grep -c 'as_f64'` is 0 (no widening accessor in this module).
  </acceptance_criteria>
  <done>The verify module scaffold compiles, is registered in lib.rs, and report.rs exposes the
  VerificationReport/Mismatch/VerifyError contracts with passing unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: compare.rs — per-axis L1/L2 comparator at source stored width</name>
  <files>src/verify/compare.rs</files>
  <read_first>
    - src/read/record.rs:21-63 (NumArray enum + the NON-CANONICAL as_f64 doc warning at :53-62)
    - src/schema/tolerance.rs:8-49 (ConformanceLevel + ToleranceContract::{L1,L2}, the numbers to import)
    - 05-PATTERNS.md lines 174-204 (compare.rs assignment: branch on NumArray variant, compare at stored width, the L1/L2 comparator core)
    - 05-RESEARCH.md lines 354-371 (the per-axis L1/L2 comparator code) and 287-294 (anti-patterns: never widen f32→f64 for L1; never re-encode tolerance numbers)
  </read_first>
  <behavior>
    - Test: `first_mismatch_f64(&[1.0, 2.0], &[1.0, 2.0], 0.0, L1BitForBit)` returns None (exact match).
    - Test: `first_mismatch_f64(&[1.0, 2.0], &[1.0, 2.5], 0.0, L1BitForBit)` returns Some(1) (Δ≠0 fails L1).
    - Test: an f32 twin compares at f32 width — `first_mismatch_f32` on equal f32 slices returns None; a one-ULP-off f32 fails L1.
    - Test: under L2Transformed an m/z pair within `1e-7` rel-err passes while a pair beyond it fails (per-axis tolerance honored).
    - Test: a length mismatch (src.len() != out.len()) is reported as a mismatch position.
  </behavior>
  <action>
    Create `src/verify/compare.rs` with a `//!` header citing VER-03 + CONTEXT Area 2. Import
    `crate::schema::{ConformanceLevel, ToleranceContract}` and `crate::read::record::NumArray` — NEVER define a
    local tolerance const (RESEARCH "Don't Hand-Roll"; PATTERNS lines 87-102).

    Provide the comparator core as two width-specific fns mirroring RESEARCH lines 360-371:
    `first_mismatch_f64(src: &[f64], out: &[f64], rel_err: f64, level: ConformanceLevel) -> Option<usize>` and an
    f32 twin `first_mismatch_f32(src: &[f32], out: &[f32], rel_err: f32, level: ConformanceLevel) -> Option<usize>`.
    On `src.len() != out.len()` return `Some(src.len().min(out.len()))`. For `L1BitForBit` the predicate is exact
    inequality (`a != b`) at the slice's own width — NO widening. For `L2Transformed` the predicate is
    `b == 0.0 ? a != b : (a - b).abs() / b.abs() > rel_err`. The f32 twin computes the rel-err in f32 and compares
    at f32 width — this is the load-bearing "compare at stored width" rule (PATTERNS line 192; record.rs:53-62
    NON-CANONICAL warning).

    Provide a `compare_axis(source: &NumArray, out: &NumArray, rel_err_f64: f64, level) -> Option<usize>` dispatcher
    that branches on the SOURCE `NumArray` variant: `F64 → first_mismatch_f64` (out must also be read as f64),
    `F32 → first_mismatch_f32` (out read as f32, rel-err cast to f32). The caller (Plan 02) supplies the per-axis
    `rel_err` from `ToleranceContract` (mz_rel_err for the m/z axis, intensity_rel_err for the intensity axis) —
    document that the comparator takes the already-selected per-axis tolerance so the m/z-vs-intensity split lives
    at the call site (CONTEXT Area 2; criterion 3).

    Add a doc note: `as_f64()` is reserved for the informational/L2 centroid-m/z path ONLY (RESEARCH Pitfall 2),
    never for an L1 Δ=0 check. Unit tests in `#[cfg(test)] mod tests` covering every <behavior> case above.
    No fenced code in the body beyond the cited comparator structure.
  </action>
  <verify>
    <automated>cargo test --lib verify::compare</automated>
  </verify>
  <acceptance_criteria>
    - `src/verify/compare.rs` exports `first_mismatch_f64`, `first_mismatch_f32`, and `compare_axis`.
    - `grep -v '^//' src/verify/compare.rs | grep -c 'const.*1e-7\|const.*MZ_TOL\|const.*1e-3'` is 0 (tolerance numbers imported, never re-encoded).
    - `grep -c 'ToleranceContract\|ConformanceLevel' src/verify/compare.rs` is ≥1 (consumes the schema contract).
    - The L1 test (`first_mismatch_f64` exact-match → None; Δ≠0 → Some) and the f32-width test both pass.
    - At least one L2 relative-error test passes (within `1e-7` → None, beyond → Some).
    - `cargo test --lib verify::compare` runs ≥5 assertions and passes.
  </acceptance_criteria>
  <done>The per-axis comparator compiles, imports the ToleranceContract numbers (never re-encodes them),
  compares at the source stored width for L1 (no f32→f64 widening), supports per-axis L2 rel-err, and passes
  its unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → verify library | Tolerance level + (later) file paths supplied by the caller (Phase-6 CLI). This plan only builds the in-memory comparator + report types; no file I/O yet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | `VerificationReport.mismatches` growth on a fully-wrong file | mitigate | Cap the reported `Vec<Mismatch>` at `MAX_REPORTED_MISMATCHES` (=20) + a `total_mismatches` counter; the Vec never grows unbounded (Security V5, RESEARCH lines 423-425). |
| T-05-02 | Tampering | Re-encoded tolerance numbers diverging from the spec contract | mitigate | `compare.rs` imports `ToleranceContract::{L1,L2}` from `src/schema/tolerance.rs`; grep gate forbids local tolerance consts (CONTEXT Area 1). |
| T-05-03 | Information Disclosure | Silent f32→f64 widening producing a false L1 pass | mitigate | Comparator compares at the source stored width (f32 twin); `as_f64()` excluded from this module (grep gate in acceptance criteria). |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase — all deps pinned `=` in committed Cargo.lock; vendored mzdata approved in Phase 0 (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cargo build` green.
- `cargo test --lib verify::` passes (report + compare unit tests).
- `cargo test` full suite stays green (read/schema/integrity/write untouched).
</verification>

<success_criteria>
- `src/verify/` exists with `mod.rs`, `report.rs`, `compare.rs`; `pub mod verify;` registered in lib.rs.
- `VerificationReport`, `Mismatch`, `MismatchAxis`, `VerifyError` are the public contracts re-exported from mod.rs.
- The comparator detects L1 Δ=0 mismatches at source width and supports per-axis L2 tolerances, importing the
  ToleranceContract numbers verbatim.
- No `as_f64()` widening and no re-encoded tolerance consts in the module (grep gates pass).
</success_criteria>

<output>
Create `.planning/phases/05-verification-roundtrip-layer/05-01-SUMMARY.md` when done.
</output>
