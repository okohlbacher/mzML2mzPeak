---
phase: 05-verification-roundtrip-layer
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - tests/verify_roundtrip.rs
autonomous: true
requirements: [VER-01, VER-02, VER-03, VER-04]
must_haves:
  truths:
    - "An end-to-end fixture (profile + F32-profile + centroid + sparse grid) writes a real archive, is verified, and reports passed() under L1"
    - "Output spectrum count equals the source count (VER-01)"
    - "Every source pixel's x/y pairs to an output index by coordinate (VER-02)"
    - "Profile m/z + intensity are bit-for-bit at source dtype under L1; at least one L2 test passes on a profile pixel (VER-03)"
    - "A centroid pixel is verified against the SOURCE as the L1 reference (not the widened peaks facet)"
    - "A raw-facet round-trip is asserted bit-for-bit; the peaks-facet m/z widening is documented as out-of-L1-scope"
    - "An ion image M[row=y][col=x] reconstructs and source-vs-output cells agree; a sparse grid does not panic (VER-04)"
  artifacts:
    - path: "tests/verify_roundtrip.rs"
      provides: "integration harness extending the Phase-4 fixture and calling verify_against_source"
      contains: "verify_against_source"
  key_links:
    - from: "tests/verify_roundtrip.rs"
      to: "src/verify/verify.rs"
      via: "verify_against_source(&fixture, &archive_path, level) -> VerificationReport"
      pattern: "verify_against_source"
    - from: "tests/verify_roundtrip.rs"
      to: "the write_fixture seam"
      via: "ImagingWriter + to_mzdata + ensure_chromatogram_facet + finish_parquet→add_index_metadata→finish"
      pattern: "ensure_chromatogram_facet"
---

<objective>
Deliver the VER-01..VER-04 integration harness: `tests/verify_roundtrip.rs`. It EXTENDS the
Phase-4 fixture (`tests/write_roundtrip.rs`) with a Float32-source-m/z profile pixel (proving
the L1 f32 path) and a sparse / non-rectangular coordinate set (exercising the presence mask),
writes a real `.mzpeak` archive via the proven `write_fixture` seam, then calls the Plan-02
`verify_against_source` core and asserts the decisive fidelity facts.

Purpose: This is where the load-bearing correctness claims become executable assertions — the
profile L1 Δ=0 raw-facet comparison, the centroid source-as-L1-reference rule, the per-axis
m/z vs intensity split, at least one L2 profile-pixel test, the ion-image M[row=y][col=x]
reconstruction, and the sparse-grid no-panic guarantee. It needs no `.ibd` (RESEARCH Pitfall 5)
because it drives `verify_against_source` over an in-code `Vec<ImagingSpectrum>`.

Output: `tests/verify_roundtrip.rs`, runnable via `cargo test --test verify_roundtrip`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-verification-roundtrip-layer/05-CONTEXT.md
@.planning/phases/05-verification-roundtrip-layer/05-RESEARCH.md
@.planning/phases/05-verification-roundtrip-layer/05-PATTERNS.md
@.planning/phases/05-verification-roundtrip-layer/05-02-SUMMARY.md
@tests/write_roundtrip.rs

<interfaces>
<!-- The exact seam + verifier surface this harness drives. Both proven/produced in earlier work. -->

write_fixture seam to REUSE (replicate from tests/write_roundtrip.rs:83-121, generalized to take the fixture Vec):
```
ImagingWriter::new(out)? ;
writer.write_run_metadata(&FileMetadataConfig::default(), &provenance, geom)? ;
for s in &fixture { writer.write_spectrum(&to_mzdata(s)?)? }
writer.ensure_chromatogram_facet()? ;           // empty facet so MzPeakReader can open (Pitfall 6)
let block = writer.imaging_metadata()?.clone();
let mut zip = writer.finish_parquet()? ;
zip.add_index_metadata("imaging", &block).map_err(WriteError::Json)? ;
zip.finish().map_err(|e| WriteError::Io(io::Error::other(e)))? ;
```
temp_out(tag) + `let _ = std::fs::remove_file(&out);` cleanup idiom: tests/write_roundtrip.rs:117-121,132.

Verifier surface from Plan 02 (src/verify):
```
verify_against_source(source: &[ImagingSpectrum], output_path: &Path, level: ConformanceLevel) -> Result<VerificationReport, VerifyError>
VerificationReport::passed(&self) -> bool   // plus per-check fields (count, coord, mz, intensity, ion-image) + mismatches
ConformanceLevel::{L1BitForBit, L2Transformed}   // mzml2mzpeak::schema
```

Fixture record type — mzml2mzpeak::read::record::{ImagingSpectrum, NumArray, Representation}; RunProvenance, StorageMode from mzml2mzpeak::read.
Imports the harness needs (from write_roundtrip.rs:28-38): use std::path::{Path, PathBuf}; the read/schema/write re-exports; mzdata::meta::FileMetadataConfig; mzpeak_prototyping::MzPeakReader (for the raw-facet bit-for-bit assertion).
Raw-facet readback for the bit-for-bit assertion (write_roundtrip.rs:149-160): get_spectrum_arrays(i)?.get(&ArrayType::MZArray)?.to_f64()/.to_f32() (mzdata::spectrum::bindata::{ArrayType, ByteArrayView}).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extended fixture + write_fixture seam + count/coordinate tests (VER-01, VER-02)</name>
  <files>tests/verify_roundtrip.rs</files>
  <read_first>
    - tests/write_roundtrip.rs (WHOLE file) — the fixture, the write_fixture seam (:83-114), temp_out + cleanup (:117-132), the coord-readback proof (:229-255). This harness extends/mirrors it.
    - 05-PATTERNS.md lines 234-266 (tests/verify_roundtrip.rs assignment: extend the fixture with a F32 profile pixel + a sparse coord set; reuse write_fixture VERBATIM; address Pitfall 5 via verify_against_source)
    - 05-RESEARCH.md Validation Architecture lines 453-471 (the Req→Test map: count_equality, coordinates_match, values_l1/l2, centroid_source_reference, raw_facet_bit_for_bit, ion_image_sanity, sparse_grid_no_panic) and Pitfall 5 lines 339-343
    - src/read/record.rs:48-71 (fixture record fields: x,y,z,mz,intensity,representation,ms_level,native_id)
  </read_first>
  <action>
    Create `tests/verify_roundtrip.rs` with a `//!` header citing VER-01..VER-04 + CONTEXT Area 4. Mirror the
    imports + helpers from `tests/write_roundtrip.rs:28-121` (Path/PathBuf, the read/schema/write re-exports,
    `FileMetadataConfig`, `MzPeakReader`, `provenance()`, `temp_out(tag)`, the per-test `remove_file` cleanup).

    Generalize the `write_fixture` helper so it takes the fixture `&[ImagingSpectrum]` (and optional geometry) and
    runs the EXACT terminal seam from write_roundtrip.rs:83-114 (`ImagingWriter` + `to_mzdata` per spectrum,
    `ensure_chromatogram_facet`, then `finish_parquet → add_index_metadata("imaging", &block) → finish`). This is
    the no-`.ibd` path (Pitfall 5/6).

    Build the extended fixture as `fn fixture() -> Vec<ImagingSpectrum>` covering CONTEXT Area 4 coverage:
    - pixel A: Profile, m/z `NumArray::F64`, intensity `NumArray::F32`, coords (1,1) — the F64-m/z profile case.
    - pixel B: Profile, m/z `NumArray::F32`, intensity `NumArray::F32`, coords (3,1) — the F32-m/z profile case
      (proves the L1 f32-width path, RESEARCH Crux — distinct x so it pairs uniquely).
    - pixel C: Centroid, m/z `NumArray::F64`, intensity `NumArray::F32`, coords (2,3) — the centroid case routing
      to the peaks facet; the sparse/non-rectangular set {(1,1),(3,1),(2,3)} leaves cells (2,1),(1,2),(3,3) etc.
      empty (exercises the presence mask, Pitfall 4).
    Keep arrays small and deterministic (distinct values per axis) so mismatches are diagnosable.

    Write `count_equality` (VER-01): write the fixture, call `verify_against_source(&fixture, &out, L1BitForBit)`,
    assert the report's count check passed and `report` reflects source_count == output_count == fixture.len().
    Write `coordinates_match` (VER-02): assert every fixture pixel paired (coordinate check passed); optionally
    cross-check one coordinate via the proven `get_spectrum_metadata + get_param_by_curie(IMS:1000050).to_i64()`
    path to belt-and-suspenders the readback. Each test uses a unique `temp_out` tag and cleans up.
  </action>
  <verify>
    <automated>cargo test --test verify_roundtrip count_equality coordinates_match</automated>
  </verify>
  <acceptance_criteria>
    - `tests/verify_roundtrip.rs` defines a `fixture()` with ≥1 F64-m/z profile pixel, ≥1 F32-m/z profile pixel, ≥1 centroid pixel, over a sparse/non-rectangular coord set {(1,1),(3,1),(2,3)}.
    - `count_equality` asserts the output count equals the source count via the report (VER-01).
    - `coordinates_match` asserts every source pixel pairs by coordinate (VER-02).
    - The harness calls `verify_against_source` (no `.ibd`; grep: `verify_against_source` present) and reuses the `ensure_chromatogram_facet` seam.
    - `cargo test --test verify_roundtrip count_equality coordinates_match` passes.
  </acceptance_criteria>
  <done>The extended fixture writes a real archive via the proven seam, and the count + coordinate integration
  tests pass against verify_against_source.</done>
</task>

<task type="auto">
  <name>Task 2: Value-fidelity + centroid-source-reference + raw-facet + L2 + ion-image tests (VER-03, VER-04)</name>
  <files>tests/verify_roundtrip.rs</files>
  <read_first>
    - 05-RESEARCH.md Pattern 1 lines 185-211 (profile data-facet bit-for-bit at source dtype), Pattern 2 lines 213-234 (centroid: source is the L1 reference, peaks-facet widening out-of-L1-scope), Pitfall 2 lines 321-325, Open Q1 lines 418-421 (make the L2 test a PROFILE pixel where rel-error is the genuine relaxation)
    - 05-RESEARCH.md Validation map lines 458-460 (values_l1, values_l2, centroid_source_reference, raw_facet_bit_for_bit, ion_image_sanity, sparse_grid_no_panic)
    - tests/write_roundtrip.rs:149-173 (facet-routing readback: get_spectrum_arrays for profile, get_spectrum_peaks_for for centroid) — the raw-facet bit-for-bit assertion uses get_spectrum_arrays
    - 05-CONTEXT.md Area 2 + Area 4 (centroid compared against source; peaks-facet m/z documented as out-of-L1-scope; ≥1 L2 test required)
    - docs/imaging-mzpeak-spec-draft.md §5.1 (ion image orientation, used to assert M[row=y][col=x])
  </read_first>
  <action>
    Add the value + ion-image integration tests to `tests/verify_roundtrip.rs`, each writing the fixture to a
    unique `temp_out` tag and cleaning up:

    - `values_l1` (VER-03, profile): `verify_against_source(&fixture, &out, L1BitForBit)`; assert the per-axis m/z
      AND intensity checks BOTH passed for the profile pixels and `report.passed()` is true for an honest
      round-trip. This is the first-class profile-pixel L1 Δ=0 raw-facet assertion (phase crux).
    - `raw_facet_bit_for_bit` (VER-03 caveat): independently re-open the archive with `MzPeakReader`, pull the
      profile pixel's `get_spectrum_arrays(i)` m/z+intensity, and assert they are bit-for-bit equal to the source
      `NumArray` at matching width (F64 m/z via `.to_f64()` exact-eq, F32 m/z via `.to_f32()` exact-eq) — proving
      the data facet is the authoritative L1 reference. Add a comment documenting that the centroid peaks-facet m/z
      is WIDENED (Float32→Float64) and is therefore NOT the L1 reference (CONTEXT Area 2; out-of-L1-scope).
    - `centroid_source_reference` (VER-03): assert the centroid pixel verifies under L1 with intensity Δ=0
      (source f32 vs peaks f32) AND that a Float32-source centroid m/z widening is NOT reported as an L1 failure
      (the report's centroid m/z handling treats it as expected/informational per Pitfall 2). Use a centroid whose
      values make the assertion unambiguous.
    - `values_l2` (VER-03, ≥1 L2 test): `verify_against_source(&fixture, &out, L2Transformed)` and assert it passes
      on the PROFILE pixels (RESEARCH Open Q1 — L2's relative-error semantics are the genuine relaxation on a
      profile pixel). At least one L2 test MUST exist (CONTEXT Area 2).
    - `ion_image_sanity` (VER-04): assert the report's ion-image-sanity check passed for the honest round-trip
      (source vs output TIC cells agree on present cells), confirming the M[row=y][col=x] reconstruction.
    - `sparse_grid_no_panic` (VER-04): the sparse/non-rectangular fixture {(1,1),(3,1),(2,3)} runs through
      `verify_against_source` WITHOUT panicking (absent cells handled, presence mask) — the test simply completing
      with a returned report (not an abort) is the assertion.

    Keep every assertion value-equality / boolean-explicit; no subjective checks. No fenced code beyond the cited
    readback snippets.
  </action>
  <verify>
    <automated>cargo test --test verify_roundtrip values_l1 raw_facet_bit_for_bit centroid_source_reference values_l2 ion_image_sanity sparse_grid_no_panic</automated>
  </verify>
  <acceptance_criteria>
    - `values_l1` asserts profile m/z AND intensity per-axis checks pass under L1 and `report.passed()` is true.
    - `raw_facet_bit_for_bit` asserts `get_spectrum_arrays` profile arrays equal the source `NumArray` bit-for-bit at matching width (F64 and F32 paths), with the peaks-facet widening documented in a comment.
    - `centroid_source_reference` asserts centroid intensity is L1 Δ=0 against the source and a F32-source centroid m/z widening is NOT an L1 failure.
    - `values_l2` exists and passes on a profile pixel under `L2Transformed` (≥1 L2 test requirement met).
    - `ion_image_sanity` asserts the ion-image check passed; `sparse_grid_no_panic` completes without panic on {(1,1),(3,1),(2,3)}.
    - `grep -c 'L2Transformed' tests/verify_roundtrip.rs` is ≥1.
    - `cargo test --test verify_roundtrip` passes ALL tests; full `cargo test` stays green.
  </acceptance_criteria>
  <done>The value-fidelity tests (profile L1 raw-facet bit-for-bit, centroid source-reference, ≥1 L2 profile
  pixel), the raw-facet bit-for-bit assertion, and the ion-image + sparse-grid tests all pass end-to-end,
  proving VER-03 and VER-04 on a synthetic round-trip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → write seam → archive → verifier | A self-contained loop: the test produces the archive and verifies it. No external/untrusted input; the threat surface is test-local. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Denial of Service | sparse/non-rectangular fixture causing an out-of-bounds panic in ion-image reconstruction | mitigate | `sparse_grid_no_panic` explicitly exercises {(1,1),(3,1),(2,3)} through the verifier and asserts it completes (Pitfall 4); the bounds-check lives in Plan 02's `IonImage::build`. |
| T-05-10 | Repudiation | a false L1 pass from comparing widened centroid m/z | mitigate | `centroid_source_reference` + `raw_facet_bit_for_bit` pin the source-as-L1-reference contract so a widening can never silently pass as L1 (CONTEXT Area 2). |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages — the harness uses only pinned in-tree + vendored deps; committed Cargo.lock (RESEARCH audit N/A). |
</threat_model>

<verification>
- `cargo test --test verify_roundtrip` runs all VER-01..VER-04 integration tests green (≥1 L1, ≥1 L2).
- `cargo test` full suite green (Phase 2/3/4 tests + the new verify lib units + this harness).
</verification>

<success_criteria>
- An end-to-end synthetic round-trip (profile F64 + profile F32 + centroid, sparse grid) writes a real archive and `verify_against_source` reports `passed()` under L1.
- VER-01 (count), VER-02 (coordinates), VER-03 (profile L1 bit-for-bit raw facet + per-axis + centroid source-reference + ≥1 L2), VER-04 (ion image M[row=y][col=x] + sparse no-panic) all have passing automated tests.
- The peaks-facet m/z widening is documented in-test as out-of-L1-scope.
</success_criteria>

<output>
Create `.planning/phases/05-verification-roundtrip-layer/05-03-SUMMARY.md` when done.
</output>
