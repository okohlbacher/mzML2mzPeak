---
phase: 06-cli-ux-acceptance-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/integrity/header.rs
  - src/verify/verify.rs
  - src/verify/mod.rs
  - tests/verify_roundtrip.rs
autonomous: true
requirements: [DAT-01, CLI-02]
must_haves:
  truths:
    - "A bounded-memory verify_streaming(reader, output, level) verifies a converted archive against a streamed source without ever collecting all source spectra"
    - "verify_streaming produces a VerificationReport equal to verify_against_source on the synthetic fixture"
    - "The bounded header parse exposes the <spectrumList count> spectrum total without reading any spectra"
    - "indicatif is a direct dependency pinned =0.17.11 (single resolved copy preserved)"
  artifacts:
    - path: "src/verify/verify.rs"
      provides: "verify_streaming bounded-memory core reusing build_coord_index + the representation branch verbatim"
      contains: "pub fn verify_streaming"
    - path: "src/integrity/header.rs"
      provides: "spectrum_count: Option<usize> on ImzmlHeader from <spectrumList count>"
      contains: "spectrum_count"
    - path: "Cargo.toml"
      provides: "indicatif direct dependency pinned =0.17.11"
      contains: "indicatif"
  key_links:
    - from: "src/verify/verify.rs::verify_streaming"
      to: "src/verify/verify.rs::build_coord_index"
      via: "output coord->index map built first, then source streamed once"
      pattern: "build_coord_index"
    - from: "src/verify/mod.rs"
      to: "verify::verify_streaming"
      via: "pub use re-export"
      pattern: "verify_streaming"
---

<objective>
Land the two pieces of genuine engineering this phase needs, plus the one sanctioned
dependency change — all in the library/config layer so the Wave-2 CLI and Wave-3 acceptance
test can wire to stable interfaces.

1. `verify_streaming` — the bounded-memory verify core (THE CRUX, DAT-01). Inverts the
   collect-all loop in `verify_roundtrip` so the source `ImagingReader` is streamed ONCE and
   each pixel is paired against a pre-built compact output coord index. Reuses
   `build_coord_index` and the representation branch verbatim (including the centroid
   f32→f64 widening rule — Pitfall 6).
2. `<spectrumList count>` extraction in the bounded header parse — the progress total source
   (CLI-02) obtained before the stream starts.
3. Promote `indicatif` to a direct dependency pinned `=0.17.11` (the single resolved copy).

Purpose: these are the only new algorithms/contracts in Phase 6; everything in Wave 2/3 is
wiring against them.
Output: `verify_streaming` (re-exported), `ImzmlHeader.spectrum_count`, `indicatif` direct dep,
and an equivalence test proving `verify_streaming == verify_against_source` on the fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cli-ux-acceptance-gate/06-RESEARCH.md
@.planning/phases/06-cli-ux-acceptance-gate/06-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase. Executor uses these directly — no exploration needed. -->

From src/verify/verify.rs (the collect-all site to replace + the parts to reuse VERBATIM):
- `pub fn verify_roundtrip(source_path: &Path, output_path: &Path, level: ConformanceLevel) -> Result<VerificationReport, VerifyError>` (lines 66-78) — KEEP unchanged; it stays the convenience collect-all entry for callers that want it.
- `pub fn verify_against_source(source: &[ImagingSpectrum], output_path: &Path, level: ConformanceLevel) -> Result<VerificationReport, VerifyError>` (lines 85-328) — KEEP for fixture tests.
- `fn build_coord_index(reader: &mut MzPeakReader, out_count: usize) -> Result<HashMap<CoordKey, u64>, VerifyError>` (lines 334-365) — already streaming + compact; REUSE verbatim.
- Report skeleton (lines 108-116): VerificationReport { count, coordinates, mz, intensity, ion_image, mismatches, total_mismatches }.
- Count gate (lines 98-104): CountResult { source_count, output_count, passed }.
- Source-collision + pairing (lines 135-147): `seen_src: HashMap<CoordKey, ()>` + `coord_to_index.get(&key)`.
- Representation branch (lines 159-298): `Profile | Unknown => get_spectrum_arrays(out_idx)`, `Centroid => get_spectrum_peaks_for(out_idx)` incl. f32→f64 centroid m/z widening at :225-231 (NOT an L1 failure).
- Ion-image tail (lines 308-325): accumulate `out_coords_tics: Vec<((i64,i64), f64)>` and source `(coord, tic_of(&s.intensity))`; `grid_dims_from_metadata(reader.file_index().metadata.get("imaging"))` at :309; `IonImage::build` + `disagreeing_cells`.
- Helpers in scope: `tic_of`, `grid_dims_from_metadata`, `IonImage` (imported at :50); `CoordKey = (i64, i64, Option<i64>)`.

From src/read/stream.rs:
- `pub fn ImagingReader::open(imzml_path: &Path) -> Result<ImagingReader, ReadError>` (consumes preflight; line 114).
- `impl Iterator for ImagingReader { type Item = Result<ImagingSpectrum, ReadError>; }` — ONE-SHOT stream.
- `ImagingSpectrum` fields used: `x: i64`, `y: i64`, `z: Option<i64>`, `representation: Representation`, `intensity` (NumArray).

From src/verify/report.rs:
- `VerificationReport` derives `PartialEq` (equivalence test target); `VerifyError` (thiserror); `MAX_REPORTED_MISMATCHES = 20`.
- `VerifyError::Read(#[from] ReadError)` exists (verify_roundtrip uses `reader.open()?` → `item?`).

From src/integrity/header.rs:
- `pub struct ImzmlHeader { pub uuid: String, pub checksum_type: ChecksumType, pub checksum_hex: String, pub ibd_file_name: Option<String> }` (lines 47-59) — ADD `pub spectrum_count: Option<usize>`.
- `pub fn parse_imzml_header_counted(path: &Path) -> Result<HeaderParseReport, IntegrityError>` (line 119) — the bounded loop that BREAKs at the first line containing `<spectrumList` (research cites :144). Extract `count="N"` on that terminating line BEFORE breaking.
- Existing attr-extraction idiom `parse_value_attr` (research cites :197-203): `let start = line.find("value=\"")? + 7; let rest = &line[start..]; let end = rest.find('"')?; rest[..end]`.

From src/verify/mod.rs (last line):
- `pub use verify::{verify_against_source, verify_roundtrip};` — ADD `verify_streaming`.

From Cargo.toml [dependencies] (existing `=`-pinned lines at :81-85: anyhow/thiserror/clap/log/env_logger):
- indicatif is NOT yet direct; Cargo.lock resolves it to 0.17.11 transitively via mzpeak_prototyping.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote indicatif to a direct dependency + extract &lt;spectrumList count&gt;</name>
  <files>Cargo.toml, src/integrity/header.rs</files>
  <read_first>
    - Cargo.toml [dependencies] block, lines 23-85 (the `=`-pinned direct deps to mirror).
    - src/integrity/header.rs lines 46-59 (ImzmlHeader struct) and the bounded loop in `parse_imzml_header_counted` (line 119+) that breaks at the first `<spectrumList` line; the `parse_value_attr` helper (~:197-203).
    - 06-PATTERNS.md "Cargo.toml — promote indicatif" (:173-181) and "src/integrity/header.rs — extract &lt;spectrumList count&gt;" (:148-169).
    - 06-RESEARCH.md "Installation" (:94-103, pin =0.17.11 NOT 0.17.10) and Pitfall 4 (:358-362, the stop-token line carries `count`).
  </read_first>
  <action>
    In Cargo.toml `[dependencies]`, add `indicatif = "=0.17.11"` alongside the other `=`-pinned
    application-boundary helpers (after env_logger at :85). Pin `=0.17.11` (NOT CLAUDE.md's stale
    0.17.10) so the single copy already in Cargo.lock is preserved — verify with
    `cargo tree -i indicatif` showing one path. This is the ONLY Cargo.toml change; no other pin
    moves, no new crate.

    In src/integrity/header.rs: add `pub spectrum_count: Option<usize>` to `ImzmlHeader` (after
    `ibd_file_name`). In `parse_imzml_header_counted`, on the terminating line that contains
    `<spectrumList`, extract the `count="N"` attribute BEFORE the `break` (mirror the
    `parse_value_attr` find/slice shape with key `count="`), parse to `usize` leniently
    (`str::parse().ok()`), and set it on the returned header. Absence or parse failure → `None`
    (degrade gracefully — `count` is mandatory on mzML `<spectrumList>` but never panic, T-6-mem
    defensive). Update `parse_imzml_header` (the convenience wrapper) and any other `ImzmlHeader`
    constructor sites so the new field is populated. The parse stays bounded — still one line, still
    breaks at `<spectrumList>`.
  </action>
  <verify>
    <automated>cargo build && cargo test --lib integrity</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` succeeds; `cargo tree -i indicatif` shows exactly one `indicatif` at 0.17.11.
    - `cargo test --lib integrity` passes; a unit test (add or extend one) asserts the real
      `data/HR2MSImouseurinarybladderS096.imzML` header parses with `spectrum_count == Some(34840)`.
    - `ImzmlHeader` has field `spectrum_count: Option<usize>`; the parse still breaks at
      `<spectrumList` (bounded — assert `bytes_consumed` stays far below file size, reusing the
      existing bounded-read proof test).
  </acceptance_criteria>
  <done>
    indicatif is a direct `=0.17.11` dep with one resolved copy; `ImzmlHeader.spectrum_count` is
    `Some(34840)` for the real file and `None` when the attribute is absent; the header parse remains
    bounded.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: verify_streaming bounded-memory core + equivalence test (THE CRUX)</name>
  <files>src/verify/verify.rs, src/verify/mod.rs, tests/verify_roundtrip.rs</files>
  <behavior>
    - Equivalence: `verify_streaming(ImagingReader::open(fixture)?, out, level)` returns a
      `VerificationReport` that `== verify_against_source(&fixture_slice, out, level)` for both
      L1BitForBit and L2Transformed on the existing tests/verify_roundtrip.rs fixture.
    - Bounded: verify_streaming never collects the source into a Vec; only one source spectrum and
      one output spectrum are live at a time (the output coord index + collision set are the only
      retained per-pixel structures).
    - Representation fidelity: a Profile/Unknown pixel routes to the data facet, a Centroid pixel to
      the peaks facet, and an F32-source centroid m/z does NOT produce an L1 mismatch (Pitfall 6).
  </behavior>
  <read_first>
    - src/verify/verify.rs lines 66-365 IN FULL (verify_roundtrip collect-all site, verify_against_source
      body, build_coord_index). This is the donor code.
    - src/verify/mod.rs (the `pub use verify::{...}` line at :28).
    - tests/verify_roundtrip.rs (the fixture builder + write_fixture seam; header :1-46 documents the
      Python-reader-crashes-on-IMS caveat and the L1 raw-facet reference).
    - 06-PATTERNS.md "verify_streaming (THE CRUX)" (:101-138) — the verbatim-reuse map with exact line refs.
    - 06-RESEARCH.md Pattern 2 (:209-261, the loop-inversion sketch + the bounded-memory ceiling +
      the ion-image caveat) and Pitfall 6 (:369-373).
  </read_first>
  <action>
    Add `pub fn verify_streaming(reader: ImagingReader, output_path: &Path, level: ConformanceLevel)
    -> Result<VerificationReport, VerifyError>` to src/verify/verify.rs.

    Structure (loop inversion of verify_roundtrip; reuse verify_against_source's pieces VERBATIM —
    extract shared helpers if cleaner, but do NOT change their logic):
    1. Open the output via `MzPeakReader::new(output_path).map_err(VerifyError::OpenOutput)?`; read
       `out_count = reader.len()`.
    2. Build the OUTPUT coord index FIRST: `let coord_to_index = build_coord_index(&mut out, out_count)?;`
       (the existing fn — already streaming, ~1.4 MB).
    3. Initialize the pre-marked report skeleton (verify.rs:108-116) and accumulators:
       `src_count = 0`, `seen_src: HashMap<CoordKey, ()>`, `coordinates_ok = true`,
       `out_coords_tics: Vec<((i64,i64), f64)>`, `src_coords_tics: Vec<((i64,i64), f64)>`,
       mismatch counters + the bounded `mismatches` Vec capped at MAX_REPORTED_MISMATCHES.
    4. Stream the SOURCE once: `for item in reader { let s = item?; src_count += 1; ... }`. Inside the
       loop, per source pixel: run the SAME collision check + `coord_to_index.get(&(s.x,s.y,s.z))`
       pairing (verify.rs:135-147), then the SAME representation branch (verify.rs:159-298) reading
       back ONLY that one paired output spectrum (`get_spectrum_arrays` / `get_spectrum_peaks_for`),
       accumulating `src_coords_tics.push(((s.x,s.y), tic_of(&s.intensity)))` and the output TIC. NEVER
       push `s` into a Vec.
    5. After the loop: set the count gate from `src_count` vs `out_count` (NOT a `.len()`), finalize
       coordinates/mz/intensity results, and build the ion image from the two scalar-per-pixel TIC vecs
       exactly as verify_against_source's tail does (verify.rs:308-325, `grid_dims_from_metadata` +
       `IonImage::build` + `disagreeing_cells`). Return `Ok(report)`.

    Preserve the centroid f32→f64 widening rule verbatim (verify.rs:225-231) — it is NOT an L1 failure.
    Do NOT introduce `as_f64()` on the L1 path (the existing grep gate of 0 must hold).

    Add `verify_streaming` to the `pub use verify::{...}` re-export in src/verify/mod.rs.

    Extend tests/verify_roundtrip.rs: add a test that runs BOTH `verify_streaming` (over a fresh
    fixture `ImagingReader` — or, since the fixture has no `.ibd`, construct the same source slice the
    fixture test already builds and feed an equivalent streaming path) AND `verify_against_source` over
    the materialized slice, asserting `report_streaming == report_slice` at L1 and at L2. If the fixture
    cannot produce a real `ImagingReader` (no `.ibd`), drive `verify_streaming` via a small in-test
    iterator/adapter that yields the same `ImagingSpectrum` values the slice holds, so the equivalence is
    over identical inputs.
  </action>
  <verify>
    <automated>cargo test --test verify_roundtrip && cargo build</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test verify_roundtrip` passes, including a new test asserting
      `report_streaming == report_slice` at both L1BitForBit and L2Transformed.
    - `verify_streaming` is exported from `imzml2mzpeak::verify` (`grep -n "verify_streaming" src/verify/mod.rs`
      shows it in the `pub use` line).
    - `grep -n "as_f64" src/verify/verify.rs | grep -v '^[0-9]*://'` shows no `as_f64` introduced on the
      L1 comparison path (the existing zero-widening gate holds).
    - `grep -c "Vec<ImagingSpectrum>" src/verify/verify.rs` does NOT increase inside `verify_streaming`
      (the streaming core never collects the source).
  </acceptance_criteria>
  <done>
    `verify_streaming` exists, is re-exported, streams the source once with bounded memory, reuses
    build_coord_index + the representation branch verbatim, and produces a report byte-equal to
    `verify_against_source` on the fixture at L1 and L2.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| imzML/.ibd file → header parser | Untrusted on-disk input crosses into the bounded Latin-1 parse |
| converted .mzpeak archive → MzPeakReader | Output re-opened read-only for verification; contents never executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-mem | Denial of Service | verify_streaming over 34,840 spectra | mitigate | Stream the source ONCE; retain only the compact output coord index (~1.4 MB) + scalar TIC vecs + one live source/output spectrum — never `Vec<ImagingSpectrum>`. Bounded regardless of dataset size. |
| T-6-count | Denial of Service | `<spectrumList count>` parse | mitigate | Extract only the count attribute on the single terminating line; parse leniently to `Option<usize>` (never panic); the parse stays bounded (breaks at `<spectrumList`). |
| T-6-widen | Tampering (data fidelity) | L1 numeric compare in verify_streaming | mitigate | Reuse the representation branch verbatim; L1 compares at SOURCE stored width with no `as_f64()` widening (grep gate = 0); F32-source centroid m/z widening is out-of-L1-scope, not a silent pass. |
| T-6-SC | Tampering | Cargo dependency (indicatif promote) | accept | No NEW crate added; indicatif is already a vetted transitive dep at the identical `=0.17.11` resolved copy (RESEARCH Package Legitimacy Audit). slopcheck N/A — zero new registry packages. |
</threat_model>

<verification>
- `cargo build` green; `cargo tree -i indicatif` → one copy at 0.17.11.
- `cargo test --lib integrity` green (spectrum_count == Some(34840) on the real header).
- `cargo test --test verify_roundtrip` green incl. the streaming==slice equivalence test (L1 + L2).
- No `as_f64` introduced on the L1 path; `verify_streaming` never collects the source.
</verification>

<success_criteria>
- `verify_streaming(reader, output, level)` exists, is re-exported, and is byte-equivalent to
  `verify_against_source` on the fixture (DAT-01 prerequisite).
- `ImzmlHeader.spectrum_count` carries the `<spectrumList count>` total (CLI-02 progress source).
- `indicatif = "=0.17.11"` is a direct dep with one resolved copy (the single sanctioned Cargo.toml change).
</success_criteria>

<output>
Create `.planning/phases/06-cli-ux-acceptance-gate/06-01-SUMMARY.md` when done.
</output>
