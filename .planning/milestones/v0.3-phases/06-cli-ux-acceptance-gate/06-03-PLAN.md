---
phase: 06-cli-ux-acceptance-gate
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - tests/acceptance.rs
autonomous: true
requirements: [DAT-01]
must_haves:
  truths:
    - "Converting the full real PXD001283 dataset (34,840 spectra) end-to-end produces a valid archive the reference reader opens"
    - "verify_streaming over the converted archive passes VER-01..04 at L1 on all 34,840 spectra"
    - "The acceptance run is bounded-memory (streamed convert + streamed verify) and #[ignore]-gated out of the default test run"
  artifacts:
    - path: "tests/acceptance.rs"
      provides: "#[ignore] full-dataset roundtrip acceptance test (DAT-01)"
      contains: "acceptance_pxd001283_full_roundtrip"
  key_links:
    - from: "tests/acceptance.rs"
      to: "mzml2mzpeak::write::convert + verify::verify_streaming"
      via: "convert(reader, &out) then verify_streaming(reader2, &out, L1)"
      pattern: "verify_streaming"
    - from: "tests/acceptance.rs"
      to: "data/HR2MSImouseurinarybladderS096.imzML"
      via: "ImagingReader::open on the real file"
      pattern: "HR2MSImouseurinarybladderS096"
---

<objective>
The milestone acceptance gate (DAT-01): an `#[ignore]`-gated integration test that converts the
REAL `data/HR2MSImouseurinarybladderS096.imzML` (with its present 815 MB `.ibd`) end-to-end and
runs `verify_streaming` asserting VER-01..04 pass at L1 on all 34,840 spectra under bounded
memory. This is the project's proof of core value — it must run the real dataset, not a synthetic
stand-in.

Purpose: prove lossless preservation on the full real-world MSI dataset.
Output: `tests/acceptance.rs` with one `#[ignore]` test, run via `cargo test --release -- --ignored acceptance`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cli-ux-acceptance-gate/06-RESEARCH.md
@.planning/phases/06-cli-ux-acceptance-gate/06-PATTERNS.md
@.planning/phases/06-cli-ux-acceptance-gate/06-01-SUMMARY.md
@.planning/phases/06-cli-ux-acceptance-gate/06-02-SUMMARY.md

<interfaces>
<!-- Wave-1/Wave-2 outputs + existing library. Executor uses these directly. -->

From mzml2mzpeak (lib, public):
- `read::ImagingReader::open(path: &Path) -> Result<ImagingReader, ReadError>` — runs preflight; ONE-SHOT iterator (convert consumes it; re-open for verify — Pitfall 2).
- `write::convert(reader: ImagingReader, out_path: &Path) -> Result<.., WriteError>` (src/write/convert.rs:38) — streaming, one spectrum at a time (IN-08). Confirm the exact return type when reading.
- `verify::verify_streaming(reader: ImagingReader, output_path: &Path, level: ConformanceLevel) -> Result<VerificationReport, VerifyError>` (Wave 1) — bounded-memory verify core.
- `schema::ConformanceLevel::L1BitForBit`.
- `VerificationReport`: `count.source_count: usize`, `passed(&self) -> bool` (src/verify/report.rs).

Real data (present locally, confirmed CONTEXT :19-21):
- `data/HR2MSImouseurinarybladderS096.imzML` (~56 MB) + `data/HR2MSImouseurinarybladderS096.ibd` (815 MB).
- Spectrum count: 34,840 (processed mode, centroid → verify routes pixels to the peaks facet; the f32→f64 centroid m/z widening is out-of-L1-scope, NOT a failure — Pitfall 6, already handled by verify_streaming reusing the representation branch).

Validation via the Rust `MzPeakReader` ONLY (verify_streaming uses it internally) — the Python
reader crashes on IMS:* (Pitfall 5, tests/verify_roundtrip.rs:31).

Acceptance skeleton — 06-PATTERNS.md :215-236 / 06-RESEARCH.md :378-409.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: #[ignore] full-dataset PXD001283 roundtrip acceptance test (DAT-01)</name>
  <files>tests/acceptance.rs</files>
  <read_first>
    - tests/verify_roundtrip.rs:1-46 (integration roundtrip structure + the Python-reader caveat at :31).
    - src/write/convert.rs:38+ (confirm convert's exact signature/return for the `.expect(...)`).
    - src/read/stream.rs:114 (open) — confirm convert consumes the reader so a second open is needed for verify.
    - 06-PATTERNS.md "tests/acceptance.rs" (:211-236) and the Shared "Reader is one-shot — open twice" (:262-264).
    - 06-RESEARCH.md "Acceptance test skeleton" (:377-409), Pitfall 2 (:346-350), Pitfall 3 (:352-355,
      double .ibd digest acceptable), Pitfall 5 (:364-367), Pitfall 6 (:369-373); CONTEXT Area 4 (:61-71,
      soft RSS only, run in --release).
  </read_first>
  <action>
    Create `tests/acceptance.rs` with a single test
    `#[test] #[ignore = "heavy: converts the full 34,840-spectrum PXD001283 dataset; run with --release"]
    fn acceptance_pxd001283_full_roundtrip()`:
    1. `let input = Path::new("data/HR2MSImouseurinarybladderS096.imzML");` —
       `assert!(input.exists(), "PXD001283 .imzML must be present")`.
    2. Output to a temp path: `let out = std::env::temp_dir().join("pxd001283_acceptance.mzpeak");`
       (clean any prior file first).
    3. Convert: `let reader = ImagingReader::open(input).expect("open source for convert");
       convert(reader, &out).expect("full-dataset conversion completes");` (streaming; one spectrum at a time).
    4. Verify (bounded; a FRESH reader — convert consumed the first, Pitfall 2):
       `let reader2 = ImagingReader::open(input).expect("re-open source for verify");
       let report = verify_streaming(reader2, &out, ConformanceLevel::L1BitForBit)
         .expect("verification runs without a typed error");`
    5. Assert VER-01..04 at L1: `assert_eq!(report.count.source_count, 34_840, "VER-01: source count");`
       and `assert!(report.passed(), "VER-01..04 must all pass at L1: {report:?}");`.
    6. Clean up: `let _ = std::fs::remove_file(&out);`.
    7. Soft RSS observation: log peak RSS if cheaply available (e.g. a `log::info!`/`eprintln!` note) —
       NO hard assertion (environment-dependent, CONTEXT Area 4).

    Validate strictly via the Rust path (verify_streaming → MzPeakReader); do NOT introduce the Python
    reader (Pitfall 5). The double .ibd digest (convert + verify each re-preflight the 815 MB sidecar)
    is acceptable for this one-shot run (Pitfall 3) — do not add an `open_preverified` optimization
    unless the run is intolerably slow.
  </action>
  <verify>
    <automated>cargo test --release --test acceptance -- --ignored acceptance_pxd001283_full_roundtrip</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test acceptance` (default, no `--ignored`) compiles the test but does NOT run it
      (`#[ignore]` keeps it out of the default suite).
    - `cargo test --release --test acceptance -- --ignored acceptance_pxd001283_full_roundtrip` runs the
      real 34,840-spectrum convert + verify and PASSES: `report.count.source_count == 34_840` and
      `report.passed()` true (VER-01..04 at L1).
    - The test re-opens the source for verify (a second `ImagingReader::open`) — never reuses the
      convert reader, never collects the source into a Vec (verify_streaming guarantees bounded memory).
    - `grep -c "verify_streaming" tests/acceptance.rs` ≥ 1 and `grep -c "verify_roundtrip" tests/acceptance.rs` == 0
      (the 34k path uses the bounded streaming core, NOT the collect-all entry).
  </acceptance_criteria>
  <done>
    The `#[ignore]` acceptance test converts the full real PXD001283 dataset and verify_streaming
    confirms VER-01..04 at L1 on all 34,840 spectra under bounded memory; it is excluded from the
    default `cargo test` run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real imzML/.ibd (815 MB) → preflight/reader | Largest untrusted input; integrity-gated before read |
| converted .mzpeak → MzPeakReader | Re-opened read-only for verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-mem | Denial of Service | full 34,840-spectrum convert + verify | mitigate | Streaming convert (IN-08) + bounded verify_streaming; the test asserts the streaming core is used (no `verify_roundtrip` collect-all). Peak RSS observed softly. |
| T-6-fidelity | Tampering (data loss) | VER-01..04 at L1 on real centroid data | mitigate | verify_streaming reuses the representation branch verbatim; centroid f32→f64 m/z widening is correctly out-of-L1-scope (Pitfall 6) so a faithful roundtrip passes, an unfaithful one fails. |
| T-6-integrity | Tampering | real .ibd preflight | mitigate | `ImagingReader::open` runs the UUID + SHA-1 preflight on the 815 MB sidecar before any read; a corrupt/wrong .ibd hard-fails before conversion. |
| T-6-SC | Tampering | npm/pip/cargo installs | accept | No new crate; this plan adds only a test file. No install task → no legitimacy checkpoint. |
</threat_model>

<verification>
- `cargo test --test acceptance` (default) compiles but skips the `#[ignore]` test.
- `cargo test --release --test acceptance -- --ignored acceptance_pxd001283_full_roundtrip` passes on
  the real dataset: source_count == 34_840 and report.passed() at L1.
- The test uses verify_streaming (bounded), not verify_roundtrip (collect-all).
</verification>

<success_criteria>
- DAT-01: the full PXD001283 (34,840-spectrum) dataset converts end-to-end with bounded memory and
  passes all VER-01..04 checks at L1, proven by the `#[ignore]` acceptance test on the real file.
</success_criteria>

<output>
Create `.planning/phases/06-cli-ux-acceptance-gate/06-03-SUMMARY.md` when done.
</output>
