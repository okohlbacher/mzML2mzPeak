---
phase: 07-reverse-read-spike-dependency-audit
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - .planning/phases/07-reverse-read-spike-dependency-audit/07-FINDINGS.md
autonomous: true
requirements: [RMZ-01, RMZ-02, RMZ-03, RMZ-04]
must_haves:
  truths:
    - "A live cargo tree audit records whether SHA-1 and MD5 are already reachable in the dependency graph"
    - "The milestone checksum term is decided (MD5 IMS:1000090 vs SHA-1 IMS:1000091) and documented for Phase 8 IBD-03, defaulting to the zero-new-crates choice"
    - "07-FINDINGS.md captures the durable empirical evidence: the spike GATE result and the checksum decision"
  artifacts:
    - path: ".planning/phases/07-reverse-read-spike-dependency-audit/07-FINDINGS.md"
      provides: "the phase's durable deliverable: dependency audit + checksum decision + read-spike evidence"
      contains: "IMS:1000090"
  key_links:
    - from: "07-FINDINGS.md"
      to: "cargo tree -i sha1 / -i md-5"
      via: "recorded live audit output"
      pattern: "cargo tree"
    - from: "07-FINDINGS.md"
      to: "Phase 8 IBD-03"
      via: "documented checksum term decision (compute_digest reuse, no new hasher)"
      pattern: "IBD-03"
---

<objective>
Settle the one true "audit" deliverable of Phase 7: run a live `cargo tree` dependency audit
to record whether SHA-1 and MD5 are already reachable, decide the milestone's checksum term
(MD5 `IMS:1000090` vs SHA-1 `IMS:1000091`) defaulting to the zero-new-crates choice, and
capture both that decision and the Plan-02 read-spike evidence into `07-FINDINGS.md` — the
phase's durable artifact that Phase 8 (IBD-03) consumes.

Purpose: Lock the checksum decision before any `.ibd` emit code is written (Phase 8), and
preserve reproducible evidence that the reverse read path is de-risked.

Output: `07-FINDINGS.md` (dependency audit table + checksum decision + read-spike GATE result).
No source code; this is the documentation/decision deliverable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-reverse-read-spike-dependency-audit/07-RESEARCH.md
@.planning/phases/07-reverse-read-spike-dependency-audit/07-02-SUMMARY.md

<interfaces>
<!-- Audit evidence + checksum machinery. Use directly. -->

Checksum machinery to be REUSED in Phase 8 (do NOT write a new hasher, do NOT cargo add):
  src/integrity/preflight.rs:144-166  compute_digest / stream_digest (chunked RustCrypto)
  src/integrity/header.rs:21-44       ChecksumType { Md5, Sha1, Sha256 } <-> IMS:1000090/91/92

Audit commands (RESEARCH already ran these live 2026-06-04 — re-run to confirm, record output):
  cargo tree -i sha1   -> sha1 v0.10.6, DIRECT dep of mzml2mzpeak (integrity preflight)
  cargo tree -i md-5   -> md-5 v0.10.6, DIRECT dep (RustCrypto, imported `as md5`)
  cargo tree -i md5    -> md5 v0.7.0, transitive via mzdata's mzML writer

Audit verdict (RESEARCH): BOTH sha1 and md-5 are already pinned direct deps -> "zero new
crates" holds for EITHER. Decision turns on spec/interop, not the dep graph.
RECOMMENDATION: emit MD5 (IMS:1000090) -- community/HR2MSI default + existing preflight default;
SHA-1 (IMS:1000091) recorded as equally zero-cost for Phase 8 if interop prefers it.
Crate caution (Pitfall 6): use RustCrypto `md-5` (preflight.rs:148), NOT the transitive `md5 v0.7.0`.

Spike evidence to fold in: the GATE: PASS output from Plan 02 Task 2 (recorded in 07-02-SUMMARY.md).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the cargo tree checksum audit and record the verdict</name>
  <files>(no source files — cargo tree audit; evidence captured in Task 2)</files>
  <read_first>
    - 07-RESEARCH.md lines 320-337 (the dependency audit code block + audit verdict) and lines 282-287 (Pitfall 6 md5 vs md-5)
    - src/integrity/header.rs lines 21-44 (ChecksumType <-> IMS accession mapping)
    - src/integrity/preflight.rs lines 144-166 (compute_digest / stream_digest — the machinery Phase 8 reuses)
    - Cargo.toml lines 49-51 (sha1/md-5/sha2 pins)
  </read_first>
  <action>
    Re-run the live dependency audit to confirm the RESEARCH finding still holds on the current
    `Cargo.toml`: `cargo tree -i sha1`, `cargo tree -i md-5`, and `cargo tree -i md5`. Capture
    the exact output. Confirm both `sha1 v0.10.6` and `md-5 v0.10.6` are direct deps of
    `mzml2mzpeak` (added by the v0.3 integrity preflight) and that `md5 v0.7.0` is a transitive
    via mzdata. This proves "zero new crates" holds for either algorithm.

    Do NOT `cargo add` anything. The audit is read-only on the dependency graph.
  </action>
  <verify>
    <automated>cargo tree -i md-5 2>&1 | grep -q 'mzml2mzpeak' && cargo tree -i sha1 2>&1 | grep -q 'mzml2mzpeak' && echo OK || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `cargo tree -i sha1` shows `sha1 v0.10.6` with `mzml2mzpeak` as a direct dependent.
    - `cargo tree -i md-5` shows `md-5 v0.10.6` with `mzml2mzpeak` as a direct dependent.
    - `cargo tree -i md5` shows `md5 v0.7.0` reached only via `mzdata` (transitive).
    - The exact captured output is preserved for Task 2 to embed in 07-FINDINGS.md.
    - No package added to `Cargo.toml` (verifiable: `git diff --stat Cargo.toml` empty).
  </acceptance_criteria>
  <done>The live audit confirms both SHA-1 and MD5 are zero-new-crate options; the md5-vs-md-5 distinction is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Author 07-FINDINGS.md — checksum decision + read-spike evidence + adversarial review</name>
  <files>.planning/phases/07-reverse-read-spike-dependency-audit/07-FINDINGS.md</files>
  <read_first>
    - 07-02-SUMMARY.md (the spike GATE: PASS output and per-test results from Plan 02)
    - 07-RESEARCH.md lines 40-46 (summary + audit decisiveness), 337 (audit verdict), 339-346 (state-of-the-art: the stale "sha1 may not be reachable" line now corrected)
    - .planning/phases/01-*/*FINDINGS.md if present (the FINDINGS doc convention to mirror — durable empirical evidence format)
    - src/integrity/header.rs lines 21-44 (accession terms to cite: IMS:1000090 MD5 / IMS:1000091 SHA-1)
  </read_first>
  <action>
    Author `07-FINDINGS.md` as the phase's durable deliverable. Include four sections:

    1. Dependency audit: the verbatim `cargo tree -i sha1 / -i md-5 / -i md5` output from
       Task 1, with the verdict that BOTH SHA-1 and MD5 are already pinned direct deps (zero
       new crates holds for either) and the note that the v0.4-SUMMARY "sha1 may not be
       reachable" line is now stale/corrected.

    2. Checksum DECISION (the gate for Phase 8 IBD-03): decide and state the chosen term.
       Default to MD5 (`IMS:1000090`) per the zero-new-crates rule + community/HR2MSI
       convention + the existing preflight default; record SHA-1 (`IMS:1000091`) as an equally
       zero-cost alternative Phase 8 may flip to with no dependency change. State that Phase 8
       reuses `src/integrity::preflight::compute_digest` (RustCrypto `md-5`, NOT the transitive
       `md5 v0.7.0`) — no new hasher, no `cargo add` (Pitfall 6). Reference IBD-03 explicitly.

    3. Read-spike evidence: fold in the Plan-02 `GATE: PASS` result on the real
       `out/HR2MSI.mzpeak` (34,840 pixels) and the five passing RMZ-01..04 tests, confirming
       count + source-dtype (no-widen) arrays + accession coords + graceful `metadata.imaging`
       + `NotImaging` hard-fail.

    4. Phase open/close adversarial review: record that the project-convention adversarial
       CODEX/CLI review was performed at phase open and close (per STATE.md process decision).
       Note findings or "none".

    Do NOT introduce any new requirement, scope, or emit-side detail (Phases 8-9 deferred).
  </action>
  <verify>
    <automated>test -f .planning/phases/07-reverse-read-spike-dependency-audit/07-FINDINGS.md && grep -q 'IMS:1000090' .planning/phases/07-reverse-read-spike-dependency-audit/07-FINDINGS.md && grep -q 'cargo tree' .planning/phases/07-reverse-read-spike-dependency-audit/07-FINDINGS.md && grep -q 'IBD-03' .planning/phases/07-reverse-read-spike-dependency-audit/07-FINDINGS.md && echo OK || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `07-FINDINGS.md` exists and contains all four sections (audit, decision, read-spike evidence, adversarial review).
    - The checksum decision names the chosen accession term (`IMS:1000090` MD5 default, `IMS:1000091` SHA-1 alternative) and references Phase 8 IBD-03 + `compute_digest` reuse.
    - The verbatim `cargo tree` output from Task 1 is embedded.
    - The read-spike `GATE: PASS` result and the five RMZ-01..04 test outcomes are recorded.
    - No new dependency, scope, or emit-side detail is introduced (Phases 8-9 stay deferred).
  </acceptance_criteria>
  <done>07-FINDINGS.md is the committed durable deliverable: checksum decided for Phase 8, dependency audit and read-spike evidence captured, adversarial review recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.ibd` checksum algorithm choice | The checksum is an INTEGRITY control (detect corruption of an untrusted archive), not a secrecy primitive — MD5/SHA-1 are spec-mandated imzML CV terms (IMS:1000090/91). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | wrong/ambiguous checksum algorithm in the emitted `.imzML` -> reader cannot verify `.ibd` integrity | mitigate | Decide and document a single checksum term (MD5 IMS:1000090) reusing the existing tested `compute_digest`; Phase 8 emits exactly that term. |
| T-07-06 | Tampering | a second/duplicate MD5 crate or wrong hasher introduced | accept (documented) | RESEARCH Pitfall 6 recorded in FINDINGS: reuse RustCrypto `md-5` (preflight.rs), never the transitive `md5 v0.7.0`; no `cargo add`. Low risk — decision doc only, no code change here. |

Note: MD5/SHA-1 cryptographic weakness is NOT a threat here — these are file-integrity
checksums fixed by the imzML spec, used to detect corruption, not to resist a forging adversary.
</threat_model>

<verification>
- `cargo tree -i sha1` / `-i md-5` confirm both are direct deps of `mzml2mzpeak`.
- `07-FINDINGS.md` exists with audit output, checksum decision (IMS:1000090 default), read-spike evidence, and adversarial review.
- No change to `Cargo.toml` (no crate added).
</verification>

<success_criteria>
- The checksum term is decided and documented for Phase 8 IBD-03 (MD5 `IMS:1000090` default, SHA-1 `IMS:1000091` recorded alternative), defaulting to zero-new-crates.
- The live `cargo tree` audit verdict is captured verbatim.
- `07-FINDINGS.md` is the durable phase deliverable consolidating audit + read-spike evidence + open/close adversarial review.
</success_criteria>

<output>
Create `.planning/phases/07-reverse-read-spike-dependency-audit/07-03-SUMMARY.md` when done.
</output>
