---
phase: 09-imzml-xml-emitter
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/reverse/imzml_writer.rs
autonomous: true
requirements: [IXML-01, IXML-02, IXML-03]

must_haves:
  truths:
    - "A fixture .imzML+.ibd pair emitted by ImzmlWriter+IbdWriter re-opens via mzdata::ImzMLReader::new without the metadata parse setting an error (SC-1)"
    - "The reader populates the required imzML metadata (uuid is Some) — proving the three <fileContent> IMS terms parsed"
    - "Iterating the emitted spectra yields Ok and round-reads each pixel's IMS:1000050/51/52 coords equal to what was written (SC-4)"
    - "Round-read array element counts (per binaryDataArray) equal the ArrayRef.count that was emitted, at the correct dtype width (SC-4)"
    - "A fixture with metadata.imaging absent still re-reads via mzdata without error (PXD001283-shape graceful degradation)"
  artifacts:
    - path: "src/reverse/imzml_writer.rs"
      provides: "#[cfg(test)] conformance block: SC-1 (mzdata opens) + SC-4 (round-read coords + array shapes) + absent-metadata re-read"
      contains: "ImzMLReader"
  key_links:
    - from: "src/reverse/imzml_writer.rs tests"
      to: "mzdata::io::imzml::ImzMLReader"
      via: "ImzMLReader::new(xml_file, ibd_file) re-read oracle"
      pattern: "ImzMLReader::new"
    - from: "src/reverse/imzml_writer.rs tests"
      to: "src/reverse/ibd.rs::IbdWriter"
      via: "real .ibd built so UUID/MD5/offsets line up with the emitted .imzML"
      pattern: "IbdWriter"
    - from: "src/reverse/imzml_writer.rs tests"
      to: "IMS:1000050 coord read-back"
      via: "scan.get_param_by_curie(&curie!(IMS:1000050)).value.to_i64()"
      pattern: "get_param_by_curie"
---

<objective>
Close the conformance loop: prove the Plan 09-01 emitter satisfies the vendored
`mzdata::ImzMLReader` contract end to end. Build a tiny fixture archive (real `.ibd` via
`IbdWriter` + matching `.imzML` via `ImzmlWriter`), then re-open it with `mzdata::ImzMLReader::new`
— the reader IS the conformance oracle. SC-1 proves it opens without error; SC-4 proves coords and
array shapes round-read to what was emitted. Also prove the absent-`metadata.imaging` (PXD001283)
shape still re-reads.

Purpose: SC-1/SC-4 are the two headline phase success criteria. The reader hard-fails if any
required term is missing; passing the re-read is the decisive proof that the emitted byte layout is
correct (IXML-01/02/03 verified against the oracle, not just by grep).

Output: a `#[cfg(test)]` conformance block in `src/reverse/imzml_writer.rs` (extends the
Plan 09-01 test block). No production-code change — Plan 09-01 owns the emitter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-imzml-xml-emitter/09-RESEARCH.md
@.planning/phases/09-imzml-xml-emitter/09-PATTERNS.md
@.planning/phases/09-imzml-xml-emitter/09-01-PLAN.md

<interfaces>
<!-- The re-read oracle + fixture builders. Use these directly. -->

mzdata re-read oracle (vendored signature, reader.rs:542):
```rust
use mzdata::io::imzml::ImzMLReader;          // re-export: vendor/.../imzml/mod.rs:24
use mzdata::prelude::*;
let mut reader = ImzMLReader::new(xml_file, ibd_file); // two std::fs::File handles
// SC-1: reader.imzml_metadata.uuid.is_some() (populated only when the 3 required <fileContent> terms parsed)
// SC-4 coord read (SAME path as the Phase 7 read half):
let s = reader.read_next().expect("first spectrum");
let scan = s.description().acquisition.first_scan().unwrap();
let x = scan.get_param_by_curie(&mzdata::curie!(IMS:1000050)).unwrap().value.to_i64().unwrap();
```

Fixture .ibd builder (Phase 8 — capture ArrayRefs + MD5 so the .imzML lines up):
```rust
// mint Uuid::new_v4(); let mut w = IbdWriter::new(ibd_path, uuid)?;
// let mz_ref = w.append(&mz_numarray)?;  let int_ref = w.append(&int_numarray)?;  (per spectrum)
// let md5_hex = w.finish()?;  // pass uuid + md5_hex + the ArrayRefs into ImzmlWriter
```

tempdir() test helper (already added in Plan 09-01; copied verbatim from ibd.rs:197-210):
```rust
fn tempdir() -> std::path::PathBuf { /* temp_dir + nanos + thread id; no tempfile crate */ }
```
</interfaces>

<read_first>
- src/reverse/imzml_writer.rs — the module + its Plan-09-01 #[cfg(test)] block (extend it here).
- src/reverse/ibd.rs::tests — fixture-construction analog (~ibd.rs:214-243): tempdir(), mint
  Uuid::new_v4(), append arrays, capture ArrayRefs, finish() for the MD5.
- vendor/mzdata/src/io/imzml/reader.rs — the oracle contract: ImzMLReader::new(file, ibd_file)
  (~:542); imzml_metadata.uuid populated only when the 3 required <fileContent> terms parse
  (~:176-201, 741-762); load_ibd_arrays sizes reads as count×dtype.size_of() (~:993) so the dtype
  term must be correct; check_ibd_file only WARNS on UUID mismatch (~:594-611) but the fixture must
  still match.
- vendor/mzdata/src/io/imzml/tests.rs — the reader's own fixture: <cv id="IMS"> + namespace
  http://psi.hupo.org/ms/mzml the test file must contain (~:11-19).
- src/bin/spike_reverse_read.rs (~:89-97) + src/read/stream.rs (~:184-190) — the EXACT coord
  read-back path to reuse: get_param_by_curie(&curie!(IMS:1000050)).value.to_i64().
- 09-RESEARCH.md §"Test strategy (SC-1, SC-4)" + Code Examples §"Re-read test" — the fixture +
  assertion recipe.
- 08-01-SUMMARY.md §"Flags for Phase 9" — empty-array offset ≥16; dtype lives in the XML; UUID/MD5
  linkage must use IbdWriter::uuid() + finish().
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: SC-1 — emit a fixture .imzML+.ibd and re-open via mzdata::ImzMLReader without error</name>
  <files>src/reverse/imzml_writer.rs</files>
  <read_first>
    vendor/mzdata/src/io/imzml/reader.rs (ImzMLReader::new ~:542; uuid populated ~:176-201),
    src/reverse/ibd.rs::tests (fixture construction), 09-RESEARCH.md §"Test strategy (SC-1, SC-4)".
  </read_first>
  <action>
    In the #[cfg(test)] block of src/reverse/imzml_writer.rs, add `roundtrip_reads`: under
    tempdir(), mint a Uuid::new_v4(); build a 2-spectrum .ibd via IbdWriter (append 4 arrays — a
    mixed-dtype set, e.g. f64 m/z + f32 intensity per spectrum — capturing each ArrayRef), call
    finish() for the md5 hex. Emit the matching .imzML via ImzmlWriter::new(xml_path, uuid, &md5_hex,
    2, None) then two write_spectrum calls (passing each spectrum's coords + the captured (dtype,
    ArrayRef) pairs) then finish(). Re-open with mzdata::io::imzml::ImzMLReader::new(File::open(xml)?,
    File::open(ibd)?). Assert SC-1: reader.imzml_metadata.uuid.is_some() (the three required
    <fileContent> terms parsed → open did not silently fail) AND that read_next() for the first
    spectrum returns Ok/Some. Use mzdata::prelude::* + mzdata::curie! as in the Phase 7 read path.
    Do not add any crate; use the tempdir() helper already present.
  </action>
  <verify>
    <automated>cargo test --lib reverse::imzml_writer::tests::roundtrip_reads</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib reverse::imzml_writer::tests::roundtrip_reads` passes (SC-1).
    - The test calls the oracle: `grep -q 'ImzMLReader::new' src/reverse/imzml_writer.rs`.
    - The test asserts uuid populated: `grep -q 'imzml_metadata.uuid' src/reverse/imzml_writer.rs`
      (or an equivalent assert that the required metadata parsed).
    - The fixture uses a REAL IbdWriter so linkage matches: `grep -q 'IbdWriter' src/reverse/imzml_writer.rs`.
    - `git diff --quiet Cargo.toml Cargo.lock` (no new crate; tempfile NOT added — tempdir() helper used).
  </acceptance_criteria>
  <done>
    A fixture .imzML+.ibd pair re-opens via mzdata::ImzMLReader with required metadata populated and
    the first spectrum iterating Ok — SC-1 proven.
  </done>
</task>

<task type="auto">
  <name>Task 2: SC-4 — round-read coords + array shapes; absent-metadata re-read</name>
  <files>src/reverse/imzml_writer.rs</files>
  <read_first>
    src/bin/spike_reverse_read.rs:89-97 + src/read/stream.rs:184-190 (coord read-back path),
    vendor/mzdata/src/io/imzml/reader.rs (load_ibd_arrays count×dtype sizing ~:993),
    09-RESEARCH.md Code Examples §"Re-read test".
  </read_first>
  <action>
    Add `coords_and_arrays_roundread`: reuse the Task-1 fixture pattern with KNOWN coords (e.g.
    spectrum 0 → x=1,y=1; spectrum 1 → x=2,y=1, 1-based) and known array element counts. After
    re-opening via ImzMLReader, for each spectrum read the first scan and assert the round-read
    IMS:1000050/51 (and 52 when emitted) equal the emitted coords via
    scan.get_param_by_curie(&curie!(IMS:1000050)).value.to_i64() (the SAME path as the Phase 7 read
    half). Then assert each spectrum's read-back m/z and intensity arrays have the element count that
    was emitted (the reader sizes reads as count×dtype.size_of(), so a correct count proves the
    dtype term is right too — guards Pitfall 3 off-by-2× width). Add `filecontent_and_scansettings`:
    (a) build a fixture with imaging=None and assert it re-reads via ImzMLReader without error
    (PXD001283 graceful-degradation shape); (b) build a fixture with Some(ImagingMetadata) carrying
    at least pixel_size_um and assert it ALSO re-reads without error (spec-rich scanSettings does not
    break re-read). Reuse the tempdir()/IbdWriter fixture helpers; no new crate.
  </action>
  <verify>
    <automated>cargo test --lib reverse::imzml_writer</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib reverse::imzml_writer::tests::coords_and_arrays_roundread` passes (SC-4):
      round-read x/y coords assert-equal the emitted 1-based values.
    - The coord read-back uses the canonical path: `grep -q 'get_param_by_curie' src/reverse/imzml_writer.rs`
      and references IMS:1000050 (`grep -q 'IMS:1000050' src/reverse/imzml_writer.rs`).
    - Array element counts round-read equal-to-emitted (assert in coords_and_arrays_roundread proves
      dtype width correct — no off-by-2×).
    - `cargo test --lib reverse::imzml_writer::tests::filecontent_and_scansettings` passes: both the
      imaging=None and imaging=Some fixtures re-read via ImzMLReader without error.
    - Full module green: `cargo test --lib reverse::imzml_writer`.
    - `git diff --quiet Cargo.toml Cargo.lock` (no new crate).
  </acceptance_criteria>
  <done>
    Coords and per-array element counts round-read exactly through mzdata at the correct dtype width
    (SC-4), and both present- and absent-metadata fixtures re-read without error.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| emitted .imzML+.ibd pair → mzdata::ImzMLReader | The conformance oracle re-parses our output; a missing/malformed required term surfaces here as a parse error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-CONF | Tampering | required-term completeness in emitted .imzML | mitigate | SC-1 re-read asserts uuid populated (3 required <fileContent> terms parsed) + first spectrum Ok; a missing term fails the test loudly. |
| T-09-WIDTH | Tampering | per-array dtype term vs read-back width | mitigate | SC-4 asserts round-read element counts equal emitted (reader sizes count×dtype.size_of()) — a wrong dtype term off-by-2× fails the assert. |
| T-09-LINK | Spoofing/Integrity | UUID/MD5 linkage .imzML vs .ibd | mitigate | Fixture builds the .ibd via the SAME minted Uuid + IbdWriter::finish() md5 passed into ImzmlWriter; check_ibd_file warns on mismatch (reader.rs:594-611). |
| T-09-FAB | Integrity | absent metadata.imaging | mitigate | filecontent_and_scansettings imaging=None fixture proves a valid file with no fabricated geometry still re-reads. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No packages added (tests reuse tempdir() helper, not tempfile). git diff --quiet Cargo.toml Cargo.lock enforced. |
</threat_model>

<verification>
- `cargo test --lib reverse::imzml_writer` green (SC-1 roundtrip_reads, SC-4
  coords_and_arrays_roundread, filecontent_and_scansettings present+absent).
- `cargo test` full suite green — no regression to v0.3 or Phase 8 tests.
- `git diff --quiet Cargo.toml Cargo.lock` — no new crate.
</verification>

<success_criteria>
- SC-1: emitted fixture .imzML re-opens via mzdata::ImzMLReader without error (required metadata populated).
- SC-4: coords (1-based IMS:1000050/51/52) and per-array element counts round-read exactly at the
  correct dtype width.
- Absent metadata.imaging (PXD001283 shape) still re-reads — graceful degradation proven.
- No new crate; full suite green.
</success_criteria>

<output>
Create `.planning/phases/09-imzml-xml-emitter/09-02-SUMMARY.md` when done.
</output>
