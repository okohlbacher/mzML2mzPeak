---
phase: 10-streaming-reverse-orchestration-reverse-cli
plan: 03
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - tests/reverse_convert.rs
  - tests/fixtures/reverse/mod.rs
autonomous: true
requirements: [RCLI-01, RCLI-02]
must_haves:
  truths:
    - "A small synthetic imaging .mzpeak reverses to an .imzML+.ibd that mzdata::ImzMLReader re-reads (coords + array shapes + shared UUID intact)"
    - "A ~5,000-pixel synthetic archive reverses via the streaming loop and re-reads without OOM (bounded-memory proof at scale)"
    - "A non-imaging .mzpeak fails fast with a distinct exit code and writes NO output (end-to-end via the built binary)"
    - "The .imzML and .ibd produced by the CLI share a stem and the same UUID"
  artifacts:
    - path: "tests/reverse_convert.rs"
      provides: "end-to-end oracle + bounded-memory + CLI-subprocess fail-fast integration tests"
      contains: "ImzMLReader"
    - path: "tests/fixtures/reverse/mod.rs"
      provides: "parameterized N-pixel imaging archive builder for the bounded-memory test"
      contains: "imaging_archive_n"
  key_links:
    - from: "tests/reverse_convert.rs"
      to: "src/reverse/convert.rs::convert"
      via: "library convert() over a synthetic fixture, then re-read via mzdata::ImzMLReader"
      pattern: "reverse::convert"
    - from: "tests/reverse_convert.rs"
      to: "the built mzml2mzpeak binary"
      via: "subprocess run on a non-imaging fixture, assert exit code + no output"
      pattern: "Command::new"
---

<objective>
Prove the wired reverse pipeline end-to-end: (1) an oracle test that a synthetic imaging
`.mzpeak` reverses to an `.imzML`+`.ibd` pair the vendored `mzdata::ImzMLReader` re-reads with
coords + array shapes + shared UUID intact (the Option-C split-and-concat document is byte-valid);
(2) a ~5,000-pixel bounded-memory proof at scale; (3) an end-to-end CLI-subprocess test that a
non-imaging input fails fast with a distinct exit code and leaves no output.

Purpose: Closes RCLI-01 (CLI surface + distinct exit codes proven end-to-end) and RCLI-02
(bounded-memory streaming proven at scale) against the reader-as-oracle, not by grep — the
load-bearing acceptance for the phase before Phase 11's real-archive roundtrip.
Output: `tests/reverse_convert.rs` (new integration test) + a parameterized N-pixel fixture
builder in `tests/fixtures/reverse/mod.rs`. No new crates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-streaming-reverse-orchestration-reverse-cli/10-RESEARCH.md
@.planning/phases/10-streaming-reverse-orchestration-reverse-cli/10-PATTERNS.md
@.planning/phases/09-imzml-xml-emitter/09-02-SUMMARY.md

<interfaces>
<!-- The oracle + fixture + CLI surfaces these tests drive. -->

From src/reverse (Plans 10-01/10-02, SHIPPED by then):
```rust
pub fn crate::reverse::convert::convert(imzml_path: &Path, ibd_path: &Path, archive: &Path)
    -> Result<(), crate::reverse::ReverseError>;
```

From mzdata::io::imzml (the re-read ORACLE — same shape Phase 9 used):
```rust
ImzMLReader::<File,File>::new(File::open(xml), File::open(ibd))   // -> reader
reader.imzml_metadata.uuid: Option<..>                            // Some => 3 fileContent IMS terms parsed
reader.read_into(&mut MultiLayerSpectrum) -> Result<..>          // fallible inherent path (NOT Iterator::next)
scan.get_param_by_curie(&curie!(IMS:1000050)).value.to_i64()     // round-read coords
ByteArrayView::data_len()                                         // round-read element count (== count x dtype size)
```

From tests/fixtures/reverse/mod.rs (SHIPPED — Phase 7):
```rust
pub fn imaging_archive() -> PathBuf;       // 2-pixel positive fixture (Profile + Centroid, F64 m/z / F32 int)
pub fn non_imaging_archive() -> PathBuf;   // no IMS coords -> read_pixel(0) yields NotImaging
// internal: temp_out(tag), write_seam(out, specs, geom), to_mzdata, ImagingSpectrum
```
Include this module from the integration test via:
`#[path = "fixtures/reverse/mod.rs"] mod reverse_fixtures;`

CLI exit codes (src/cli.rs): EXIT_COORDINATE = 4 (NotImaging maps here).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parameterized N-pixel imaging fixture builder (bounded-memory test input)</name>
  <files>tests/fixtures/reverse/mod.rs, tests/reverse_convert.rs</files>
  <read_first>
    - tests/fixtures/reverse/mod.rs (the whole file — imaging_archive lines 118-158, write_seam 77-109, temp_out 61-71, to_mzdata usage)
    - src/read/record.rs (ImagingSpectrum, NumArray, Representation)
    - .planning/phases/10-streaming-reverse-orchestration-reverse-cli/10-RESEARCH.md §"Bounded-Memory Proof Strategy" (N≈5,000, small arrays, sub-second target; A2 timing assumption)
  </read_first>
  <behavior>
    - imaging_archive_n(5000) writes a valid .mzpeak with 5,000 distinct-coordinate Profile pixels, each with small F64 m/z + F32 intensity arrays
    - the produced archive re-opens via MzPeakReader with len() == 5000
    - distinct (x,y) per pixel so a sampled coord round-read is meaningful
  </behavior>
  <action>
    Add `pub fn imaging_archive_n(n: u32) -> PathBuf` to `tests/fixtures/reverse/mod.rs`,
    generalizing the existing `imaging_archive` (lines 118-158): build `n` `ImagingSpectrum`s with
    distinct coordinates (e.g. `x = (i % grid_w) + 1`, `y = (i / grid_w) + 1` for a roughly-square
    grid), each carrying a SMALL `NumArray::F64` m/z + `NumArray::F32` intensity (2-3 elements — the
    test proves the loop streams at scale, not large arrays), `Representation::Profile`, ms_level 1,
    a unique native_id. Reuse `to_mzdata`, the `ImagingRunMetadata` geometry block (grid sized to
    n), `temp_out("imaging_n")`, and `write_seam` verbatim. Keep the existing `imaging_archive`
    (2-pixel) intact — the new fn is additive. Do NOT use the `tempfile` crate (the module already
    hand-rolls temp paths via `temp_out`). Caller removes the returned file.
  </action>
  <verify>
    <automated>cargo test --test reverse_convert builds_n_pixel_fixture -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'pub fn imaging_archive_n' tests/fixtures/reverse/mod.rs` returns a match.
    - A smoke test (`builds_n_pixel_fixture`, added in this task in tests/reverse_convert.rs) builds `imaging_archive_n(5000)`, opens it via `MzPeakReader::new`, asserts `len() == 5000`, then removes the file — passes.
    - The existing `imaging_archive` (2-pixel) signature is unchanged (`grep -n 'pub fn imaging_archive(' tests/fixtures/reverse/mod.rs` still present).
    - `git diff --quiet Cargo.toml Cargo.lock` is clean.
  </acceptance_criteria>
  <done>A parameterized N-pixel imaging fixture exists, produces a valid len()==N archive, reuses the shipped write seam, adds no crate, and leaves the 2-pixel builder intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end oracle + bounded-memory + CLI fail-fast integration tests</name>
  <files>tests/reverse_convert.rs</files>
  <read_first>
    - .planning/phases/09-imzml-xml-emitter/09-02-SUMMARY.md (the emit_fixture / roundtrip_reads / coords_and_arrays_roundread oracle shape to reuse — ImzMLReader::new, imzml_metadata.uuid.is_some(), read_into, data_len)
    - src/reverse/imzml_writer.rs (the #[cfg(test)] oracle block, ~lines 820-924, for the exact ImzMLReader assertion pattern)
    - tests/fixtures/reverse/mod.rs (imaging_archive, imaging_archive_n from Task 1, non_imaging_archive)
    - src/cli.rs (EXIT_COORDINATE = 4 — the NotImaging exit code the subprocess test asserts; derive_reverse_paths semantics for the -o stem)
    - .planning/phases/10-streaming-reverse-orchestration-reverse-cli/10-RESEARCH.md §"Validation Architecture" + §"Bounded-Memory Proof Strategy" + Pitfall 4 (no partial output)
    - .planning/phases/10-streaming-reverse-orchestration-reverse-cli/10-VALIDATION.md (Wave 0 requirements — the four tests this task delivers)
  </read_first>
  <behavior>
    - reverse::convert over the 2-pixel imaging fixture produces an .imzML+.ibd that ImzMLReader re-opens with uuid.is_some(), and each pixel's IMS:1000050/051 coords + per-array element counts round-read equal to the source
    - reverse::convert over imaging_archive_n(5000) produces an archive ImzMLReader re-reads with the right pixel count and a sampled coord/array intact (bounded-memory proof at scale)
    - the .ibd 16-byte UUID header and the .imzML IMS:1000080 reference the SAME uuid; .imzML and .ibd share a stem
    - running the built binary on a non-imaging fixture exits with code 4 (EXIT_COORDINATE) and leaves NO .imzML/.ibd on disk
  </behavior>
  <action>
    Create `tests/reverse_convert.rs`. Include the fixtures via `#[path = "fixtures/reverse/mod.rs"]
    mod reverse_fixtures;`. Use the no-`tempfile` temp-dir pattern (`std::env::temp_dir()` +
    process-id/monotonic name) for output paths; remove all produced files at test end.

    Test `oracle_roundreads_coords_and_shapes` (RCLI-01/02 load-bearing): build `imaging_archive()`,
    derive output paths sharing a stem, call `crate::reverse::convert::convert(&imzml, &ibd,
    &archive)`, then re-open via `mzdata::io::imzml::ImzMLReader::<File,File>::new(File::open(&imzml)?,
    File::open(&ibd)?)`. Assert `reader.imzml_metadata.uuid.is_some()` (the 3 fileContent IMS terms
    parsed). For each pixel, `read_into(&mut MultiLayerSpectrum)` Ok, round-read the 1-based
    `IMS:1000050/051` coords via `scan.get_param_by_curie(...).value.to_i64()` and assert they equal
    the fixture's (3,7) and (11,5); assert each array's round-read element count (`ByteArrayView::
    data_len`) equals the source element count (mixed F64 m/z / F32 intensity — a correct count
    proves the dtype-term width). Reuse the exact assertion shape from the Phase-9
    `coords_and_arrays_roundread` oracle.

    Test `uuid_and_stem_linkage` (SC-4): assert the produced `.imzML` and `.ibd` share a stem
    (same file_stem), and that `reader.imzml_metadata.uuid` (from the .imzML) matches the UUID
    embedded in the first 16 bytes of the `.ibd` (read the header bytes, parse as Uuid). One mint,
    threaded into both.

    Test `bounded_memory_at_scale` (RCLI-02): build `imaging_archive_n(5000)`, run
    `reverse::convert`, re-open via `ImzMLReader`, assert the spectrum count is 5000 and a sampled
    pixel's coord + array element-count round-read correctly. Keep arrays tiny so the run is
    sub-second (A2). This proves the streaming loop holds at scale — an accidental "collect all
    decoded arrays" would also fail the contract; the structural no-collect guard is asserted in
    the Plan 10-01 unit test.

    Test `non_imaging_cli_fails_fast` (RCLI-01, Pitfall 4): build `non_imaging_archive()`, run the
    BUILT binary as a subprocess (`std::process::Command::new(env!("CARGO_BIN_EXE_mzml2mzpeak"))`
    — or the cargo-provided bin-exe env var; mirror `tests/cli.rs::run_cli`) with the non-imaging
    `.mzpeak` as input and `-o <tmp_stem>`. Assert the process exit code is 4 (EXIT_COORDINATE,
    where NotImaging maps), and assert NEITHER `<tmp_stem>.imzML` NOR `<tmp_stem>.ibd` exists
    afterward (no partial output on a non-imaging input). Assert the stderr carries an actionable
    "not an imaging mzPeak" message.
  </action>
  <verify>
    <automated>cargo test --test reverse_convert</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test reverse_convert` passes ALL of: oracle_roundreads_coords_and_shapes, uuid_and_stem_linkage, bounded_memory_at_scale, non_imaging_cli_fails_fast.
    - oracle test asserts `reader.imzml_metadata.uuid.is_some()` AND coord equality (3,7)/(11,5) AND per-array `data_len()` equals the source element counts.
    - uuid_and_stem_linkage asserts the .imzML uuid equals the .ibd 16-byte-header uuid AND `imzml.file_stem() == ibd.file_stem()`.
    - bounded_memory_at_scale asserts the re-read spectrum count == 5000.
    - non_imaging_cli_fails_fast asserts the subprocess exit code == 4 AND `!stem.with_extension("imzML").exists()` AND `!stem.with_extension("ibd").exists()` AND stderr contains "not an imaging mzPeak".
    - `cargo test` (full suite) is green — no regression.
    - `git diff --quiet Cargo.toml Cargo.lock` is clean (no new crate).
  </acceptance_criteria>
  <done>The end-to-end oracle, UUID/stem linkage, ~5,000-pixel bounded-memory, and CLI non-imaging fail-fast tests all pass against the vendored ImzMLReader and the built binary; no new crate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| synthetic fixture `.mzpeak` → reverse pipeline → mzdata oracle | the test exercises the full untrusted-input → output → re-read path that production will see |
| built binary subprocess → exit code + filesystem | end-to-end proof that a non-imaging input is rejected with a distinct code and leaves no partial output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-ORACLE | (correctness) | Option-C split document validity | mitigate | The reader-as-oracle test re-reads the assembled `.imzML`+`.ibd` via `mzdata::ImzMLReader` — proves the split-and-concat byte layout is valid, not asserted by grep (Task 2) |
| T-10-MEM | Denial of Service | 34,840-pixel scale | mitigate | A ~5,000-pixel synthetic archive (sub-second, no real 432 MB file in CI) proves the streaming loop holds at scale (Task 1+2, RCLI-02); the structural no-collect guard lives in Plan 10-01 |
| T-10-PART | Tampering (integrity) | non-imaging input | mitigate | CLI-subprocess test asserts a non-imaging input exits code 4 AND leaves no `.imzML`/`.ibd` — proves the orchestrator's pre-check + cleanup (Task 2, Pitfall 4) |
| T-10-DRIFT | Tampering | UUID linkage | mitigate | uuid_and_stem_linkage test asserts the `.imzML` IMS:1000080 equals the `.ibd` header UUID (one mint, threaded — Task 2, SC-4) |
| T-10-SC | Tampering | dependencies | mitigate | Zero new crates (no `tempfile`); `git diff --quiet Cargo.toml Cargo.lock` asserted |
</threat_model>

<verification>
- `cargo test --test reverse_convert` — oracle, linkage, bounded-memory, CLI fail-fast all green.
- `cargo test` — full suite green, no regression.
- `git diff --quiet Cargo.toml Cargo.lock` — no new dependency.
</verification>

<success_criteria>
- A synthetic imaging `.mzpeak` reverses to an `.imzML`+`.ibd` that `mzdata::ImzMLReader` re-reads with coords + array shapes + shared UUID intact (RCLI-01).
- A ~5,000-pixel archive reverses via the streaming loop and re-reads at scale (RCLI-02 bounded-memory at scale).
- A non-imaging input fails fast end-to-end with a distinct exit code and no partial output (RCLI-01).
- The `.imzML` and `.ibd` share a stem and the same UUID (SC-4).
</success_criteria>

<output>
Create `.planning/phases/10-streaming-reverse-orchestration-reverse-cli/10-03-SUMMARY.md` when done
</output>
