---
milestone: v0.6
audited: 2026-06-06
status: passed
scores:
  requirements: 21/21
  phases: 6/6
  integration: 21/21
  flows: 5/5
---

# Milestone v0.6 Audit — Spec conformance: dtypes + CV/geometry/provenance

**Verdict: PASSED.** All 6 phases complete + verified; all 21 requirements satisfied across three
independent sources (VERIFICATION.md status `passed`, SUMMARY `requirements-completed`, REQUIREMENTS
traceability); cross-phase integration check found 0 orphaned exports, 0 missing connections, 0 broken
flows. `cargo test --no-fail-fast` → 335 passed, 0 failed, 3 `#[ignore]`'d (real-data gates).

## Phase Verifications

| Phase | Reqs | VERIFICATION | Tests |
|-------|------|--------------|-------|
| 16 Canonical-width dtype conformance | DTY-01..07 | passed | suite green |
| 17 cv_list file-level CV declaration | CVL-01/02 | passed | 259 green |
| 18 scan_settings_list authoritative geometry | GEO-01/02/03 | passed | 273 green |
| 19 source_files[] provenance | SRC-01/02 | passed | 277 green |
| 20 Optical image auto-discovery & auto-embed | OPT-01..04 | passed | 314 green |
| 21 Reverse optical image export | RIMG-01..03 | passed | 335 green |

## Requirements Coverage (3-source cross-reference)

21/21 **satisfied**. Each REQ-ID: VERIFICATION `passed` + listed in SUMMARY frontmatter +
REQUIREMENTS traceability — and the integration checker mapped each to a live WIRED cross-phase path.
No orphaned or unsatisfied requirements.

## Cross-Phase Integration (highlights)

- **Single forward seam** (`convert.rs`): `cv_list` + `scan_settings_list` (when geometry) + `imaging`
  (with `images[]` folded) all emitted via `add_index_metadata` before `finish()` — index written last,
  no facet clobbering.
- **`convert_with` signature evolved cleanly**: Phase 18 added `geometry`, Phase 19 added `input_path`,
  both trailing `Option`; the back-compat `convert()` wrapper passes `None`/`None` so all prior callers
  are behaviour-identical.
- **Anti-drift shared constants verified — no drift**: CV URIs (`cv.rs` ↔ reverse `<cvList>`), geometry
  IMS accessions (`scan_settings.rs` ↔ reverse `<scanSettings>`), optical `IMS:1006xxx` (`optical.rs`
  consumed by forward parse + reverse emit + inverse fold).
- **Forward→reverse optical round-trip is a true inverse**: Phase-20 `map_descriptive` ↔ Phase-21
  `recover_descriptive`; sha256 byte fidelity proven.
- **Phase-16 dtype contract holds end-to-end**: canonical f64/f32 forward, value-equal-at-canonical-width
  verify + reverse, not undermined by later phases.

## Tech Debt (carried — non-blocking)

- **Vendored `mzpeak_prototyping` FileEntry-serde fork** (v0.5): still load-bearing — Phase 21 reverse
  image-member read-back depends on it. File the upstream issue + drop the fork when fixed.
- **Vendored `mzdata` fork** (v0.3 `count_chromatograms` patch): carried; one cosmetic edition-2024
  unused-import warning lives in it (not our crate).
- **PXD001283 full-dataset acceptance** (`acceptance_pxd001283_full_roundtrip`) remains `#[ignore]`-gated
  pending the real 815 MB `.ibd` (not in checkout). Canonical-width invariant otherwise proven by the
  synthetic mixed-dtype regression. Outstanding manual run when the `.ibd` is local.

## Deferred (correctly out of scope — v0.7+)

GEO-F forward declared-geometry threading beyond parsed; `pixel` facet (F6); continuous-mode emit (F7);
full `image` entity / `images.parquet` blob + CV-governed registration / true co-registration (F8);
CV governance / canonical IMS URI minting (F9 — the `TODO(F9)` placeholder); L2 conformance (F10);
reverse `<sourceFileList>` copy (RSRC); perfectly-bijective descriptive optical round-trip.

## Nyquist

No VALIDATION.md authored for v0.6 phases (research skipped per the spec-driven nature of the work);
not applicable for this milestone.

**Status: passed → proceed to complete-milestone.**
