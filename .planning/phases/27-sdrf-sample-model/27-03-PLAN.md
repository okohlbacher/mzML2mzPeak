---
phase: 27-sdrf-sample-model
plan: 03
type: execute
wave: 3
depends_on: [27-01, 27-02]
files_modified:
  - src/sdrf/sample_list.rs
  - src/sdrf/mod.rs
  - src/schema/sample.rs
  - src/schema/mod.rs
  - src/write/writer.rs
  - src/write/convert.rs
  - schema/sample_list.json
  - tests/sdrf_sample_list.rs
autonomous: true
requirements: [SDRF-03, SDRF-04]
user_setup: []

must_haves:
  truths:
    - "A file-level sample_list block carries one entry per distinct SDRF source name, with characteristics[*] projected as parameters."
    - "A characteristic maps to a CV-typed parameter when an accession exists, else a userParam whose name is the exact SDRF column header (reversible)."
    - "Each spectrum carries a promoted Int64 assay_ref column foreign-keying into sample_list by index."
    - "A run→sample binding is recorded in file-level metadata."
    - "Re-opening the archive yields sample_list with the projected characteristics and the assay_ref column readable per spectrum."
  artifacts:
    - path: "src/schema/sample.rs"
      provides: "SampleList / SampleEntry / SampleParam serde types (id/name/parameters)"
      min_lines: 30
    - path: "src/sdrf/sample_list.rs"
      provides: "project_sample_list(table) → SampleList + run→sample binding"
      min_lines: 40
    - path: "schema/sample_list.json"
      provides: "JSON schema for the sample_list file-level block (three-places rule)"
      min_lines: 20
  key_links:
    - from: "src/write/convert.rs"
      to: "zip.add_index_metadata"
      via: "sample_list + run binding keys at the terminal seam"
      pattern: "add_index_metadata\\(\"sample_list\""
    - from: "src/write/writer.rs"
      to: "add_spectrum_scan_field"
      via: "assay_ref promoted Int64 column via CustomBuilderFromParameter::from_spec"
      pattern: "assay_ref"
---

<objective>
Project the SDRF model into the spec's existing `sample_list` file-level metadata member, add the
per-spectrum `assay_ref` promoted Int64 column, and record the run→sample binding. These are query
PROJECTIONS over the verbatim embed (Plan 02), not authoritative.

Purpose: SDRF-03 (characteristics keyed by source name) + SDRF-04 (per-spectrum assay_ref +
run→sample). Output: `sample_list` JSON block, `assay_ref` Int64 column, run binding metadata, JSON
schema, round-trip test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-sdrf-sample-model/27-CONTEXT.md
@docs/mzpeak-extension-contract.md
@docs/sdrf-mzpeak-integration.md
@src/write/writer.rs
@src/schema/columns.rs

<interfaces>
<!-- sample_list does NOT yet exist as a FORWARD-emitted block — this plan CREATES it.
     (The only existing `sample_list` references are the reverse imzML optical sample-list,
     which is unrelated.) Mechanism = File-Level Metadata JSON (contract §3.5):
       sample entry: {id: String, name: String, parameters: [SampleParam]}
       SampleParam: {value, accession?: String, name: String, unit?: String}  (mzML cvParam/userParam)
       sample id = SDRF `source name`. -->

Promoted-column seam (src/write/writer.rs::new_with_encoding, ~L211, mirrors imaging_scan_fields):
  builder = builder.add_spectrum_scan_field(
      CustomBuilderFromParameter::from_spec(curie, name, DataType::Int64));
  // visitor.rs CustomBuilderFromParameter accepts ONLY Null/Bool/Int64/Float64/LargeUtf8 and
  // from_spec PANICS on any dtype other than Int64 for this path → assay_ref MUST be Int64.

ImagingColumnSpec (src/schema/columns.rs): { curie: CURIE, name: &'static str, dtype: DataType, required: bool }.
  assay_ref has no IMS coordinate accession — use a stable token CURIE (track in docs/cv-requests.md
  per contract §4 — "assay reference" structural term is a pending PSI-MS mint, Q3/§B).

Terminal add_index_metadata cluster lives in src/write/convert.rs (cv_list/scan_settings_list/
  imaging/sdrf). Add `sample_list` + the run-binding extension here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SampleList types + project_sample_list from the SDRF model</name>
  <files>src/schema/sample.rs, src/schema/mod.rs, src/sdrf/sample_list.rs, src/sdrf/mod.rs, schema/sample_list.json</files>
  <behavior>
    - Test: project_sample_list(PXD020187 table) yields one SampleEntry per distinct `source name`,
      id == source name.
    - Test: each `characteristics[organism]` cell becomes a SampleParam whose name is the EXACT
      header `characteristics[organism]` (userParam, no accession) when no CV accession is derivable,
      preserving the cell value verbatim (reversibility, Q9).
    - Test: a characteristic carrying an embedded `AC=` accession token (SDRF lexical form, e.g.
      `NT=...;AC=PRIDE:0000627`) maps to a CV-typed SampleParam with that accession.
    - Test: project_sample_list also returns a run→sample binding map (run/data-file → source name
      list) for the matched rows.
    - Test: SampleList serializes to JSON shaped {samples: [{id, name, parameters: [...]}]} and
      validates against schema/sample_list.json (structural keys present).
  </behavior>
  <action>
    Create `src/schema/sample.rs`: `SampleList { samples: Vec<SampleEntry> }`,
    `SampleEntry { id: String, name: String, parameters: Vec<SampleParam> }`,
    `SampleParam { name: String, #[serde(skip_serializing_if=Option::is_none)] accession: Option<String>,
    value: String, #[serde(skip_serializing_if=Option::is_none)] unit: Option<String> }` — the spec
    sample member shape (contract §3.5). Export from src/schema/mod.rs. Create
    `src/sdrf/sample_list.rs::project_sample_list(table: &SdrfTable, matched_rows: &[usize])
    -> (SampleList, RunSampleBinding)`. For each distinct source name among matched rows, build a
    SampleEntry; map every `characteristics[*]` column to a SampleParam: parse the SDRF cell's lexical
    form — if it contains an `AC=<CURIE>` token (EFO/PSI-MS/NCBITaxon/Unimod/PRIDE), emit accession +
    name; otherwise a userParam whose `name` is the exact column header (reversible). `RunSampleBinding`
    = `{ run: String, sample_refs: Vec<String> }` per matched data file. Write `schema/sample_list.json`
    (the three-places JSON schema). Do NOT collapse repeated characteristics — keep each column.
  </action>
  <verify>
    <automated>cargo test --lib sdrf::sample_list schema::sample 2>&1 | tail -20</automated>
  </verify>
  <done>sample_list projection keyed by source name; characteristics → cv/userParams reversibly; run→sample binding produced; schema/sample_list.json present + validated structurally.</done>
</task>

<task type="auto">
  <name>Task 2: assay_ref promoted Int64 column + wire sample_list/run-binding at the terminal seam</name>
  <files>src/write/writer.rs, src/write/convert.rs, src/schema/columns.rs</files>
  <action>
    Add an `assay_ref` ImagingColumnSpec (stable-token CURIE for the "assay reference" structural term —
    file/track it in docs/cv-requests.md per contract §4; dtype DataType::Int64, required: false). In
    `ImagingWriter::new_with_encoding`, register it via `add_spectrum_scan_field(from_spec(curie,
    "assay_ref", Int64))` ONLY when an SDRF is in play (gate on a writer flag/param so non-SDRF output
    stays byte-identical — do NOT add the column unconditionally). Thread the per-spectrum assay_ref
    value: for label-free / fractionation, each spectrum's assay_ref = the sample index in sample_list
    for that run (contract §3.5 "per-spectrum binding"); set it as an Int64 scan param on the
    reconstructed spectrum before write (mirror how IMS coordinate scan params are attached on the read
    path). For isobaric runs, assay_ref points to the plex's run record (the per-channel sample binding
    lives in channel_list, Plan 04). In `convert_with`, when sdrf is Some, after computing the
    projection (Plan 01/this plan), call `zip.add_index_metadata("sample_list", &sample_list)` and fold
    the run→sample binding into the existing file-level `run` block (extend it, do not replace) via an
    additional `add_index_metadata` key (`run_sample_binding`) — alongside the cv_list/imaging/sdrf
    cluster, before finish(). No --sdrf ⇒ none of these keys + no assay_ref column (byte-identical).
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -5 && cargo test --lib write:: 2>&1 | tail -15</automated>
  </verify>
  <done>assay_ref is a conditional promoted Int64 column; sample_list + run_sample_binding emitted only when --sdrf present; non-SDRF output unchanged; build green.</done>
</task>

<task type="auto">
  <name>Task 3: sample_list + assay_ref round-trip test</name>
  <files>tests/sdrf_sample_list.rs</files>
  <action>
    Integration test converting an imaging fixture WITH `--sdrf` PXD020187 (label-free). Re-open with
    MzPeakReader and assert: (a) `file_index().metadata.get("sample_list")` deserializes to a SampleList
    whose sample ids == the distinct source names; a known characteristic value round-trips verbatim;
    (b) `metadata.get("run_sample_binding")` is present; (c) the per-spectrum `assay_ref` column is
    readable (inflected column name present in spectrum metadata) and holds an Int64 FK; (d) a control
    conversion WITHOUT `--sdrf` has neither key and no assay_ref column (byte-identical concern). This
    is the SDRF-03/04 XRT round-trip assertion.
  </action>
  <verify>
    <automated>cargo test --test sdrf_sample_list 2>&1 | tail -20</automated>
  </verify>
  <done>sample_list + assay_ref + run binding round-trip green on PXD020187; no-SDRF control clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDRF cell text → JSON parameter value | Untrusted cell text projected into metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06 | Tampering | accession token parse (`AC=`) | mitigate | only emit an accession when the token matches a CURIE shape (`<PREFIX>:<digits>`); otherwise fall back to userParam — a malformed token never produces a forged CV accession |
| T-27-07 | Information disclosure | assay_ref FK index | accept | assay_ref is an integer index into the same file's sample_list; leaks nothing beyond the embedded SDRF |
| T-27-SC | Tampering | (no new package installs) | accept | no installs in this plan |
</threat_model>

<verification>
- `cargo test --test sdrf_sample_list` green.
- `cargo test --lib` green (assay_ref column gating does not regress non-SDRF write path).
- Three-places: `schema/sample_list.json` exists; spec write-up deferred to Plan 06 doc sweep.
</verification>

<success_criteria>
- SDRF-03: sample_list carries characteristics[*] keyed by source name, reversibly typed.
- SDRF-04: per-spectrum assay_ref (Int64 promoted column) + run→sample binding emitted.
- Non-SDRF output byte-identical (conditional emission).
</success_criteria>

<output>
Create `.planning/phases/27-sdrf-sample-model/27-03-SUMMARY.md` when done.
</output>
