---
phase: 27-sdrf-sample-model
plan: 05
type: execute
wave: 5
depends_on: [27-01, 27-02, 27-03, 27-04]
files_modified:
  - src/bin/spike_reporter_channel_id.rs
  - src/sdrf/reporter_quant.rs
  - src/sdrf/mod.rs
  - src/write/writer.rs
  - src/write/convert.rs
  - Cargo.toml
  - tests/sdrf_reporter_quant.rs
  - .planning/phases/27-sdrf-sample-model/27-05-SPIKE-FINDINGS.md
autonomous: false
requirements: [CHAN-03]
user_setup: []

must_haves:
  truths:
    - "A throwaway spike PROVES (or disproves) that a channel_id stored in auxiliary_arrays[].parameters survives the add_spectrum_array_override read-back path in the Rust reader, BEFORE the storage contract is committed."
    - "Reporter-ion intensities are stored as a per-MS2 auxiliary array channel-keyed so that peak → channel → sample resolves against channel_list."
    - "The auxiliary array imposes NO sorting rank and is absent for MS1 spectra."
    - "Re-opening the archive yields the reporter aux array with channel_id resolving to channel_list."
  artifacts:
    - path: "src/bin/spike_reporter_channel_id.rs"
      provides: "throwaway read-back spike harness for channel_id in auxiliary_arrays[].parameters"
      min_lines: 40
    - path: ".planning/phases/27-sdrf-sample-model/27-05-SPIKE-FINDINGS.md"
      provides: "spike verdict: channel_id survives read-back (or the documented alternate keying)"
      min_lines: 20
    - path: "src/sdrf/reporter_quant.rs"
      provides: "reporter aux-array builder keyed by channel_id, gated on the spike verdict"
      min_lines: 40
  key_links:
    - from: "src/sdrf/reporter_quant.rs"
      to: "src/sdrf/channel_list.rs"
      via: "channel_id values index channel_list entries"
      pattern: "channel_id"
    - from: "src/write/writer.rs"
      to: "add_spectrum_array_override"
      via: "reporter aux array registered via the BufferOverrideTable / aux path"
      pattern: "add_spectrum_array_override|BufferOverride"
---

<objective>
Deliver reporter-ion quantitation as a channel-keyed auxiliary array — the payoff of the channel
model — BUT GATE the storage contract behind a read-back spike. The locked CONTEXT decision: SPIKE
the aux-array `channel_id` read-back BEFORE committing the storage contract. Task 1 is a throwaway
spike; a blocking decision checkpoint (Task 2) ratifies the keying; only then (Task 3) is the
production builder + round-trip committed.

Purpose: CHAN-03 (reporter-quant aux array with channel_id, confirmed via read-back spike). Output:
spike harness + findings, production reporter-quant builder, round-trip test joining peak → channel
→ sample.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-sdrf-sample-model/27-CONTEXT.md
@docs/mzpeak-extension-contract.md
@src/write/writer.rs

<interfaces>
<!-- CHAN-03 mechanism = Auxiliary Data Arrays (contract §3.7): a per-MS2 aux array on the
     `auxiliary_arrays` list column of spectra_metadata.parquet, with channel_id recorded in the
     auxiliary array's `parameters` list (spec-defined per-item field). Makes peak→channel→sample
     resolvable WITHOUT schema changes. NO sorting rank (secondary to m/z). Absent for MS1
     (varies in length / may be absent → excluded from being a schema column per the spec's
     Arrays-and-Columns rule). -->

Upstream aux-array seams (vendor/mzpeak_prototyping):
  writer.rs:1471  builder = builder.add_spectrum_array_override(k.clone(), v.clone());  // BufferOverrideTable entries
  src/write/writer.rs:44  use mzpeak_prototyping::buffer_descriptors::BufferOverrideTable;  (already imported)
  reader.rs:1108  load_auxiliary_arrays_for_spectrum(index) -> Vec<DataArray>   // the read-back path under test
  chunk_series.rs:965  AuxiliaryArray::from_data_array(arr)  // how an aux array + its params are built

The SPIKE must prove: a channel_id carried in the aux array's `parameters` (NOT a separate column)
survives write → MzPeakReader → load_auxiliary_arrays_for_spectrum read-back. If parameters DO NOT
survive, the documented fallback is a sidecar channel↔column map (docs/sdrf-mzpeak-integration.md
"or a sidecar channel↔column map") — the decision checkpoint selects between them.

Spike bins are declared explicitly in Cargo.toml [[bin]] (mirror spike_reverse_read) and are
THROWAWAY (committed for reproducibility only — durable evidence in SPIKE-FINDINGS.md).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Throwaway spike — does channel_id in auxiliary_arrays[].parameters survive read-back?</name>
  <files>src/bin/spike_reporter_channel_id.rs, Cargo.toml, .planning/phases/27-sdrf-sample-model/27-05-SPIKE-FINDINGS.md</files>
  <action>
    Add a `[[bin]] name = "spike_reporter_channel_id"` block to Cargo.toml (explicit, mirroring
    spike_reverse_read). Write `src/bin/spike_reporter_channel_id.rs`: build a tiny mzPeak archive with
    one MS2 spectrum carrying a reporter auxiliary array (a small Float32/Float64 intensity array
    parallel to N channels) whose `parameters` list includes a `channel_id` param (try the
    `add_spectrum_array_override` / BufferOverrideTable path AND attaching channel_id to the
    AuxiliaryArray's params). Re-open with `MzPeakReader::new` and call
    `load_auxiliary_arrays_for_spectrum` (or the public spectrum-arrays accessor that surfaces aux
    arrays); print whether (a) the aux array is present on read-back, (b) its `channel_id` param
    survived and equals the written value. Record the verdict in
    `27-05-SPIKE-FINDINGS.md`: SURVIVES (→ commit the parameters-keyed contract) or DROPPED (→ document
    the sidecar channel↔column-map fallback and what it costs). Keep the spike minimal — it is durable
    evidence, not production code.
  </action>
  <verify>
    <automated>cargo run --bin spike_reporter_channel_id 2>&1 | tail -20</automated>
  </verify>
  <done>Spike runs end-to-end; SPIKE-FINDINGS.md records a clear SURVIVES/DROPPED verdict with the read-back evidence; the chosen keying is named.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Ratify the reporter-quant channel_id storage keying (decision gate)</name>
  <action>BLOCKING DECISION GATE — pause for the developer. Present the Task-1 spike verdict from
    27-05-SPIKE-FINDINGS.md and have the developer select the storage keying (parameters-keyed vs
    sidecar-map). Do NOT proceed to Task 3 until the keying is ratified; the production builder MUST
    match the spike-validated option.</action>
  <decision>Ratify the reporter-quant channel_id storage keying based on the spike verdict.</decision>
  <context>
    CONTEXT locks: "SPIKE the aux-array channel_id read-back BEFORE committing the reporter-quant
    storage contract." Task 1 produced a verdict. The production builder (Task 3) MUST match it. Do
    not commit a storage contract the spike did not validate.
  </context>
  <options>
    <option id="parameters-keyed" label="channel_id in auxiliary_arrays[].parameters (the design draft)">
      <pros>No schema change; peak→channel→sample resolvable; matches contract §3.7 + Q8</pros>
      <cons>Only valid if the spike showed parameters survive read-back</cons>
    </option>
    <option id="sidecar-map" label="Sidecar channel↔column map (file-level) if parameters are dropped on read-back">
      <pros>Survives even if aux-array params are not preserved by the reader</pros>
      <cons>Extra file-level structure; join is one indirection further; documented fallback</cons>
    </option>
  </options>
  <resume-signal>Select: parameters-keyed or sidecar-map (must match SPIKE-FINDINGS.md verdict)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Production reporter-quant aux-array builder + round-trip (per ratified keying)</name>
  <files>src/sdrf/reporter_quant.rs, src/sdrf/mod.rs, src/write/writer.rs, src/write/convert.rs, tests/sdrf_reporter_quant.rs</files>
  <action>
    Implement `src/sdrf/reporter_quant.rs` building a per-MS2 reporter auxiliary array keyed by
    channel_id per the RATIFIED option. Extract reporter intensities for each channel's reporter_mz
    (from channel_list, Plan 04) within tolerance from the MS2 peaks (docs note: "reporter ions are
    MS2 peaks, optional quant via reporter_mz ± tol"). Attach channel_id so each value resolves to its
    channel_list entry. Impose NO sorting rank; emit NOTHING for MS1 spectra (only isobaric MS2). Wire
    it into the writer's aux-array registration (add_spectrum_array_override / BufferOverrideTable in
    src/write/writer.rs) and the convert path, gated on `sdrf.is_some()` AND isobaric — non-SDRF /
    label-free output stays byte-identical. Add `tests/sdrf_reporter_quant.rs`: convert an imaging
    fixture WITH `--sdrf` PXD011799 (TMT-10), re-open with MzPeakReader, assert an MS2 spectrum carries
    the reporter aux array, its channel_id resolves to a channel_list entry (peak→channel→sample join
    works), MS1 spectra carry none, and a label-free `--sdrf` control emits no reporter aux array. This
    is the CHAN-03 XRT round-trip assertion.
  </action>
  <verify>
    <automated>cargo test --test sdrf_reporter_quant 2>&1 | tail -20</automated>
  </verify>
  <done>Reporter aux array channel-keyed per the ratified contract; channel_id resolves to channel_list on read-back; MS1 + label-free emit none; non-SDRF byte-identical; round-trip green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MS2 peaks → reporter intensity extraction | reporter_mz ± tol window over spectrum peaks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-11 | Tampering | channel_id binding | mitigate | channel_id values index channel_list (the authoritative map); the spike proves the binding survives read-back before the contract is committed |
| T-27-12 | Denial of Service | per-MS2 reporter extraction | mitigate | bounded m/z-window scan per channel (constant channel count); no per-spectrum unbounded allocation |
| T-27-13 | Information disclosure | aux array on MS1 | mitigate | reporter array emitted ONLY for isobaric MS2; MS1 carries none (no spurious data) |
| T-27-SC | Tampering | spike `[[bin]]` add to Cargo.toml | accept | no new external package; only a local throwaway bin target (mirrors existing spike bins) |
</threat_model>

<verification>
- `cargo run --bin spike_reporter_channel_id` produces a clear verdict (Task 1).
- Blocking decision checkpoint ratifies the keying (Task 2) — autonomous=false.
- `cargo test --test sdrf_reporter_quant` green (Task 3) under the ratified contract.
</verification>

<success_criteria>
- CHAN-03: reporter-ion quant stored as a channel-keyed auxiliary array; channel_id PROVEN to survive read-back (spike) and resolves to channel_list.
- No sorting rank; absent for MS1; non-SDRF/label-free byte-identical.
</success_criteria>

<output>
Create `.planning/phases/27-sdrf-sample-model/27-05-SUMMARY.md` when done (reference SPIKE-FINDINGS.md).
</output>
