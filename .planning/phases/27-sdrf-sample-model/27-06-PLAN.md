---
phase: 27-sdrf-sample-model
plan: 06
type: execute
wave: 6
depends_on: [27-02, 27-03, 27-04, 27-05]
files_modified:
  - src/sdrf/precedence.rs
  - src/sdrf/mod.rs
  - docs/sdrf-precedence.md
  - docs/mzpeak-imaging-spec-suggestions.md
  - tests/sdrf_precedence.rs
  - tests/sdrf_xrt.rs
autonomous: true
requirements: [SDRF-05]

must_haves:
  truths:
    - "When an embedded SDRF and a repo SDRF disagree, the repo SDRF wins — applied in code and documented."
    - "The three-places rule is satisfied for every Phase-27 structured addition (src + spec-suggestions doc + schema/*.json)."
    - "A masking-aware L1 round-trip still holds with an SDRF present (the SDRF facets do not perturb the data-facet bit-for-bit guarantee)."
    - "mzPeakValidator passes on an SDRF-bearing archive (or any new column's sorting_rank gating is recognized)."
  artifacts:
    - path: "src/sdrf/precedence.rs"
      provides: "repo-SDRF-wins resolution when embedded vs repo SDRF differ"
      min_lines: 25
    - path: "docs/sdrf-precedence.md"
      provides: "the documented precedence rule (SDRF-05)"
      min_lines: 15
    - path: "tests/sdrf_xrt.rs"
      provides: "masking-aware L1 + validator-pass assertion with SDRF present"
      min_lines: 30
  key_links:
    - from: "src/sdrf/precedence.rs"
      to: "src/sdrf/parse.rs"
      via: "compare embedded vs repo SdrfTable; repo wins on conflict"
      pattern: "repo.*win|RepoWins|precedence"
    - from: "docs/mzpeak-imaging-spec-suggestions.md"
      to: "schema/*.json"
      via: "three-places SDRF section references the new schemas"
      pattern: "sample_list|channel_list|sdrf"
---

<objective>
Close SDRF-05 (repo-SDRF-wins precedence) and the standing cross-cutting XRT criterion for every
Phase-27 facet: the three-places rule (src + spec-suggestions doc + schema), masking-aware L1
preservation, and a mzPeakValidator pass on an SDRF-bearing archive. SPEC-02 proposal SUBMISSION
stays QUEUED (held to end of v0.7) — only the local spec write-up + queue references land here.

Purpose: SDRF-05 + the phase's XRT closure. Output: precedence resolver + doc, the SDRF
spec-suggestions section (three-places completion), an XRT test asserting masking-aware L1 + validator
pass with SDRF present.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-sdrf-sample-model/27-CONTEXT.md
@docs/mzpeak-extension-contract.md
@docs/mzpeak-spec-proposal-queue.md
@docs/sdrf-open-questions.md

<interfaces>
<!-- Precedence (SDRF-05 / Q1): the canonical repo `*.sdrf.tsv` is the lossless source; the embedded
     copy is a denormalized convenience that MUST agree when present. On conflict, repo SDRF wins.
     In THIS converter the `--sdrf` path IS the repo SDRF (the one the user names), so "repo wins"
     means: if an mzPeak being re-processed already carries an embedded SDRF and a NEW --sdrf is
     supplied, the --sdrf (repo) replaces/overrides the embedded copy, with the conflict logged.

     Three-places rule (contract Locked Rule 3): every structured addition lands in src/ +
     docs/mzpeak-imaging-spec-suggestions.md + schema/*.json. Plans 02-05 created the src + schema
     halves; THIS plan completes the spec-suggestions doc half (the SDRF section) for all of
     sdrf-embed / sample_list / assay_ref / channel_list / reporter-quant. SPEC-02 SUBMISSION stays
     QUEUED (docs/mzpeak-spec-proposal-queue.md P-02..P-05) — do NOT submit.

     XRT (REQUIREMENTS.md standing criterion): (a) forward↔reverse symmetry + a src/verify round-trip
     assertion — for SDRF the reverse fate is "re-serve the embedded rows verbatim" (the embed IS the
     round-trip source; projections are query-only, Q10); (b) masking-aware L1 intact; (c)
     mzPeakValidator passes with new columns' sorting_rank gating recognized (assay_ref/reporter aux
     impose no sorting rank). Existing L1 + validator harnesses: tests/write_roundtrip.rs,
     tests/verify_roundtrip.rs, the acceptance harness. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: repo-SDRF-wins precedence resolver + doc</name>
  <files>src/sdrf/precedence.rs, src/sdrf/mod.rs, docs/sdrf-precedence.md, tests/sdrf_precedence.rs</files>
  <behavior>
    - Test: resolve_precedence(embedded=Some(table_a), repo=Some(table_b)) where the two disagree on a
      cell returns the REPO table (table_b) and reports a conflict list (which columns/rows differ).
    - Test: resolve_precedence(embedded=Some(a), repo=Some(a)) (agree) returns the table with NO
      conflict reported.
    - Test: resolve_precedence(embedded=None, repo=Some(b)) returns repo (the normal --sdrf path).
  </behavior>
  <action>
    Create `src/sdrf/precedence.rs::resolve_precedence(embedded: Option<&SdrfTable>, repo:
    Option<&SdrfTable>) -> (Option<SdrfTable>, Vec<SdrfConflict>)`. Rule: repo wins; when both present
    and disagree, return the repo table + a list of `SdrfConflict { location, embedded_value,
    repo_value }`; when they agree, empty conflict list; embedded-only is unusual for this converter
    but return embedded with a note. The convert path uses the `--sdrf` (repo) table as authoritative
    and, if an embedded SDRF is already present on a re-processed input, logs each conflict via the
    `log` facade (library stays anyhow-free; cli surfaces the count). Document the rule in
    `docs/sdrf-precedence.md` (SDRF-05): repo authoritative, embedded is convenience, conflicts logged,
    repo wins.
  </action>
  <verify>
    <automated>cargo test --lib sdrf::precedence 2>&1 | tail -15 && cargo test --test sdrf_precedence 2>&1 | tail -15</automated>
  </verify>
  <done>repo-SDRF-wins resolver returns repo on conflict + reports conflicts; documented in docs/sdrf-precedence.md.</done>
</task>

<task type="auto">
  <name>Task 2: Complete the three-places spec write-up (SDRF section, SUBMISSION held)</name>
  <files>docs/mzpeak-imaging-spec-suggestions.md</files>
  <action>
    Add an SDRF/sample-metadata section to docs/mzpeak-imaging-spec-suggestions.md covering all
    Phase-27 facets (verbatim embed + sample-metadata/sdrf member; sample_list characteristics +
    assay_ref; channel_list + ms_run.channel_set/plex_id; reporter-quant aux array). For each: name the
    spec mechanism (cite contract §3.4-§3.7), the new JSON-schema file (schema/sample_list.json,
    schema/channel_list.json), and the pending CV tokens (TMTpro 132-135, sample-metadata entity type)
    cross-referencing docs/cv-requests.md. State explicitly that SUBMISSION to
    HUPO-PSI/mzPeak-specification is QUEUED (docs/mzpeak-spec-proposal-queue.md P-02..P-05), held to the
    END of v0.7 — this plan only completes the LOCAL write-up half of the three-places rule. Do NOT
    open any PR / submit anything.
  </action>
  <verify>
    <automated>grep -c "channel_list\|sample_list\|sample-metadata\|reporter" docs/mzpeak-imaging-spec-suggestions.md | grep -qv '^0$' && echo "spec write-up present"</automated>
  </verify>
  <done>Spec-suggestions doc carries the SDRF section for every facet (three-places rule complete); submission explicitly marked QUEUED/held.</done>
</task>

<task type="auto">
  <name>Task 3: XRT — masking-aware L1 + validator pass with SDRF present</name>
  <files>tests/sdrf_xrt.rs</files>
  <action>
    Add `tests/sdrf_xrt.rs` asserting the standing XRT criterion holds with an SDRF present:
    (a) MASKING-AWARE L1 — convert an imaging fixture WITH `--sdrf` and WITHOUT, and assert the
    spectral data facets (m/z + intensity, masking-aware) are bit-for-bit identical between the two
    (the SDRF facets are additive metadata + an optional aux array on MS2; they MUST NOT perturb the L1
    data-facet guarantee on MS1/profile data). Reuse the L1 comparison from src/verify (verify_streaming
    at L1BitForBit) on the non-SDRF data facets. (b) VALIDATOR — run the project's mzPeakValidator path
    (or the existing acceptance/validator harness) on the SDRF-bearing archive and assert it passes,
    with the new columns (assay_ref, reporter aux) recognized as imposing no sorting rank. (c)
    FileIndex survival is already covered in Plan 02; reference it. If the validator harness is external,
    gate that sub-assertion behind the same mechanism the existing acceptance tests use.
  </action>
  <verify>
    <automated>cargo test --test sdrf_xrt 2>&1 | tail -20</automated>
  </verify>
  <done>Masking-aware L1 holds with/without SDRF (data facets bit-identical); validator passes on the SDRF-bearing archive; sorting_rank gating recognized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| embedded SDRF vs repo SDRF | conflicting sample-metadata sources resolved by precedence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-14 | Spoofing | embedded SDRF overriding repo | mitigate | repo-SDRF-wins is enforced + every conflict logged; a stale/forged embedded copy can never silently override the user-named repo SDRF |
| T-27-15 | Repudiation | round-trip authority | mitigate | round-trip re-serves the verbatim embed (Q10); projections are query-only and cannot be mistaken for the source of truth |
| T-27-SC | Tampering | (no new package installs) | accept | no installs in this plan |
</threat_model>

<verification>
- `cargo test --test sdrf_precedence sdrf_xrt` green; `cargo test --lib sdrf::precedence` green.
- `cargo test` full suite green (no regression to the existing L1 / validator / file-index harnesses).
- Three-places complete: every Phase-27 facet appears in src/ + spec-suggestions doc + schema/*.json.
</verification>

<success_criteria>
- SDRF-05: repo-SDRF-wins precedence applied + documented.
- XRT closed: masking-aware L1 intact with SDRF; validator passes; reverse fate = verbatim embed re-serve; three-places complete; SPEC-02 submission QUEUED only.
</success_criteria>

<output>
Create `.planning/phases/27-sdrf-sample-model/27-06-SUMMARY.md` when done.
</output>
