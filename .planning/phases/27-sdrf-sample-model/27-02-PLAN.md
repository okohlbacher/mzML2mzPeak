---
phase: 27-sdrf-sample-model
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - src/cli.rs
  - src/write/convert.rs
  - src/sdrf/embed.rs
  - src/sdrf/mod.rs
  - tests/sdrf_embed.rs
autonomous: true
requirements: [SDRF-01, SDRF-02]
user_setup: []

must_haves:
  truths:
    - "A new `--sdrf <PATH>` flag (EXPLICIT only, never auto-discovered) threads an SDRF into conversion."
    - "The SDRF file is stored byte-for-byte as a typed ZIP member (entity_type sample-metadata / data_kind sdrf, other fallback)."
    - "A file-level metadata.sdrf back-reference records the member name + dataset accession + sdrf uri."
    - "Re-opening the archive with MzPeakReader still yields a non-empty FileIndex (the SDRF Other-member does not drop the index) AND the embedded bytes equal the source byte-for-byte."
    - "No --sdrf reproduces the prior no-SDRF output byte-for-byte (the embed key is omitted)."
  artifacts:
    - path: "src/sdrf/embed.rs"
      provides: "embed_sdrf(zip, sdrf_path) → registers verbatim ZIP member + returns the back-ref struct"
      min_lines: 40
    - path: "tests/sdrf_embed.rs"
      provides: "end-to-end embed + MzPeakReader read-back + FileIndex-survival + byte-equality test"
      min_lines: 40
  key_links:
    - from: "src/write/convert.rs"
      to: "src/sdrf/embed.rs"
      via: "embed_sdrf at the terminal ZIP seam (after finish_parquet, before finish)"
      pattern: "embed_sdrf"
    - from: "src/cli.rs"
      to: "convert_with"
      via: "sdrf: Option<&Path> threaded argument"
      pattern: "convert_with\\("
---

<objective>
Implement the LOSSLESS ANCHOR first (locked build order): a `--sdrf <PATH>` flag that embeds the
SDRF verbatim as a typed ZIP member + a file-level back-reference, and proves the embed round-trips
(bytes survive + the FileIndex is not dropped). All structured projections in later plans are
denormalized convenience copies of this anchor.

Purpose: SDRF-02 requires the embedded copy to be the lossless source of truth; nothing structured
is authoritative. Getting the embed + round-trip green first de-risks every later projection.
Output: `--sdrf` CLI flag, `convert_with(.., sdrf)` threading, `src/sdrf/embed.rs`, an
end-to-end embed round-trip integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-sdrf-sample-model/27-CONTEXT.md
@docs/mzpeak-extension-contract.md
@src/write/convert.rs
@src/cli.rs

<interfaces>
<!-- The verbatim-embed mechanism mirrors the v0.5 optical-TIFF Other-member path
     (src/write/convert.rs::embed_one_image → zip.add_file_from_read). KEY DIFFERENCE:
     pass an EXPLICIT FileEntry to get the TYPED registration that degrades to `other`. -->

From vendor/mzpeak_prototyping/src/archive/sync.rs:
  pub fn add_file_from_read<S: AsRef<str>>(&mut self, read: &mut impl io::Read,
      name: Option<&S>, entry: Option<FileEntry>) -> io::Result<()>;
      // entry=Some(FileEntry) → start_for_entry (TYPED); name=Some → start_other (Other/Other).
  pub fn add_index_metadata(&mut self, key: &str, value: &impl Serialize) -> Result<(), serde_json::Error>;

From vendor/mzpeak_prototyping/src/archive/file_index.rs:
  pub struct FileEntry { pub name, pub entity_type: EntityType, pub data_kind: DataKind, .. }
  EntityType::Other(String) / DataKind::Other(String)  — #[serde(untagged)] → bare string,
      symmetric with FromStr (the file_index serde drop-bug is fixed upstream @a5c222c;
      tests/file_index_roundtrip.rs pins it). Unknown values degrade to `other` in readers.
  pub fn FileEntry::new(name: String, entity_type: EntityType, data_kind: DataKind) -> Self

Terminal seam in convert_with (src/write/convert.rs): after `let mut zip = writer.finish_parquet()?;`
  the existing code embeds images then calls add_index_metadata("cv_list"/"scan_settings_list"/
  "imaging"), then zip.finish(). The SDRF embed + back-ref go in this SAME block, BEFORE finish().

Re-export from mzpeak_prototyping: archive::{DataKind, EntityType, FileEntry, ZipArchiveWriter}.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --sdrf flag + thread sdrf through convert_with</name>
  <files>src/cli.rs, src/write/convert.rs</files>
  <action>
    In cli.rs add `#[arg(long = "sdrf", value_name = "PATH")] pub sdrf: Option<PathBuf>` to
    ConvertCli (EXPLICIT only — NO auto-discovery, per SDRF-01 / Out-of-Scope; mirror how `--image`
    is documented, but single-valued). In `run_forward`, pass `cli.sdrf.as_deref()` into
    `convert_with`. Extend the `convert_with` signature to
    `convert_with(reader, out_path, image_paths, opts, geometry, input_path, sdrf: Option<&Path>)`
    exactly as Phases 18/19 threaded geometry/input_path. Update the back-compat `convert()` wrapper
    (convert.rs) to pass `None`. Reject `--sdrf` on the reverse path and the plain-mzML path with an
    actionable forward-imaging-only error (mirror the `--image` rejection in run_reverse /
    run_forward_mzml) — SDRF embed is forward-only in this phase. anyhow/log stay in cli.rs only;
    convert.rs uses WriteError (thiserror). Add a WriteError::Sdrf variant wrapping the
    sdrf::SdrfError / embed error so the library boundary stays anyhow-free.
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -5 && cargo test --lib cli:: 2>&1 | tail -15</automated>
  </verify>
  <done>`--sdrf PATH` parses; convert_with carries `sdrf: Option<&Path>`; convert() back-compat passes None; reverse + plain-mzML reject --sdrf; build green.</done>
</task>

<task type="auto">
  <name>Task 2: embed_sdrf verbatim member + metadata.sdrf back-ref at the terminal seam</name>
  <files>src/sdrf/embed.rs, src/sdrf/mod.rs, src/write/convert.rs</files>
  <action>
    Create `src/sdrf/embed.rs` with `embed_sdrf(zip: &mut ZipArchiveWriter<File>, sdrf_path: &Path)
    -> Result<SdrfBackRef, EmbedError>`. Stream the SDRF bytes into the ZIP via
    `zip.add_file_from_read(&mut File::open(sdrf_path)?, None::<&str>, Some(entry))` where
    `entry = FileEntry::new("sample_metadata.sdrf.tsv".into(),
    EntityType::Other("sample-metadata".into()), DataKind::Other("sdrf".into()))` — the TYPED
    registration the extension-contract §3.4 specifies (degrades to `other` in old readers).
    Stream in 64 KiB chunks (add_file_from_read already does this — never whole-file load).
    Build `SdrfBackRef { member: String, dataset_accession: Option<String>, sdrf_uri: Option<String> }`
    (serde Serialize) per contract §3.4: derive `dataset_accession` from the SDRF filename stem when
    it looks like a PRIDE/MTBLS accession (e.g. `PXD011799`, `MTBLS1129`), else None; `sdrf_uri` =
    None for a local file (left for a future repo-SDRF flag). In `convert_with`, at the terminal seam
    (after `writer.finish_parquet()?`, alongside the image embed and BEFORE the `add_index_metadata`
    cluster + `zip.finish()`), when `sdrf` is Some: call `embed_sdrf`, then
    `zip.add_index_metadata("sdrf", &back_ref)`. When `sdrf` is None: emit NOTHING (byte-identical
    output). Add `pub mod embed;` to src/sdrf/mod.rs.
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -5 && cargo test --lib sdrf::embed 2>&1 | tail -15</automated>
  </verify>
  <done>embed_sdrf writes a typed `sample_metadata.sdrf.tsv` member + a `metadata.sdrf` back-ref; no --sdrf path emits neither; build green.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end embed round-trip test (bytes + FileIndex survival)</name>
  <files>tests/sdrf_embed.rs</files>
  <action>
    Add an integration test that converts a small imaging fixture WITH `--sdrf` pointing at
    `data/sdrf-examples/PXD020187/PXD020187.sdrf.tsv` (label-free — exercises the embed independent
    of channel logic), then re-opens with `mzpeak_prototyping::MzPeakReader::new`. Assert: (a) the
    `FileIndex` is non-empty and `metadata.imaging` is still present (the SDRF Other-member did NOT
    drop the index — the upstream serde fix holds; this is the XRT FileIndex-survival assertion the
    CONTEXT requires); (b) `file_index().metadata.get("sdrf")` carries the back-ref with member ==
    `sample_metadata.sdrf.tsv`; (c) the embedded member bytes read back equal the source file bytes
    EXACTLY (read the member out of the ZIP via the reader's archive accessor, or re-open the
    `.mzpeak` as a `zip::ZipArchive` and read `sample_metadata.sdrf.tsv` — byte-for-byte
    `assert_eq!`). Reuse the smallest existing imaging test fixture (mirror how tests/cv_list.rs /
    tests/scan_settings.rs build the input + call the convert seam). Also assert a control conversion
    WITHOUT `--sdrf` has no `"sdrf"` metadata key.
  </action>
  <verify>
    <automated>cargo test --test sdrf_embed 2>&1 | tail -20</automated>
  </verify>
  <done>Embed round-trip green: source bytes == embedded bytes; FileIndex + metadata.imaging survive the Other-member; back-ref present; no-SDRF control has no sdrf key.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `--sdrf` path → ZIP member | Untrusted user file copied verbatim into the archive |
| archive member name | Must be a fixed, non-attacker-controlled name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03 | Spoofing | archive member name | mitigate | member name is the FIXED `sample_metadata.sdrf.tsv` constant, never derived from the source basename (no path-injection surface, unlike the image path) |
| T-27-04 | Tampering | embedded bytes vs source | mitigate | stream verbatim (no transform); round-trip test asserts byte-equality so a silent corruption fails CI |
| T-27-05 | Information disclosure | embedding an arbitrary file | accept | `--sdrf` is explicit + forward-only; the user names the file knowingly (auto-discovery is out of scope by decision) |
| T-27-SC | Tampering | (no new package installs in this plan) | accept | csv was vetted in Plan 01; no installs here |
</threat_model>

<verification>
- `cargo test --test sdrf_embed` green (bytes + FileIndex survival + back-ref + no-SDRF control).
- `cargo build` green; reverse/plain-mzml reject `--sdrf`.
- A diff of a no-`--sdrf` conversion vs the pre-Phase-27 output is byte-identical for the embed concern (the sdrf key is absent).
</verification>

<success_criteria>
- SDRF-01: `--sdrf <PATH>` ingests a sibling SDRF, explicit-only.
- SDRF-02: SDRF embedded verbatim as a typed `sample-metadata`/`sdrf` member + `metadata.sdrf` back-ref; embed lands BEFORE any projection.
- XRT: FileIndex + metadata.imaging survive the Other-member round-trip (asserted); byte-for-byte embed fidelity.
</success_criteria>

<output>
Create `.planning/phases/27-sdrf-sample-model/27-02-SUMMARY.md` when done.
</output>
