---
phase: 30-sample-metadata-spec-cv
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/mzpeak-extension-contract.md
  - docs/mzpeak-spec-proposal-queue.md
autonomous: true
requirements: [SMSPEC-01, SMSPEC-02]
must_haves:
  truths:
    - "The Q1–Q10 positions are ratified against the canonical HUPO-PSI/mzPeak-specification and recorded: repo-SDRF-wins precedence, the sample-metadata/sdrf|isa open-enum tokens, Q2 (sample/SDRF) agreement"
    - "The sample-metadata structural-term + samples-as-channels write-ups are PREPARED + QUEUED for the single END-of-v0.8 batch — not submitted (owner holds to Phase 37)"
    - "The extension-contract is extended with the v0.8 sample-metadata facet→mechanism mappings (metadata.study, metadata.sample_list, sample-metadata/sdrf|isa member, MS:1002602 samples-as-channels) so Phases 31+ implement against a binding contract"
  artifacts:
    - path: "docs/mzpeak-spec-proposal-queue.md"
      provides: "A v0.8 sample-metadata batch section: ratified Q1–Q10 + the queued (not-submitted) structural-term/samples-as-channels proposals"
      contains: "v0.8"
    - path: "docs/mzpeak-extension-contract.md"
      provides: "v0.8 sample-metadata facet→spec-mechanism bindings (the contract Phases 31+ cite)"
      contains: "sample-metadata"
  key_links:
    - from: "docs/mzpeak-spec-proposal-queue.md"
      to: "docs/mzpeak-extension-contract.md"
      via: "proposal rows cite contract sections (mechanism not re-derived)"
      pattern: "extension-contract|§"
---

<objective>
Ratify the Q1–Q10 spec-alignment positions against the canonical `HUPO-PSI/mzPeak-specification` and
PREPARE + QUEUE the v0.8 sample-metadata spec proposals — NOT submitted (owner holds submission to the
END-of-v0.8 batch / Phase 37). This is the spec-governance "place" of the three-places rule (SMSPEC-01,
SMSPEC-02): extend the binding extension-contract with the v0.8 sample-metadata facet→mechanism mappings
so Phases 31+ implement against a settled contract, and move the v0.8 batch proposal rows from "deferred"
to "queued-and-spec'd".

Purpose: the proposal-queue + extension-contract already carry v0.8 placeholders (P-02..P-05 marked
"deferred-to-v0.8"; the SDRF §5.7 committee questions relocated to the v0.8 batch). This plan finalizes
those into a coherent QUEUED v0.8 batch reflecting the ratified cornerstones (A passthrough, E
samples-as-channels / NO channel_list, F list-valued sample_ref, G lean posture) so nothing is
re-decided downstream. NO submission happens — the git push policy + owner gate hold all upstream
filing to Phase 37.

Output: extended `docs/mzpeak-extension-contract.md` (a v0.8 sample-metadata section binding each facet
to its spec mechanism) and `docs/mzpeak-spec-proposal-queue.md` (the v0.8 batch: ratified Q1–Q10 + the
queued structural-term + samples-as-channels write-ups, status QUEUED/HELD).

This plan is DOCS-ONLY governance. It touches no code and submits nothing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-sample-metadata-spec-cv/30-CONTEXT.md

# The authoritative ratified positions (cornerstones A–G, §0c, §13 Q1–Q10 resolutions):
@.planning/milestones/v0.8-DESIGN-DRAFT.md

# The binding contract to EXTEND with the v0.8 sample-metadata bindings (it already has v0.8 placeholders):
@docs/mzpeak-extension-contract.md

# The proposal queue to EXTEND (it already has a "v0.8 batch (SDRF/channels — deferred)" section + the
# relocated SDRF §5.7 committee questions — finalize them into a QUEUED v0.8 batch):
@docs/mzpeak-spec-proposal-queue.md

<interfaces>
<!-- The extension-contract already names the five spec mechanisms (File-Level Metadata JSON, Column -->
<!-- Name Inflection, parameters list, Adding a Data Kind, Adding an Entity Type) in §2. v0.8 facets -->
<!-- bind to these EXISTING mechanisms — do not invent new mechanisms (Locked Rule 4). -->

v0.8 sample-metadata facet → mechanism bindings to record (from the design draft §5):
- verbatim SDRF/ISA embed → "Adding a new Data Kind" (data_kind sdrf|isa) + "Adding a new Entity Type"
  (entity_type sample-metadata); retrieval by deterministic archive name (no reader dispatch on the token).
- metadata.study (accession/title/back-ref + run_sample_binding shadow) → File-Level Metadata JSON
  (index.json metadata map, key "study").
- metadata.sample_list (id/name/parameters, REUSED) → File-Level Metadata JSON (key "sample_list").
- samples-as-channels → labeled sample_list entries via MS:1002602 "sample label" + reagent children,
  bound by the list-valued ms_run.sample_ref. The channel_list / plex_id / channel_set construct is
  DROPPED (RATIFIED-E) — record P-04 as superseded.

Q1–Q10 ratified resolutions (design §13): Q1 repo-wins precedence + verbatim anchor; Q2 sample/SDRF
agreed; Q3 list-valued ms_run.sample_ref [F]; Q4 samples-as-channels, NO channel_list [E]; Q6/Q7
deferred ≥v0.9 [G]; Q9 reinforces no-OBO-bundle passthrough [A]; Q8 reporter-quant optional/off; Q10
verbatim-bytes are the roundtrip source.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the extension-contract with v0.8 sample-metadata facet→mechanism bindings</name>
  <files>docs/mzpeak-extension-contract.md</files>
  <action>
    Add a "## v0.8 Sample-Metadata Facet Bindings" section (or promote the existing DEFERRED §3.4–§3.7
    blocks into a live v0.8 contract section) recording each facet → existing spec mechanism per the
    interfaces block: verbatim SDRF/ISA embed (Data Kind sdrf|isa + Entity Type sample-metadata, retrieval
    by deterministic archive name); metadata.study (File-Level Metadata JSON, key "study", incl. the
    run_sample_binding provenance shadow); metadata.sample_list (File-Level Metadata JSON, key "sample_list",
    REUSED id/name/parameters); samples-as-channels (labeled sample_list entries via MS:1002602 + list-valued
    ms_run.sample_ref). Explicitly record that the v0.7 §3.6 channel_list/plex_id/channel_set construct is
    SUPERSEDED/DROPPED (RATIFIED-E). Cite cv.rs (single-source CV), schema/study.json + schema/sample_list.json
    (Plan 30-03), and the carve-out tokens (Plan 30-02). Update the Stable-Token Register (§4) and the
    "Consumed By" table (§5) to add the v0.8 phases (30/31/32/34) and flip the sample-metadata entity_type /
    sdrf|isa data_kind rows from "→ v0.8 fallback" to "v0.8 stable token in use (docs/cv-requests.md)". Do NOT
    invent CURIEs — defer missing terms to docs/cv-requests.md (Plan 30-02). Do NOT change the v0.7 BINDING
    facets.
  </action>
  <verify>
    <automated>grep -v '^>' docs/mzpeak-extension-contract.md | grep -c "sample-metadata"</automated>
  </verify>
  <done>
    The extension-contract carries a live v0.8 sample-metadata section binding every facet to an existing
    spec mechanism; channel_list recorded as superseded (RATIFIED-E); §4/§5 updated; no invented CURIEs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Finalize the QUEUED (not-submitted) v0.8 batch + ratify Q1–Q10</name>
  <files>docs/mzpeak-spec-proposal-queue.md</files>
  <action>
    Transform the existing "## 1b. v0.8 batch (SDRF/channels — deferred)" section into a finalized QUEUED
    v0.8 sample-metadata batch reflecting the ratified cornerstones: restate P-02 (verbatim embed +
    sample-metadata/sdrf|isa tokens), P-03 (sample_list reuse; note per-spectrum assay_ref is deferred
    ≥v0.9, run-level binding only), P-04 (REFRAMED: samples-as-channels via MS:1002602 + list-valued
    ms_run.sample_ref; channel_list/plex_id DROPPED — RATIFIED-E), and the metadata.study proposal; add a
    new row for the list-valued ms_run.sample_ref upstream field (cross-ref Phase 30b / UPSTREAM-BIND-01).
    Set each row's readiness to `queued` (spec-able now from the contract) and cite the extension-contract
    v0.8 section (mechanism not re-derived). Convert the Section 3 SDRF §5.7 committee questions (Q1–Q10)
    into RATIFIED resolutions per design §13 (Q1 repo-wins; Q2 sample/SDRF agreed; Q3 [F]; Q4 [E] no
    channel_list; Q6/Q7 [G] deferred ≥v0.9; Q8 optional/off; Q9 [A] passthrough; Q10 verbatim-bytes
    roundtrip) — check each box and record the resolution inline. Update the submission-status banner and
    the §4b v0.8 gate checklist to state the batch is QUEUED + HELD for Phase 37, submission owner-gated
    (push policy: HUPO-PSI is outside okohlbacher → explicit interactive authorization). NOTHING is
    submitted in this plan; the gate label reflects that this is a HELD/queued doc edit only.
  </action>
  <verify>
    <automated>grep -ci "queued\|held\|ratified" docs/mzpeak-spec-proposal-queue.md</automated>
  </verify>
  <done>
    The proposal queue has a finalized QUEUED v0.8 sample-metadata batch (P-02/P-03/P-04-reframed/study +
    sample_ref upstream row, readiness `queued`); Q1–Q10 recorded as ratified resolutions; submission
    status banner + §4b gate state QUEUED/HELD for Phase 37; nothing submitted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local doc edit → upstream HUPO-PSI submission | Submission MUST NOT happen here (owner-gated, push policy) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-07 | Elevation of privilege | An accidental upstream PR/submission to HUPO-PSI from a queue edit | mitigate | This plan edits LOCAL docs only; submission status stays HELD; §4b gate requires explicit owner authorization (push policy — HUPO-PSI is outside okohlbacher) |
| T-30-08 | Spoofing | A proposal row claiming a mechanism the spec does not have (ad-hoc structure) | mitigate | Every v0.8 facet binds to one of the five EXISTING named spec mechanisms (Locked Rule 4); rows cite the extension-contract section, not a new mechanism |
| T-30-09 | Tampering | npm/pip/cargo installs | accept | No new dependency this phase; docs-only plan — no install task exists |
</threat_model>

<verification>
- `docs/mzpeak-extension-contract.md` carries a live v0.8 sample-metadata facet-binding section
  (grep "sample-metadata" in non-quote lines ≥ 1).
- `docs/mzpeak-spec-proposal-queue.md` shows the v0.8 batch as `queued`/`HELD` with Q1–Q10 ratified.
- Neither doc records any actual submission; submission status remains HELD for Phase 37.
- No code touched; no Cargo.toml change.
</verification>

<success_criteria>
- Q1–Q10 ratified against the canonical spec and recorded (SMSPEC-01): repo-wins precedence,
  sample-metadata/sdrf|isa tokens, Q2 sample/SDRF agreement.
- The sample-metadata + samples-as-channels write-ups are PREPARED + QUEUED for the single END-of-v0.8
  batch — not submitted (SMSPEC-02; owner holds to Phase 37).
- The extension-contract binds every v0.8 facet to an existing spec mechanism (Phases 31+ cite it).
- channel_list recorded as superseded; list-valued ms_run.sample_ref cross-referenced to Phase 30b.
</success_criteria>

<output>
Create `.planning/phases/30-sample-metadata-spec-cv/30-04-SUMMARY.md` when done. Confirm submission status
is HELD (nothing filed upstream) and list the queued v0.8 batch rows.
</output>
