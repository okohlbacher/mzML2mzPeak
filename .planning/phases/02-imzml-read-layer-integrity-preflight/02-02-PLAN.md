---
phase: 02-imzml-read-layer-integrity-preflight
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/integrity/mod.rs
  - src/integrity/header.rs
  - src/integrity/preflight.rs
  - tests/integrity_preflight.rs
  - tests/fixtures/imaging/Corrupt_BadChecksum.imzML
  - tests/fixtures/imaging/Corrupt_BadChecksum.ibd
autonomous: true
requirements: [IN-07]
must_haves:
  truths:
    - "A converter-owned preflight reads the imzML header CV params (UUID IMS:1000080, checksum IMS:1000090/91/92) via byte-level Latin-1 parsing"
    - "Preflight hard-fails (returns Err / non-zero) on UUID mismatch between imzML IMS:1000080 and the .ibd first-16-byte RFC-4122 UUID"
    - "Preflight hard-fails on .ibd checksum mismatch vs the declared IMS:1000090/91/92 (handles whichever checksum type the imzML declares)"
    - "Preflight hard-fails on a missing .ibd sidecar with a clear, actionable message"
    - "A deliberately-corrupted fixture (mismatched checksum) and a missing-.ibd case both produce a non-zero exit asserted by an integration test"
  artifacts:
    - path: "src/integrity/header.rs"
      provides: "Latin-1 byte-level imzML header parse for IMS:1000080 UUID + IMS:1000090/91/92 checksum + type"
      contains: "IMS:1000080"
    - path: "src/integrity/preflight.rs"
      provides: "run_preflight() that compares UUID + checksum + .ibd presence and returns a typed error on any failure"
      contains: "fn preflight"
    - path: "tests/integrity_preflight.rs"
      provides: "integration tests asserting hard-fail on bad checksum, mismatched UUID, and missing .ibd"
      min_lines: 30
  key_links:
    - from: "src/integrity/preflight.rs"
      to: "src/integrity/header.rs"
      via: "parsed header params feed the comparison"
      pattern: "header::"
    - from: "src/lib.rs"
      to: "src/integrity/preflight.rs"
      via: "pub mod integrity re-export"
      pattern: "pub mod preflight|pub use"
---

<objective>
Build the converter-owned integrity preflight (IN-07) that runs BEFORE any streaming and
hard-fails on a bad .ibd. mzdata only `warn!`s on UUID mismatch and does not check the
checksum at all (PITFALLS.md Pitfall 3), so the converter must own this gate. The
preflight reuses the proven approach from `src/bin/verify_ibd.rs` (RFC-4122 first-16-byte
UUID compare + whole-file SHA-1) but generalizes it: the expected UUID and checksum are
parsed from the .imzML header (not hard-coded constants) via a byte-level Latin-1 parser,
and the checksum type is whatever the imzML declares (IMS:1000090 MD5 / IMS:1000091 SHA-1
/ IMS:1000092 SHA-256).

Purpose: refuse to vouch for a mismatched, corrupt, or missing .ibd with a clear error and
a non-zero exit — the converter must never silently read the wrong binary.

Output: an `integrity` module with a `header` parser and a `preflight` entry point, plus
a deliberately-corrupted fixture and integration tests proving the hard-fail behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/research/PITFALLS.md
@.planning/phases/02-imzml-read-layer-integrity-preflight/02-01-PLAN.md

<read_first>
- src/bin/verify_ibd.rs — the WORKING reference for the integrity check: RFC-4122 UUID
  byte compare (first 16 bytes of .ibd) and whole-file SHA-1. This plan generalizes it:
  expected values come from the imzML header, not the EXPECTED_UUID_BYTES/EXPECTED_SHA1
  constants, and the checksum type is data-driven.
- src/bin/spike_coords.rs (lines 129-190) — the WORKING reference for byte-level Latin-1
  XML scanning (`std::fs::read` + split on b'\n' + `String::from_utf8_lossy` + match ASCII
  tokens). imzML headers are ISO-8859-1; a UTF-8 line reader silently stops at the first
  non-ASCII byte (Phase 1 landmine). Reuse this raw-byte pattern for the header parse.
- src/integrity/mod.rs — created as a stub by Plan 02-01; this plan fills it.
- tests/fixtures/imaging/Example_Continuous.imzML + .ibd — the committed clean fixture
  (UUID 554a27fa-79d2-4766-9a2c-862e6d78b1f3, SHA-1 a5be532d...; see 01-FINDINGS.md) used
  as the PASS case and as the basis for the corrupted fixture.
</read_first>

<imzml_header_facts>
From 01-FINDINGS.md and verify_ibd.rs (empirically confirmed):
- IMS:1000080 "universally unique identifier" — the imzML-declared .ibd UUID (textual, e.g.
  {C7822330-F1A8-4D11-AD30-504B30B33722}). Compared RFC-4122 / big-endian byte-for-byte to
  the .ibd first 16 bytes (this is the REQUIRED path; the .NET mixed-endian reading is a
  diagnostic only — see verify_ibd.rs).
- IMS:1000091 "ibd SHA-1" — declared whole-file SHA-1 (HR2MSI: F8C24417...; continuous
  fixture: a5be532d...). IMS:1000090 = MD5, IMS:1000092 = SHA-256 are the other possible
  declarations; handle whichever ONE the imzML contains.
- These params live in the imzML <fileDescription> header, before the spectrumList — the
  byte-level Latin-1 scan only needs to read the head of the file, not stream the whole thing.
- The checksum digest itself spans the WHOLE .ibd (byte 0..EOF, UUID bytes included).
</imzml_header_facts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Latin-1 imzML header parser for UUID + checksum</name>
  <files>src/integrity/header.rs, src/integrity/mod.rs</files>
  <behavior>
    - Parsing the committed Example_Continuous.imzML header yields uuid="554a27fa-79d2-4766-9a2c-862e6d78b1f3" (normalized lowercase, braces/dashes handled) and checksum=("SHA1", "a5be532d25997b71be6d20c76561ddc4d5307ddd") case-insensitively.
    - An imzML missing IMS:1000080 yields a typed "missing UUID declaration" error, not a panic.
    - An imzML declaring no checksum param (no IMS:1000090/91/92) yields a typed "missing checksum declaration" error.
    - Reading is byte-level (std::fs::read + Latin-1 lossy), so a header with non-ASCII bytes before the params still parses (no UTF-8 early stop).
  </behavior>
  <action>
  In `src/integrity/header.rs` define a `pub struct ImzmlHeader { pub uuid: String, pub checksum_type: ChecksumType, pub checksum_hex: String }` and `pub enum ChecksumType { Md5, Sha1, Sha256 }`. Define
  `pub fn parse_imzml_header(path: &Path) -> Result<ImzmlHeader, IntegrityError>` that reads the file
  as raw bytes (`std::fs::read`) and scans line-by-line over `split(|&b| b == b'\n')` decoding each line
  with `String::from_utf8_lossy` (mirror the Latin-1 technique in spike_coords.rs:129-190; the tokens
  matched — accession=, value= — are pure ASCII). Capture the `value="..."` attribute of the cvParam
  whose `accession="IMS:1000080"` as the UUID (normalize: strip surrounding `{}`/whitespace, lowercase,
  keep dashes); capture the checksum from whichever of `IMS:1000090` (Md5) / `IMS:1000091` (Sha1) /
  `IMS:1000092` (Sha256) appears, recording its type and the hex value (lowercased). Stop scanning once
  both UUID and a checksum are found OR the spectrumList start tag (`<spectrumList`) is reached
  (bounded read — never scan the whole 56MB imzML). Reuse a `parse_value_attr`-style helper like
  spike_coords.rs:184. Define a `pub enum IntegrityError` (use `thiserror`) in `src/integrity/mod.rs`
  with variants at least: `MissingIbd { path: PathBuf }`, `MissingUuidDeclaration`,
  `MissingChecksumDeclaration`, `UuidMismatch { declared: String, found: String }`,
  `ChecksumMismatch { kind: ChecksumType, declared: String, computed: String }`, `Io(std::io::Error)`,
  each with a clear `#[error(...)]` message. Update `src/integrity/mod.rs` to `pub mod header;` and
  `pub mod preflight;` (preflight delivered in Task 2) and re-export `ImzmlHeader`, `ChecksumType`,
  `IntegrityError`.
  </action>
  <read_first>src/bin/spike_coords.rs (129-190), src/bin/verify_ibd.rs, src/integrity/mod.rs</read_first>
  <verify>
    <automated>cargo test --test integrity_preflight header_parse_continuous_fixture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test integrity_preflight header_parse_continuous_fixture` (or a `--lib` unit test) asserts the parsed uuid and ("SHA1", "a5be532d25997b71be6d20c76561ddc4d5307ddd") for tests/fixtures/imaging/Example_Continuous.imzML.
    - A test feeds a tiny imzML snippet with non-ASCII (Latin-1) bytes before the params and asserts the UUID is still parsed (proves byte-level, not UTF-8-line, parsing).
    - `grep -n 'IMS:1000080' src/integrity/header.rs` and `grep -n 'IMS:1000091' src/integrity/header.rs` both match.
  </acceptance_criteria>
  <done>The header parser extracts the declared UUID and the declared checksum (type + hex) from the imzML via byte-level Latin-1 scanning, with typed errors on missing declarations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preflight gate + corrupted fixture + hard-fail integration tests</name>
  <files>src/integrity/preflight.rs, tests/integrity_preflight.rs, tests/fixtures/imaging/Corrupt_BadChecksum.imzML, tests/fixtures/imaging/Corrupt_BadChecksum.ibd</files>
  <behavior>
    - preflight on the clean Example_Continuous pair returns Ok and a RunProvenance-compatible summary (uuid + checksum).
    - preflight on a pair whose imzML declares a SHA-1 that does not match the .ibd returns Err(ChecksumMismatch).
    - preflight on an imzML whose IMS:1000080 does not match the .ibd first-16-byte UUID returns Err(UuidMismatch).
    - preflight on an imzML whose sibling .ibd is absent returns Err(MissingIbd) with the expected .ibd path in the message.
  </behavior>
  <action>
  In `src/integrity/preflight.rs` define `pub fn preflight(imzml_path: &Path) -> Result<PreflightReport, IntegrityError>`
  that: (1) derives the sibling .ibd path from the imzml stem (try `.ibd` then `.IBD`, matching mzdata's
  open_path behavior) and returns `IntegrityError::MissingIbd` if neither exists; (2) calls
  `header::parse_imzml_header`; (3) reads the .ibd first 16 bytes and compares them BYTE-FOR-FOR-FOR to
  the declared UUID parsed to RFC-4122 bytes (reuse verify_ibd.rs's EXPECTED_UUID_BYTES comparison logic
  but build the expected bytes from the parsed hex string, not a constant) — return
  `IntegrityError::UuidMismatch` on inequality (the .NET mixed-endian reading is emitted only as a
  diagnostic in the error message, never accepted); (4) compute the whole-file digest matching the
  declared `ChecksumType` over the entire .ibd (byte 0..EOF) and compare case-insensitively — return
  `IntegrityError::ChecksumMismatch` on inequality. For the digest: compute it in-Rust streaming the
  file in bounded chunks (do NOT load 815MB into memory; read in a fixed buffer loop) — if a pure-std
  SHA-1/MD5/SHA-256 is impractical without a new crate, shell out to `shasum -a 1|256` / `md5` exactly
  as verify_ibd.rs does for SHA-1 (it streams the file), selecting the tool by ChecksumType; document
  the choice in the SUMMARY. Return a `pub struct PreflightReport { pub uuid: String, pub checksum_type: ChecksumType, pub checksum_hex: String }` on success. Add `tests/integrity_preflight.rs`: a PASS test on
  the clean Example_Continuous pair; a bad-checksum test using a committed
  `tests/fixtures/imaging/Corrupt_BadChecksum.{imzML,ibd}` (create it: copy the Example_Continuous .ibd
  but flip one byte in the body — NOT the first 16 UUID bytes — and copy the .imzML keeping its original
  declared SHA-1 so the declared checksum no longer matches; keep both files tiny by truncating the .ibd
  body if needed AND updating the declared external array length is NOT required because preflight never
  reads arrays); a missing-.ibd test using a temp dir containing only an .imzML; assert each returns the
  matching `IntegrityError` variant. Also add a thin `examples/preflight.rs` OR a `#[test]` that invokes
  preflight and maps Err -> non-zero process exit so the "non-zero exit" success criterion is checkable
  (prefer asserting the Err variant in-test; the non-zero exit is the binary's job in Phase 6, but
  document that preflight's Err is what the binary converts to a non-zero exit).
  </action>
  <read_first>src/bin/verify_ibd.rs, src/integrity/header.rs, src/read/record.rs (RunProvenance shape)</read_first>
  <verify>
    <automated>cargo test --test integrity_preflight</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test integrity_preflight` passes: `preflight_ok_on_clean_fixture`, `preflight_fails_on_bad_checksum` (asserts `IntegrityError::ChecksumMismatch`), `preflight_fails_on_uuid_mismatch` (asserts `UuidMismatch`), `preflight_fails_on_missing_ibd` (asserts `MissingIbd`).
    - The corrupted fixtures exist: `test -f tests/fixtures/imaging/Corrupt_BadChecksum.imzML && test -f tests/fixtures/imaging/Corrupt_BadChecksum.ibd`.
    - The bad-checksum fixture's .ibd first 16 bytes still equal the declared UUID (so the checksum test isolates the checksum path, not the UUID path): a test asserts the UUID check passes while the checksum check fails on that fixture.
    - The full-file digest is computed without loading the whole .ibd into memory (chunked read loop or shasum/md5 shell-out, as in verify_ibd.rs) — `grep -nE 'shasum|chunk|read_exact|read\(' src/integrity/preflight.rs` shows a bounded-read or shell-out approach (no `fs::read(ibd)` of the whole sidecar).
  </acceptance_criteria>
  <done>preflight hard-fails (typed Err) on UUID mismatch, checksum mismatch, and missing .ibd, passes on the clean fixture, computes the digest with bounded memory, and the behavior is proven by committed-fixture integration tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .imzML/.ibd files → preflight | Untrusted external binary; the .ibd may be the wrong file, truncated, or corrupt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Spoofing | wrong .ibd paired with imzML | mitigate | RFC-4122 first-16-byte UUID compare AND whole-file checksum compare; both must pass or preflight returns Err (IN-07) |
| T-02-03 | Tampering | corrupted/truncated .ibd body | mitigate | whole-file checksum over byte 0..EOF vs declared IMS:1000090/91/92; mismatch is a hard Err |
| T-02-04 | Denial of Service | 815MB .ibd loaded into memory for hashing | mitigate | bounded chunked read or `shasum`/`md5` shell-out (streams the file), per verify_ibd.rs; never `fs::read` the whole sidecar |
| T-02-05 | Repudiation | silent acceptance of mismatch (mzdata only warns) | mitigate | converter-owned gate returns a typed IntegrityError with a clear message; the binary maps it to a non-zero exit |
</threat_model>

<verification>
- `cargo test --test integrity_preflight` passes all four named tests.
- Corrupted fixtures committed and tiny.
- Digest computation does not load the full .ibd into memory.
</verification>

<success_criteria>
A converter-owned preflight parses the imzML-declared UUID and checksum via byte-level
Latin-1 parsing, hard-fails (typed Err → non-zero exit) on UUID mismatch, checksum
mismatch (whichever type the imzML declares), and missing .ibd, passes on the clean
fixture, and hashes the .ibd with bounded memory — all proven by committed-fixture
integration tests (IN-07).
</success_criteria>

<output>
Create `.planning/phases/02-imzml-read-layer-integrity-preflight/02-02-SUMMARY.md` when done.
</output>
