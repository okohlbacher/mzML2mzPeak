---
phase: 18-scan-settings-list-authoritative-geometry-facet
plan: 03
type: execute
wave: 3
depends_on: ["18-02"]
files_modified:
  - tests/scan_settings.rs
autonomous: true
requirements: [GEO-03]
must_haves:
  truths:
    - "Converting a committed fixture and re-opening the archive shows a scan_settings_list block in file_index().metadata"
    - "The metadata.imaging geometry equals the geometry carried in scan_settings_list (derived copy matches the authoritative facet)"
    - "scan_settings_list params use the correct IMS accessions and the UO:0000017 µm unit on the µm geometry terms"
  artifacts:
    - path: "tests/scan_settings.rs"
      provides: "GEO-03 read-back consistency test over the produced archive"
      min_lines: 70
  key_links:
    - from: "tests/scan_settings.rs"
      to: "MzPeakReader.file_index().metadata"
      via: "read scan_settings_list + imaging blocks back from the produced archive"
      pattern: "file_index\\(\\)\\.metadata"
    - from: "tests/scan_settings.rs"
      to: "src/write/convert.rs (forward emission)"
      via: "convert the committed fixture then assert facet⇔imaging consistency"
      pattern: "scan_settings_list"
---

<objective>
Prove GEO-03: read-back consistency. Convert a committed fixture, re-open the produced archive
with the reference `MzPeakReader`, and assert (a) a `scan_settings_list` block is present, (b) the
`metadata.imaging` geometry equals the geometry carried in `scan_settings_list` (the derived copy
matches the authoritative facet), and (c) the facet's params use the correct IMS accessions + the
UO:0000017 µm unit on the µm geometry terms.

Purpose: lock the authoritative-facet ⇔ derived-copy invariant at the archive level so any future
drift fails loudly in CI.
Output: `tests/scan_settings.rs` integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-scan-settings-list-authoritative-geometry-facet/18-CONTEXT.md

<interfaces>
<!-- The established convert+read-back test seam to mirror EXACTLY (tests/cv_list.rs):
     ImagingReader::open(fixture) -> convert(reader, &out, &[]) -> MzPeakReader::new(&out)
     -> mzreader.file_index().metadata.get("<key>").  Committed fixture only; NO --image,
     NO network/.ibd dependency beyond what the processed fixture already satisfies. -->
const PROCESSED_FIXTURE: &str = "tests/fixtures/imaging/Example_Processed.imzML";
mzml2mzpeak::read::ImagingReader::open(path) -> ImagingReader
mzml2mzpeak::write::convert(reader, &out, &[]) -> Result<(), WriteError>   // library wrapper
mzpeak_prototyping::MzPeakReader::new(&out).file_index().metadata: HashMap<String, serde_json::Value>

<!-- NOTE: the library `convert(..)` wrapper passes geometry=None (Plan 18-02). To exercise the
     facet⇔imaging consistency from a DECLARED-grid source, the test should drive emission with
     declared geometry. Two acceptable approaches — pick the one that matches the codebase:
     (A) if the committed fixture's <scanSettings> declares grid counts, call the geometry-aware
         path: parse_scan_settings(fixture) then convert_with(reader, &out, &[], &EncodingOptions
         ::legacy(), Some(&geom));  (convert_with gained the geometry param in 18-02).
     (B) if the fixture declares no <scanSettings> geometry, the facet has empty parameters and the
         imaging-block geometry is None/observed_max — still assert the block is PRESENT and that
         whatever geometry params it DOES carry match the imaging block, plus the µm-unit shape on
         any present µm term. Prefer (A) when the fixture (or a small committed declared-grid
         fixture already used by geometry_parse.rs) provides declared grid counts. -->
crate::schema::parse_scan_settings, crate::write::convert_with, crate::write::EncodingOptions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GEO-03 read-back consistency test (facet ⇔ derived imaging copy)</name>
  <files>tests/scan_settings.rs</files>
  <read_first>
    - tests/cv_list.rs (the convert+reopen+metadata.get seam to mirror exactly — temp_out, open_fixture, file_index().metadata.get)
    - tests/geometry_parse.rs (to find a committed fixture that declares <scanSettings> grid counts, if any, for the declared-grid assertion path)
    - src/write/convert.rs (convert + convert_with signatures after 18-02 — whether to use convert(None) or convert_with(Some(&geom)))
    - src/schema/scan_settings.rs (the param shape: cv_ref/accession/name/value/unit_cv_ref/unit_accession — what to read back)
    - src/schema/metadata.rs (ImagingMetadata field names: pixel_count{x,y}, pixel_size_um{x,y}, max_dimension_um{x,y}, scan_pattern — the derived-copy targets)
  </read_first>
  <action>
    Create `tests/scan_settings.rs`. Mirror tests/cv_list.rs structure (temp_out helper,
    open_fixture, convert, MzPeakReader::new, file_index().metadata). Drive emission so a
    `scan_settings_list` is produced — use the geometry-aware path
    `convert_with(reader, &out, &[], &EncodingOptions::legacy(), Some(&geom))` after
    `let geom = parse_scan_settings(fixture)?` when the chosen committed fixture declares grid
    geometry; otherwise fall back to the library `convert(reader, &out, &[])` and assert the
    block-present + intra-archive-consistency invariants that still hold for an all-None geom.

    Assertions:
    (1) PRESENCE: `metadata.get("scan_settings_list")` is `Some`, a JSON array, with at least one
        entry carrying required keys `id` (string) and `parameters` (array).
    (2) ACCESSION/UNIT SHAPE: for every µm geometry param present in scan_settings_list
        (accessions IMS:1000044/45/46/47/53/54), assert `unit_cv_ref == "UO"` and
        `unit_accession == "UO:0000017"`; for grid-count params IMS:1000042/43 and any presence-only
        scan-pattern param, assert NO unit_accession key. Assert each param's `accession` matches
        its `cv_ref` prefix ("IMS:" for geometry terms).
    (3) DERIVED-COPY CONSISTENCY: read back BOTH `metadata.imaging` and `scan_settings_list`. For
        each geometry datum the imaging block carries, assert the facet carries the SAME value under
        the corresponding accession: imaging `pixel_count.x/y` == facet IMS:1000042/43 values (when
        pixel_count_source is "declared"); imaging `pixel_size_um.x/y` == facet IMS:1000046/47;
        imaging `max_dimension_um.x/y` == facet IMS:1000044/45; imaging `scan_pattern` CURIE ==
        the presence-only scan-pattern param's accession in the facet. Where the imaging block's
        pixel_count_source is "observed_max" (no declared grid), assert the facet carries NO
        IMS:1000042/43 grid param (observed_max is index-only — never fabricated into the facet).
    Clean up the temp archive at the end (mirror cv_list.rs `remove_file`).
  </action>
  <verify>
    <automated>cargo test --test scan_settings</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test scan_settings` passes.
    - The test reads scan_settings_list back from the produced archive via MzPeakReader.file_index().metadata and asserts the block is present + well-formed (id + parameters).
    - µm geometry params (IMS:1000044/45/46/47/53/54) are asserted to carry unit_accession "UO:0000017"; grid-count + scan-pattern params are asserted to carry NO unit.
    - The metadata.imaging geometry is asserted EQUAL to the matching scan_settings_list params (derived copy matches the authoritative facet).
    - observed_max pixel_count (when applicable) is asserted ABSENT from scan_settings_list (no fabrication).
  </acceptance_criteria>
  <done>GEO-03 read-back consistency is proven at the archive level; the facet⇔derived-copy invariant fails loudly on any future drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| produced archive → MzPeakReader | Read-back of the converter's own output via the reference reader. No external untrusted input; committed fixture only. No HIGH threats. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Tampering | silent facet⇔imaging drift shipping undetected | mitigate | This test is the archive-level gate: equality assertions fail CI on any divergence between the authoritative facet and its derived copy. |
</threat_model>

<verification>
- `cargo test --test scan_settings` green.
- The test depends only on a committed fixture (no `.ibd`/network/`--image`).
</verification>

<success_criteria>
GEO-03: a committed-fixture conversion is re-opened and proven to carry a `scan_settings_list`
block whose geometry equals the `metadata.imaging` derived copy, with correct IMS accessions + the
UO:0000017 µm unit, and observed_max pixel_count absent from the authoritative facet.
</success_criteria>

<output>
Create `.planning/phases/18-scan-settings-list-authoritative-geometry-facet/18-03-SUMMARY.md` when done
</output>
