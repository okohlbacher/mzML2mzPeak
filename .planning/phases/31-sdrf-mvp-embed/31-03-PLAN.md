---
phase: 31-sdrf-mvp-embed
plan: 03
type: execute
wave: 3
depends_on: [31-01, 31-02]
files_modified:
  - src/cli.rs
  - src/write/mzml.rs
  - src/sdrf/embed.rs
  - src/sdrf/mod.rs
  - docs/mzpeak-extension-contract.md
  - tests/sdrf_embed.rs
autonomous: true
requirements: [SM-01, SM-02, SM-03, SM-04]
user_setup: []

must_haves:
  truths:
    - "A `--sdrf <PATH>` flag (EXPLICIT only, never auto-discovered) threads an SDRF into the plain-mzML conversion via convert_mzml; it is rejected on the reverse path."
    - "On --sdrf, the SDRF is parsed (Plan 01), file-row matched against the input mzML (loud diagnostic on zero/multi), embedded verbatim as a typed sample-metadata/sdrf member (Plan 02), and a metadata.study back-ref is written via the Phase-30 study_metadata() contract."
    - "The metadata.study back-ref carries dataset_accession + title + sample_metadata_ref (the embedded member name) and validates against schema/study.json (deny_unknown_fields / additionalProperties:false)."
    - "No --sdrf reproduces the prior no-SDRF convert_mzml output byte-for-byte (the study key + member are absent)."
    - "Re-opening the archive yields a non-empty FileIndex with metadata.study, and the embedded member bytes equal the source SDRF byte-for-byte (the BYTE-IDENTICAL re-serve XRT)."
    - "The repo-SDRF-wins precedence rule is documented."
  artifacts:
    - path: "tests/sdrf_embed.rs"
      provides: "end-to-end PXD020187 label-free embed: FileIndex survival + metadata.study back-ref + byte-identical re-serve + no-SDRF control"
      min_lines: 60
  key_links:
    - from: "src/cli.rs"
      to: "src/write/mzml.rs convert_mzml"
      via: "cli.sdrf.as_deref() threaded into convert_mzml(.., sdrf: Option<&Path>)"
      pattern: "convert_mzml\\("
    - from: "src/write/mzml.rs"
      to: "crate::schema::study_metadata"
      via: "metadata.study back-ref built from the matched SDRF + embedded member name"
      pattern: "study_metadata\\("
    - from: "src/write/mzml.rs"
      to: "src/sdrf::embed::embed_sdrf_member"
      via: "verbatim member written at the convert_mzml terminal seam (Plan 02 insertion point)"
      pattern: "embed_sdrf_member"
---

<objective>
Close the MVP vertical: add the `--sdrf <PATH>` CLI layer (R3-H4, explicit-only), thread it into
`convert_mzml(.., sdrf: Option<&Path>)`, and at the Plan-02 terminal seam parse + file-row-match + embed
the SDRF verbatim and write the `metadata.study` provenance back-ref via the Phase-30
`schema::study_metadata()` contract. Document the repo-SDRF-wins precedence rule. Prove the END STATE: a
label-free SDRF (PXD020187) embeds losslessly and re-serves BYTE-IDENTICAL — a complete, demoable,
upstream-independent vertical.

Purpose (SM-01..04 end-to-end): everything Plans 01–02 built becomes reachable from the CLI and produces a
valid mzPeak that round-trips the embedded bytes exactly. The back-ref uses the already-shipped Phase-30
`study.rs` / `schema/study.json` contract (three-places already satisfied for that block in Phase 30).

Output: `--sdrf` flag; `convert_mzml` SDRF arm (parse → match → embed → back-ref); precedence doc note; the
PXD020187 byte-identical re-serve acceptance test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-sdrf-mvp-embed/31-CONTEXT.md
@src/cli.rs
@src/write/mzml.rs
@src/schema/study.rs
@schema/study.json
@docs/mzpeak-extension-contract.md

<interfaces>
<!-- The metadata.study back-ref is the Phase-30 contract — DO NOT define a new key or struct.
     schema::study_metadata(dataset_accession, title, sample_metadata_ref) -> StudyMetadata is the
     constructor; it serializes to {dataset_accession, title, sample_metadata_ref} (run_sample_binding is
     Phase-32, omitted here). Write it under the "study" key via zip.add_index_metadata("study", &block). -->

From src/schema/study.rs (Phase 30 — shipped):
  pub fn study_metadata(dataset_accession: impl Into<String>, title: impl Into<String>,
      sample_metadata_ref: impl Into<String>) -> StudyMetadata;
  // serializes to {dataset_accession, title, sample_metadata_ref}; schema/study.json governs it
  // (required: [dataset_accession, title, sample_metadata_ref], additionalProperties:false).
  // re-exported: crate::schema::{StudyMetadata, study_metadata}.

From Plan 01 (src/sdrf):
  parse_sdrf(path) -> Result<SampleMetadataDoc, SdrfError>;
  match_rows_for_data_file(&doc, mzml_path) -> MatchResult { rows, diagnostics };
  // SampleMetadataDoc carries source_names(), assays, factor_levels, verbatim (raw header + rows),
  // diagnostics. A dataset_accession hint may be derived from characteristics[proteomexchange accession
  // number] or the SDRF filename stem (PXD…/MTBLS…).

From Plan 02 (src/sdrf::embed):
  embed_sdrf_member(zip, sdrf_bytes_path, member_name) -> Result<EmbedFacts, EmbedError>;
  // member_name is caller-supplied; pass the FIXED constant "sample_metadata/sdrf.tsv".
  // Insertion point: the documented seam in convert_mzml between finish_parquet() and the transform KV.

From src/cli.rs:
  run() dispatches by extension: .imzML→run_forward, .mzML→run_forward_mzml, .mzpeak→run_reverse.
  --sdrf belongs on the plain-mzML forward path (run_forward_mzml → convert_mzml). The imaging
  run_forward path does NOT thread --sdrf in this phase (SDRF accompanies proteomics mzML, design §5).
  Reject --sdrf on run_reverse with an actionable forward-only message (mirror the --image rejection).

Embedded member name + back-ref contract (design §5.1):
  member name = "sample_metadata/sdrf.tsv" (deterministic, fixed).
  metadata.study.sample_metadata_ref = that member name.
  precedence = "repo_wins" (documented; SM-04). For THIS MVP the embed_scope is applicable_rows+header
  by default (a valid sub-SDRF reconstructed from VerbatimBundle); --embed-full-sdrf (whole file) MAY be
  added but is OPTIONAL — if time-boxed, embed the WHOLE source file (simplest byte-identical anchor) and
  record embed_scope:"full"; either way the re-serve must be byte-identical to what was embedded.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --sdrf flag + thread sdrf through convert_mzml (reject on reverse)</name>
  <files>src/cli.rs, src/write/mzml.rs</files>
  <action>
    In cli.rs add to ConvertCli: `#[arg(long = "sdrf", value_name = "PATH")] pub sdrf: Option<PathBuf>`
    (EXPLICIT only — NO auto-discovery, per SM-01 / Non-goals; document it like `--image` but single-valued
    and proteomics-mzML-only). In `run_forward_mzml`, pass `cli.sdrf.as_deref()` into `convert_mzml`. Extend
    `convert_mzml`'s signature to `convert_mzml(input, output, opts, sdrf: Option<&Path>)`. Update the two
    existing callers in tests/mzml_convert.rs is NOT in scope of this plan's files — instead keep the new
    parameter and update the call sites that ARE in scope: cli.rs (run_forward_mzml) passes the flag; any
    library/test call in mzml.rs's own `#[cfg(test)]` passes `None`. (tests/mzml_convert.rs and
    tests/sorting_rank.rs / tests/conformance_l2.rs call convert_mzml — UPDATE those call sites to pass
    `None` as the new trailing arg; add them to files_modified is unnecessary because they are mechanical
    None-passes, BUT they MUST compile — make the edits.) In run_reverse, reject `--sdrf` with an actionable
    forward-only error (mirror the `--image` rejection in run_reverse). Leave the imaging `run_forward` path
    UNCHANGED — `--sdrf` on a `.imzML` input is out of scope this phase; if `cli.sdrf.is_some()` reaches
    run_forward, reject with "—sdrf accompanies plain proteomics .mzML, not imaging .imzML (use a .mzML
    input)". anyhow/log stay in cli.rs only; mzml.rs uses MzmlConvertError (thiserror) — add a
    `MzmlConvertError::Sdrf` variant wrapping the sdrf parse/embed error so the library boundary stays
    anyhow-free.
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -8 && cargo test --lib cli:: 2>&1 | tail -15</automated>
  </verify>
  <done>`--sdrf PATH` parses; convert_mzml carries `sdrf: Option<&Path>`; all in-tree convert_mzml call sites updated to compile; reverse + imaging reject --sdrf with actionable messages; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire parse → match → embed → metadata.study back-ref into the convert_mzml seam</name>
  <files>src/write/mzml.rs, src/sdrf/embed.rs, src/sdrf/mod.rs</files>
  <action>
    In `convert_mzml`, when `sdrf` is `Some(path)`, at the Plan-02 insertion point (after
    `let mut zip = writer.finish_parquet()?;`, BEFORE the transform KV and `zip.finish()`):
      1. `parse_sdrf(path)` → doc (a parse error surfaces as MzmlConvertError::Sdrf — do NOT abort the whole
         conversion silently; this is an explicit user flag so a hard error is acceptable AND actionable).
      2. `match_rows_for_data_file(&doc, input)` → MatchResult; for each Diagnostic, `log::warn!` it (the
         zero-match/multi-match LOUD diagnostic, R9/R10) — but NEVER fail the conversion on a binding miss
         (the spectral data is valid regardless).
      3. Embed the verbatim SDRF as the typed member at the fixed name `"sample_metadata/sdrf.tsv"` via
         `embed_sdrf_member(&mut zip, path, "sample_metadata/sdrf.tsv")`. For the MVP, embedding the WHOLE
         source file is the simplest byte-identical anchor — embed_scope:"full"; (the applicable-rows
         sub-SDRF option is a v0.9 refinement, OK to skip per the §5.1 default-can-be-full note — but the
         re-serve MUST be byte-identical to whatever was written).
      4. Build the back-ref via the Phase-30 contract:
         `let study = crate::schema::study_metadata(accession, title, "sample_metadata/sdrf.tsv");`
         where `accession` is derived from the SDRF (`characteristics[proteomexchange accession number]`
         value, else the filename stem if it matches PXD…/MTBLS…, else the filename stem verbatim) and
         `title` is a best-effort study title (the SDRF carries none cleanly → use the accession or filename
         stem as the informative title — title is informative per schema/study.json). Then
         `zip.add_index_metadata("study", &study).map_err(MzmlConvertError::Json)?;`
      5. Record the precedence/scope provenance: since `metadata.study` (schema/study.json) is
         `additionalProperties:false` and only carries {dataset_accession,title,sample_metadata_ref}, the
         `precedence:"repo_wins"` + `sha256` + `embed_scope` facts go in a SEPARATE
         `metadata.sample_metadata` KV (a free-form provenance map, design §5.1) —
         `zip.add_index_metadata("sample_metadata", &provenance_json)?;` where provenance_json is a
         serde_json::Value (or a small local serde struct) carrying
         {member, sha256 (from EmbedFacts), size_bytes, precedence:"repo_wins", embed_scope:"full",
         dataset_accession}. This keeps the study block schema-clean while still recording precedence + the
         staleness guard (sha256). When `sdrf` is `None`: emit NOTHING (byte-identical output preserved).
    Keep the existing transform KV + zip.finish() AFTER this block. Update MzmlConvertReport to optionally
    carry the match diagnostics count (or just log them — a report field is optional). Add the `EmbedError`
    → `MzmlConvertError::Sdrf` From-conversion if not already present.
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -8 && cargo test --lib write::mzml sdrf:: 2>&1 | tail -20</automated>
  </verify>
  <done>convert_mzml with Some(sdrf) parses + matches (loud diagnostic on miss, never fatal) + embeds the verbatim typed member + writes metadata.study (Phase-30 contract) + metadata.sample_metadata (precedence/sha256/scope); None emits nothing; build green.</done>
</task>

<task type="auto">
  <name>Task 3: PXD020187 byte-identical re-serve acceptance test + precedence doc</name>
  <files>tests/sdrf_embed.rs, docs/mzpeak-extension-contract.md</files>
  <action>
    Add `tests/sdrf_embed.rs`: an integration test that converts the committed
    `tests/fixtures/mzml/tiny.pwiz.1.1.mzML` WITH `sdrf = Some(data/sdrf-examples/PXD020187/PXD020187.sdrf.tsv)`
    via `convert_mzml(input, out, &EncodingOptions::lossless(), Some(sdrf_path))`, then re-open with
    `mzpeak_prototyping::MzPeakReader::new`. Assert:
      (a) FileIndex SURVIVAL — the reader opens; `file_index().metadata.get("study")` is present and
          deserializes to the {dataset_accession,title,sample_metadata_ref} shape with
          sample_metadata_ref == "sample_metadata/sdrf.tsv" (the upstream serde fix holds — this is the XRT
          FileIndex-survival assertion the CONTEXT requires).
      (b) BYTE-IDENTICAL RE-SERVE — re-open the produced `.mzpeak` as a `zip::ZipArchive`, read the
          `sample_metadata/sdrf.tsv` member bytes, and `assert_eq!` them to `std::fs::read` of the source
          PXD020187 SDRF file — BYTE FOR BYTE (the MVP end-state: a label-free SDRF embeds losslessly and
          re-serves byte-identical). Also assert `metadata.sample_metadata` carries precedence:"repo_wins"
          and a 64-hex sha256.
      (c) NO-SDRF CONTROL — a second conversion WITHOUT sdrf (`None`) has NO "study" and NO
          "sample_metadata" metadata key, and the two no-SDRF archives are byte-identical to each other
          (determinism, reuses the Plan-02 guard intent at the integration level).
    Mirror the harness shape of tests/mzml_convert.rs (tmp_out helper, remove_file cleanup).
    In `docs/mzpeak-extension-contract.md`, add a short "SDRF precedence (SM-04)" subsection stating the
    repo-SDRF-wins rule: when the embedded SDRF snapshot and the live repository SDRF disagree, the
    REPOSITORY copy is authoritative; the embedded member is a point-in-time snapshot guarded by
    `metadata.sample_metadata.sha256` + the dataset_accession back-ref so staleness is detectable. (This is
    the doc half of the three-places rule for the precedence fact; the schema/study.json + src halves for
    metadata.study were already shipped in Phase 30.)
  </action>
  <verify>
    <automated>cargo test --test sdrf_embed 2>&1 | tail -25</automated>
  </verify>
  <done>The PXD020187 label-free SDRF embeds and re-serves BYTE-IDENTICAL; metadata.study (Phase-30 shape) + metadata.sample_metadata (precedence/sha256) present; the no-SDRF control has neither key and is deterministic; the repo-wins precedence rule is documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `--sdrf` path → conversion | The explicit user-named TSV crosses into parse + verbatim embed |
| metadata.study back-ref | Must validate against schema/study.json (additionalProperties:false) |
| precedence/staleness | The embedded snapshot may go stale vs the live repo SDRF |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-07 | Tampering | embedded SDRF vs source bytes | mitigate | verbatim stream (Plan 02) + the acceptance test's byte-for-byte re-serve assertion; a silent corruption fails CI |
| T-31-08 | Repudiation | embedded snapshot goes stale vs repo | mitigate | record `metadata.sample_metadata.sha256` + `dataset_accession` + `precedence:"repo_wins"` so the embedded copy is falsifiable against the live repository (design §5.1 / R12) |
| T-31-09 | Spoofing | mzML matches ZERO SDRF rows (wrong file passed) | mitigate | a LOUD `log::warn!` diagnostic (R9) — but still embed verbatim + back-ref and DO NOT fail (spectral data is valid); never silently proceed as if bound |
| T-31-10 | Elevation of privilege | metadata.study carrying undeclared keys | mitigate | use the Phase-30 `study_metadata()` constructor → only the three declared keys; schema/study.json deny_unknown_fields/additionalProperties:false already guards it (Phase-30 tests) |
| T-31-SC | Tampering | (no new package installs in this plan) | accept | csv vetted in Plan 01; no installs here |
</threat_model>

<verification>
- `cargo build` green; `cargo test --test sdrf_embed --test mzml_convert` green.
- `cargo test --lib cli:: write::mzml sdrf::` green.
- A no-`--sdrf` plain-mzML conversion is byte-identical to a second no-`--sdrf` conversion (the study/sample_metadata keys + member are absent); the embedded SDRF re-serves byte-for-byte equal to the source.
- `--sdrf` rejected on the reverse and imaging paths with actionable messages.
</verification>

<success_criteria>
- SM-01: the unified `SampleMetadataDoc` drives the conversion arm (parse + match + accession/title derivation).
- SM-02: the `csv` reader feeds the convert_mzml seam; `--sdrf` is the explicit-only CLI layer.
- SM-03: file-row matching binds the input; zero/multi-match → loud diagnostic, never fatal.
- SM-04: verbatim typed `sample-metadata`/`sdrf` member + `metadata.study` back-ref (Phase-30 contract) + `metadata.sample_metadata` precedence (repo_wins) + sha256; **MVP end-state: PXD020187 embeds losslessly and re-serves BYTE-IDENTICAL**; no-SDRF output byte-identical; precedence documented.
</success_criteria>

<output>
Create `.planning/phases/31-sdrf-mvp-embed/31-03-SUMMARY.md` when done.
</output>
