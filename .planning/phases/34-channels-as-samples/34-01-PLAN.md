---
phase: 34-channels-as-samples
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/sdrf/channels.rs
  - src/sdrf/mod.rs
requirements: [CHAN-01, CHAN-02, CHAN-03]
autonomous: true

must_haves:
  truths:
    - "An isobaric label (e.g. TMT127N, iTRAQ114) is recognized; SILAC/label-free labels are NOT"
    - "Each TMT/iTRAQ reagent resolves to its MS:1002602 child accession and its nominal reporter m/z from a shipped table"
    - "An unresolved label (TMTpro 132–135) yields reporter_mz = None (never a sentinel float) and an honest free-text fallback"
    - "Channel role (sample/pooled/carrier/reference) is derived from the row + dedicated columns, defaulting to sample"
  artifacts:
    - path: "src/sdrf/channels.rs"
      provides: "Reagent constant table + isobaric label classification + per-channel resolution (label CURIE child, reporter_mz, source, role, tag_modification)"
      min_lines: 120
  key_links:
    - from: "src/sdrf/channels.rs"
      to: "src/schema/cv.rs"
      via: "sample_label_curie() / channel_role_token() / reporter_ion_mz_token() single-source accessors"
      pattern: "sample_label_curie|channel_role_token|reporter_ion_mz_token"
---

<objective>
Build the pure-logic channel-resolution core for isobaric (TMT/iTRAQ) channels: a SHIPPED reagent
constant table (TMT 126–131 +N/C, iTRAQ 113–121) mapping each reagent label to its MS:1002602 child
accession and its nominal reporter-ion m/z (source recorded), plus the classification predicate that
admits ONLY isobaric labels (excludes `label free sample` and `SILAC light|medium|heavy`), and the
role derivation (sample/pooled/carrier/reference).

Purpose: CHAN-01..03 hinge on a correct, testable label→(reagent child, reporter m/z, role,
tag_modification) mapping. Isolating it as a dependency-free module lets Plan 02 wire it into
`project_sample_list` without touching the write seam. No new crate dependency (pinned stack).

Output: `src/sdrf/channels.rs` (new), exported from `src/sdrf/mod.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-channels-as-samples/34-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Single-source CV accessors already shipped in src/schema/cv.rs (Phase 30). Channels.rs MUST
     call these — it must NOT restate "1002602" / the role / reporter-mz token literals (no-drift
     gates no_drift_sample_label_curie / no_drift_channel_role_token / no_drift_reporter_ion_mz_token
     scan src/write + src/reverse + src/verify; src/sdrf/ is NOT scanned, but call the accessors
     anyway to keep one source of truth). -->

From src/schema/cv.rs:
- `pub fn sample_label_curie() -> mzdata::params::CURIE`   // MS:1002602 umbrella ("sample label")
- `pub fn channel_role_token() -> &'static str`            // "mzml2mzpeak:channel-role"
- `pub fn reporter_ion_mz_token() -> &'static str`         // "mzml2mzpeak:reporter-ion-mz"

mzdata::params::CURIE: construct children via `mzdata::curie!(MS:1002616)`; Display renders "MS:1002616".

From src/sdrf/model.rs (already shipped):
- `pub struct Assay { pub id, pub sample_refs: Vec<String>, pub data_files: Vec<String>,
                      pub parameters: Vec<TypedValue>, pub label: Option<String> }`
  // `label` is the verbatim comment[label] cell, e.g. "TMT127N".
</interfaces>

<reagent-facts>
<!-- Authoritative reagent → MS:1002602-child accession + nominal reporter m/z. SOURCE to record in
     reporter_mz_source: "psi-ms-reagent-table" (PSI-MS CV child terms) for resolved entries.
     TMT 6-plex children begin at MS:1002616 (TMT126). Use the PSI-MS CV ordering for the 10/11-plex
     +N/+C reagents. iTRAQ children begin at MS:1002622. The EXACT child accession per reagent must be
     pinned in this module as the constant table; the values below are the reporter m/z constants the
     fixtures exercise (TMT126..TMT131 and the +N/+C variants present in PXD011799/PXD014145). -->

TMT nominal reporter m/z (monoisotopic, reagent table):
  TMT126 = 126.127726, TMT127N = 127.124761, TMT127C = 127.131081,
  TMT128N = 128.128116, TMT128C = 128.134436, TMT129N = 129.131471,
  TMT129C = 129.137790, TMT130N = 130.134825, TMT130C = 130.141145,
  TMT131  = 131.138180 (a.k.a. TMT131N), TMT131C = 131.144500.
iTRAQ nominal reporter m/z (4/8-plex reagent table):
  iTRAQ113 = 113.107873, iTRAQ114 = 114.111228, iTRAQ115 = 115.108263,
  iTRAQ116 = 116.111618, iTRAQ117 = 117.114973, iTRAQ118 = 118.111958,
  iTRAQ119 = 119.115313, iTRAQ121 = 121.122003.
TMTpro 16/18-plex high channels (132N/132C/133N/133C/134N/134C/135N): NO resolved reporter m/z in
  this table → reporter_mz = None, reporter_mz_source = "unresolved", honest free-text fallback.
</reagent-facts>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Reagent table + isobaric classification (RED→GREEN)</name>
  <files>src/sdrf/channels.rs, src/sdrf/mod.rs</files>
  <behavior>
    - `is_isobaric_label("TMT127N")` → true; `is_isobaric_label("iTRAQ114")` → true.
    - `is_isobaric_label("label free sample")` → false; `is_isobaric_label("SILAC light")` → false;
      `is_isobaric_label("SILAC medium")` / `"SILAC heavy"` → false; empty/unknown free-text → false.
    - `resolve_reagent("TMT126")` → Some with reporter_mz ≈ 126.127726 (within 1e-4) and an MS-prefixed
      child accession whose Display starts with "MS:" (NOT the umbrella "MS:1002602" itself), and
      reporter_mz_source == "psi-ms-reagent-table".
    - `resolve_reagent("TMT131C")` and `resolve_reagent("TMT127N")` resolve (fixtures use them).
    - `resolve_reagent("iTRAQ114")` → Some(114.111228 ± 1e-4).
    - `resolve_reagent("TMT132N")` (TMTpro high channel) → reporter_mz == None AND
      reporter_mz_source == "unresolved" (honest free-text fallback; never a sentinel float).
    - The reagent child accession is built via `mzdata::curie!` and is distinct per reagent (no two
      reagents share one accession).
  </behavior>
  <action>
    Create `src/sdrf/channels.rs`. Define a `ChannelReagent` value type carrying: the reagent label
    string (verbatim, e.g. "TMT127N"), the MS:1002602-child `accession` (an `mzdata::params::CURIE`
    built via `mzdata::curie!`), `reporter_mz: Option<f64>`, and `reporter_mz_source: &'static str`
    (one of "psi-ms-reagent-table" | "unresolved"). Back it with a SHIPPED constant table (a `match`
    or a static slice) covering TMT 126–131 incl. all +N/+C variants the fixtures use, plus iTRAQ
    113–121, using the reporter m/z constants in <reagent-facts>. Reference the umbrella via
    `crate::schema::cv::sample_label_curie()` in a doc-comment / assertion so the umbrella is
    single-sourced; the per-reagent child accessions are pinned here (PSI-MS child terms, MS:1002616+
    for TMT, MS:1002622+ for iTRAQ — record the SOURCE in a module doc-comment per R1-M4).

    Implement `pub fn is_isobaric_label(label: &str) -> bool` that returns true ONLY for a reagent
    present in the table OR a recognized TMTpro high channel (132–135 N/C), and explicitly false for
    the exclusion sentinels `label free sample` and `SILAC light|medium|heavy` (case-insensitive
    trim). Implement `pub fn resolve_reagent(label: &str) -> Option<ChannelReagent>`: a table hit
    returns the resolved reagent; a recognized-but-unresolved TMTpro high channel returns a
    `ChannelReagent` with `reporter_mz = None`, `reporter_mz_source = "unresolved"` (so the caller can
    still emit the label cvParam as an honest free-text fallback); anything else returns None.

    Do NOT inline any fenced code. Do NOT touch src/write/ or src/cli.rs. Register the module: add
    `pub mod channels;` and a curated re-export `pub use channels::{ChannelReagent, is_isobaric_label,
    resolve_reagent};` in src/sdrf/mod.rs.
  </action>
  <verify>
    <automated>cargo test --lib sdrf::channels 2>&1 | tail -20</automated>
  </verify>
  <done>
    `cargo test --lib sdrf::channels` passes; `is_isobaric_label` excludes SILAC/label-free; resolved
    reagents carry nominal reporter m/z + a distinct MS child accession + source "psi-ms-reagent-table";
    TMTpro high channels resolve with reporter_mz=None and source "unresolved".
  </done>
</task>

<task type="tdd">
  <name>Task 2: Role derivation (RED→GREEN)</name>
  <files>src/sdrf/channels.rs</files>
  <behavior>
    - `derive_role(label, &[], &[])` (no carrier/reference columns, not pooled) → role token value
      "sample".
    - `derive_role("TMT131C", carrier=&["TMT131C"], reference=&[])` → "carrier".
    - `derive_role("TMT126", carrier=&[], reference=&["TMT126"])` → "reference".
    - `derive_role(label, ..., is_pooled=true)` → "pooled" (e.g. when the row's source name is a pool /
      characteristics[pooled sample] is set). Precedence: carrier/reference (explicit columns) win over
      pooled; pooled wins over the default "sample".
    - The returned role string is one of exactly {"sample","pooled","carrier","reference"}.
  </behavior>
  <action>
    Add `pub fn derive_role(label: &str, carrier_channels: &[String], reference_channels: &[String],
    is_pooled: bool) -> &'static str` to channels.rs. It matches the row's reagent label against the
    values of the dedicated `comment[carrier channel]` / `comment[reference channel]` columns
    (R1-H2 — the column VALUE is the channel label), returning "carrier" / "reference" on a hit;
    otherwise "pooled" when `is_pooled`; otherwise "sample". Pair this token with
    `crate::schema::cv::channel_role_token()` at the emit site (Plan 02) — derive_role returns only the
    VALUE; the attribute key is the channel_role_token. Document that absent carrier/reference columns
    (the common case — none of the three fixtures ship them) degrade to "sample" without error.
  </action>
  <verify>
    <automated>cargo test --lib sdrf::channels::tests::role 2>&1 | tail -20</automated>
  </verify>
  <done>
    Role tests pass; carrier/reference resolved from dedicated-column values with correct precedence;
    pooled and default "sample" honored; only the four legal role tokens are returned.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDRF cell → reagent table | untrusted free-text label cell crosses into a lookup; unknown/garbage labels must degrade, never panic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | `resolve_reagent` reporter m/z table | mitigate | constants pinned in-module with a value-pinning test; source recorded as "psi-ms-reagent-table" — drift fails the test |
| T-34-02 | Information disclosure | unresolved TMTpro high channels | accept | reporter_mz=None + source "unresolved" is the honest fallback (CHAN-03); no fabricated float |
| T-34-03 | Denial of service | garbage/empty label cell | mitigate | `resolve_reagent` returns None and `is_isobaric_label` returns false for any non-table, non-TMTpro input — no panic, no unwrap on lookup |
| T-34-SC | Tampering | npm/pip/cargo installs | mitigate | NONE — this plan adds NO dependency (pinned stack); no install task exists |
</threat_model>

<verification>
- `cargo test --lib sdrf::channels` green.
- `cargo build` succeeds with the pinned stack (no new crate in Cargo.toml — confirm `git diff Cargo.toml` is empty).
- `grep -n "1002602" src/sdrf/channels.rs` returns at most doc/comment lines, not an independent code literal for the umbrella (umbrella comes from `sample_label_curie()`).
</verification>

<success_criteria>
- Reagent table resolves every TMT/iTRAQ reagent present in PXD011799 / PXD009465 / PXD014145 to a
  distinct MS:1002602 child + nominal reporter m/z + source "psi-ms-reagent-table" (CHAN-01).
- `is_isobaric_label` excludes `label free sample` and `SILAC light|medium|heavy` (CHAN-03).
- TMTpro 16/18-plex high channels resolve with reporter_mz=None + source "unresolved" (CHAN-03 honest fallback).
- `derive_role` yields {sample,pooled,carrier,reference} with carrier/reference from dedicated columns (CHAN-02).
- No new dependency; pinned stack unchanged.
</success_criteria>

<output>
Create `.planning/phases/34-channels-as-samples/34-01-SUMMARY.md` when done.
</output>
