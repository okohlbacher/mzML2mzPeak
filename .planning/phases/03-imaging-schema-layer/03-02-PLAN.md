---
phase: 03-imaging-schema-layer
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/schema/geometry.rs
  - tests/geometry_parse.rs
  - tests/fixtures/imaging/Synthetic_FullGeometry.imzML
  - tests/fixtures/imaging/Synthetic_MissingGrid.imzML
  - tests/fixtures/imaging/Synthetic_Latin1ScanSettings.imzML
autonomous: true
requirements: [SPA-03]
must_haves:
  truths:
    - "parse_scan_settings() on the real HR2MSI file returns grid_x=260, grid_y=134 and records scan-geometry child terms IMS:1000401/413/480/491 as present"
    - "parse_scan_settings() on a full-geometry fixture (plural name variant, value-less child terms, UO-united numerics) returns grid 3x3, pixel size 100µm, max dimension 300µm"
    - "parse_scan_settings() on a missing-grid scanSettings does NOT hard-fail and leaves grid fields None (D-03 lenient capture)"
    - "parse_scan_settings() honors the ISO-8859-1 prolog: Latin-1 high bytes around scanSettings parse without error"
    - "The parse is bounded — it stops at </scanSettings> and never buffers the whole file"
  artifacts:
    - path: "src/schema/geometry.rs"
      provides: "ImagingRunMetadata type + quick-xml <scanSettings> parser (lenient, Latin-1-safe)"
      contains: "fn parse_scan_settings"
    - path: "tests/geometry_parse.rs"
      provides: "Integration tests: hr2msi_ground_truth, full_geometry, lenient_missing_grid, latin1_prolog"
      contains: "hr2msi_ground_truth"
    - path: "tests/fixtures/imaging/Synthetic_FullGeometry.imzML"
      provides: "Full-geometry scanSettings fixture (plural names, value-less child terms, pixel size + max dim)"
      contains: "scanSettings"
  key_links:
    - from: "src/schema/geometry.rs"
      to: "quick_xml::Reader (encoding feature)"
      via: "Reader::from_reader(BufReader::new(File)) started at the prolog"
      pattern: "from_reader"
    - from: "tests/geometry_parse.rs"
      to: "data/HR2MSImouseurinarybladderS096.imzML"
      via: "real-data ground-truth assertion"
      pattern: "HR2MSI"
---

<objective>
Build the imzML `<scanSettings>` geometry parser (D-01/D-02/D-03, SPA-03): a `quick-xml`
parse that honors the ISO-8859-1 prolog, captures every present geometry CV term keyed on
ACCESSION ONLY (never name), leaves absent terms `None` (never hard-fails), and stops at
`</scanSettings>`. Populate the `ImagingRunMetadata` type stubbed in Plan 01 and prove it
against the real HR2MSI file plus synthetic fixtures.

Purpose: mzdata does NOT surface `<scanSettings>` geometry (Phase-1 FINDINGS, confirmed) —
this direct XML parse is the primary path for SPA-03. D-01 builds it NOW (not Phase 4) to
de-risk the riskiest unproven step against real data within this phase.

Output: a working `parse_scan_settings(path) -> Result<ImagingRunMetadata, GeometryParseError>`,
three synthetic fixtures, and `tests/geometry_parse.rs` covering all four VALIDATION.md
geometry behaviors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-imaging-schema-layer/03-CONTEXT.md
@.planning/phases/03-imaging-schema-layer/03-RESEARCH.md
@.planning/phases/03-imaging-schema-layer/03-PATTERNS.md
@.planning/phases/03-imaging-schema-layer/03-VALIDATION.md

<fixture_ground_truth>
<!-- VERIFIED this session by direct inspection of the real file. -->
data/HR2MSImouseurinarybladderS096.imzML lines 68-77 — the REAL <scanSettings> (minimal shape):
  <scanSettingsList count="1">
    <scanSettings id="scansettings1">
      <cvParam cvRef="IMS" accession="IMS:1000401" name="top down" value=""/>
      <cvParam cvRef="IMS" accession="IMS:1000413" name="flyback" value=""/>
      <cvParam cvRef="IMS" accession="IMS:1000480" name="horizontal line scan" value=""/>
      <cvParam cvRef="IMS" accession="IMS:1000491" name="linescan left right" value=""/>
      <cvParam cvRef="IMS" accession="IMS:1000042" name="max count of pixel x" value="260"/>
      <cvParam cvRef="IMS" accession="IMS:1000043" name="max count of pixel y" value="134"/>
    </scanSettings>
  </scanSettingsList>
  --> grid 260x134 (260*134 = 34,840 ✓); NO pixel size, NO max dimension; child terms value="".

⚠ CORRECTION TO RESEARCH/PATTERNS (verified this session): the committed
tests/fixtures/imaging/Example_Continuous.imzML has ZERO <scanSettings> (it is a 380-line
9-pixel fixture). RESEARCH Pitfall 1 / PATTERNS claimed it carries a full geometry block —
that is FALSE for the actual committed file. Therefore the full-geometry-shape test MUST use
a NEW synthetic fixture (Synthetic_FullGeometry.imzML) authored in this plan, NOT the
continuous fixture. The continuous fixture DOES carry Latin-1 high bytes in its <contact>
block (lines 24-26: "Gießen", "Schubertstraße"), so it remains usable as an auxiliary Latin-1
witness, but the dedicated latin1_prolog test uses a synthetic fixture for determinism.
</fixture_ground_truth>

<interfaces>
<!-- quick-xml 0.30.0 API, VERIFIED at source (cargo cache). Use these EXACT signatures. -->
use quick_xml::Reader;
use quick_xml::events::Event;
use std::io::BufReader;
use std::fs::File;

let mut reader = Reader::from_reader(BufReader::new(File::open(path)?)); // MUST start at <?xml ?>
reader.trim_text(true);
let mut buf: Vec<u8> = Vec::new();
loop {
    match reader.read_event_into(&mut buf)? {   // 0.30 buffered-read method name
        Event::Start(e) | Event::Empty(e) => { /* e.local_name().as_ref() == b"cvParam" etc. */ }
        Event::End(e) => { /* e.local_name().as_ref() == b"scanSettings" -> break (bounded) */ }
        Event::Eof => break,
        _ => {}
    }
    buf.clear();
}
// Attribute decode honoring the refined ISO-8859-1 encoding:
//   for attr in e.attributes().flatten() {
//       let key = attr.key.as_ref();                                   // &[u8]
//       let val = attr.decode_and_unescape_value(&reader)?.into_owned(); // String, encoding-aware
//   }
// NOTE: self-closing <cvParam .../> arrives as Event::Empty, NOT Event::Start — handle BOTH.
// NOTE: encoding refines ONLY on the first Decl event; starting mid-file re-introduces the
//       Latin-1 bug. Always open from the file start.

From Plan 01 (already defined in src/schema/geometry.rs stub — fill the bodies):
  pub enum GeometryParseError { Io(#[from] std::io::Error), /* + Xml(quick_xml::Error) */ }
  pub struct ImagingRunMetadata { /* all-Option geometry fields + child-term presence fields */ }
  pub fn parse_scan_settings(path: &std::path::Path) -> Result<ImagingRunMetadata, GeometryParseError>
</interfaces>

<analog>
src/integrity/header.rs — the closest structural analog: thiserror error model
(IntegrityError lines 73-99, including `Io(#[from] std::io::Error)`), bounded-read stop
condition (lines 144-146 break at `<spectrumList`; geometry breaks at `</scanSettings>`),
and verbatim-accession matching (checksum_type_of lines 183-193). D-02 swaps the parsing
MECHANISM to quick-xml but the module structure, error model, and bounded discipline carry over.

tests/integrity_preflight.rs — the integration-test idiom: fixture-path consts +
ground-truth value consts (lines 18-24), `#[test]` fns asserting parser output (lines 31-41),
and the RAW-BYTE synthetic-fixture write technique for Latin-1 (lines 70-100: build a
`Vec<u8>`, push raw high bytes like 0xDF/0xE4, `fs::write`, then parse). Reuse this exact
technique for the synthetic fixtures.
</analog>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author three synthetic scanSettings fixtures</name>
  <files>tests/fixtures/imaging/Synthetic_FullGeometry.imzML, tests/fixtures/imaging/Synthetic_MissingGrid.imzML, tests/fixtures/imaging/Synthetic_Latin1ScanSettings.imzML</files>
  <read_first>
    - tests/fixtures/imaging/Example_Continuous.imzML lines 1-30 (a minimal valid imzML header shape to model the fixtures on — prolog `<?xml version="1.0" encoding="ISO-8859-1"?>`, mzML/fileDescription wrapper)
    - tests/integrity_preflight.rs lines 70-100 (the raw-byte Latin-1 write idiom — for the Latin-1 fixture, high bytes MUST be written as raw bytes, NOT a Rust &str)
    - <fixture_ground_truth> in this plan's <context> (the HR2MSI minimal shape to contrast against)
    - 03-RESEARCH.md §Common Pitfalls Pitfall 2 (the two valid cvParam shapes: value="", real value, and NO value attribute) and Pitfall 4 (real files omit pixel size / max dimension)
  </read_first>
  <action>
    Create three small, hand-authored imzML fixtures under tests/fixtures/imaging/. Each starts
    with the ISO-8859-1 prolog and a minimal `<mzML><fileDescription>...</fileDescription>` wrapper
    followed by a `<scanSettingsList count="1"><scanSettings id="scansettings1">...</scanSettings>
    </scanSettingsList>` and a trivial `<run><spectrumList count="0"></spectrumList></run>` close so
    the parse has a clean bounded structure.

    (a) Synthetic_FullGeometry.imzML — the FULL-geometry shape (replaces the mistaken
        continuous-fixture assumption). Include: grid `IMS:1000042` value="3" and `IMS:1000043`
        value="3" using the PLURAL name variant "max count of pixels x" / "...y" (proves
        accession-only matching); max dimension `IMS:1000044` value="300" and `IMS:1000045`
        value="300" with `unitCvRef="UO" unitAccession="UO:0000017" unitName="micrometer"`; pixel
        size `IMS:1000046` value="100.0" and `IMS:1000047` value="100.0"; and the four scan-geometry
        child terms `IMS:1000401`/`IMS:1000413`/`IMS:1000480`/`IMS:1000491` written with NO `value`
        attribute at all (e.g. `<cvParam cvRef="IMS" accession="IMS:1000413" name="flyback"/>`) to
        exercise the value-less self-closing-Empty path.

    (b) Synthetic_MissingGrid.imzML — a `<scanSettings>` with ONLY the four child terms (value="")
        and NO grid counts, NO pixel size, NO max dimension. Proves D-03 lenient capture: the parse
        must succeed and leave grid/pixel/max-dim fields None.

    (c) Synthetic_Latin1ScanSettings.imzML — MUST be written as RAW BYTES (a Rust &str is UTF-8 and
        cannot represent lone Latin-1 high bytes). Author it via a tiny committed generator OR by
        writing the bytes file directly: include the ISO-8859-1 prolog, a contact/name cvParam line
        carrying raw high bytes 0xDF ('ß') and 0xE4 ('ä') (mimic "Gießen"/"Müller") BEFORE the
        scanSettings, then a valid `<scanSettings>` with grid `IMS:1000042`=5 / `IMS:1000043`=7.
        Proves the quick-xml encoding feature decodes the prolog and does not choke on the high bytes
        (the Phase-1 landmine). If a `.imzML` file cannot hold raw high bytes when committed, instead
        have tests/geometry_parse.rs synthesize this fixture into a tempdir at test time using the
        raw-byte `Vec<u8>` write idiom (tests/integrity_preflight.rs lines 70-100) — in that case do
        NOT commit Synthetic_Latin1ScanSettings.imzML and update files_modified accordingly in the
        SUMMARY. Prefer the committed-raw-bytes file if it round-trips cleanly through git.
  </action>
  <verify>
    <automated>test -f tests/fixtures/imaging/Synthetic_FullGeometry.imzML && test -f tests/fixtures/imaging/Synthetic_MissingGrid.imzML && grep -c 'IMS:1000042' tests/fixtures/imaging/Synthetic_FullGeometry.imzML</automated>
  </verify>
  <acceptance_criteria>
    - Synthetic_FullGeometry.imzML exists, contains `IMS:1000042` (grid), `IMS:1000046` (pixel size), `IMS:1000044` (max dim), all four child terms, AND uses the plural "pixels" name variant.
    - Synthetic_MissingGrid.imzML exists, contains the four child terms, and contains NO `IMS:1000042`/`IMS:1000043` (`grep -c 'IMS:1000042'` == 0).
    - The Latin-1 fixture either exists as a committed raw-byte file containing byte 0xDF, OR the SUMMARY documents that geometry_parse.rs synthesizes it at test time.
    - All fixtures begin with `<?xml version="1.0" encoding="ISO-8859-1"?>`.
  </acceptance_criteria>
  <done>Three synthetic fixtures cover the full-geometry (plural names, value-less child terms, units), missing-grid (D-03), and Latin-1-prolog shapes the parser tests require — the committed continuous fixture's missing scanSettings no longer blocks coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the quick-xml lenient <scanSettings> parser into ImagingRunMetadata</name>
  <files>src/schema/geometry.rs</files>
  <behavior>
    - parse_scan_settings(HR2MSI path) -> grid_x=Some(260), grid_y=Some(134); scan_pattern=Some("IMS:1000413"), linescan_sequence=Some("IMS:1000401"), scan_type=Some("IMS:1000480"), line_scan_direction=Some("IMS:1000491"); pixel_size_x/y=None, max_dimension_x/y=None.
    - parse_scan_settings(Synthetic_FullGeometry) -> grid_x=Some(3), grid_y=Some(3), pixel_size_x=Some(100.0), max_dimension_x=Some(300), and the four child terms present DESPITE having no value attribute (matched on accession).
    - parse_scan_settings(Synthetic_MissingGrid) -> Ok(...) with grid_x=None, grid_y=None, pixel_size_*=None, max_dimension_*=None (NO error — D-03 lenient).
    - parse_scan_settings(Latin-1 fixture) -> Ok(...) with grid_x=Some(5), grid_y=Some(7) — the high bytes before scanSettings do not abort the parse.
    - A malformed numeric value (e.g. value="abc" on a grid term) yields None for that field via str::parse error->None, NEVER a panic/unwrap.
  </behavior>
  <read_first>
    - src/schema/geometry.rs (the Plan-01 stub — fill the parse_scan_settings body and finalize ImagingRunMetadata fields)
    - 03-RESEARCH.md §Architecture Patterns "Pattern 2: quick-xml <scanSettings> parse honoring ISO-8859-1" (lines 214-254 — the VERIFIED 0.30 event loop, the 0.30 API note about read_event_into / Event::Empty / decode_and_unescape_value) and §Common Pitfalls Pitfalls 1-4
    - 03-PATTERNS.md "src/schema/geometry.rs" section (the header.rs analog: thiserror model, bounded-read stop at </scanSettings>, accession-verbatim matching; ground-truth shapes)
    - src/integrity/header.rs lines 73-99 (IntegrityError thiserror shape incl. Io #[from]) and lines 144-146 (bounded break) and lines 183-193 (checksum_type_of verbatim-accession matching)
    - <interfaces> + <fixture_ground_truth> in this plan's <context>
    - 03-RESEARCH.md §Security Domain (bound to </scanSettings>; quick-xml has no XXE by default; malformed values -> str::parse error->None)
  </read_first>
  <action>
    Finalize `ImagingRunMetadata` in src/schema/geometry.rs (D-04): `#[derive(Debug, Clone, Default,
    PartialEq)]` with all-Option numeric geometry fields — grid_x/grid_y/grid_z: Option<i64>,
    pixel_size_x/pixel_size_y: Option<f64>, max_dimension_x/max_dimension_y: Option<i64>,
    absolute_offset_x/absolute_offset_y: Option<f64> — and scan-geometry child-term presence fields
    scan_pattern/scan_type/line_scan_direction/linescan_sequence: Option<String> (store the matched
    accession CURIE string, e.g. "IMS:1000413"). Keep it DISTINCT from RunProvenance (composed at a
    higher level in Phase 4); do NOT bolt these onto RunProvenance.

    Finalize `GeometryParseError` (thiserror): an `Io(#[from] std::io::Error)` arm and an
    `Xml(quick_xml::Error)` (or `#[from]`) arm for genuine malformed-XML failures ONLY. Per D-03,
    missing/partial geometry is NEVER an error — absent terms become None fields. Contrast with
    header.rs which DOES hard-fail on missing integrity terms; geometry is best-effort.

    Implement `parse_scan_settings(path: &Path) -> Result<ImagingRunMetadata, GeometryParseError>`
    using the quick-xml 0.30 event loop from <interfaces> / RESEARCH Pattern 2:
    - Open `Reader::from_reader(BufReader::new(File::open(path)?))` FROM THE FILE START so the reader
      auto-refines encoding from the `<?xml encoding="ISO-8859-1"?>` Decl (the Latin-1 fix). NEVER
      open mid-file.
    - `reader.trim_text(true)`; loop on `read_event_into(&mut Vec<u8>)`.
    - Track an `in_scan_settings` flag set on `Event::Start` local_name == b"scanSettings", and BREAK
      on `Event::End` local_name == b"scanSettings" (bounded — never read past it, mirrors header.rs's
      `<spectrumList` break and the Security Domain DoS mitigation). Also break on `Event::Eof`.
    - For `cvParam` events — handle BOTH `Event::Start` AND `Event::Empty` (self-closing arrives as
      Empty) — read the `accession` and (optional) `value` attributes via
      `attr.decode_and_unescape_value(&reader)` (encoding-aware). Dispatch on ACCESSION ONLY, never
      on the name string (names vary: "pixel x" vs "pixels x"): IMS:1000042->grid_x, 1000043->grid_y,
      1000052/coordinate-z analog n/a here, 1000044->max_dimension_x, 1000045->max_dimension_y,
      1000046->pixel_size_x, 1000047->pixel_size_y, 1000053->absolute_offset_x, 1000054->
      absolute_offset_y, and the child terms 1000401->linescan_sequence, 1000413->scan_pattern,
      1000480->scan_type, 1000491->line_scan_direction (record the accession string; IGNORE the value
      — child terms are presence flags, value may be "" or absent).
    - Numeric fields: parse the value with `str::parse` and map any error (including empty string "")
      to None — NEVER unwrap (Security Domain: malformed/oversized values must not panic).
    - `buf.clear()` each iteration.
    Verify the exact 0.30 method/signature names at first compile (Assumption A1 in RESEARCH); if
    `read_event_into` does not exist on this exact 0.30 build, use the sibling `read_event` per the
    RESEARCH fallback note and record the adjustment in the SUMMARY.

    Add an inline `#[cfg(test)] mod tests` with a small unit test asserting a malformed numeric value
    maps to None (feed a tiny in-memory/temp scanSettings with value="abc" on IMS:1000042). The
    fixture-driven integration assertions live in tests/geometry_parse.rs (Task 3).
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -5 && cargo test --lib schema::geometry 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` exits 0 (parser compiles against quick-xml 0.30).
    - `cargo test --lib schema::geometry` exits 0 (the malformed-value->None unit test passes).
    - `grep -c 'fn parse_scan_settings' src/schema/geometry.rs` == 1.
    - `grep -q 'scanSettings' src/schema/geometry.rs` AND the End-event break is present (bounded parse stops at `</scanSettings>`).
    - The parser dispatches on accession strings (`IMS:1000042` etc.) — `grep -c 'IMS:1000042' src/schema/geometry.rs` >= 1 — and NEVER matches on the `name` attribute for dispatch.
    - No `.unwrap()` on a `str::parse` result in the file (`grep -n 'parse().*unwrap\|parse::<.*unwrap' src/schema/geometry.rs` returns nothing).
    - `cargo clippy -- -D warnings` exits 0 for the new code.
  </acceptance_criteria>
  <done>parse_scan_settings() compiles, captures geometry keyed on accession with lenient None for absent/malformed terms, honors the ISO-8859-1 prolog, and stops bounded at </scanSettings> — the SPA-03 primary path is built.</done>
</task>

<task type="auto">
  <name>Task 3: Integration tests over real HR2MSI + synthetic fixtures (SPA-03, D-02, D-03)</name>
  <files>tests/geometry_parse.rs</files>
  <read_first>
    - tests/geometry_parse.rs (the file being created)
    - tests/integrity_preflight.rs lines 18-100 (fixture-path consts + ground-truth value consts + `#[test]` assertion fns + the raw-byte Latin-1 synthetic-fixture write idiom to reuse if the Latin-1 fixture is synthesized at test time)
    - 03-VALIDATION.md "Per-Task Verification Map" rows for SPA-03 (the four required test fn names: hr2msi_ground_truth, continuous_full→renamed full_geometry, lenient_missing_grid, latin1_prolog)
    - <fixture_ground_truth> in this plan's <context> (HR2MSI grid 260x134 + child terms; the continuous-fixture correction)
    - src/schema/geometry.rs (the parse_scan_settings signature + ImagingRunMetadata fields from Task 2)
  </read_first>
  <action>
    Create tests/geometry_parse.rs as a library integration test (`use imzml2mzpeak::schema::
    {parse_scan_settings, ImagingRunMetadata};` — adjust to the actual re-export path from Plan 01's
    mod.rs). Define path consts: HR2MSI = "data/HR2MSImouseurinarybladderS096.imzML",
    FULL = "tests/fixtures/imaging/Synthetic_FullGeometry.imzML", MISSING =
    "tests/fixtures/imaging/Synthetic_MissingGrid.imzML". Write four `#[test]` fns:

    - `hr2msi_ground_truth` — parse HR2MSI; assert grid_x == Some(260), grid_y == Some(134);
      assert scan_pattern == Some("IMS:1000413".into()), linescan_sequence == Some("IMS:1000401"),
      scan_type == Some("IMS:1000480"), line_scan_direction == Some("IMS:1000491"); assert
      pixel_size_x == None and max_dimension_x == None (real file omits them — Pitfall 4). This is the
      real-data ground-truth gate.
    - `full_geometry` — parse Synthetic_FullGeometry; assert grid_x == Some(3), grid_y == Some(3),
      pixel_size_x == Some(100.0), max_dimension_x == Some(300), and all four child terms Some(...)
      DESPITE the value-less + plural-name shape (proves accession-only matching).
    - `lenient_missing_grid` — parse Synthetic_MissingGrid; assert the call returns Ok (NO error) and
      grid_x == None, grid_y == None, pixel_size_x == None, max_dimension_x == None (D-03).
    - `latin1_prolog` — either parse the committed Synthetic_Latin1ScanSettings.imzML OR synthesize it
      into a tempdir using the raw-byte Vec<u8> write idiom from integrity_preflight.rs (push 0xDF,
      0xE4 before the scanSettings); assert parse_scan_settings returns Ok with grid_x == Some(5),
      grid_y == Some(7) — proving the Latin-1 high bytes do not abort the parse (D-02 encoding feature).

    Do NOT point any geometry test at Example_Processed.imzML or Example_Continuous.imzML — both lack
    `<scanSettings>` (a test against them would silently parse an empty result and pass — RESEARCH
    Pitfall 1).
  </action>
  <verify>
    <automated>cargo test --test geometry_parse 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test geometry_parse hr2msi_ground_truth` exits 0 (real-data grid 260x134 + child terms verified).
    - `cargo test --test geometry_parse full_geometry` exits 0 (grid 3x3, pixel 100µm, max-dim 300µm, value-less child terms matched).
    - `cargo test --test geometry_parse lenient_missing_grid` exits 0 (Ok + None grid — D-03, no hard-fail).
    - `cargo test --test geometry_parse latin1_prolog` exits 0 (Latin-1 prolog honored).
    - `cargo test --test geometry_parse` runs all four and exits 0.
    - `grep -c 'Example_Processed\|Example_Continuous' tests/geometry_parse.rs` == 0 (no geometry test points at a scanSettings-less fixture).
  </acceptance_criteria>
  <done>All four SPA-03/D-02/D-03 geometry behaviors are proven against the real HR2MSI file and the synthetic fixtures — the geometry parser ships tested, closing the criterion-3 SUMMARY gap on real data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted imzML file → quick-xml geometry parser | XML content (incl. Latin-1 high bytes, malformed cvParam values) crosses into the parser |
| (no network / auth / session surface — local-file CLI library) | — |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering / DoS | XML entity expansion / XXE in `<scanSettings>` | mitigate | quick-xml does NOT resolve external DTDs/entities by default (RESEARCH Security Domain V5); parse is bounded to the scanSettings element — no full-document buffering. |
| T-03-04 | DoS | Unbounded read of a hostile multi-GB imzML header | mitigate | Stop iteration at `</scanSettings>` (precedes `<spectrumList>`); stream via `BufReader`, never `fs::read` the whole (up-to-56MB+) file — mirror Phase-2 bounded discipline (header.rs). |
| T-03-05 | Tampering | Malformed/oversized cvParam values | mitigate | Lenient capture stores values as owned `String`; numeric parse uses `str::parse` mapping error->`None` (D-03), NEVER `unwrap` — enforced by the no-unwrap acceptance criterion + the malformed-value unit test. |
| T-03-06 | Tampering | Encoding confusion (Latin-1 mis-decode) | mitigate | quick-xml `encoding` feature auto-refines from the `<?xml encoding="ISO-8859-1"?>` Decl; reader opened from file start; proven by the latin1_prolog test. |
</threat_model>

<verification>
- `cargo build` green; `cargo test --lib schema::geometry` green; `cargo test --test geometry_parse` green (all four behaviors).
- `cargo test` full suite green (no regression in Phase-2 integrity tests).
- `cargo clippy -- -D warnings` clean for src/schema/geometry.rs and tests/geometry_parse.rs.
</verification>

<success_criteria>
- ImagingRunMetadata (D-04) holds all-Option geometry + child-term presence fields, distinct from RunProvenance.
- parse_scan_settings() honors ISO-8859-1, matches on accession only, is lenient (None for absent/malformed), and is bounded at </scanSettings>.
- Real HR2MSI grid 260x134 + child terms IMS:1000401/413/480/491 verified (criterion 2/3, SPA-03 primary path proven on real data).
- Synthetic fixtures cover full-geometry, missing-grid (D-03), and Latin-1-prolog (D-02) shapes.
</success_criteria>

<output>
Create `.planning/phases/03-imaging-schema-layer/03-02-SUMMARY.md` when done. RECORD the
continuous-fixture correction (no scanSettings) and the final Latin-1-fixture disposition
(committed raw-byte file vs synthesized-at-test-time).
</output>
