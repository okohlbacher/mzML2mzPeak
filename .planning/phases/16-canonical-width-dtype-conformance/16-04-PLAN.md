---
phase: 16-canonical-width-dtype-conformance
plan: 04
type: tdd
wave: 3
depends_on: ["16-01", "16-02", "16-03"]
files_modified:
  - tests/verify_roundtrip.rs
  - tests/write_roundtrip.rs
  - tests/reverse_read_spike.rs
  - tests/reverse_roundtrip.rs
  - tests/acceptance.rs
  - tests/fixtures/reverse/mod.rs
autonomous: true
requirements: [DTY-07]
must_haves:
  truths:
    - "All dtype-preservation tests are updated from the bit-for-bit-at-source-width bar to value-equal-at-canonical-width"
    - "A new regression test proves a mixed-/narrowing-dtype source (f32 m/z and/or f64 intensity) converts and verifies green at canonical width"
    - "The reverse_read_spike NO-widening assertion is inverted to expect canonical width on a widened source"
    - "The PXD001283 acceptance gate passes UNCHANGED (already f64 m/z + f32 intensity → no narrowing, no warning)"
  artifacts:
    - path: "tests/fixtures/reverse/mod.rs"
      provides: "A mixed-/narrowing-dtype imaging fixture (f32 m/z + f64 intensity) for the regression test"
    - path: "tests/verify_roundtrip.rs"
      provides: "Profile F32-m/z pixel B asserted value-equal at canonical f64 width (no false dtype-divergence fail)"
    - path: "tests/reverse_read_spike.rs"
      provides: "The inverted count_and_dtype assertion: widened source reads back at canonical width"
    - path: "tests/acceptance.rs"
      provides: "PXD001283 gate still green at L1 with no narrowing note/warning"
  key_links:
    - from: "tests/fixtures/reverse/mod.rs"
      to: "tests/verify_roundtrip.rs + tests/reverse_roundtrip.rs"
      via: "shared mixed-dtype fixture exercised forward + reverse at canonical width"
      pattern: "F32|F64|canonical"
---

<objective>
Update all dtype-preservation tests to the new value-equal-at-canonical-width bar, ADD a regression test proving a mixed-/narrowing-dtype source converts + verifies green at canonical width, INVERT the `reverse_read_spike` no-widening assertion, and confirm the PXD001283 acceptance gate still passes UNCHANGED.

This is the test-update tail of the phase (DTY-07). It depends on Plan 01 (canonical cast + narrowing record/warn), Plan 02 (L1 redefinition + verify comparators), and Plan 03 (reverse read canonical-width contract). The five named test files plus the reverse fixtures module move off the old "bit-for-bit at source width" expectation.

Purpose: lock the redefined contract behind CI. The decisive new artifact is a mixed-dtype fixture (f32 m/z + f64 intensity — the case PXD001283 does NOT cover) that exercises BOTH the lossless widening (m/z) and the lossy narrowing (intensity) end-to-end.
Output: a green test suite at the canonical-width bar, including a mixed-/narrowing-dtype regression and an unchanged PXD001283 acceptance gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-canonical-width-dtype-conformance/16-CONTEXT.md
@.planning/phases/16-canonical-width-dtype-conformance/16-01-SUMMARY.md
@.planning/phases/16-canonical-width-dtype-conformance/16-02-SUMMARY.md
@.planning/phases/16-canonical-width-dtype-conformance/16-03-SUMMARY.md

<interfaces>
<!-- Extracted from the test suite; executor uses these directly. -->

tests/verify_roundtrip.rs — fixture() has 3 pixels:
  A: Profile, F64 m/z, F32 intensity, (1,1)  — all-canonical
  B: Profile, F32 m/z, F32 intensity, (3,1)  — the F32-m/z path (now WIDENED to f64 in output)
  C: Centroid, F64 m/z, F32 intensity, (2,3)
  Tests: count_equality, coordinates_match, values_l1, raw_facet_bit_for_bit, centroid_source_reference,
         values_l2, ion_image_sanity, sparse_grid_no_panic.
  The doc + `raw_facet_bit_for_bit` (VER-03 caveat) talk about "bit-for-bit at source width" — must move
  to canonical-width value-equal.

tests/reverse_read_spike.rs — count_and_dtype (L184-231): asserts the fixture's stored m/z stays F64 and
  intensity stays F32 "no widening". For the CURRENT fixture (already f64 m/z) this stays true; the
  INVERSION applies to a widened source — assert that a widened (f32-source) pixel reads back at
  canonical f64.

tests/fixtures/reverse/mod.rs — imaging_archive() builds ImagingSpectrum pixels (currently f64 m/z +
  f32 intensity) and writes via the shared write_seam. ADD a mixed-dtype variant here.

tests/acceptance.rs (L80) — verify_streaming(reader2, &out, ConformanceLevel::L1BitForBit) over the full
  PXD001283 dataset; report.passed() must stay true. NO narrowing (already canonical) → no note, no warning.

NOTE: if Plan 02 renamed ConformanceLevel::L1BitForBit, update the identifier in these tests accordingly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a mixed-/narrowing-dtype fixture and the canonical-width regression test</name>
  <read_first>
    - tests/fixtures/reverse/mod.rs (whole file — imaging_archive() + write_seam())
    - tests/verify_roundtrip.rs (fixture() L52+ pixel A/B/C; the test set; the doc header L1-37)
    - tests/reverse_roundtrip.rs (whole file — the value-equal roundtrip harness + UnsupportedDtype mapping L57-58)
    - src/write/spectrum.rs (canonical cast from Plan 01 — what widths the data facet now emits)
    - .planning/phases/16-canonical-width-dtype-conformance/16-01-SUMMARY.md and 16-02-SUMMARY.md
  </read_first>
  <behavior>
    - Test: a mixed-dtype imaging fixture pixel with F32 m/z + F64 intensity converts to a mzPeak whose data facet is f64 m/z + f32 intensity, and verify_streaming/verify_against_source returns report.passed() == true at L1 (value-equal at canonical width).
    - Test: the widened m/z (f32→f64) values read back exactly equal the source f32 widened to f64 (no perturbation).
    - Test: the narrowed intensity (f64→f32) values read back equal the source f64 narrowed to f32 (value-equal at f32), and a genuinely perturbed intensity still fails verify.
    - Test (optional but preferred): the mixed-dtype fixture round-trips mzPeak → imzML → mzPeak at the value-equal bar.
  </behavior>
  <action>
    In `tests/fixtures/reverse/mod.rs`, add a `mixed_dtype_imaging_archive()` (or extend the fixtures module) that builds at least one Profile pixel with `NumArray::F32` m/z AND `NumArray::F64` intensity (the case PXD001283 does not cover — exercising BOTH lossless widening on m/z and lossy narrowing on intensity), written through the existing `write_seam`. Keep distinct, deterministic per-axis values so a mismatch is diagnosable.

    Add the regression test (place it in `tests/verify_roundtrip.rs` or a new mixed-dtype test in the most natural existing harness — do NOT create a brand-new test file unless required) that: converts the mixed-dtype fixture, reads the data facet back, asserts the stored columns are exactly f64 m/z + f32 intensity (DTY-01), asserts the widened m/z equals `source.as_f64()` element-wise (DTY-02), asserts the narrowed intensity equals the source narrowed to f32 (value-equal), and asserts `verify_*` reports `passed()` at L1 (DTY-05/DTY-07). Use the redefined `ConformanceLevel::L1*` identifier (match whatever Plan 02 settled).

    Because this is a TDD plan, write the failing test FIRST (it should fail only if the canonical cast / comparator is wrong), then confirm it passes against the Plan 01–03 implementation. Commit RED then GREEN per the TDD cycle.
  </action>
  <verify>
    <automated>cargo test --test verify_roundtrip && cargo test --test reverse_roundtrip</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/reverse/mod.rs` exposes a mixed-dtype fixture with F32 m/z + F64 intensity.
    - A regression test asserts the converted data facet is f64 m/z + f32 intensity, the widened m/z is value-equal to `source.as_f64()`, the narrowed intensity is value-equal at f32, and `verify_*` passes at L1.
    - `cargo test --test verify_roundtrip` and `cargo test --test reverse_roundtrip` pass.
  </acceptance_criteria>
  <done>A mixed-/narrowing-dtype source converts to canonical f64 m/z + f32 intensity and verifies green at L1, with value-equal widening and narrowing proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate the dtype-preservation tests to canonical width + invert the reverse_read_spike assertion + confirm PXD001283 unchanged</name>
  <read_first>
    - tests/verify_roundtrip.rs (the doc header + values_l1 + raw_facet_bit_for_bit + centroid_source_reference — the "bit-for-bit at source width" language)
    - tests/write_roundtrip.rs (the F64/F32 fixture pixels L55-66 + L300-311; any source-width assertions)
    - tests/reverse_read_spike.rs (count_and_dtype L184-231 — the "no widening" assertions to invert for a widened source)
    - tests/acceptance.rs (L60-110 — the PXD001283 convert + verify_streaming L80 gate)
    - .planning/phases/16-canonical-width-dtype-conformance/16-CONTEXT.md (Tests to update list; the Invariant)
  </read_first>
  <behavior>
    - Test (verify_roundtrip): pixel B (F32 source m/z) verifies green at L1 against the now-f64 output facet — its m/z is asserted at canonical f64 value-equal, not f32-bit-for-bit.
    - Test (reverse_read_spike): a widened (f32-source) pixel reads back at canonical f64 width (the inverted assertion); the existing already-canonical fixture pixel still reads f64/f32 as before.
    - Test (acceptance): the full PXD001283 conversion + verify_streaming at L1 reports passed() == true UNCHANGED, with no narrowing note and no CLI warning emitted (PXD001283 is already canonical).
  </behavior>
  <action>
    Migrate the dtype-preservation assertions across the five named test files from "bit-for-bit at the SOURCE stored width (no widen/narrow)" to "value-equal at canonical width (f64 m/z, f32 intensity)":

    - `tests/verify_roundtrip.rs`: update the module doc and the `raw_facet_bit_for_bit` / `values_l1` / `centroid_source_reference` tests so pixel B (F32 m/z) is asserted to land in an f64 output facet value-equal to its widened source, NOT as a source-width f32 match. Rename/retarget any "bit-for-bit at source width" wording to canonical-width value-equal. Keep the masking-aware and ion-image checks intact.
    - `tests/write_roundtrip.rs`: update any assertion that expected the source dtype to be preserved on the PROFILE data facet so it now expects canonical f64 m/z + f32 intensity. (The centroid peaks-facet assertions are unchanged — that path was always canonical.)
    - `tests/reverse_read_spike.rs`: INVERT the `count_and_dtype` "NO widening" assertion (CONTEXT: this assertion inverts). For a pixel whose source m/z was f32, assert the read-back is canonical f64 (widening is now expected). For the existing already-f64 fixture pixel, the read-back stays f64/f32. Make the inversion explicit and commented so the contract change is auditable.
    - `tests/reverse_roundtrip.rs`: ensure the roundtrip bar is value-equal at canonical width (not dtype-identical); update any source-width expectation.
    - `tests/acceptance.rs`: confirm the PXD001283 gate (convert + `verify_streaming(..., L1*)` at L80) still passes UNCHANGED. PXD001283 is already f64 m/z + f32 intensity, so there is NO narrowing → assert (or document) that no narrowing provenance note and no CLI warning are produced for it. If a `ConformanceLevel` identifier changed in Plan 02, update the call site. Do NOT weaken the gate — it must stay a real assertion on the full dataset.

    Across all files: if Plan 02 renamed `ConformanceLevel::L1BitForBit`, update every reference here.
  </action>
  <verify>
    <automated>cargo test --test verify_roundtrip --test write_roundtrip --test reverse_read_spike --test reverse_roundtrip</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'bit-for-bit at source\|no widen\|source width' tests/` returns no STALE assertion claiming source-width preservation on the profile data facet (comments explaining the history are acceptable if clearly marked as superseded).
    - `tests/reverse_read_spike.rs::count_and_dtype` asserts canonical f64 read-back for a widened (f32-source) pixel (the inverted assertion is present and commented).
    - `cargo test --test verify_roundtrip --test write_roundtrip --test reverse_read_spike --test reverse_roundtrip` passes.
    - `tests/acceptance.rs` still gates the full PXD001283 dataset at L1 with `report.passed()` and is unchanged in strictness; no narrowing note/warning is expected for it.
  </acceptance_criteria>
  <done>All five named test files assert the canonical-width value-equal bar, the reverse_read_spike no-widening assertion is inverted, and the PXD001283 acceptance gate passes unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full Phase 16 canonical-width dtype conformance: forward canonical cast + narrowing record/warn (Plan 01), L1 redefinition + verify comparators (Plan 02), reverse canonical-width contract (Plan 03), and the updated test suite + mixed-dtype regression (this plan).</what-built>
  <how-to-verify>
    1. Run the full suite: `cargo test` — all tests pass.
    2. Run the PXD001283 acceptance gate specifically (it is the real-data invariant): `cargo test --test acceptance -- --include-ignored` (or the project's normal acceptance invocation if PXD001283 is present locally). Confirm `report.passed()` is true and the run prints NO intensity-narrowing warning (PXD001283 is already canonical).
    3. Convert a mixed-dtype source (the new fixture, or any imzML with f32 m/z / f64 intensity if available) and confirm a CLI WARNING naming the narrowed axis + Float64→Float32 appears for the narrowing case, and that NO warning appears for a pure-widening (f32 m/z only) case.
    4. Confirm `cargo clippy` / `cargo build` are clean.
  </how-to-verify>
  <resume-signal>Type "approved" once the full suite is green, the PXD001283 gate is unchanged, and the narrowing warning/provenance behaves per DTY-03/DTY-04; otherwise describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → converter | synthetic in-code fixtures; no untrusted external input introduced by tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | Tampering | acceptance gate strictness | mitigate | the PXD001283 gate stays a real `report.passed()` assertion over the FULL dataset (not weakened); the canonical-width migration must not silently relax it |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | no new dependencies; fixtures use existing NumArray + write_seam |
</threat_model>

<verification>
- `cargo test` (full suite) passes at the canonical-width bar.
- The mixed-dtype regression proves value-equal widening + narrowing end-to-end.
- The PXD001283 acceptance gate passes unchanged with no narrowing note/warning.
</verification>

<success_criteria>
- All dtype-preservation tests are migrated to value-equal-at-canonical-width and a mixed-/narrowing-dtype regression is green (DTY-07).
- The reverse_read_spike no-widening assertion is inverted (DTY-07).
- PXD001283 acceptance passes unchanged (DTY-07 invariant).
</success_criteria>

<output>
Create `.planning/phases/16-canonical-width-dtype-conformance/16-04-SUMMARY.md` when done.
</output>
