---
phase: 17-cv-list-file-level-cv-declaration
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - tests/cv_list.rs
autonomous: true
requirements: [CVL-02]
must_haves:
  truths:
    - "Converting the committed processed fixture produces an archive whose FileIndex.metadata.cv_list is present and declares MS, IMS and UO"
    - "Every CV code the archive actually references (MS, IMS, UO) is declared in cv_list — none is left undeclared"
    - "No declared CV is spurious — the declared set equals the referenced set"
    - "The read-back check runs as an automated cargo test (no --image, no .ibd dependency on the committed processed fixture)"
  artifacts:
    - path: "tests/cv_list.rs"
      provides: "CVL-02 read-back consistency test: convert fixture -> MzPeakReader -> assert cv_list declared == referenced"
      contains: "cv_list"
  key_links:
    - from: "tests/cv_list.rs"
      to: "metadata.cv_list"
      via: "MzPeakReader::new(out).file_index().metadata.get(\"cv_list\")"
      pattern: "file_index\\(\\)\\.metadata"
    - from: "tests/cv_list.rs"
      to: "mzml2mzpeak::write::convert"
      via: "convert(reader, &out, &[]) on the committed processed fixture"
      pattern: "convert\\("
---

<objective>
Prove the declared `cv_list` is consistent with the CV accessions the archive actually uses
(CVL-02): convert the committed processed fixture, open the produced archive with the reference
`MzPeakReader`, and assert that every referenced CV code (MS, IMS, UO) is declared in `cv_list`
and that no declared CV is spurious.

Purpose: CVL-01 emits the block; CVL-02 guarantees it stays truthful — a read-back/validation
check that fails loudly if a future change references a CV without declaring it, or declares a CV
that is never referenced.

Output: `tests/cv_list.rs` — an automated, fixture-driven consistency test using the existing
`convert(reader, &out, &[])` -> `MzPeakReader::new(out)` -> `file_index().metadata` seam.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-cv-list-file-level-cv-declaration/17-CONTEXT.md

<interfaces>
<!-- The established convert+read test seam (tests/image_import.rs:40-61, 210-218):
     - committed fixture: tests/fixtures/imaging/Example_Processed.imzML  (no .ibd / --image needed)
     - ImagingReader::open(path) -> reader; convert(reader, &out, &[]) writes the archive
     - MzPeakReader::new(&out) opens it; reader.file_index().metadata.get("cv_list") -> Option<&Value>
     - metadata.get("imaging") is the sibling block already asserted by image_import.rs -->
use mzml2mzpeak::read::ImagingReader;
use mzml2mzpeak::write::convert;
use mzpeak_prototyping::MzPeakReader;

const PROCESSED_FIXTURE: &str = "tests/fixtures/imaging/Example_Processed.imzML";

<!-- The CV identity facts + referenced-CV set come from src/schema/cv.rs::cv_list() (plan 17-01).
     The "referenced" set for this converter is exactly {MS, IMS, UO}: MS from column-name
     inflection / params, IMS from coordinate columns (IMS:1000050/51), UO from µm units. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CVL-02 read-back consistency test (declared CVs == referenced CVs)</name>
  <files>tests/cv_list.rs</files>
  <read_first>
    - tests/image_import.rs:40-61 and :210-218 (the open_fixture / convert(reader,&out,&[]) / MzPeakReader::new / file_index().metadata.get(..) pattern to mirror, including the no-image path)
    - src/schema/cv.rs (cv_list() and CvEntry from plan 17-01 — the expected declared set MS/IMS/UO)
    - src/write/convert.rs (confirms cv_list is emitted under key "cv_list" alongside "imaging")
    - .planning/phases/17-cv-list-file-level-cv-declaration/17-CONTEXT.md (CVL-02 decision: every referenced CV present, none spurious)
  </read_first>
  <action>
    Create tests/cv_list.rs as an integration test. Open the committed processed fixture with
    ImagingReader::open(PROCESSED_FIXTURE), convert to a unique temp output via
    convert(reader, &out, &[]) (no --image, so no .ibd dependency beyond what the processed fixture
    already satisfies in the existing image_import/write_roundtrip tests), then open the output with
    MzPeakReader::new(&out). Read reader.file_index().metadata.get("cv_list") and assert it is
    present (CVL-01 emission is in place) and is a JSON array. Build the DECLARED set: collect each
    entry's "id" string. Build the REFERENCED set as the converter's known CV codes {"MS", "IMS",
    "UO"} (sourced by intent: MS column-name inflection + params, IMS coordinate columns, UO µm
    units). Assert declared ⊇ referenced (CVL-02: no referenced CV undeclared) AND declared ⊆
    referenced (no spurious declared CV) — i.e. the two sets are EQUAL. Additionally assert each
    declared entry carries non-empty id/full_name/uri (schema-required fields) and that the IMS and
    MS uri values equal src/schema/cv.rs::cv_list()'s strings (single-source-of-truth check). Clean
    up the temp output. Mirror the helper/temp-path conventions in image_import.rs. If a referenced
    CV set ever needs to be derived from emitted column names rather than the fixed trio, leave a
    brief comment noting the fixed-set choice is justified because the converter always references
    all three (per 17-CONTEXT decision).
  </action>
  <verify>
    <automated>cargo test --test cv_list</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test cv_list` passes.
    - The test reads cv_list from a REAL produced archive via MzPeakReader and asserts the declared CV id set equals {MS, IMS, UO} (⊇ and ⊆ both checked).
    - The test fails loudly if cv_list is absent, missing a referenced CV, or carries a spurious CV.
    - No reliance on a network fetch or a missing .ibd; uses only committed fixtures and the existing convert seam.
  </acceptance_criteria>
  <done>tests/cv_list.rs converts the fixture, reads back cv_list, and proves declared CVs == referenced CVs (MS/IMS/UO) — CVL-02 satisfied by an automated read-back check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| input imzML/.ibd → converter | Untrusted input; guarded by the v0.3 integrity preflight before any read. This plan is test-only and runs on committed fixtures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Tampering | cv_list declared-vs-referenced drift | mitigate | This test IS the mitigation — a read-back equality check that fails on any undeclared or spurious CV. |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No new crates; test uses already-pinned deps (mzpeak_prototyping reader, serde_json, mzml2mzpeak). |
</threat_model>

<verification>
- `cargo test --test cv_list` passes.
- `cargo test` shows no regression in the existing convert/read tests (image_import, write_roundtrip).
</verification>

<success_criteria>
- An automated test opens a produced archive and proves cv_list's declared CV set equals the referenced set {MS, IMS, UO} — no undeclared, no spurious CV (CVL-02).
- The test is fixture-driven and reproducible (no network, no missing-.ibd dependency).
</success_criteria>

<output>
Create `.planning/phases/17-cv-list-file-level-cv-declaration/17-02-SUMMARY.md` when done
</output>
