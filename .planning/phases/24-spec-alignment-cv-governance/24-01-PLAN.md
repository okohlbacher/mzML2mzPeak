---
phase: 24-spec-alignment-cv-governance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/schema/cv.rs
  - src/reverse/imzml_writer.rs
  - knowledge/cv/obo/imagingMS.obo
  - docs/cv-requests.md
autonomous: true
requirements: [CVG-01, CVG-02]
user_setup: []

must_haves:
  truths:
    - "The TODO(F9) IMS URI placeholder is gone from src/schema/cv.rs; the IMS entry carries a stable, recorded URI."
    - "The reverse imzML <cvList> reads its id/full_name/URI strings from cv.rs::cv_list() — the single source — instead of hardcoded literals, so forward emit and reverse <cvList> can never drift."
    - "A test proves forward cv_list() and the reverse <cvList> emit the SAME id/full_name/URI for all three CVs (MS/IMS/UO)."
    - "The vendored imagingMS.obo is refreshed from imzML/imzML with its source revision recorded; the new header is no longer the 2018-stale copy (or, if no newer upstream exists, that determination is recorded)."
    - "A CV-requests doc lists every token needing a canonical CURIE (imaging IMS terms, TMTpro 132–135) with the stable token used locally + where the request is filed."
    - "A guard test asserts the converter decodes CV concepts by CURIE (get_param_by_curie), not by column name, across the read/verify/reverse paths."
  artifacts:
    - path: "src/schema/cv.rs"
      provides: "Single-source cv_list() with resolved IMS URI + a function the reverse path consumes"
      contains: "pub fn cv_list"
    - path: "src/reverse/imzml_writer.rs"
      provides: "Reverse <cvList> driven by cv_list() (no hardcoded CV literals)"
      contains: "cv_list"
    - path: "knowledge/cv/obo/imagingMS.obo"
      provides: "Refreshed imaging CV ontology with recorded source rev"
    - path: "docs/cv-requests.md"
      provides: "Tracked list of tokens awaiting canonical CURIEs (imaging, TMTpro)"
      min_lines: 20
  key_links:
    - from: "src/reverse/imzml_writer.rs"
      to: "src/schema/cv.rs::cv_list"
      via: "import + iterate cv_list() to emit <cv> rows"
      pattern: "cv_list"
    - from: "src/schema/cv.rs (test)"
      to: "reverse <cvList> strings"
      via: "no-drift assertion over the same constants"
      pattern: "cv_list"
---

<objective>
Make `src/schema/cv.rs` the ONE authoritative source of CV identity facts and prove
forward↔reverse non-drift by construction. Resolve the v0.6 `TODO(F9)` IMS URI placeholder,
refresh the vendored `imagingMS.obo`, drive the reverse imzML `<cvList>` from `cv_list()`
(today it hardcodes the same three CV literals independently — a latent drift surface), and add
a guard test that the converter decodes CV concepts by CURIE, not by column name. File the
missing-term CV requests rather than inventing canonical CURIEs.

Purpose: CVG-01 + CVG-02 — close the single-source-of-truth + decode-by-CURIE gates so every
later term-emitting phase (25–28) builds on stable, non-drifting CV facts.
Output: hardened `cv.rs`, reverse-cvList wiring, refreshed obo, `docs/cv-requests.md`, two tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-spec-alignment-cv-governance/24-CONTEXT.md
@src/schema/cv.rs

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase — do not go exploring. -->

From src/schema/cv.rs (single source of CV identity facts, FORWARD direction):
```rust
pub struct CvEntry { pub id: String, pub full_name: String, pub uri: String, pub version: Option<String> }
pub fn cv_list() -> Vec<CvEntry>   // returns MS, IMS, UO in that order
```

From src/reverse/imzml_writer.rs (~L318-334) — the REVERSE <cvList> is currently emitted with
HARDCODED string literals that DUPLICATE cv_list()'s id/full_name/URI for MS/IMS/UO. This is the
drift surface CVG-01 names. The block emits, in order:
```text
<cvList count="3">
  <cv id="MS"  fullName="PSI-MS controlled vocabulary" URI="…/psi-ms.obo"/>
  <cv id="IMS" fullName="Mass Spectrometry Imaging controlled vocabulary" URI="…/imagingMS.obo"/>
  <cv id="UO"  fullName="Unit Ontology" URI="…/unit.obo"/>
</cvList>
```
The reverse emitter uses a local `emit_raw(sink, &str)` helper and quick-xml `escape`. The XML
attribute is spelled `URI=` (uppercase), the JSON field is `uri` (lowercase) — keep both spellings.

Decode-by-CURIE is ALREADY the established pattern across the converter (do not regress it):
- src/reverse/source.rs, src/reverse/convert.rs, src/verify/verify.rs all read coordinates via
  `get_param_by_curie(&curie!(IMS:1000050))` etc. The guard test must lock this in.

cv.rs already has a test module asserting the three reverse-equal literals (T-17-02). Those
assertions move from "literal equality to a copy" to "structural equality to the SAME source".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh imagingMS.obo + resolve the TODO(F9) IMS URI in cv.rs</name>
  <files>knowledge/cv/obo/imagingMS.obo, src/schema/cv.rs, docs/cv-requests.md</files>
  <action>
    Refresh the vendored imaging CV and resolve the F9 placeholder per the CONTEXT.md LOCKED rule
    ("canonical OBO PURL where one exists, else a stable token + recorded request" — do NOT invent
    canonical CURIEs, do NOT block on IMS URI minting).

    1. Fetch the current imagingMS.obo from the imzML/imzML repo
       (https://raw.githubusercontent.com/imzML/imzML/master/imagingMS.obo). If the upstream
       `data-version` / `date` header is newer than the vendored copy (vendored is `data-version: 1.1.0`,
       `date: 04:01:2018`), overwrite `knowledge/cv/obo/imagingMS.obo` with the fetched content. If the
       upstream is byte-identical / not newer, KEEP the vendored file unchanged and record that
       determination in docs/cv-requests.md (do not fabricate a refresh).
    2. In src/schema/cv.rs: remove the `TODO(F9)` comment block on the IMS `CvEntry`. The IMS `uri`
       stays the stable imzML/imzML raw OBO URL (no canonical OLS/OBO-Foundry PURL exists yet — that is
       exactly the "stable token + recorded request" branch). Replace the TODO prose with a short
       doc-comment that states: (a) the IMS CV has no OBO-Foundry PURL yet, (b) this stable URL is the
       recorded local token, (c) the canonical-CURIE request is tracked in docs/cv-requests.md. Bump the
       IMS `version` string ONLY if the refreshed obo's `data-version` changed; otherwise leave it.
    3. Create docs/cv-requests.md: a tracked table of every token needing a canonical CURIE/home —
       at minimum (a) the imaging IMS CV home/PURL (stable token = the imzML/imzML raw URL; request =
       file with HUPO-PSI / OBO Foundry), and (b) TMTpro 16/18-plex reporter channels 132–135 (PSI-MS
       CV gap, per REQUIREMENTS CHAN-04 / CONTEXT "TMTpro 16/18-plex gap"). Columns: token, local
       stable value, where filed, status. Record the imagingMS.obo source rev/date used in step 1.
  </action>
  <verify>
    <automated>grep -L 'TODO(F9)' src/schema/cv.rs >/dev/null && grep -v '^#' docs/cv-requests.md | grep -ci 'tmtpro' | grep -qv '^0$' && cargo build 2>&1 | tail -3</automated>
  </verify>
  <done>No `TODO(F9)` remains in cv.rs; docs/cv-requests.md exists listing the IMS home + TMTpro 132–135 tokens with stable values + filing status; imagingMS.obo refresh-or-unchanged determination is recorded; crate builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drive the reverse imzML &lt;cvList&gt; from cv_list() + prove no-drift</name>
  <files>src/reverse/imzml_writer.rs, src/schema/cv.rs</files>
  <behavior>
    - Test (cv.rs or imzml_writer.rs): the reverse &lt;cvList&gt; output contains exactly one
      `<cv id="X" fullName="…" URI="…"/>` per `cv_list()` entry, with id/fullName/URI byte-equal to
      that entry's `id`/`full_name`/`uri` — iterating cv_list(), not comparing against frozen literals.
    - Test: `count="N"` in the emitted `<cvList count="N">` equals `cv_list().len()` (currently 3).
    - Test: changing a CV string would require changing ONLY cv.rs (the reverse path has no independent
      literal) — assert by constructing the expected substring from `cv_list()` and finding it in output.
  </behavior>
  <action>
    Replace the three hardcoded `<cv .../>` `emit_raw` literals (~L318-334 of imzml_writer.rs) with a
    loop over `crate::schema::cv::cv_list()` that emits one `<cv id="{id}" fullName="{full_name}"
    URI="{uri}"/>` row per entry, and sets `<cvList count="{len}">` from `cv_list().len()`. Keep the XML
    attribute spelling `URI=` (uppercase) and run each emitted string field through the existing
    quick-xml `escape` so attribute values stay well-formed. Import the cv module
    (`use crate::schema::cv;`). The emitted bytes for the current MS/IMS/UO set MUST be byte-identical to
    today's output (the existing IXML-03 / WR-1 tests at ~L868/L1096 that assert `<cvList count="3">`,
    `<cv id="IMS"`, `<cv id="UO"` must still pass — do not change their expectations). Then update the
    cv.rs `cv_list_is_ms_ims_uo_with_reverse_uris` test (and add a new no-drift test if clearer) so it
    asserts structural equality to the SAME source rather than "equal to a hand-copied reverse literal":
    the test reuses `cv_list()` to build the expected `<cv>` substrings and asserts they appear in the
    reverse header output, closing the duplication.
  </action>
  <verify>
    <automated>cargo test --lib cv 2>&1 | tail -15 && cargo test --lib imzml_writer 2>&1 | tail -15</automated>
  </verify>
  <done>The reverse `<cvList>` is generated from `cv_list()` (no independent CV literals remain in imzml_writer.rs); existing IXML-03/WR-1 cvList tests pass unchanged; a no-drift test proves forward cv_list() and reverse `<cvList>` share one source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Decode-by-CURIE guard test (CVG-02)</name>
  <files>src/schema/cv.rs</files>
  <behavior>
    - Test asserts the converter's CV decode is keyed by CURIE, not by inflected column name. Concretely:
      a small source-level guard that scans the read/verify/reverse decode sites and asserts they use
      `get_param_by_curie` (the CURIE-keyed accessor), with NO `IMS_1000050`-style column-name string
      matching used as the decode key for coordinate/CV concepts.
    - Test documents (in a comment + an assertion message) that the B1/B2/B3/C1/C3/D11 drift classes are
      UPSTREAM-READER issues (Python/R reference readers), NOT this converter — our converter already
      decodes by CURIE; this guard prevents regressing to name-keyed decode.
  </behavior>
  <action>
    Add a `cvg_02_decode_is_curie_keyed` guard test to src/schema/cv.rs's test module. Implement it as a
    source-scan over the decode modules (read `src/reverse/source.rs`, `src/reverse/convert.rs`,
    `src/verify/verify.rs` at test time via `std::fs::read_to_string`, mirroring cv.rs's existing
    `load_schema()` file-read pattern) and assert each file that reads coordinates does so with
    `get_param_by_curie` and contains NO inflected-name decode key of the form `"IMS_1000050"` /
    `"IMS_1000051"` used as a lookup string. Use `grep -v '^#'`-equivalent filtering in-test (skip `//`
    comment lines) so doc-comment mentions of inflected names don't self-invalidate the gate. Add a
    top-of-test comment citing docs/mzpeak-spec-conformance-issues.md: B1/B2/B3/C1/C3/D11 are the
    upstream reference readers' name-keyed decode bugs; this converter decodes by CURIE and this guard
    keeps it that way (CVG-02).
  </action>
  <verify>
    <automated>cargo test --lib cvg_02_decode_is_curie_keyed 2>&1 | tail -12</automated>
  </verify>
  <done>`cvg_02_decode_is_curie_keyed` passes; it proves the read/verify/reverse decode sites key on `get_param_by_curie` and use no column-name decode key, with the upstream-vs-ours distinction recorded.</done>
</task>

</tasks>

<verification>
- `cargo build` succeeds (pinned stack unchanged — no new deps).
- `cargo test --lib` green; specifically the cv-drift + cvg-02 + IXML-03/WR-1 cvList tests.
- No `TODO(F9)` string anywhere in src/.
- The reverse imzML output for an existing fixture is byte-unchanged for the `<cvList>` block (existing reverse round-trip integration tests still pass).
</verification>

<success_criteria>
- CVG-01: canonical IMS accession facts declared ONCE in cv.rs; TODO(F9) gone; forward emit and reverse `<cvList>` read the SAME constants, proven not to drift; imagingMS.obo refreshed (or unchanged-with-reason) before any new accession is referenced; missing terms (imaging home, TMTpro 132–135) use stable tokens + a filed request in docs/cv-requests.md.
- CVG-02: CV decode keyed by CURIE (not column name), with a guard test; the documented B1/B2/B3/C1/C3/D11 classes are recorded as upstream-reader issues; the TMTpro 16/18-plex gap is documented with a filed term request.
- XRT: forward↔reverse `<cvList>` symmetry holds by construction; existing masking-aware L1 + validator behavior untouched (no facet emitted).
</success_criteria>

<output>
Create `.planning/phases/24-spec-alignment-cv-governance/24-01-SUMMARY.md` when done.
</output>
