---
phase: 24-spec-alignment-cv-governance
plan: 03
type: execute
wave: 2
depends_on: [24-02]
files_modified:
  - docs/mzpeak-spec-proposal-queue.md
autonomous: true
requirements: [SPEC-02]
user_setup: []

must_haves:
  truths:
    - "A queued batch-proposal stub lists every write-up that will be submitted to HUPO-PSI/mzPeak-specification at the END of v0.7 (NOT now): cv_list adoption, SDRF/sample/channel/reporter-quant extensions, declared-geometry/scan_settings, L2 transform record."
    - "Each queued item names the spec mechanism it extends (from the design contract), its source phase, and its readiness status — so the end-of-v0.7 batch is assemble-by-checklist."
    - "The committee's SDRF open questions (§5.7) are tracked as a checklist item with a pointer to docs/sdrf-open-questions.md, flagged for committee ratification."
    - "The stub explicitly records that submission is HELD by the owner (no PR/proposal filed in this phase) — prepare + queue only."
  artifacts:
    - path: "docs/mzpeak-spec-proposal-queue.md"
      provides: "The tracked end-of-v0.7 batch-proposal queue (drafted, not submitted)"
      min_lines: 40
  key_links:
    - from: "docs/mzpeak-spec-proposal-queue.md"
      to: "docs/mzpeak-extension-contract.md"
      via: "each queued item cites its contract facet + mechanism"
      pattern: "extension-contract"
    - from: "docs/mzpeak-spec-proposal-queue.md"
      to: "docs/sdrf-open-questions.md"
      via: "committee §5.7 open-questions tracker pointer"
      pattern: "5.7|sdrf-open-questions"
---

<objective>
Stand up the END-of-v0.7 batch-proposal queue: a single tracked checklist of every spec extension
write-up that will be submitted to `HUPO-PSI/mzPeak-specification` as ONE batch at milestone end —
DRAFTED and QUEUED only, never submitted here (owner holds all upstream PR/proposal submission). Track
the committee's SDRF §5.7 open questions for ratification.

Purpose: SPEC-02 — prepare + queue the SDRF/sample/channel (and the cv_list-adoption, geometry, L2)
write-ups for a mergeable-by-design batch; submission is HELD.
Output: docs/mzpeak-spec-proposal-queue.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-spec-alignment-cv-governance/24-CONTEXT.md
@.planning/REQUIREMENTS.md
@docs/mzpeak-extension-contract.md
@docs/sdrf-open-questions.md

<constraints>
- CONTEXT.md LOCKED: "SPEC-02 = prepare + QUEUE, do not submit." One batch proposal at the END of v0.7.
  PR submission is HELD by the owner. Track the committee open questions (SDRF §5.7).
- MEMORY (git push policy): this is docs-only; no remote pushes, no PRs filed. Queue is a local doc.
- The proposal queue cites the binding contract (Plan 02) for each item's mechanism — it does not
  re-derive mechanisms.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the end-of-v0.7 batch-proposal queue stub</name>
  <files>docs/mzpeak-spec-proposal-queue.md</files>
  <action>
    Create docs/mzpeak-spec-proposal-queue.md — the tracked, NOT-YET-SUBMITTED batch-proposal queue for
    HUPO-PSI/mzPeak-specification, per the CONTEXT.md LOCKED "prepare + queue, do not submit" rule.
    Structure:

    1. Header / status banner: "DRAFTED + QUEUED — submission HELD by owner; ONE batch at the END of
       v0.7 (mergeable-by-design)." State plainly that NO PR/proposal is filed in this phase.
    2. A queue table (item, spec mechanism extended, source phase + requirement, contract reference,
       readiness status one of {drafted / pending-phase / blocked}). Rows — cover all of:
       - cv_list adoption proposal (SPEC-03 / Phase 24) — proposes the spec adopt a CV-declaration
         File-Level Metadata block; status: drafted (note already reconciled in the conformance doc).
       - SDRF verbatim-embed + sample_list characteristics + assay_ref (SDRF-01..05 / Phase 27).
       - channel_list + ms_run.channel_set/plex_id + reporter-quant aux array (CHAN-01/02/03 / Phase 27).
       - declared-geometry / scan_settings_list fill (GEOF-01 / Phase 25) — note the spec's
         scan_settings_list is a TODO slot this would help define.
       - L2 transform-record convention (L2-01 / Phase 28).
       - (Note, do NOT queue:) imaging-structure facets (PIX/ROI/CONT/IMG) — deferred beyond v1.0,
         explicitly OUT of this batch. Record this exclusion so the batch scope is unambiguous.
    3. A "Committee open questions (SDRF §5.7)" checklist section: pull the open questions from
       docs/sdrf-open-questions.md (the verbatim-embed-vs-sample-list decision, the repo-SDRF-wins
       precedence rule, the (sample × data-file) scope decomposition, the ROI/sample intersection
       flagged deferred) — each as a checkbox flagged "needs committee ratification," pointing at
       docs/sdrf-open-questions.md as the canonical detail.
    4. A "Submission checklist (run at v0.7 end)" section: the gating steps to assemble + submit the
       batch (all phase write-ups drafted, three-places rule satisfied per facet, contract finalized,
       owner authorization to push — referencing the push policy). Leave it unchecked.

    For each item, cite docs/mzpeak-extension-contract.md for the mechanism (do not re-derive). This is a
    living checklist consumed at milestone end — directive prose + tables only.
  </action>
  <verify>
    <automated>test -f docs/mzpeak-spec-proposal-queue.md && grep -qi 'HELD' docs/mzpeak-spec-proposal-queue.md && grep -qi 'extension-contract' docs/mzpeak-spec-proposal-queue.md && grep -qi 'sdrf-open-questions\|5.7' docs/mzpeak-spec-proposal-queue.md && for t in cv_list channel_list scan_settings L2 SDRF; do grep -qi "$t" docs/mzpeak-spec-proposal-queue.md || { echo "MISSING: $t"; exit 1; }; done && echo OK</automated>
  </verify>
  <done>docs/mzpeak-spec-proposal-queue.md exists with a status banner stating submission is HELD; a queue table covering cv_list, SDRF, channel_list/reporter-quant, geometry, L2 (and explicitly excluding the deferred imaging cluster), each citing the contract; an SDRF §5.7 committee-questions checklist pointing at docs/sdrf-open-questions.md; and an unchecked end-of-v0.7 submission checklist.</done>
</task>

</tasks>

<verification>
- docs/mzpeak-spec-proposal-queue.md exists and passes the content greps above.
- No PR/proposal filed; no remote push; no source/schema change (docs-only).
- The queue references the contract (Plan 02) and the SDRF open-questions doc — wave-2 dependency on Plan 02 is satisfied (contract exists before the queue cites it).
</verification>

<success_criteria>
- SPEC-02: the SDRF/sample/channel (plus cv_list/geometry/L2) extension write-ups are prepared and QUEUED for a single BATCH proposal/PR to HUPO-PSI/mzPeak-specification at the END of v0.7 — not submitted incrementally; submission is recorded as HELD by the owner; the committee's SDRF §5.7 open questions are tracked for ratification.
</success_criteria>

<output>
Create `.planning/phases/24-spec-alignment-cv-governance/24-03-SUMMARY.md` when done.
</output>
