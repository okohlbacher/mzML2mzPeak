---
phase: 00-environment-foundations
plan: 02
type: execute
wave: 2
depends_on: ["00-01"]
files_modified:
  - data/HR2MSImouseurinarybladderS096.ibd
  - src/bin/verify_ibd.rs
autonomous: false
requirements: [ENV-02]
user_setup:
  - service: PRIDE Archive (EBI)
    why: "One-time acquisition of the PXD001283 .ibd binary sidecar (runtime auto-fetch is OUT OF SCOPE per REQUIREMENTS)"
    dashboard_config:
      - task: "Download HR2MSImouseurinarybladderS096.ibd into data/ (large binary; confirm exact filename casing on the server first)"
        location: "https://ftp.pride.ebi.ac.uk/pride/data/archive/2014/11/PXD001283/"
must_haves:
  truths:
    - "The PXD001283 .ibd binary is present in data/ next to the existing .imzML"
    - "The .ibd embedded UUID (first 16 bytes) matches C7822330-F1A8-4D11-AD30-504B30B33722 from the .imzML"
    - "The .ibd SHA-1 matches F8C24417B294BFA168D75A470BBB361009BC2671 (IMS:1000091 in the .imzML)"
  artifacts:
    - path: "data/HR2MSImouseurinarybladderS096.ibd"
      provides: "Binary array sidecar required by every downstream read path"
    - path: "src/bin/verify_ibd.rs"
      provides: "Tiny verifier that prints UUID-match + SHA-1-match against the .imzML-declared values"
      min_lines: 20
  key_links:
    - from: "data/HR2MSImouseurinarybladderS096.ibd"
      to: "data/HR2MSImouseurinarybladderS096.imzML (IMS:1000080 UUID)"
      via: "first 16 bytes of .ibd decoded as a little-endian GUID == imzML UUID"
      pattern: "C7822330-F1A8-4D11-AD30-504B30B33722"
    - from: "data/HR2MSImouseurinarybladderS096.ibd"
      to: "data/HR2MSImouseurinarybladderS096.imzML (IMS:1000091 ibd SHA-1)"
      via: "SHA-1 digest of the whole .ibd == declared digest"
      pattern: "F8C24417B294BFA168D75A470BBB361009BC2671"
---

<objective>
Acquire the PXD001283 `.ibd` binary sidecar into `data/` and prove its integrity against the
already-present `.imzML`: the embedded UUID must match `C7822330-F1A8-4D11-AD30-504B30B33722`
(imzML `IMS:1000080`) and the file SHA-1 must match `F8C24417B294BFA168D75A470BBB361009BC2671`
(imzML `IMS:1000091`). Without this verified binary, no later phase can read a single spectrum.

Purpose: ENV-02. Establishes a trustworthy on-disk test dataset. The integrity gate here also lays
the groundwork for the converter-owned hard-fail preflight in Phase 2 (Pitfall #2: mzdata only warns
on UUID mismatch and never checks the SHA-1).
Output: `data/HR2MSImouseurinarybladderS096.ibd` (git-ignored) and `src/bin/verify_ibd.rs` (committed),
with `cargo run --bin verify_ibd` printing two PASS lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/SUMMARY.md
@.planning/research/STACK.md

<facts>
<!-- Resolved during planning. -->
- The .imzML is already present and tracked: data/HR2MSImouseurinarybladderS096.imzML
- Verified terms grepped from that .imzML header:
    IMS:1000080 universally unique identifier = "{C7822330-F1A8-4D11-AD30-504B30B33722}"
    IMS:1000091 ibd SHA-1                      = "F8C24417B294BFA168D75A470BBB361009BC2671"
    IMS:1000031 processed                       (storage mode = processed; no IMS:1000030 present)
  NOTE: only a SHA-1 (IMS:1000091) is declared — there is NO MD5 (IMS:1000090) in this file. Verify SHA-1.
- PXD001283 PRIDE archive FTP base (publicationDate 2014-11, confirmed via PRIDE API):
    https://ftp.pride.ebi.ac.uk/pride/data/archive/2014/11/PXD001283/
  Planning probe: a HEAD on .../HR2MSImouseurinarybladderS096.ibd at 2014/11 returned 403 (Apache
  forbids HEAD/listing but typically serves GET); the 2014/12 path returned 404 (wrong month). The
  executor must confirm the EXACT filename + casing via the PRIDE file API before downloading:
    https://www.ebi.ac.uk/pride/ws/archive/v2/files/byProject?accession=PXD001283
- The .ibd is large (the .imzML alone is ~56 MB; the .ibd holds all binary arrays and is larger).
  This is a one-time, possibly interactive/long download -> this plan is autonomous: false.
- UUID byte order: imzML/imzMLConverter writes the UUID into the first 16 bytes of the .ibd. The .NET
  Guid byte layout is mixed-endian (first three groups little-endian, last two big-endian). The
  verifier must reconstruct the canonical GUID string from those 16 bytes using the .NET Guid layout
  and compare case-insensitively to C7822330-F1A8-4D11-AD30-504B30B33722.
- Tools available locally: /usr/bin/shasum (SHA-1), /sbin/sha1sum.
</facts>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <what-built>
    Nothing automated yet — this step acquires a large external binary that requires a network fetch
    and confirmation of the exact server filename. Runtime auto-fetch is explicitly OUT OF SCOPE per
    REQUIREMENTS, so this is a deliberate one-time manual/assisted acquisition.
  </what-built>
  <how-to-verify>
    1. Resolve the exact .ibd filename + casing from the PRIDE file API:
       `curl -s "https://www.ebi.ac.uk/pride/ws/archive/v2/files/byProject?accession=PXD001283" | grep -i '\.ibd'`
       (expected: HR2MSImouseurinarybladderS096.ibd, matching the local .imzML stem).
    2. Download it into data/ (resumable):
       `curl -L --fail -C - -o data/HR2MSImouseurinarybladderS096.ibd \
         "https://ftp.pride.ebi.ac.uk/pride/data/archive/2014/11/PXD001283/HR2MSImouseurinarybladderS096.ibd"`
       If the PRIDE API returns a different public URL (FTP/Aspera/alternate mirror), use that exact
       URL instead — the API is authoritative over the guessed path.
    3. Confirm the file landed and is non-trivial in size:
       `ls -l data/HR2MSImouseurinarybladderS096.ibd` (must be many MB, not an HTML error page).
    4. Quick sanity check it is not an HTML 403/404 body saved as .ibd:
       `file data/HR2MSImouseurinarybladderS096.ibd` should report data/binary, not HTML/ASCII text.
  </how-to-verify>
  <resume-signal>Type "downloaded" once data/HR2MSImouseurinarybladderS096.ibd exists and is a multi-MB binary, or describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Build a tiny .ibd integrity verifier (UUID match + SHA-1 match)</name>
  <read_first>
    - data/HR2MSImouseurinarybladderS096.imzML (confirm IMS:1000080 UUID and IMS:1000091 SHA-1 values)
    - .planning/research/SUMMARY.md ("Critical Pitfalls" #2: UUID warn-only + unimplemented checksum in mzdata)
    - Cargo.toml (created in 00-01; the verifier is a src/bin in the same crate)
  </read_first>
  <files>src/bin/verify_ibd.rs</files>
  <action>
    Create `src/bin/verify_ibd.rs`, a standalone binary in the existing crate (so it builds under the
    same pinned toolchain/deps). It must NOT depend on mzdata's reader (that path is Phase 1/2) —
    keep it pure std plus an SHA-1 routine. Behavior:
    (a) Read the first 16 bytes of `data/HR2MSImouseurinarybladderS096.ibd`, reconstruct the canonical
        GUID string using the .NET Guid mixed-endian layout (Data1 u32 LE, Data2 u16 LE, Data3 u16 LE,
        Data4 8 bytes big-endian/as-is), uppercase it, and compare to the expected
        C7822330-F1A8-4D11-AD30-504B30B33722. Print `UUID: PASS <uuid>` or `UUID: FAIL got=<x> want=<y>`.
    (b) Compute the SHA-1 of the entire .ibd (stream it in chunks; do not load 56MB+ into one Vec at
        peak — read in a buffered loop) and compare uppercase-hex to
        F8C24417B294BFA168D75A470BBB361009BC2671. Print `SHA1: PASS <digest>` or `SHA1: FAIL ...`.
    For SHA-1, either add the `sha1` crate at a pinned version to Cargo.toml OR shell out to
    `/usr/bin/shasum -a 1` and parse — prefer adding `sha1 = "=0.10.6"` so verification is
    self-contained and reproducible (record the choice in the SUMMARY). The expected UUID and SHA-1
    may be hardcoded constants in the verifier (sourced from the .imzML), but add a comment citing
    IMS:1000080 / IMS:1000091 as their origin. Exit non-zero if EITHER check fails (this verifier is
    the integrity gate; a partial pass is a failure).
  </action>
  <acceptance_criteria>
    - `src/bin/verify_ibd.rs` exists, references both IMS:1000080 and IMS:1000091 in comments (grep both accessions).
    - `cargo build --bin verify_ibd` exits 0.
    - The SHA-1 reference value computed independently matches: `shasum -a 1 data/HR2MSImouseurinarybladderS096.ibd` outputs a digest equal (case-insensitive) to F8C24417B294BFA168D75A470BBB361009BC2671.
    - `cargo run --bin verify_ibd` exits 0 and stdout contains both `UUID: PASS` and `SHA1: PASS`.
  </acceptance_criteria>
  <verify>
    <automated>cargo build --bin verify_ibd -q && cargo run --bin verify_ibd 2>/dev/null | grep -q 'UUID: PASS' && cargo run --bin verify_ibd 2>/dev/null | grep -q 'SHA1: PASS' && test "$(shasum -a 1 data/HR2MSImouseurinarybladderS096.ibd | awk '{print toupper($1)}')" = "F8C24417B294BFA168D75A470BBB361009BC2671" && echo IBD_VERIFIED</automated>
  </verify>
  <done>cargo run --bin verify_ibd prints UUID: PASS and SHA1: PASS and exits 0; independent shasum confirms the SHA-1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PRIDE FTP/HTTPS -> local data/ | A large untrusted binary is downloaded onto the dev machine. |
| .ibd binary -> verifier | The downloaded bytes are read and digested; a corrupted/wrong file must be rejected, not silently accepted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-04 | Tampering | downloaded .ibd (wrong/corrupted/MITM) | mitigate | Verify SHA-1 against the imzML-declared IMS:1000091 digest AND the embedded UUID against IMS:1000080; non-zero exit on any mismatch. |
| T-00-05 | Spoofing | download served an HTML 403/404 saved as .ibd | mitigate | `file` type check in the download checkpoint + SHA-1 verification (an HTML body will never match the declared digest). |
| T-00-06 | Information Disclosure | committing a multi-hundred-MB binary to git | accept/avoid | `.gitignore` (set in 00-01) excludes `data/*.ibd`; the binary stays local, only the verifier is committed. |
| T-00-SC | Tampering | `sha1` crate install (if added) | mitigate | Pin `sha1 = "=0.10.6"` (RustCrypto, widely used, legitimate) OR shell out to system `shasum`; either way the result is cross-checked against the system `shasum` in the verify gate. |
</threat_model>

<verification>
- `data/HR2MSImouseurinarybladderS096.ibd` exists and is a multi-MB binary (not an HTML error body).
- Independent `shasum -a 1` digest == F8C24417B294BFA168D75A470BBB361009BC2671.
- `cargo run --bin verify_ibd` prints `UUID: PASS` + `SHA1: PASS` and exits 0.
- A deliberately altered byte (manual spot-test, optional) would flip the verifier to a non-zero exit.
</verification>

<success_criteria>
ENV-02 satisfied: the PXD001283 .ibd is present in data/, its embedded UUID matches the imzML
IMS:1000080 value, and its SHA-1 matches the imzML IMS:1000091 value — proven by a committed verifier
that exits non-zero on any mismatch.
</success_criteria>

<output>
Create `.planning/phases/00-environment-foundations/00-02-SUMMARY.md` when done.
Record: the resolved download URL actually used, the .ibd file size in bytes, the computed SHA-1,
and whether the verifier used the `sha1` crate or shelled out to `shasum`.
</output>
