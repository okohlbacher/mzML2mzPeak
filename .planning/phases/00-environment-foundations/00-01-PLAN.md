---
phase: 00-environment-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rust-toolchain.toml
  - Cargo.toml
  - Cargo.lock
  - .gitignore
  - src/main.rs
autonomous: true
requirements: [ENV-01]
must_haves:
  truths:
    - "cargo build succeeds on a pinned Rust 1.85+/edition-2024 toolchain"
    - "cargo tree shows exactly one mzdata copy and exactly one arrow copy"
    - "The unified mzdata feature set includes the non-default imzml feature (proven via cargo tree -e features)"
    - "mzpeak_prototyping is pinned to a specific git commit rev (not a branch), and the resolved rev is recorded in Cargo.lock"
    - "src/main.rs compiles importing the exact re-exported symbol mzpeak_prototyping::MzPeakWriter with default-features = false"
  artifacts:
    - path: "rust-toolchain.toml"
      provides: "Pinned toolchain channel >=1.85 for edition 2024"
      contains: "channel"
    - path: "Cargo.toml"
      provides: "Core compatibility pins exact + app deps pinned with = + imzml feature + git-pinned writer"
      contains: "mzpeak_prototyping"
    - path: "Cargo.lock"
      provides: "Reproducible lock recording the resolved mzpeak_prototyping git rev + transitive versions"
      contains: "mzpeak_prototyping"
    - path: "src/main.rs"
      provides: "Compilable binary entry point importing mzpeak_prototyping::MzPeakWriter and referencing mzdata::io::imzml"
      min_lines: 3
  key_links:
    - from: "Cargo.toml"
      to: "mzdata imzml feature"
      via: "features = [\"imzml\"] on the mzdata dependency"
      pattern: "mzdata.*features.*imzml"
    - from: "Cargo.toml"
      to: "HUPO-PSI/mzPeak commit d1aaaf84595202e2e7f622c576c1d6ba9154e379"
      via: "git + rev pin"
      pattern: "rev.*d1aaaf8"
    - from: "src/main.rs"
      to: "mzpeak_prototyping::MzPeakWriter"
      via: "use of the exact re-exported writer symbol"
      pattern: "mzpeak_prototyping::MzPeakWriter"
---

<objective>
Stand up a reproducible Rust build skeleton with the core compatibility crates pinned EXACTLY
(arrow/parquet 57.0.0, zip 4.1.0, mzdata 0.63.3, mzpeaks 1.0.9), the application-boundary deps pinned
with `=`, the non-default `mzdata` `imzml` feature enabled, and the git-only writer pinned to a concrete
commit. Prove the dependency graph reconciles to a single copy of `mzdata` and a single copy of `arrow`
(no duplicate-major fracture), that the resolved imzml feature is actually unified ON, and that the exact
re-exported writer symbol `mzpeak_prototyping::MzPeakWriter` compiles with `default-features = false`.
Commit `Cargo.lock` so the resolved git rev and transitive set are reproducible.

Purpose: This is the hard prerequisite for every later phase — nothing reads, writes, or spikes
until this compiles. It also pre-empts Pitfall #1 (mzdata version/feature mismatch) by forcing the
workspace to unify on one `mzdata` that carries `imzml`, even though upstream's own pin omits it.
Output: `rust-toolchain.toml`, `Cargo.toml`, `Cargo.lock`, `.gitignore`, `src/main.rs`, a green
`cargo build`, a committed `Cargo.lock` recording the resolved git rev, and `cargo tree` proofs of
single-copy resolution and active imzml feature.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md

<facts>
<!-- Concrete values resolved during planning. Use these directly; do not re-derive. -->
- Local toolchain: NO rustc/cargo/rustup currently installed (must be installed first).
- mzpeak_prototyping local clone: /tmp/mzpeak_prototyping
  - HEAD commit (use as the git rev pin): d1aaaf84595202e2e7f622c576c1d6ba9154e379 (confirmed to exist)
  - Clone's `origin` remote is https://github.com/mobiusklein/mzpeak_prototyping.git
    which redirects to https://github.com/HUPO-PSI/mzPeak. Either URL resolves the same
    commit; prefer the canonical https://github.com/HUPO-PSI/mzPeak in Cargo.toml.
- CONFIRMED re-export (grep of /tmp/mzpeak_prototyping/src/lib.rs line 17): `pub use writer::MzPeakWriter;`
  The skeleton MUST import this EXACT symbol path: `mzpeak_prototyping::MzPeakWriter` (not a vague "writer type").
- Upstream mzpeak_prototyping/Cargo.toml CORE pins (match exactly with `=`):
    arrow = "57.0.0"
    parquet = { version = "57.0.0", features = ["encryption"] }
    zip = "4.1.0"
    mzdata = { version = "0.63.3", features = ["serde","bruker_tdf","nalgebra","zstd","numpress"] }   # NOTE: NO imzml here
    mzpeaks = "1.0.9"
    edition = "2024"
    mzpeak_prototyping default features = ["async"]  (pulls tokio/async_zip/object_store/opendal/url/futures)
- Our crate MUST request mzdata with features = ["imzml"]. Cargo unions features across the graph,
  so the single resolved mzdata copy will carry imzml + upstream's set. Pin our mzdata to "=0.63.3".
- Per STACK.md "Stack Patterns by Variant": depend on mzpeak_prototyping with
  default-features = false for a local-file-only v1 to drop the async/object_store/opendal/tokio stack.
- SCOPE OF PIN CLAIM (corrected per review): we pin the CORE compatibility crates exact and pin the
  app-boundary helpers with `=`. We do NOT claim to mirror every upstream transitive dependency.
  Cargo.lock (committed) is the source of truth for the full reproducible transitive set.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install + pin the Rust toolchain (edition 2024 / >=1.85)</name>
  <read_first>
    - .planning/research/STACK.md (rows: Rust toolchain 1.85+, rust-toolchain.toml)
    - /tmp/mzpeak_prototyping/Cargo.toml (confirm `edition = "2024"`)
  </read_first>
  <files>rust-toolchain.toml, .gitignore</files>
  <action>
    If `cargo --version` is absent, install rustup non-interactively via the official rustup script
    (https://sh.rustup.rs) with the stable channel, then ensure `~/.cargo/bin` is on PATH for this
    session. Create `rust-toolchain.toml` with a `[toolchain]` table pinning `channel = "1.85.0"`
    (the minimum that supports edition 2024; a newer stable is acceptable only if >=1.85 — but pin a
    concrete version string, not "stable", for reproducibility) and `components = ["rustfmt", "clippy"]`.
    Create `.gitignore` ignoring `/target` and the large binary sidecar (the `.ibd` is fetched in plan
    00-02 and must never be committed). Use precise ignore patterns so the rules cannot collide with a
    tracked small header: ignore `/target` and `data/*.ibd`. Do NOT add a broad `*.imzML` ignore rule
    (the header is already tracked and is small); if you want a comment, do not let it create an actual
    ignore line for `.imzML`.
  </action>
  <acceptance_criteria>
    - `rustc --version` prints a version >= 1.85.0.
    - `rust-toolchain.toml` contains `channel = "1.85.0"` under `[toolchain]` (grep: `grep -q 'channel *= *"1.85' rust-toolchain.toml`).
    - `.gitignore` contains both `/target` and a `data/*.ibd` rule (grep: `grep -q 'data/\*.ibd' .gitignore`).
    - No ACTIVE `.imzML` ignore pattern exists (precise check, ignores comments and matches only real ignore globs):
      `! rg '^\s*\*?\.?imzML$|^\s*data/.*\.imzML' .gitignore`
  </acceptance_criteria>
  <verify>
    <automated>rustc --version | grep -Eq '1\.(8[5-9]|9[0-9]|[0-9]{3})' && grep -q 'channel *= *"1.85' rust-toolchain.toml && grep -q 'data/\*.ibd' .gitignore && ! rg '^\s*\*?\.?imzML$|^\s*data/.*\.imzML' .gitignore</automated>
  </verify>
  <done>A pinned >=1.85 toolchain is active and rust-toolchain.toml + .gitignore exist with the correct rules (no active .imzML ignore).</done>
</task>

<task type="auto">
  <name>Task 2: Author Cargo.toml (core pins exact + app deps =) + git-pinned writer + minimal main.rs, generate Cargo.lock</name>
  <read_first>
    - .planning/research/STACK.md (sections "Installation / Cargo.toml", "Stack Patterns by Variant", "Version Compatibility")
    - .planning/research/SUMMARY.md ("Recommended Stack", "Critical Pitfalls" #1)
    - /tmp/mzpeak_prototyping/Cargo.toml (the core dependency pins to mirror)
    - /tmp/mzpeak_prototyping/src/lib.rs (CONFIRMED: line 17 `pub use writer::MzPeakWriter;` — import this exact path)
  </read_first>
  <files>Cargo.toml, src/main.rs, Cargo.lock</files>
  <action>
    Create a single binary crate `Cargo.toml` (`[package] name = "imzml2mzpeak"`, `edition = "2024"`,
    `rust-version = "1.85"`). Pin the CORE compatibility crates EXACTLY (these are the ones that fracture
    the arrow/mzdata type graph if they drift): `arrow = "=57.0.0"`,
    `parquet = { version = "=57.0.0", features = ["encryption"] }`, `zip = "=4.1.0"`,
    `mzdata = { version = "=0.63.3", features = ["imzml","serde","bruker_tdf","nalgebra","zstd","numpress"] }`
    (the `imzml` feature is the NON-default addition that makes `mzdata::io::imzml` exist; the other
    features mirror upstream so the unioned copy is identical to upstream's plus imzml),
    `mzpeaks = "=1.0.9"`, and
    `mzpeak_prototyping = { git = "https://github.com/HUPO-PSI/mzPeak", rev = "d1aaaf84595202e2e7f622c576c1d6ba9154e379", default-features = false }`
    (default-features=false drops the async/object_store/opendal/tokio stack per STACK.md local-file-only
    variant). Pin the app-boundary helpers with `=` at the upstream-aligned versions: `anyhow = "=1.0.102"`,
    `thiserror = "=2.0.18"`, `clap = { version = "=4.5.38", features = ["derive"] }`, `log = "=0.4.27"`,
    `env_logger = "=0.11.8"`. Do NOT attempt to mirror every upstream transitive (serde_json/serde_with/
    indicatif etc.) here — add those only when a later phase actually uses them; Cargo.lock is the
    reproducibility source of truth for transitives.
    Write `src/main.rs` as a minimal compile-proof entry point that:
    (a) imports the EXACT confirmed re-exported writer symbol with `use mzpeak_prototyping::MzPeakWriter;`
        (behind `#[allow(unused_imports)]` is fine — the point is that the symbol resolves at compile time),
    (b) references `mzdata::io::imzml` (e.g. a `use mzdata::io::imzml::ImzMLReaderType;` behind
        `#[allow(unused_imports)]`) so the imzml feature module is exercised at compile time, and
    (c) a `main()` that just logs a startup line. Do NOT inline writer logic — this is a skeleton.
    Run `cargo generate-lockfile` (or let `cargo build` produce it) so `Cargo.lock` is created and records
    the resolved mzpeak_prototyping git rev + the full transitive version set; this file MUST be committed.
    If `cargo build` reveals that `default-features = false` on mzpeak_prototyping removes the
    `MzPeakWriter` symbol or another symbol the skeleton needs, record the exact missing symbol in the
    SUMMARY and re-enable only the minimal feature required — do not blanket-enable `async`. Explicitly
    record in the SUMMARY that the sync writer symbol `MzPeakWriter` compiles with defaults off (or which
    minimal feature was needed).
  </action>
  <acceptance_criteria>
    - `grep -E 'mzdata.*features.*"imzml"' Cargo.toml` matches (imzml feature present on mzdata).
    - `grep 'rev *= *"d1aaaf84595202e2e7f622c576c1d6ba9154e379"' Cargo.toml` matches (writer pinned to concrete rev).
    - All CORE exact pins present (grep each): `arrow = "=57.0.0"`, `"=57.0.0"` for parquet, `zip = "=4.1.0"`, `mzdata = { version = "=0.63.3"`, `mzpeaks = "=1.0.9"`.
    - `grep 'default-features = false' Cargo.toml` matches on the mzpeak_prototyping line.
    - `src/main.rs` imports the EXACT symbol: `grep -q 'mzpeak_prototyping::MzPeakWriter' src/main.rs`.
    - `src/main.rs` references `mzdata::io::imzml` (grep: `grep -q 'mzdata::io::imzml' src/main.rs`), proving the feature module exists.
    - `cargo build` exits 0 (the binary compiles with default-features=false, linking mzdata+imzml and the git-pinned writer symbol MzPeakWriter).
    - `Cargo.lock` is generated and records the resolved git rev: `grep -q 'd1aaaf84595202e2e7f622c576c1d6ba9154e379' Cargo.lock` (Cargo writes the resolved git source/rev into the lock).
  </acceptance_criteria>
  <verify>
    <automated>grep -Eq 'mzdata.*features.*"imzml"' Cargo.toml && grep -q 'rev *= *"d1aaaf84595202e2e7f622c576c1d6ba9154e379"' Cargo.toml && grep -q 'default-features = false' Cargo.toml && grep -q 'mzpeak_prototyping::MzPeakWriter' src/main.rs && grep -q 'mzdata::io::imzml' src/main.rs && cargo build -q && test -f Cargo.lock && grep -q 'd1aaaf84595202e2e7f622c576c1d6ba9154e379' Cargo.lock && echo BUILD_AND_LOCK_OK</automated>
  </verify>
  <done>cargo build succeeds with default-features=false; Cargo.toml carries the core exact pins + `=`-pinned app deps + imzml feature + concrete git rev; main.rs imports `mzpeak_prototyping::MzPeakWriter` and references `mzdata::io::imzml`; Cargo.lock is generated and records the resolved rev.</done>
</task>

<task type="auto">
  <name>Task 3: Prove single-copy resolution of mzdata + arrow AND that the imzml feature is unified ON</name>
  <read_first>
    - .planning/research/SUMMARY.md ("Critical Pitfalls" #1: duplicate-crate type fracture)
    - .planning/research/STACK.md ("Version Compatibility" table)
  </read_first>
  <files>(no source files; produces recorded cargo tree proofs in the SUMMARY)</files>
  <action>
    Run inverted dependency views to confirm no duplicate majors of the two crates that fracture the
    type graph (`mzdata`, `arrow`): `cargo tree -i mzdata` and `cargo tree -i arrow`. Confirm each
    reports exactly one version (`0.63.3` for mzdata, `57.0.0` for arrow). Then PROVE the imzml feature
    is actually activated on the resolved mzdata copy (not merely that one mzdata version resolved):
    `cargo tree -e features -i mzdata | rg imzml` must match (the feature-edge view shows the `imzml`
    feature edge into mzdata). If `cargo tree` shows two arrow or two mzdata versions, the build is
    fractured — record the conflicting versions and dependency path (`cargo tree -i <crate>@<bad-version>`)
    in the SUMMARY and treat the plan as blocked rather than passing. Capture the exact resolved-version
    lines AND the imzml feature-edge line in the plan SUMMARY as permanent proof artifacts.
  </action>
  <acceptance_criteria>
    - `cargo tree -i mzdata | grep -c '^mzdata v'` equals 1 (exactly one mzdata version node), showing `v0.63.3`.
    - `cargo tree -i arrow | grep -c '^arrow v'` equals 1 (exactly one arrow version node), showing `v57.0.0`.
    - `cargo tree -e features -i mzdata | rg imzml` matches (imzml feature edge proven active on the resolved mzdata).
    - The SUMMARY records the literal `mzdata v0.63.3` / `arrow v57.0.0` lines AND the imzml feature-edge line.
  </acceptance_criteria>
  <verify>
    <automated>test "$(cargo tree -i mzdata 2>/dev/null | grep -c '^mzdata v')" = "1" && cargo tree -i mzdata 2>/dev/null | grep -q '^mzdata v0.63.3' && test "$(cargo tree -i arrow 2>/dev/null | grep -c '^arrow v')" = "1" && cargo tree -i arrow 2>/dev/null | grep -q '^arrow v57.0.0' && cargo tree -e features -i mzdata 2>/dev/null | rg -q imzml && echo SINGLE_COPY_AND_IMZML_OK</automated>
  </verify>
  <done>cargo tree proves exactly one mzdata (0.63.3) and one arrow (57.0.0), AND the feature-edge view proves the imzml feature is unified ON; all proof lines recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crates.io / git registry -> local build | Third-party crate source + a git-pinned dependency are compiled and run on the dev machine. |
| rustup install script -> shell | Remote install script executed during toolchain setup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Tampering | git dependency `mzpeak_prototyping` | mitigate | Pin to immutable commit rev `d1aaaf84595202e2e7f622c576c1d6ba9154e379` (not a mutable branch); committed Cargo.lock records the resolved rev. |
| T-00-02 | Tampering | crates.io deps (arrow/parquet/zip/mzdata/mzpeaks + `=`-pinned app helpers) | mitigate | Core compatibility crates pinned exact, app helpers pinned with `=`, committed Cargo.lock locks the full transitive set; core versions cross-verified against upstream Cargo.toml in research. |
| T-00-SC | Tampering | crate/toolchain install (cargo build, rustup) | mitigate | Package Legitimacy: all core deps are upstream-sanctioned pins from `mzpeak_prototyping`'s own Cargo.toml (same author Joshua Klein); the git dep IS the reference implementation. App helpers (anyhow/thiserror/clap/log/env_logger) are ecosystem-standard RustCrypto/clap-team crates. No [ASSUMED]/[SUS] third-party packages introduced. rustup fetched from official https://sh.rustup.rs. |
| T-00-03 | Denial of Service | duplicate-major arrow/mzdata or missing imzml feature | accept-then-verify | Task 3 hard-checks single-copy resolution AND imzml feature activation; a fracture or missing feature blocks the plan rather than shipping a broken graph. |
</threat_model>

<verification>
- `cargo build` exits 0 under the pinned toolchain with `default-features = false` on the writer.
- `cargo tree -i mzdata` => single node `mzdata v0.63.3`.
- `cargo tree -i arrow` => single node `arrow v57.0.0`.
- `cargo tree -e features -i mzdata | rg imzml` => imzml feature edge present.
- `Cargo.toml` carries the imzml feature, the concrete git rev, core exact pins, and `=`-pinned app deps.
- `src/main.rs` imports `mzpeak_prototyping::MzPeakWriter` and references `mzdata::io::imzml`.
- `Cargo.lock` is generated, records the resolved git rev, and is committed.
</verification>

<success_criteria>
ENV-01 satisfied: a reproducible, pinned edition-2024 build skeleton compiles with the core upstream
compatibility pins exact (arrow/parquet 57.0.0, zip 4.1.0, mzdata 0.63.3, mzpeaks 1.0.9) plus
`=`-pinned app deps, the non-default mzdata `imzml` feature enabled and proven unified ON, the writer
pinned to a concrete commit imported via its exact symbol `mzpeak_prototyping::MzPeakWriter` with
`default-features = false`, single-copy resolution of mzdata and arrow proven, and a committed Cargo.lock.
</success_criteria>

<output>
Create `.planning/phases/00-environment-foundations/00-01-SUMMARY.md` when done.
Record in it: the active rustc version; the verbatim `cargo tree -i mzdata` / `cargo tree -i arrow`
single-copy lines; the verbatim `cargo tree -e features -i mzdata | rg imzml` feature-edge line; the
resolved mzpeak_prototyping git rev as it appears in Cargo.lock; and whether `default-features = false`
on mzpeak_prototyping required re-enabling any minimal feature for the `MzPeakWriter` symbol (and which).
</output>
</content>
</invoke>
