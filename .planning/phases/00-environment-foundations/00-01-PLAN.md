---
phase: 00-environment-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rust-toolchain.toml
  - Cargo.toml
  - .gitignore
  - src/main.rs
autonomous: true
requirements: [ENV-01]
must_haves:
  truths:
    - "cargo build succeeds on a pinned Rust 1.85+/edition-2024 toolchain"
    - "cargo tree shows exactly one mzdata copy and exactly one arrow copy"
    - "The unified mzdata feature set includes the non-default imzml feature"
    - "mzpeak_prototyping is pinned to a specific git commit rev (not a branch)"
  artifacts:
    - path: "rust-toolchain.toml"
      provides: "Pinned toolchain channel >=1.85 for edition 2024"
      contains: "channel"
    - path: "Cargo.toml"
      provides: "Exact upstream dependency pins + imzml feature + git-pinned writer"
      contains: "mzpeak_prototyping"
    - path: "src/main.rs"
      provides: "Compilable binary entry point that links mzdata(imzml) + mzpeak_prototyping"
      min_lines: 3
  key_links:
    - from: "Cargo.toml"
      to: "mzdata imzml feature"
      via: "features = [\"imzml\"] on the mzdata dependency"
      pattern: "mzdata.*features.*imzml"
    - from: "Cargo.toml"
      to: "HUPO-PSI/mzPeak commit d1aaaf84595202e2e7f622c576c1d6ba9154e379"
      via: "git + rev pin"
      pattern: "rev.*d1aaaf8"
---

<objective>
Stand up a reproducible Rust build skeleton with every dependency pinned EXACTLY to the
upstream `mzpeak_prototyping` set, the non-default `mzdata` `imzml` feature enabled, and the
git-only writer pinned to a concrete commit. Prove the dependency graph reconciles to a single
copy of `mzdata` and a single copy of `arrow` (no duplicate-major fracture).

Purpose: This is the hard prerequisite for every later phase — nothing reads, writes, or spikes
until this compiles. It also pre-empts Pitfall #1 (mzdata version/feature mismatch) by forcing the
workspace to unify on one `mzdata` that carries `imzml`, even though upstream's own pin omits it.
Output: `rust-toolchain.toml`, `Cargo.toml`, `.gitignore`, `src/main.rs`, a green `cargo build`,
and a `cargo tree` proof of single-copy resolution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md

<facts>
<!-- Concrete values resolved during planning. Use these directly; do not re-derive. -->
- Local toolchain: NO rustc/cargo/rustup currently installed (must be installed first).
- mzpeak_prototyping local clone: /tmp/mzpeak_prototyping
  - HEAD commit (use as the git rev pin): d1aaaf84595202e2e7f622c576c1d6ba9154e379
  - Clone's `origin` remote is https://github.com/mobiusklein/mzpeak_prototyping.git
    which redirects to https://github.com/HUPO-PSI/mzPeak. Either URL resolves the same
    commit; prefer the canonical https://github.com/HUPO-PSI/mzPeak in Cargo.toml.
- Upstream mzpeak_prototyping/Cargo.toml pins (must match exactly):
    arrow = "57.0.0"
    parquet = { version = "57.0.0", features = ["encryption"] }
    zip = "4.1.0"
    mzdata = { version = "0.63.3", features = ["serde","bruker_tdf","nalgebra","zstd","numpress"] }   # NOTE: NO imzml here
    mzpeaks = "1.0.9"
    edition = "2024"
    mzpeak_prototyping default features = ["async"]  (pulls tokio/async_zip/object_store/opendal/url/futures)
- Our crate MUST request mzdata with features = ["imzml"]. Cargo unions features across the graph,
  so the single resolved mzdata copy will carry imzml + upstream's set. Pin our mzdata to "=0.63.3".
- Per STACK.md "Stack Patterns by Variant": depend on mzpeak_prototyping with
  default-features = false for a local-file-only v1 to drop the async/object_store/opendal/tokio stack.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install + pin the Rust toolchain (edition 2024 / >=1.85)</name>
  <read_first>
    - .planning/research/STACK.md (rows: Rust toolchain 1.85+, rust-toolchain.toml)
    - /tmp/mzpeak_prototyping/Cargo.toml (confirm `edition = "2024"`)
  </read_first>
  <files>rust-toolchain.toml, .gitignore</files>
  <action>
    If `cargo --version` is absent, install rustup non-interactively via the official rustup script
    (https://sh.rustup.rs) with the stable channel, then ensure `~/.cargo/bin` is on PATH for this
    session. Create `rust-toolchain.toml` with a `[toolchain]` table pinning `channel = "1.85.0"`
    (the minimum that supports edition 2024; a newer stable is acceptable only if >=1.85 — but pin a
    concrete version string, not "stable", for reproducibility) and `components = ["rustfmt", "clippy"]`.
    Create `.gitignore` ignoring `/target` and the large binary sidecar `data/*.ibd` (the `.ibd` is
    fetched in plan 00-02 and must never be committed). Do NOT ignore `data/*.imzML` (the header is
    already tracked and is small).
  </action>
  <acceptance_criteria>
    - `rustc --version` prints a version >= 1.85.0.
    - `rust-toolchain.toml` contains `channel = "1.85.0"` under `[toolchain]` (grep: `grep -q 'channel *= *"1.85' rust-toolchain.toml`).
    - `.gitignore` contains both `/target` and a `data/*.ibd` rule (grep: `grep -q 'data/\*.ibd' .gitignore`).
    - `.gitignore` does NOT contain a `*.imzML` ignore rule (grep -v check: `! grep -q 'imzML' .gitignore`).
  </acceptance_criteria>
  <verify>
    <automated>rustc --version | grep -Eq '1\.(8[5-9]|9[0-9]|[0-9]{3})' && grep -q 'channel *= *"1.85' rust-toolchain.toml && grep -q 'data/\*.ibd' .gitignore</automated>
  </verify>
  <done>A pinned >=1.85 toolchain is active and rust-toolchain.toml + .gitignore exist with the correct rules.</done>
</task>

<task type="auto">
  <name>Task 2: Author Cargo.toml with exact pins + imzml feature + git-pinned writer, and a minimal main.rs</name>
  <read_first>
    - .planning/research/STACK.md (sections "Installation / Cargo.toml", "Stack Patterns by Variant", "Version Compatibility")
    - .planning/research/SUMMARY.md ("Recommended Stack", "Critical Pitfalls" #1)
    - /tmp/mzpeak_prototyping/Cargo.toml (the dependency pins to mirror)
    - /tmp/mzpeak_prototyping/src/lib.rs (confirm re-exported symbols: MzPeakWriter, MzPeakReader)
  </read_first>
  <files>Cargo.toml, src/main.rs</files>
  <action>
    Create a single binary crate `Cargo.toml` (`[package] name = "imzml2mzpeak"`, `edition = "2024"`,
    `rust-version = "1.85"`). Pin dependencies to EXACTLY: `arrow = "=57.0.0"`,
    `parquet = { version = "=57.0.0", features = ["encryption"] }`, `zip = "=4.1.0"`,
    `mzdata = { version = "=0.63.3", features = ["imzml","serde","bruker_tdf","nalgebra","zstd","numpress"] }`
    (the `imzml` feature is the NON-default addition that makes `mzdata::io::imzml` exist; the other
    features mirror upstream so the unioned copy is identical to upstream's plus imzml),
    `mzpeaks = "=1.0.9"`, and
    `mzpeak_prototyping = { git = "https://github.com/HUPO-PSI/mzPeak", rev = "d1aaaf84595202e2e7f622c576c1d6ba9154e379", default-features = false }`
    (default-features=false drops the async/object_store/opendal/tokio stack per STACK.md local-file-only
    variant). Add app-boundary helpers at upstream pins: `anyhow = "=1.0.102"`, `thiserror = "=2.0.18"`,
    `clap = { version = "=4.5.38", features = ["derive"] }`, `log = "=0.4.27"`, `env_logger = "=0.11.8"`.
    Write `src/main.rs` as a minimal compile-proof entry point that imports `mzpeak_prototyping`'s
    re-exported writer type and references `mzdata::io::imzml` so the imzml feature is exercised at
    compile time (e.g. a `use` of the imzml reader type behind `#[allow(unused_imports)]`), with a
    `main()` that just logs a startup line. Do NOT inline writer logic — this is a skeleton.
    If `cargo build` reveals that `default-features = false` on mzpeak_prototyping removes a symbol the
    skeleton needs, document the exact missing symbol in the SUMMARY and re-enable only the minimal
    feature required — do not blanket-enable `async`.
  </action>
  <acceptance_criteria>
    - `grep -E 'mzdata.*features.*"imzml"' Cargo.toml` matches (imzml feature present on mzdata).
    - `grep 'rev *= *"d1aaaf84595202e2e7f622c576c1d6ba9154e379"' Cargo.toml` matches (writer pinned to concrete rev).
    - All of these EXACT version pins are present (grep each): `arrow = "=57.0.0"`, `"=57.0.0"` for parquet, `zip = "=4.1.0"`, `mzdata = { version = "=0.63.3"`, `mzpeaks = "=1.0.9"`.
    - `grep 'default-features = false' Cargo.toml` matches on the mzpeak_prototyping line.
    - `cargo build` exits 0 (the binary compiles, linking mzdata+imzml and the git-pinned writer).
    - `src/main.rs` references `mzdata::io::imzml` (grep: `grep -q 'imzml' src/main.rs`), proving the feature module exists.
  </acceptance_criteria>
  <verify>
    <automated>grep -Eq 'mzdata.*features.*"imzml"' Cargo.toml && grep -q 'rev *= *"d1aaaf84595202e2e7f622c576c1d6ba9154e379"' Cargo.toml && cargo build 2>&1 | tail -5; cargo build -q && echo BUILD_OK</automated>
  </verify>
  <done>cargo build succeeds; Cargo.toml carries every exact pin, the imzml feature, default-features=false on the git-pinned writer, and main.rs proves the imzml module links.</done>
</task>

<task type="auto">
  <name>Task 3: Prove single-copy dependency resolution for mzdata and arrow</name>
  <read_first>
    - .planning/research/SUMMARY.md ("Critical Pitfalls" #1: duplicate-crate type fracture)
    - .planning/research/STACK.md ("Version Compatibility" table)
  </read_first>
  <files>(no source files; produces a recorded cargo tree proof in the SUMMARY)</files>
  <action>
    Run `cargo tree` and inspect for duplicate majors of the two crates that fracture the type graph:
    `mzdata` and `arrow`. Use de-duplication-aware invocations: `cargo tree -i mzdata` and
    `cargo tree -i arrow` (the `-i`/invert view lists every distinct resolved version). Confirm each
    reports exactly one version (`0.63.3` for mzdata, `57.0.0` for arrow). If `cargo tree` shows two
    arrow or two mzdata versions, the build is fractured — record the conflicting versions and the
    dependency path (`cargo tree -i <crate>@<bad-version>`) in the SUMMARY and treat the plan as
    blocked rather than passing. Capture the exact resolved-version lines in the plan SUMMARY as the
    permanent proof artifact.
  </action>
  <acceptance_criteria>
    - `cargo tree -i mzdata | grep -c '^mzdata v'` equals 1 (exactly one mzdata version node), and that line shows `v0.63.3`.
    - `cargo tree -i arrow | grep -c '^arrow v'` equals 1 (exactly one arrow version node), and that line shows `v57.0.0`.
    - The SUMMARY records the literal `mzdata v0.63.3` and `arrow v57.0.0` lines from cargo tree.
  </acceptance_criteria>
  <verify>
    <automated>test "$(cargo tree -i mzdata 2>/dev/null | grep -c '^mzdata v')" = "1" && cargo tree -i mzdata 2>/dev/null | grep -q '^mzdata v0.63.3' && test "$(cargo tree -i arrow 2>/dev/null | grep -c '^arrow v')" = "1" && cargo tree -i arrow 2>/dev/null | grep -q '^arrow v57.0.0' && echo SINGLE_COPY_OK</automated>
  </verify>
  <done>cargo tree proves exactly one mzdata (0.63.3) and one arrow (57.0.0); proof lines recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crates.io / git registry -> local build | Third-party crate source + a git-pinned dependency are compiled and run on the dev machine. |
| rustup install script -> shell | Remote install script executed during toolchain setup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Tampering | git dependency `mzpeak_prototyping` | mitigate | Pin to immutable commit rev `d1aaaf84595202e2e7f622c576c1d6ba9154e379` (not a mutable branch); Cargo.lock records the resolved rev. |
| T-00-02 | Tampering | crates.io deps (arrow/parquet/zip/mzdata/mzpeaks) | mitigate | Exact `=` version pins + committed Cargo.lock; versions cross-verified against upstream Cargo.toml in research. |
| T-00-SC | Tampering | crate/toolchain install (cargo build, rustup) | mitigate | Package Legitimacy: all deps are upstream-sanctioned pins from `mzpeak_prototyping`'s own Cargo.toml (same author Joshua Klein); the git dep IS the reference implementation. No [ASSUMED]/[SUS] third-party packages introduced. rustup fetched from official https://sh.rustup.rs. |
| T-00-03 | Denial of Service | duplicate-major arrow/mzdata | accept-then-verify | Task 3 hard-checks single-copy resolution; a fracture blocks the plan rather than shipping a broken graph. |
</threat_model>

<verification>
- `cargo build` exits 0 under the pinned toolchain.
- `cargo tree -i mzdata` => single node `mzdata v0.63.3`.
- `cargo tree -i arrow` => single node `arrow v57.0.0`.
- `Cargo.toml` carries the imzml feature and the concrete git rev.
- `Cargo.lock` is generated (commit it so the resolved git rev + transitive set are reproducible).
</verification>

<success_criteria>
ENV-01 satisfied: a reproducible, pinned edition-2024 build skeleton compiles with the exact upstream
dependency set, the non-default mzdata `imzml` feature enabled, and the writer pinned to a concrete
commit — with proven single-copy resolution of mzdata and arrow.
</success_criteria>

<output>
Create `.planning/phases/00-environment-foundations/00-01-SUMMARY.md` when done.
Record in it: the active rustc version, the verbatim `cargo tree -i mzdata` and `cargo tree -i arrow`
single-copy lines, and whether `default-features = false` on mzpeak_prototyping required re-enabling
any minimal feature (and which).
</output>
