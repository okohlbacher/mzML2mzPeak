---
phase: 25-forward-declared-geometry-threading
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - tests/fixtures/imaging/gen_declared_geometry_fixture.py
  - tests/fixtures/imaging/Synthetic_DeclaredGrid.imzML
  - tests/fixtures/imaging/Synthetic_DeclaredGrid.ibd
  - src/verify/geometry.rs
  - src/verify/mod.rs
  - tests/geometry_roundtrip.rs
  - docs/mzpeak-imaging-spec-suggestions.md
autonomous: true
requirements: [GEOF-01]
must_haves:
  truths:
    - "A convertible fixture pairs a DECLARED <scanSettings> grid WITH a valid .ibd, so the full convert path can exercise declared geometry end-to-end"
    - "Converting the declared-grid fixture emits pixel_count_source == \"declared\" AND a scan_settings_list facet carrying the declared grid"
    - "Forward declared geometry survives forward→reverse symmetry: the reverse <scanSettings> re-parses to the same declared grid (a src/verify/ assertion proves it)"
    - "The XRT round-trip + masking-aware L1 contract for declared geometry is asserted, not assumed"
  artifacts:
    - path: "tests/fixtures/imaging/gen_declared_geometry_fixture.py"
      provides: "deterministic generator for a declared-grid imzML + matching .ibd (mirrors gen_processed_fixture.py)"
    - path: "tests/fixtures/imaging/Synthetic_DeclaredGrid.imzML"
      provides: "committed declared-grid fixture (grid + pixel size + max dim) with an .ibd"
    - path: "src/verify/geometry.rs"
      provides: "declared-geometry forward↔reverse symmetry assertion (parse forward geom == reverse re-emitted geom)"
      min_lines: 30
    - path: "tests/geometry_roundtrip.rs"
      provides: "integration test: convert declared-grid fixture → assert declared flip + facet, then reverse → re-parse → symmetry"
    - path: "docs/mzpeak-imaging-spec-suggestions.md"
      provides: "GEOF-01 declared-vs-observed consistency note appended to the scan_settings_list section (three-places rule)"
  key_links:
    - from: "tests/geometry_roundtrip.rs"
      to: "convert_with(.., Some(&geom), ..)"
      via: "forward convert of the declared-grid fixture"
      pattern: "convert_with"
    - from: "tests/geometry_roundtrip.rs"
      to: "crate::reverse::convert::convert"
      via: "reverse the produced archive, then re-parse <scanSettings>"
      pattern: "reverse::convert"
    - from: "src/verify/geometry.rs"
      to: "parse_scan_settings"
      via: "re-parse the reverse-emitted .imzML and compare to the forward declared geom"
      pattern: "parse_scan_settings"
---

<objective>
Lock GEOF-01 with the evidence the standing XRT criterion demands: (a) a real convertible fixture that pairs
a DECLARED `<scanSettings>` grid WITH a valid `.ibd` (no committed fixture does this today —
`Synthetic_FullGeometry` declares a grid but has no `.ibd`; `Example_Processed` has an `.ibd` but declares no
grid), (b) an end-to-end convert-path test that proves the declared flip (`pixel_count_source == "declared"`)
AND the `scan_settings_list` facet through the full public path, and (c) a `src/verify/` forward↔reverse
SYMMETRY assertion proving the declared grid survives the round-trip (forward declared geom → mzPeak →
reverse `<scanSettings>` → re-parse → equal). Finish the three-places rule by appending the GEOF-01
declared-vs-observed consistency note to `docs/mzpeak-imaging-spec-suggestions.md` (the implementation +
schema already exist; only the spec write-up needs the consistency rule).

Purpose: GEOF-01 success criteria require declared geometry to be honored end-to-end AND to round-trip
symmetrically; both must be PROVEN, not assumed. PXD001283 acceptance must stay green (the new fixture is the
CI-fast proxy; the `#[ignore]` PXD gate is unchanged).
Output: committed declared-grid fixture + generator, the `src/verify/geometry.rs` symmetry assertion, the
integration test, and the spec-suggestions consistency note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-forward-declared-geometry-threading/25-CONTEXT.md
@docs/mzpeak-extension-contract.md
@tests/fixtures/imaging/gen_processed_fixture.py
@tests/scan_settings.rs

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase. -->

From src/schema (re-exported): `parse_scan_settings(path) -> Result<ImagingRunMetadata, _>`,
`scan_settings_list_from_geometry(&ImagingRunMetadata) -> Vec<ScanSettings>`,
`ImagingRunMetadata { grid_x/y: Option<i64>, pixel_size_x/y: Option<f64>, max_dimension_x/y: Option<i64>,
 absolute_offset_x/y: Option<i64>, scan_pattern/scan_type/line_scan_direction/linescan_sequence: Option<String> }`.

From src/write: `convert_with(reader, out, image_paths, &EncodingOptions, Some(&geom), Some(&input)) ->
Result<ConversionOutcome, WriteError>`; `EncodingOptions::legacy()`.
From src/read: `ImagingReader::open(path)`.

From src/reverse/convert.rs: `pub fn convert(imzml_path: &Path, ibd_path: &Path, archive: &Path)
-> Result<(), ReverseError>`. The reverse path reads the archive's `metadata.imaging` block (the derived copy
that carries the declared grid as pixel_count/pixel_size/etc.) and emits `<scanSettings>` via
`imzml_writer::write_scan_settings_to`.

From src/verify/mod.rs: declares `pub mod report; pub mod compare; pub mod ion_image; pub mod verify;` and
re-exports flat. Add `pub mod geometry;` plus the symmetry-fn re-export WITHOUT editing existing bodies
(mirrors the module's documented "append a pub mod line" convention).

From tests/fixtures/imaging/gen_processed_fixture.py: the canonical generator pattern — 3×3 processed,
`.ibd` = `[16-byte UUID][per-spectrum mz bytes][intensity bytes]...`, IMS:1000102/103/104 offsets/lengths,
whole-file SHA-1 (IMS:1000091) + UUID (IMS:1000080) computed from emitted bytes so the pair passes the
integrity preflight. The new generator ADDS a `<scanSettingsList>`/`<scanSettings>` block declaring the grid.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declared-grid imzML + .ibd fixture (generator + committed artifacts)</name>
  <files>tests/fixtures/imaging/gen_declared_geometry_fixture.py, tests/fixtures/imaging/Synthetic_DeclaredGrid.imzML, tests/fixtures/imaging/Synthetic_DeclaredGrid.ibd</files>
  <action>
    Copy `gen_processed_fixture.py` to `gen_declared_geometry_fixture.py` and modify it to emit
    `Synthetic_DeclaredGrid.imzML` + `Synthetic_DeclaredGrid.ibd`. Keep the SAME `.ibd` layout, the same
    IMS:1000102/103/104 external-array encoding, and the same UUID/SHA-1 integrity wiring (so the pair passes
    the Plan 02-02 integrity preflight unchanged). The ONE structural addition: emit a
    `<scanSettingsList count="1"><scanSettings id="scansettings1">` block (BEFORE the run, mirroring
    `Synthetic_FullGeometry.imzML`) declaring `IMS:1000042`/`IMS:1000043` (grid x/y), `IMS:1000046`/`1000047`
    (pixel size, µm `UO:0000017`), and `IMS:1000044`/`1000045` (max dimension, µm). Make the declared grid
    CONSISTENT with the emitted pixel coordinates: emit a 3×3 grid of 9 spectra at coordinates (1,1)..(3,3)
    and declare `max count of pixel x/y = 3` so observed extent (3×3) == declared grid (no inconsistency
    warning — this fixture proves the HAPPY declared path). Run the generator from the repo root and COMMIT
    both produced artifacts. The fixture is small (9 spectra) so CI cost is negligible. Do NOT delete or
    modify the existing fixtures.
  </action>
  <verify>
    <automated>python3 tests/fixtures/imaging/gen_declared_geometry_fixture.py && ls -l tests/fixtures/imaging/Synthetic_DeclaredGrid.imzML tests/fixtures/imaging/Synthetic_DeclaredGrid.ibd && grep -c 'IMS:1000042' tests/fixtures/imaging/Synthetic_DeclaredGrid.imzML</automated>
  </verify>
  <done>
    `Synthetic_DeclaredGrid.imzML` + `.ibd` exist and are committed; the imzML declares a 3×3 grid via
    IMS:1000042/43 plus pixel-size + max-dimension µm terms; the `.ibd` UUID/SHA-1 match the imzML (passes
    preflight); the fixture has 9 spectra at coords (1,1)..(3,3) consistent with the declared grid.
  </done>
</task>

<task type="auto">
  <name>Task 2: src/verify/ declared-geometry forward↔reverse symmetry assertion</name>
  <files>src/verify/geometry.rs, src/verify/mod.rs</files>
  <action>
    Add `src/verify/geometry.rs` exposing a `pub fn` that asserts forward↔reverse declared-geometry SYMMETRY:
    given the forward-parsed `ImagingRunMetadata` (from the source imzML) and the path to the reverse-emitted
    `.imzML`, re-parse the reverse `.imzML` with `parse_scan_settings` and compare the geometry fields that
    declared geometry round-trips through `metadata.imaging` — `grid_x`/`grid_y` (the declared pixel_count),
    `pixel_size_x`/`y`, `max_dimension_x`/`y`, `absolute_offset_x`/`y`, and the scan-pattern child CURIEs.
    Return a typed result (a small `GeometrySymmetry` report struct with a `passed()` and the per-field
    mismatches, OR a `Result<(), VerifyError-like>`); follow the existing `src/verify/report.rs` style
    (thiserror, no `anyhow`). The comparison must mirror the documented round-trip fate of declared geometry:
    the forward declared grid lands as `metadata.imaging.pixel_count` (the derived copy), the reverse reads
    that block and re-emits `<scanSettings>`, so the reverse-parsed `grid_x`/`grid_y` MUST equal the forward
    `grid_x`/`grid_y`. Wire the module into `src/verify/mod.rs` by APPENDING `pub mod geometry;` and a flat
    re-export of the public symmetry fn/type — do NOT edit any existing body in `mod.rs` (its doc comment
    sanctions appending a `pub mod` line). Add `#[cfg(test)]` unit tests in `geometry.rs` driving the
    comparison directly (equal geoms → passed; a mismatched grid_x → fails, naming the field).
  </action>
  <verify>
    <automated>cargo test --lib verify::geometry 2>&1 | tail -20</automated>
  </verify>
  <done>
    `src/verify/geometry.rs` exists with a public declared-geometry symmetry assertion + unit tests; it is
    re-exported from `src/verify/mod.rs` (append-only); `cargo test --lib verify::geometry` passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: End-to-end declared-geometry round-trip integration test + spec-suggestions note</name>
  <files>tests/geometry_roundtrip.rs, docs/mzpeak-imaging-spec-suggestions.md</files>
  <action>
    Add `tests/geometry_roundtrip.rs` as the GEOF-01 acceptance proof, using the new
    `Synthetic_DeclaredGrid` fixture (has `.ibd`, declares a grid — the gap the existing two-fixture
    `scan_settings.rs` split could not cover). Test flow: (1) `parse_scan_settings` the fixture → assert the
    declared 3×3 grid surfaced; (2) `ImagingReader::open` + `convert_with(.., Some(&geom), Some(&fixture))`
    to a temp `.mzpeak`; (3) re-open with the reference `MzPeakReader` and assert
    `metadata.imaging.pixel_count_source == "declared"` AND `pixel_count == {x:3,y:3}` AND a non-empty
    `scan_settings_list` carrying IMS:1000042/43 — proving the declared flip end-to-end through the public
    path (the happy path that Plan 01's unit tests cover only at the `fold_into` level); (4) run
    `crate::reverse::convert::convert(out.imzML, out.ibd, &mzpeak)` to reverse the produced archive; (5)
    call the Plan-02 Task-2 `src/verify/geometry` symmetry assertion with the forward `geom` + the
    reverse-emitted `.imzML`, asserting it passes (declared grid survives forward→reverse). Reuse the temp-
    file + cleanup discipline from `tests/scan_settings.rs`. Then APPEND to
    `docs/mzpeak-imaging-spec-suggestions.md`, inside/after the `scan_settings_list` section (Edit 3), a
    short GEOF-01 note: declared geometry is honored as `pixel_count_source: "declared"` ONLY when the
    declared grid is consistent with the observed pixel extent; when observed coordinates EXCEED the declared
    grid the converter keeps `observed_max` and emits a counted warning (it never fabricates a declared count
    the data contradicts). This completes the three-places rule for GEOF-01 (src/ = Plan 01 + this plan's
    verify/test; schema/scan_settings.json + schema/imaging.json already carry the structures unchanged;
    docs = this note).
  </action>
  <verify>
    <automated>cargo test --test geometry_roundtrip 2>&1 | tail -25 && grep -c 'GEOF-01\|observed_max' docs/mzpeak-imaging-spec-suggestions.md</automated>
  </verify>
  <done>
    `tests/geometry_roundtrip.rs` proves: declared grid parses, converts to `pixel_count_source == declared`
    + a `scan_settings_list` facet, reverses, and the forward↔reverse declared-geometry symmetry assertion
    passes. `docs/mzpeak-imaging-spec-suggestions.md` carries the GEOF-01 declared-vs-observed consistency
    note. `cargo test --test geometry_roundtrip` is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reverse-emitted `.imzML` → re-parse | The reverse output is re-parsed and compared to the forward source; a silent geometry drop/alter must be caught by the symmetry assertion, not assumed absent. |
| fixture `.ibd` → integrity preflight | The generated `.ibd` must satisfy the UUID/SHA-1 preflight; a malformed fixture would mask, not test, the convert path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Tampering | reverse geometry drop | mitigate | The `src/verify/geometry.rs` symmetry assertion fails loudly if the reverse `<scanSettings>` does not re-parse to the forward declared grid (Task 2/3). |
| T-25-05 | Repudiation | fixture integrity | mitigate | The generator computes the `.ibd` UUID + SHA-1 from emitted bytes (mirrors gen_processed_fixture.py), so the committed fixture passes preflight provably (Task 1). |
| T-25-06 | DoS | CI cost | accept | The fixture is 9 spectra; the round-trip test is sub-second. The 815 MB PXD001283 gate stays `#[ignore]`-gated (unchanged). |
| T-25-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; the generator uses the Python stdlib only (`hashlib`/`struct`/`uuid`), pinned Rust stack unchanged, no new deps. |
</threat_model>

<verification>
- `cargo test` (default) is green including the new `tests/geometry_roundtrip.rs` (fixture is committed, no
  network).
- The new fixture passes the integrity preflight (the convert step in the round-trip test would error
  otherwise).
- `src/verify/geometry.rs` is re-exported and its unit tests pass; `mod.rs` is edited append-only.
- XRT criterion satisfied: forward↔reverse declared-geometry symmetry asserted (Task 2/3), masking-aware L1
  unaffected (declared geometry is metadata-only — it does not touch the data facets), the spec-suggestions
  consistency note completes the three-places rule.
- PXD001283 acceptance gate (`tests/acceptance.rs`, `#[ignore]`) is untouched and still passes when run
  (declared-geometry threading is additive metadata; the L1 data compare is unchanged).
</verification>

<success_criteria>
GEOF-01 fully proven end-to-end: a declared, consistent grid converts to `pixel_count_source: "declared"` +
a `scan_settings_list` facet; declared `absolute_offset_um` is populated where the source declares it
(already wired in `assemble_imaging_metadata`, now exercised by the fixture if it declares offsets);
forward↔reverse geometry symmetry is asserted via `src/verify/`. Three-places rule satisfied (src/, schema/
unchanged-and-sufficient, docs/ note added). No new deps; pinned stack unchanged.
</success_criteria>

<output>
Create `.planning/phases/25-forward-declared-geometry-threading/25-02-SUMMARY.md` when done.
</output>
