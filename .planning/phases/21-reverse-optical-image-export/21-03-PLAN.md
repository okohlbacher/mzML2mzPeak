---
phase: 21-reverse-optical-image-export
plan: 03
type: execute
wave: 3
depends_on: ["21-02"]
files_modified:
  - tests/reverse_optical_export.rs
  - docs/mzpeak-imaging-spec-suggestions.md
autonomous: true
requirements: [RIMG-03]
must_haves:
  truths:
    - "A forward-convert (auto-embed) → reverse-convert round-trip exports the optical image as an external file beside the .imzML with bytes (sha256) equal to the source image"
    - "The reverse .imzML <sample> carries IMS:1006008 → the exported file plus IMS:1006015 (H&E) + IMS:1006017 recovered, re-readable by mzdata::ImzMLReader / parse_optical_images"
    - "A no-images imaging archive reverses with NO <sampleList> optical params and byte-identical spectral output (clean no-op)"
    - "A missing/unreadable image member warns and the spectral reverse still succeeds (soft posture)"
    - "The affine/registration is NOT re-emitted as a CV param (no imzML transform term) — documented degrade"
  artifacts:
    - path: "tests/reverse_optical_export.rs"
      provides: "the forward→reverse round-trip + no-op + soft-posture integration tests"
      min_lines: 80
  key_links:
    - from: "tests/reverse_optical_export.rs"
      to: "src/write forward convert_with (auto-embed) + src/reverse::convert"
      via: "forward-convert Synthetic_OpticalRef → reverse-convert the .mzpeak → assert exported file + <sample>"
      pattern: "convert_with|reverse::convert|parse_optical_images"
    - from: "docs/mzpeak-imaging-spec-suggestions.md (Edit 7)"
      to: "RIMG reverse export + affine degrade"
      via: "spec doc note (three-places rule)"
      pattern: "Reverse|RIMG|affine"
---

<objective>
Prove and document RIMG-03 (and lock the RIMG-01/02 round-trip): an end-to-end test that
forward-converts the `Synthetic_OpticalRef` fixture (auto-discovery embeds the optical image),
reverse-converts the resulting `.mzpeak`, and asserts (a) the external image file exists beside the
reverse `.imzML` with sha256 equal to the source image, (b) the reverse `.imzML` `<sample>` carries
`IMS:1006008` → that file plus the recovered `IMS:1006015`/`IMS:1006017`, (c) `mzdata::ImzMLReader` /
`parse_optical_images` re-reads the location + staining + alignment. Plus a no-images NO-OP test
(byte-unchanged spectral output, no `<sampleList>`) and a missing-member SOFT-POSTURE test. Finally,
the spec doc Edit 7 reverse-export + affine-degrade note (three-places rule).

Purpose: This is the requirement-closing evidence for the whole phase — it exercises the real
forward auto-embed → reverse export path (not a synthetic shortcut), proves the affine is NOT
re-emitted as a CV, and records the documented degrade. The no-op + soft-posture tests guard the
two non-negotiable invariants (no spurious emission; auxiliary image never fails spectral reverse).

Output: `tests/reverse_optical_export.rs` (3 tests) + the spec doc Edit 7 reverse paragraph.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-reverse-optical-image-export/21-CONTEXT.md

<read_first>
- .planning/phases/21-reverse-optical-image-export/21-CONTEXT.md — round-trip test shape (lines 88-96), no-op + soft-posture + affine-degrade requirements
- tests/optical_auto_discovery.rs — the forward auto-embed harness: `convert_with(reader, out, image_paths, opts, geometry, input_path)`, `ImagingReader::open`, the synthetic-imzML + sibling-image temp-dir pattern, the capturing logger for warning assertions (reuse this to assert the soft-posture warning)
- tests/fixtures/imaging/Synthetic_OpticalRef.imzML + .ibd + the sibling optical_4x3.tiff it references (IMS:1006008 value="optical_4x3.tiff", IMS:1006011, IMS:1006013 "tumor", and staining/alignment) — the round-trip source
- src/reverse/convert.rs — `convert(imzml_path, ibd_path, archive)` — the reverse entry point under test
- src/schema/optical.rs — `parse_optical_images(path) -> Vec<OpticalImageRef>` — the re-read oracle for (c)
- src/write/convert.rs:620-658 — `map_descriptive` (the fold the round-trip relies on inverting)
- docs/mzpeak-imaging-spec-suggestions.md lines 105-152 — Edit 7, especially the existing line 149-152 "Reverse (mzPeak → imzML) image export is OUT OF SCOPE for v0.5 — the reverse path drops embedded optical images" which this plan UPDATES to reflect RIMG
- knowledge/cv/CV terms - optical image.md lines 31-32 — "NO registration transform term" (the affine-degrade rationale)
</read_first>

<interfaces>
Reverse entry point under test:
```
pub fn convert(imzml_path: &Path, ibd_path: &Path, archive: &Path) -> Result<(), ReverseError>
```
Re-read oracles:
```
mzdata::io::imzml::ImzMLReader::<File,File>::new(File, File)  // re-reads the produced .imzML+.ibd
mzml2mzpeak::schema::optical::parse_optical_images(&Path) -> Result<Vec<OpticalImageRef>, _>  // IMS:1006008 + descriptive
```
Forward auto-embed seam (to produce the .mzpeak with an embedded optical image):
```
mzml2mzpeak::write::convert_with(reader, out, image_paths, opts, geometry, input_path)
// input_path points at the Synthetic_OpticalRef.imzML so IMS:1006008 auto-discovery embeds optical_4x3.tiff
```
sha256 helper: reuse the in-tree `sha2` crate (no new crate), mirroring src/write/image.rs::sha256_and_size.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Forward→reverse round-trip + no-op + soft-posture integration tests</name>
  <files>tests/reverse_optical_export.rs</files>
  <action>
    Create tests/reverse_optical_export.rs with three integration tests, reusing the temp-dir +
    forward-convert harness shape from tests/optical_auto_discovery.rs (open `Synthetic_OpticalRef`
    as the spectrum source and pass its path as `input_path` so the forward auto-discovery embeds the
    sibling `optical_4x3.tiff` — copy the fixture .imzML/.ibd/.tiff into the temp dir first so the
    relative IMS:1006008 location resolves).

    Test 1 — round-trip (RIMG-01 + RIMG-02 closure): forward-convert `Synthetic_OpticalRef` into a
    temp `.mzpeak` (auto-embed). Reverse-convert it via `reverse::convert(out_imzml, out_ibd,
    &mzpeak)`. Assert: (a) an external image file exists beside `out_imzml` whose sha256 equals the
    sha256 of the committed `optical_4x3.tiff` source bytes; (b) the reverse `.imzML` text contains a
    `<sample>` with `accession="IMS:1006008"` whose value is that exported filename, plus
    `IMS:1006015` (staining — the fixture's H&E if present, escaped) and/or `IMS:1006017` (alignment)
    and `IMS:1006013` value="tumor" + `IMS:1006011`; (c) feed the reverse `.imzML` to
    `parse_optical_images` and assert the recovered `OpticalImageRef` carries the exported location +
    the recovered staining/alignment/subject/morphology that the fixture declared (best-effort clean
    values that DO invert). Optionally also open via `mzdata::ImzMLReader` to confirm it re-reads.

    Test 2 — no-op (RIMG-03): build (or reuse) an imaging `.mzpeak` with NO `images[]` (the existing
    `imaging_archive` pattern in src/reverse/convert.rs tests, or convert a fixture with no optical
    ref). Reverse-convert it. Assert the reverse `.imzML` contains NO `<sampleList`/`IMS:1006008`
    optical block, AND the spectral portion is byte-identical to a baseline reverse of the same
    no-images archive (capture the spectral `<spectrum>` bytes / full document minus any sample block
    and assert equality — i.e. the no-op leaves spectral output unchanged). Confirm the affine is NOT
    present as any CV param in the reverse output (assert no `registration`/affine matrix cvParam
    string appears) — the RIMG-03 affine-degrade.

    Test 3 — soft posture (RIMG-03 / OPT-03 mirror): produce an imaging `.mzpeak` that has an
    `images[]` entry but whose ZIP member is absent/unreadable (e.g. construct the archive then
    remove/rename the `images/image_0000.*` member, OR craft an `images[]` entry pointing at a
    non-existent archive_path). Reverse-convert it and assert `reverse::convert` returns `Ok` (the
    spectral reverse SUCCEEDS), the `.imzML`/`.ibd` exist and re-read via `mzdata::ImzMLReader`, and a
    `log::warn!` was captured (reuse the capturing-logger pattern from optical_auto_discovery.rs).

    Use the in-tree `sha2` crate for the byte comparison (no new crate); use the std-only temp-dir
    pattern (no `tempfile`).
  </action>
  <verify>
    <automated>cargo build && cargo test --test reverse_optical_export</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test reverse_optical_export` passes all three tests.
    - Round-trip test: an external image file exists beside the reverse `.imzML` with sha256 == the committed `optical_4x3.tiff` source sha256, AND the reverse `.imzML` `<sample>` carries `IMS:1006008` → that file + `IMS:1006013` "tumor" + `IMS:1006011`, AND `parse_optical_images` re-reads the exported location + recovered descriptive attrs.
    - No-op test: the reverse output of a no-images archive contains NO `<sampleList`/`IMS:1006008` and its spectral bytes are byte-identical to the baseline reverse; no affine/registration cvParam appears.
    - Soft-posture test: a missing image member yields `reverse::convert` == `Ok`, the spectral `.imzML`/`.ibd` re-read via mzdata, and a warning was logged.
  </acceptance_criteria>
  <done>
    The forward→reverse optical round-trip is proven end-to-end (sha256 byte fidelity + IMS:1006008 +
    recovered descriptive + mzdata/parse re-read); the no-images no-op (byte-unchanged spectral, no
    spurious emit, no affine CV) and the missing-member soft posture are proven. All green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Spec doc Edit 7 — reverse export + affine degrade (three-places rule)</name>
  <files>docs/mzpeak-imaging-spec-suggestions.md</files>
  <action>
    Update Edit 7 in docs/mzpeak-imaging-spec-suggestions.md. Replace the now-stale
    "Reverse (mzPeak → imzML) image export is OUT OF SCOPE for v0.5 — the reverse path drops embedded
    optical images" note (around lines 149-152) with a paragraph describing the v0.6 RIMG behavior:
    on `mzPeak → imzML` conversion the converter now reads each embedded `images/image_NNNN.<ext>`
    member back out as an external file beside the produced `.imzML` (named from `source_name`,
    path-guarded) and re-emits a `<sampleList>/<sample>` carrying `IMS:1006008` (the exported file
    location) plus the descriptive attributes recovered by inverting the forward fold —
    `IMS:1006015` staining, `IMS:1006017` alignment, `IMS:1006011`/`1006012` subject, `IMS:1006013`
    morphology. State explicitly the DOCUMENTED DEGRADE (RIMG-03): the mzPeak-only `affine` /
    registration is NOT re-emitted because imzML has NO CV transform term (`IMS:1006017` names an
    alignment method as free text only — see `knowledge/cv` "NO registration transform term"), and an
    archive with no embedded images is a clean no-op (no spurious `IMS:1006008`). Note the descriptive
    round-trip is BEST-EFFORT (the free-text fold is not perfectly bijective for values containing
    `"; "`/`"aligned: "`). Do NOT add a new `schema/*.json` — the reverse output is imzML XML governed
    by the imzML spec (per CONTEXT three-places-reduced rule: src + this doc only).
  </action>
  <verify>
    <automated>grep -n "RIMG\|reverse" docs/mzpeak-imaging-spec-suggestions.md | grep -i "reverse" | head</automated>
  </verify>
  <acceptance_criteria>
    - The stale "Reverse ... OUT OF SCOPE for v0.5 ... drops embedded optical images" sentence is gone/updated.
    - Edit 7 now documents: reverse member export → external file beside `.imzML`; re-emit IMS:1006008 + recovered descriptive params; the affine/registration degrade (no imzML transform term); the no-images no-op; best-effort fidelity.
    - No new `schema/*.json` file is added (grep: `git status --porcelain schema/` shows no new schema file).
  </acceptance_criteria>
  <done>
    Edit 7 reflects the v0.6 reverse optical export + the affine degrade + no-op, satisfying the
    reduced three-places rule (src + doc, no schema file).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crafted archive (missing member / hostile source_name) → reverse converter | the soft-posture + path-guard tests exercise this boundary end-to-end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-07 | Denial of Service | missing/unreadable image member during reverse | mitigate | Test 3 asserts the spectral reverse still returns `Ok` + warns (soft posture proven, not just claimed) |
| T-21-08 | Tampering | spurious optical emission on a no-images archive | mitigate | Test 2 asserts no `<sampleList>`/`IMS:1006008` and byte-identical spectral output (no-op proven) |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | no new crates added (sha2/zip/quick-xml already pinned per CONTEXT) — no package-install task in this phase |
</threat_model>

<verification>
- `cargo test --test reverse_optical_export` — all three tests green.
- `grep -i reverse docs/mzpeak-imaging-spec-suggestions.md` shows the updated RIMG note.
- `git status --porcelain schema/` — no new schema file.
</verification>

<success_criteria>
RIMG-03 closed and the whole phase proven: forward→reverse optical round-trip (sha256 byte fidelity +
IMS:1006008 + recovered descriptive + mzdata re-read), no-images no-op (byte-unchanged spectral, no
affine CV), and missing-member soft posture all pass; the spec doc Edit 7 records the reverse export
and the affine degrade.
</success_criteria>

<output>
Create `.planning/phases/21-reverse-optical-image-export/21-03-SUMMARY.md` when done.
</output>
