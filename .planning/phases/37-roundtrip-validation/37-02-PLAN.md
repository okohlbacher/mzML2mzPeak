---
phase: 37-roundtrip-validation
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - src/sdrf/validate.rs
  - src/sdrf/mod.rs
  - src/cli.rs
  - tests/sample_metadata_validate.rs
autonomous: true
requirements: [VAL-02]
must_haves:
  truths:
    - "mzml2mzpeak --validate-sample-metadata, when paired with --sdrf/--isa, detects the reference oracle on PATH (sdrf-pipelines for SDRF, isa-api/isatools for ISA) and shells to it as a NON-BLOCKING check"
    - "When the oracle is ABSENT from PATH, the conversion records 'validator not found, skipped' and proceeds — the build/release NEVER fails for an absent oracle"
    - "When the oracle is PRESENT and reports a failure, the result is RECORDED (logged + captured) but the conversion still succeeds — VAL-02 never fails the build (CONTEXT: NEVER a release gate)"
    - "The oracle outcome (found/not-found, pass/fail, captured output) is recorded so CI/fixtures can read it"
    - "anyhow + log are used ONLY in the binary boundary; the validate library function stays typed (thiserror), per CLAUDE.md"
    - "Without --validate-sample-metadata, behavior is byte-identical to Plan 01 (the flag is opt-in; no oracle is ever invoked)"
  artifacts:
    - path: "src/sdrf/validate.rs"
      provides: "detect_validator() + run_validator() — PATH detection + non-blocking external-process shell-out returning a typed ValidationOutcome (never an error that aborts conversion)"
      min_lines: 60
    - path: "tests/sample_metadata_validate.rs"
      provides: "non-blocking-when-absent test (oracle not on PATH => Skipped outcome, conversion succeeds) + flag parse/gating tests"
      min_lines: 50
    - path: "src/cli.rs"
      provides: "--validate-sample-metadata flag + the non-blocking invoke-and-record wiring on the --sdrf/--isa path"
      contains: "validate-sample-metadata"
  key_links:
    - from: "src/cli.rs"
      to: "src/sdrf/validate.rs"
      via: "the --validate-sample-metadata arm calls detect_validator + run_validator after a successful convert"
      pattern: "detect_validator|run_validator"
    - from: "src/sdrf/validate.rs"
      to: "external oracle process"
      via: "std::process::Command, non-blocking — outcome captured, never propagated as a conversion error"
      pattern: "Command::new"
---

<objective>
Deliver VAL-02 — the BONUS, NON-BLOCKING external-validator oracle. `--validate-sample-metadata`
shells to the reference tool (`sdrf-pipelines` for SDRF, `isa-api`/`isatools` for ISA) ONLY when it is
present on PATH, records the result, and NEVER fails the build/release whether the oracle is absent or
reports a failure. This keeps Python entirely out of the hard path (Cornerstone B): the converter does
its job with no Python on PATH; the oracle is a recorded-when-available correctness signal for
CI/fixtures only.

Purpose: give CI/fixtures a real third-party correctness check on the embedded SDRF/ISA without ever
making the all-Rust conversion depend on Python at runtime. The HARD gate is VAL-01 (Plan 01); this is
strictly additive and strictly non-blocking.

Output: `src/sdrf/validate.rs` (PATH detection + non-blocking shell-out → typed `ValidationOutcome`),
the `--validate-sample-metadata` CLI wiring (`src/cli.rs`), and `tests/sample_metadata_validate.rs`
(proves absent-oracle => skipped + conversion still succeeds).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/37-roundtrip-validation/37-CONTEXT.md
@.planning/milestones/v0.8-DESIGN-DRAFT.md

# This plan extends the CLI Plan 01 left (the --reconstruct-* mode + the existing --sdrf forward path):
@src/cli.rs
# Mirror the SDRF module's typed-error convention (thiserror in the library, anyhow only in cli.rs):
@src/sdrf/mod.rs
@src/sdrf/embed.rs
@.planning/phases/37-roundtrip-validation/37-01-SUMMARY.md

<interfaces>
<!-- Use these directly. -->

From src/cli.rs (the forward --sdrf path this flag rides on):
  // run_forward_mzml(cli) converts a plain .mzML, calling convert_mzml(..., cli.sdrf.as_deref()).
  // The --validate-sample-metadata invocation happens AFTER a successful convert, on the source
  // SDRF/ISA file that was just embedded (validate the SOURCE the oracle understands, not the .mzpeak).

CLAUDE.md boundary rule:
  // anyhow + indicatif + log live ONLY in src/cli.rs / src/bin/*. The read/write/verify/schema/sdrf
  // library modules stay free of anyhow. src/sdrf/validate.rs must return a typed outcome/Result
  // (thiserror), and cli.rs wraps it.

Cornerstone B (RATIFIED, do not re-decide):
  // optional / CI-and-fixtures only / NEVER required at runtime. Absent oracle => skipped, success.
  // Present-and-failing oracle => recorded, success. The flag NEVER changes the process exit code.
</interfaces>

# Reference oracles (detected on PATH, never bundled, never a dependency):
#   SDRF: `parse_sdrf --validate` from sdrf-pipelines (CLI name commonly `parse_sdrf`); detect via `which`-style PATH probe.
#   ISA:  `isatools` / isa-api validation entrypoint; detect on PATH.
# Detection is a PATH lookup only (no network, no pip). The exact subcommand/argv can be a documented
# best-effort; the load-bearing contract is detect→(present? run : skip)→record, never abort.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: validate.rs — PATH detection + non-blocking oracle shell-out</name>
  <files>src/sdrf/validate.rs, src/sdrf/mod.rs</files>
  <behavior>
    - detect_validator(format) returns Some(program) when the oracle is on PATH, None otherwise (PATH probe only — no network, no install).
    - run_validator on an ABSENT oracle returns ValidationOutcome::Skipped{reason} — never an Err that could abort a conversion.
    - run_validator on a PRESENT oracle captures stdout/stderr + exit status into ValidationOutcome::{Passed, Failed{detail}} — a non-zero oracle exit is Failed, NOT a propagated error.
    - ValidationOutcome is a plain typed enum (thiserror for any internal IO error, but the public run_validator returns Ok(outcome) even when the oracle fails — failure is data, not an error).
  </behavior>
  <action>
    Create src/sdrf/validate.rs with: a `SampleMetadataFormat { Sdrf, Isa }` selector; `detect_validator(fmt) -> Option<PathBuf>`
    that probes PATH for the oracle program (split `PATH` on the platform separator, check each dir for
    the program name — no extra crate); `run_validator(fmt, source: &Path) -> ValidationOutcome` that, when
    detect returns None, yields `ValidationOutcome::Skipped { reason: "<oracle> not found on PATH" }`, and
    otherwise spawns the oracle with `std::process::Command` against `source`, captures output, and maps a
    zero exit to `Passed` / non-zero to `Failed { detail }`. The public surface MUST NOT return an Err that
    aborts conversion (an internal IO failure spawning the process degrades to `Skipped { reason }`, logged
    but non-fatal). Define `ValidationOutcome { Skipped{reason}, Passed, Failed{detail} }` with a Display
    summary. Re-export from src/sdrf/mod.rs. NO anyhow here (typed library, per CLAUDE.md). NO new crate —
    `std::process::Command` + std PATH parsing only.
  </action>
  <verify>
    <automated>cargo test -p mzml2mzpeak --lib sdrf::validate -- --nocapture</automated>
  </verify>
  <done>detect_validator + run_validator + ValidationOutcome exist and are re-exported; a unit test with a guaranteed-absent oracle name returns Skipped (never Err); no anyhow import in validate.rs.</done>
</task>

<task type="auto">
  <name>Task 2: --validate-sample-metadata CLI wiring (non-blocking, recorded)</name>
  <files>src/cli.rs, tests/sample_metadata_validate.rs</files>
  <action>
    Add `--validate-sample-metadata` (bool flag) to `ConvertCli`. In `run_forward_mzml`, AFTER a
    successful convert_mzml, when the flag is set AND a sample-metadata source was supplied (--sdrf, or the
    Phase-33 --isa once present), call `crate::sdrf::run_validator(fmt, source)` and RECORD the outcome:
    `log::info!` for Skipped/Passed, `log::warn!` for Failed — and NEVER change the return value / exit
    code based on the outcome (VAL-02 is never a gate). If the flag is set but no source was supplied, emit
    a single actionable warning ("--validate-sample-metadata has no effect without --sdrf/--isa") and
    proceed. Add a `--validate-sample-metadata` parse test, a "flag-without-source warns but succeeds" path,
    and create tests/sample_metadata_validate.rs asserting: (a) a conversion with --sdrf
    --validate-sample-metadata on a host WITHOUT the oracle on PATH still SUCCEEDS and the recorded outcome
    is Skipped; (b) without the flag, no oracle is invoked (byte-identical to Plan 01). Use a temp PATH with
    the oracle guaranteed absent (or assert on the Skipped outcome via the library function directly) so the
    test is deterministic in CI. Per CONTEXT: anyhow/log binary-only — the validate logic stays in
    validate.rs; cli.rs only wraps + logs.
  </action>
  <verify>
    <automated>cargo test --test sample_metadata_validate -- --nocapture && cargo test -p mzml2mzpeak --lib cli -- --nocapture</automated>
  </verify>
  <done>--validate-sample-metadata parses; a --sdrf conversion with the flag succeeds when the oracle is absent (outcome Skipped, exit 0); the flag never changes the exit code; without the flag no oracle is invoked. `cargo build` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| converter → external oracle process | untrusted third-party Python tool spawned from PATH; its presence, output, and exit status are all untrusted |
| PATH env → program resolution | a PATH entry could resolve to an unexpected program of the oracle's name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37V-01 | Denial of service | oracle hangs/fails | mitigate | non-blocking by contract — any spawn IO failure degrades to Skipped (logged); the oracle is never on the release-gate path |
| T-37V-02 | Elevation of privilege | running a PATH-resolved program | accept | the oracle is opt-in (--validate-sample-metadata) + only invoked when the user explicitly asks; documented as CI/fixtures-only |
| T-37V-03 | Tampering | a failing oracle silently fails the build | mitigate | VAL-02 NEVER changes the exit code; Failed is logged as a warning only (CONTEXT: never a release gate) |
| T-37V-SC | Tampering | pip/cargo installs | mitigate | NO crate added (std::process::Command only); the oracle is an EXTERNAL process detected on PATH, never installed/vendored by us — nothing to slopcheck |
</threat_model>

<verification>
- `cargo build` clean.
- `cargo test --test sample_metadata_validate` passes: absent-oracle conversion succeeds with a Skipped outcome and exit 0.
- `cargo test -p mzml2mzpeak --lib sdrf::validate cli` passes.
- anyhow stays binary-only: `grep -c 'use anyhow' src/sdrf/validate.rs` is 0.
- No new dependency: `git diff Cargo.toml` shows no added crate.
- The flag never gates: `grep -n "validate" src/cli.rs` shows the outcome is logged (info/warn), never returned as an Err that reaches classify_exit.
</verification>

<success_criteria>
VAL-02 is a strictly non-blocking BONUS: `--validate-sample-metadata` runs the reference oracle only
when it is on PATH, records found/skipped/pass/fail, and NEVER fails the build or changes the exit code —
absent oracle or failing oracle, the conversion succeeds. Python stays out of the hard path entirely
(no new dependency, std::process::Command only), and anyhow/log remain binary-only.
</success_criteria>

<output>
Create `.planning/phases/37-roundtrip-validation/37-02-SUMMARY.md` when done.
</output>
