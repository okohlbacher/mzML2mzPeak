---
phase: 37-roundtrip-validation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/upstream/v0.8-spec-batch-bundle.md
  - docs/upstream/ms-run-sample-ref-writer-pr.md
  - docs/mzpeak-spec-proposal-queue.md
autonomous: true
requirements: [UPSTREAM-PR]
must_haves:
  truths:
    - "A single submission-ready bundle assembles the v0.8 batch spec proposals P-02..P-09 (one section per proposal) drawn from the Phase-30 queue, each restated as PR-ready prose against the canonical spec"
    - "The bundle includes the list-valued ms_run.sample_ref writer PR text (the Phase-30b upstream change), distinct from the spec-prose proposals"
    - "The bundle is PREPARED AND HELD — it carries an explicit 'NOT SUBMITTED — owner-gated' banner and a submission checklist mirroring v0.7's prepared-and-held pattern; no PR is filed and no push occurs"
    - "The proposal queue's §4b v0.8 gate checklist is updated to mark the implementation gates that Phases 31/32 satisfied and to point at the assembled bundle"
    - "The push-policy constraint is stated in the bundle (HUPO-PSI is outside github.com/okohlbacher → explicit interactive owner authorization required before any PR)"
  artifacts:
    - path: "docs/upstream/v0.8-spec-batch-bundle.md"
      provides: "the assembled submission-ready v0.8 spec batch (P-02..P-09 as PR-ready sections) + held banner + submission checklist"
      min_lines: 80
    - path: "docs/upstream/ms-run-sample-ref-writer-pr.md"
      provides: "the list-valued ms_run.sample_ref writer PR text (HUPO-PSI/mzPeak reference impl + spec field), prepared and held"
      min_lines: 40
  key_links:
    - from: "docs/upstream/v0.8-spec-batch-bundle.md"
      to: "docs/mzpeak-spec-proposal-queue.md"
      via: "the bundle cites the queue's P-02..P-09 rows and the extension-contract §3.9–§3.13 mechanisms (does not re-derive)"
      pattern: "P-0[2-9]"
    - from: "docs/upstream/v0.8-spec-batch-bundle.md"
      to: "docs/upstream/ms-run-sample-ref-writer-pr.md"
      via: "the bundle references the writer PR as the Phase-30b upstream-first change (P-09)"
      pattern: "ms_run.sample_ref|ms-run-sample-ref"
---

<objective>
Deliver UPSTREAM-PR (HELD) — assemble the batched v0.8 spec proposals (the Phase-30 queued items
P-02..P-09) plus the list-valued `ms_run.sample_ref` writer PR text into a single submission-ready
bundle, then HOLD it. Mirror v0.7's prepared-and-held pattern: the bundle is complete enough to file as
PRs the moment the owner authorizes, but NO PR is filed and NO push happens here (push policy:
HUPO-PSI is outside github.com/okohlbacher → explicit interactive owner authorization required).

Purpose: the v0.8 sample-metadata milestone's spec/upstream debt is paid down to "one owner click from
submission" without ever crossing the owner-gated push boundary. The proposals already exist as queued
rows + mechanism citations in the Phase-30 proposal queue; this plan turns them into PR-ready prose and
records the held state.

Output: `docs/upstream/v0.8-spec-batch-bundle.md` (the assembled batch + held banner + submission
checklist), `docs/upstream/ms-run-sample-ref-writer-pr.md` (the writer PR text), and an updated
`docs/mzpeak-spec-proposal-queue.md` §4b gate reflecting the assembled-and-held state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/37-roundtrip-validation/37-CONTEXT.md
@.planning/milestones/v0.8-DESIGN-DRAFT.md

# The Phase-30 queue is the SOURCE of the proposals to assemble (P-02..P-09 rows + per-item write-up
# notes + the §4b HELD gate checklist). The bundle RESTATES these as PR-ready prose; it cites the
# extension-contract mechanisms, it does NOT re-derive them (Locked Rule 4):
@docs/mzpeak-spec-proposal-queue.md
# The mechanism contract the proposals bind to (§3.9–§3.13 for sample-metadata facets):
@docs/mzpeak-extension-contract.md
# The committee Q1–Q10 ratifications (already recorded; the bundle references, does not re-litigate):
@docs/sdrf-open-questions.md

<interfaces>
<!-- The proposal set to assemble (from docs/mzpeak-spec-proposal-queue.md §1b, all `queued`): -->
  P-02  Verbatim SDRF/ISA embed — sdrf/isa Data Kind + sample-metadata Entity Type   (contract §3.9)
  P-03  sample_list reuse + run-level run→sample binding                              (contract §3.11)
  P-04  [REFRAMED-E] Samples-as-channels via MS:1002602 + list-valued ms_run.sample_ref — NO channel_list (contract §3.12)
  P-05  Reporter-ion quant auxiliary-array binding (channel_id → sample_list.id)      (contract §3.13)
  P-08  metadata.study global study context (accession/title/back-ref + run_sample_binding shadow) (contract §3.10)
  P-09  List-valued ms_run.sample_ref upstream schema field (Phase 30b / UPSTREAM-BIND-01) (contract §3.12)

  // Push policy (CRITICAL, from memory + CONTEXT): NEVER push outside github.com/okohlbacher
  // without explicit interactive owner authorization; warn first even then. The spec target
  // HUPO-PSI/mzPeak-specification and the writer HUPO-PSI/mzPeak are both outside okohlbacher.
  // This plan PREPARES files in-repo only — it files nothing and pushes nothing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble the v0.8 spec batch bundle (P-02..P-09), HELD</name>
  <files>docs/upstream/v0.8-spec-batch-bundle.md</files>
  <action>
    Create docs/upstream/v0.8-spec-batch-bundle.md as the single submission-ready batch. Lead with a bold
    "STATUS: PREPARED AND HELD — NOT SUBMITTED. Owner-gated (push policy: HUPO-PSI/mzPeak-specification is
    outside github.com/okohlbacher → explicit interactive authorization required before filing any PR)."
    banner mirroring v0.7's prepared-and-held pattern. Then one PR-ready section per proposal P-02, P-03,
    P-04 (reframed samples-as-channels — explicitly note channel_list is DROPPED / RATIFIED-E), P-05, P-08,
    P-09, each: (a) title + the spec mechanism it extends, (b) the proposed addition stated in the spec's
    own voice ("Proposed addition to index.md §…"), (c) a citation to the extension-contract section
    (§3.9–§3.13) for the mechanism — DO NOT re-derive mechanisms (Locked Rule 4), (d) the source
    requirement IDs + implementing phase, (e) any pending CURIE refs (point to docs/cv-requests.md, no
    inline invention). Reference the Q1–Q10 ratifications by pointer (docs/sdrf-open-questions.md), do not
    re-litigate. Close with a submission checklist (one box per proposal + the owner-authorization box) and
    an explicit "v0.7 SPEC-02 is imaging-only; no double-ownership" note. This file is documentation only —
    it files nothing.
  </action>
  <verify>
    <automated>test -f docs/upstream/v0.8-spec-batch-bundle.md && grep -q "PREPARED AND HELD" docs/upstream/v0.8-spec-batch-bundle.md && for p in P-02 P-03 P-04 P-05 P-08 P-09; do grep -q "$p" docs/upstream/v0.8-spec-batch-bundle.md || { echo "missing $p"; exit 1; }; done && echo OK</automated>
  </verify>
  <done>The bundle exists, carries the HELD/owner-gated banner, has a PR-ready section for each of P-02/P-03/P-04/P-05/P-08/P-09 citing the extension-contract mechanism, and ends with a submission checklist including the owner-authorization gate. Nothing is filed or pushed.</done>
</task>

<task type="auto">
  <name>Task 2: Writer PR text for list-valued ms_run.sample_ref + queue gate update</name>
  <files>docs/upstream/ms-run-sample-ref-writer-pr.md, docs/mzpeak-spec-proposal-queue.md</files>
  <action>
    Create docs/upstream/ms-run-sample-ref-writer-pr.md: the prepared-and-held PR text for the Phase-30b
    upstream-first change — add a list-valued `ms_run.sample_ref` field (`[String]`, list of
    `sample_list.id`; scalar = 1:1 case; list ≥2 = isobaric / fraction×multiplex; mirrors mzML's
    `<run sampleRef>`) to the HUPO-PSI/mzPeak writer schema + spec prose. Include: the same HELD/owner-gated
    banner; the exact field shape + JSON example; why it is the single minimal upstream ask (JK confirmed
    Q3); the cross-ref to P-09 / UPSTREAM-BIND-01 and to the design draft §5.2; and a note that until this
    merges, `metadata.study.run_sample_binding` (phase32_shadow) is the interim provenance carrier. Then
    update docs/mzpeak-spec-proposal-queue.md §4b: check the implementation gates Phases 31 and 32 have
    satisfied (verbatim embed + tokens in real output; sample_list + metadata.study in real output), add a
    pointer line "Assembled into docs/upstream/v0.8-spec-batch-bundle.md (PREPARED AND HELD, Phase 37)", and
    leave the owner-authorization + submission-confirmed boxes UNCHECKED (still HELD). Do NOT check the
    Phase-34/35 gates unless those phases have shipped at execution time — leave them as-is if not. File
    nothing; push nothing.
  </action>
  <verify>
    <automated>test -f docs/upstream/ms-run-sample-ref-writer-pr.md && grep -q "sample_ref" docs/upstream/ms-run-sample-ref-writer-pr.md && grep -q "PREPARED AND HELD" docs/upstream/ms-run-sample-ref-writer-pr.md && grep -q "v0.8-spec-batch-bundle.md" docs/mzpeak-spec-proposal-queue.md && echo OK</automated>
  </verify>
  <done>The writer PR text exists with the list-valued field shape + held banner; the proposal queue §4b points at the assembled bundle and reflects the Phase 31/32 gates; the owner-authorization and submission boxes remain unchecked (HELD).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-repo docs → external HUPO-PSI repos | the bundle is submission-ready text; crossing to an actual PR is the owner-gated push boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37U-01 | Elevation of privilege | an agent files/pushes the PR without owner authorization | mitigate | this plan writes in-repo docs ONLY; every file carries a HELD/owner-gated banner; no gh/git-push step exists in any task |
| T-37U-02 | Tampering | inventing a non-canonical CURIE in proposal prose | mitigate | proposals point to docs/cv-requests.md for pending CURIEs; no inline accession invention (Locked Rule) |
| T-37U-03 | Repudiation | double-ownership of terms with v0.7 SPEC-02 | mitigate | bundle states v0.7 SPEC-02 is imaging-only; sample-metadata/channel terms owned exclusively by this v0.8 batch |
| T-37U-SC | Tampering | package installs | mitigate | docs-only plan; no installs, no code — slopcheck N/A |
</threat_model>

<verification>
- `test -f docs/upstream/v0.8-spec-batch-bundle.md` and `test -f docs/upstream/ms-run-sample-ref-writer-pr.md`.
- Both new files carry "PREPARED AND HELD" + the push-policy/owner-gated line: `grep -l "okohlbacher" docs/upstream/*.md`.
- The bundle covers every proposal: `for p in P-02 P-03 P-04 P-05 P-08 P-09; do grep -q "$p" docs/upstream/v0.8-spec-batch-bundle.md; done`.
- The queue points at the bundle: `grep -q "v0.8-spec-batch-bundle.md" docs/mzpeak-spec-proposal-queue.md`.
- Submission stays HELD: the §4b owner-authorization + submission-confirmed boxes are NOT checked; no `gh pr`, no `git push` appears in the SUMMARY's command log.
</verification>

<success_criteria>
The v0.8 spec batch (P-02..P-09) and the list-valued `ms_run.sample_ref` writer PR are assembled into
submission-ready, owner-gated bundles that are PREPARED AND HELD — one owner authorization away from
filing, with nothing filed and nothing pushed. The proposal queue's §4b gate reflects the assembled state
and the Phase 31/32 implementation gates, while the owner-authorization and submission boxes stay
unchecked. The prepared-and-held discipline mirrors v0.7 exactly.
</success_criteria>

<output>
Create `.planning/phases/37-roundtrip-validation/37-03-SUMMARY.md` when done.
</output>
